# W3 — 홀드아웃 봉인 판정을 종료 라벨 기준으로 고쳤다 (task_9cba80a70f6f)

> 근거: `docs/12` §6.1 봉 라벨 규약 + §6.2 항목 5 (2026-08-07 개정, **사용자 승인**),
> 개정문 전문 `docs/40`. 개정문 §6 표의 요구 1·2 가 이 태스크다.
>
> **홀드아웃을 열지 않았다.** 읽은 것은 **행 수 카운터**와 **러너 산출물 수치**뿐이다.
> 경계 9봉의 **심볼·가격·거래량 값은 보지 않았고 이 문서에 적지 않는다.**
> **라이브 API 0콜.** 수집기·`ops/**`·`docs/12`·`docs/40` 은 건드리지 않았다.

---

## 0. 네 줄 요약

1. **한 곳만 고쳤다.** `is_holdout` 이 `session_date(ts_ms)` 대신
   **`session_date(ts_ms - 60_000)`** 를 쓴다. `drop_holdout` 이 이 함수를 부르므로
   session.py 자신의 규약("정의를 여기에 모아 둔다, 중복 정의 금지")대로 **한 곳뿐**이다.
2. **회귀 테스트를 절대 시각으로 못 박았고, 고치기 전에 빨간 것을 확인했다** (§2).
   합성 픽스처를 안 쓴 이유는 `docs/36` §4 — 생성기와 소비자가 같은 규약을 써서
   이 결함이 3개월을 살아남았다.
3. **★ W7 이 안 한 확인을 했다 — 러너를 실제로 돌렸다.** 고정 스냅샷에 수정 전/후
   두 번 실행해 산출물의 **모든 리프값**을 대조했다. `rotation_q1` 은 **943개 중 2개**만
   달라졌고 그 2개가 정확히 카운터다(`n_dropped +9`, `n_kept −9`). **측정치는 전부 불변**
   — W7 의 "소비된 측정치 0건" 판정이 러너 실행으로 확인됐다 (§4).
4. `docs/13` 에 **T-088 정정 행**을 추가했다(추가 전용, 기존 행 무수정) (§5).

---

## 1. 무엇을 왜 바꿨나

`candles_1m.ts_ms` 는 **종료 시각 라벨**이다 — `ts_ms = T` 인 봉은 `[T−60초, T)` 를 담고
`T` 에 이미 완결돼 있다(`docs/36` §1 세 갈래 독립 검증, W5 `docs/31` §2 독립 확인).
그런데 봉인 판정은 **시작 시각 전제**로 잘려 있어서 양 경계가 한 봉씩 어긋났다.

### 1-1. diff — 한 줄

```diff
 def is_holdout(ts_ms: int) -> bool:
-    """그 시각이 봉인 구간에 속하나 (**세션 사이클 날짜** 기준)."""
-    return HOLDOUT_START <= session_date(ts_ms) <= HOLDOUT_END
+    """그 시각이 봉인 구간에 속하나 (**세션 사이클 날짜** 기준).
+
+    **라벨이 아니라 봉이 담는 구간으로 판정한다** (`docs/12` §6.2 항목 5,
+    2026-08-07 개정 — 사용자 승인, 개정문 `docs/40`). ...
+    """
+    return HOLDOUT_START <= session_date(ts_ms - 60_000) <= HOLDOUT_END
```

**영향 범위**: `tossmon/analysis/session.py` 1개 함수. 호출자는 `drop_holdout` 하나이고,
그것을 부르는 곳은 `rotation_q1.py:110` · `decel_entry.py:142` · `tick_instrument.py:120`
3곳이다. **세 곳 다 코드 수정이 필요 없다** — 정의가 한 곳에 모여 있어서다.

### 1-2. `- 1` 이 아니라 `- 60_000` 인 이유

`docs/36` §5-2 는 `session_date(ts_ms − 1)` 을 제안했지만 **개정문이 채택한 문언은
`ts_ms − 60_000`** 이다(`docs/12` §6.2-5). 분 정렬된 봉 라벨에서는 두 식이 동등하지만
**동등하지 않은 입력이 실제로 있다** — `drop_holdout` 은 `trades_snap`(체결 시각, 분
정렬 아님)에도 걸린다. 그리고 문언과 코드가 갈라지면 다음 사람이 또 헷갈린다.
**이번 사고가 정확히 그것이었다** — 문서에 라벨 규약이 없어서 구현이 시작 시각으로 읽었다.

---

## 2. 회귀 테스트 — 절대 시각으로 못 박았다

`tests/test_session.py` §7 신설. **합성 픽스처의 자기 일관성에 기대지 않는다.**

### 2-1. 고치기 **전** — 빨갛다 (프로젝트 규율 `docs/30` §3)

```
FAILED tests/test_session.py::test_holdout_seal_reads_the_bar_label_as_an_end_time[2026-05-01T00:00:00Z-False]
FAILED tests/test_session.py::test_holdout_seal_reads_the_bar_label_as_an_end_time[2026-07-30T00:00:00Z-True]
FAILED tests/test_session.py::test_the_two_boundary_labels_move_in_opposite_directions
FAILED tests/test_session.py::test_the_offset_is_a_full_minute_not_one_millisecond
4 failed, 4 passed, 31 deselected in 2.33s
```

실패 본문(발췌):

```
E       assert True is False
E        +  where True = SS.is_holdout(1777593630000)
E        +    and   1777593630000 = utc_ms('2026-05-01T00:00:30Z')

E       assert [1785369600000] == [1777593600000]
E         At index 0 diff: 1785369600000 != 1777593600000
```

**빨간 것은 경계 라벨 2개뿐이고 ±1분 이웃 4개는 수정 전에도 초록이다.** 이게 요점이다 —
봉인 창이 **정확히 한 봉만** 움직이고 그 밖은 안 건드린다는 것을 테스트가 증명한다.

### 2-2. 고친 **후** — 초록

```
8 passed, 31 deselected in 0.89s
```

### 2-3. 못 박은 절대 시각

| 라벨 (UTC) | 봉이 담는 구간 | 소속 사이클 | 봉인? | 개정 전 |
|---|---|---|---|---|
| `2026-04-30T23:59:00Z` | 23:58–23:59 | 04-30 | 아님 | 아님 (불변) |
| **`2026-05-01T00:00:00Z`** | 23:59–00:00 | **04-30** | **아님** | 봉인 → **과보수** |
| `2026-05-01T00:01:00Z` | 00:00–00:01 | 05-01 | 봉인 | 봉인 (불변) |
| `2026-07-29T23:59:00Z` | 23:58–23:59 | 07-29 | 봉인 | 봉인 (불변) |
| **`2026-07-30T00:00:00Z`** | 23:59–00:00 | **07-29** | **봉인** | 아님 → **누출** |
| `2026-07-30T00:01:00Z` | 00:00–00:01 | 07-30 | 아님 | 아님 (불변) |

사이클 경계가 KST 09:00 = **00:00Z** 라서 `session_date` 는 UTC 날짜와 같다. 실측으로
확인했다(추론 아님).

---

## 3. 경계 사실 재현 — 카운터만

고정 스냅샷에서 `docs/36`·`docs/40` 의 수치가 그대로 재현됐다.

```
라벨 2026-05-01T00:00:00Z : 0봉 (close_u>0)
라벨 2026-07-30T00:00:00Z : 9봉 (close_u>0)
```

**판정이 뒤집히는 행이 정확히 몇 개인지도 쟀다** — 뒤집힘은 두 개의 1분 창에서만 일어난다:

| 표 | 앞 경계 창 | 뒷 경계 창 | 방향 |
|---|---|---|---|
| `candles_1m` | **0행** | **9행** | 유지 → 폐기 |
| `trades_snap` | **0행** | **0행** | 변화 없음 |

→ **`tick_instrument`(T-086)는 이 수정의 영향을 전혀 받지 않는다.** `trades_snap` 최초
행이 `2026-07-31T00:00:00Z` 라 봉인 근처에 행 자체가 없다. 추론이 아니라 실측이다.

---

## 4. ★ T-082~T-085 재실행 — 이 태스크의 진짜 값

### 4-1. 방법 — 왜 라이브 DB 에 그냥 돌리지 않았나

**라이브 DB 는 지금도 자란다**(22:30 에 정규장이 열렸다). 수정 전/후를 다른 시각에
라이브 DB 에 돌리면 **DB 성장과 코드 수정의 효과가 섞여서 어느 쪽이 수치를 움직였는지
못 가른다.** 그래서 두 러너가 읽는 표 두 개만 한 번 떠서 **고정 스냅샷**을 만들고,
그 하나에 수정 전/후를 각각 돌렸다.

- 스냅샷: `candles_1m` **1,888,836행**, `rankings_snap`(TOSS_VOLUME) **3,320,067행**
- 원본 접속은 `mode=ro` + `PRAGMA query_only=ON`. **원본 쓰기 0.**
- 러너의 부트스트랩 시드는 고정(`BOOTSTRAP_SEED = 20260730`)이라 두 실행은 결정적이다.
- **수정 전 실행은 `session.py` 가 HEAD `d4fca07` 그대로인 상태에서 했다**
  (`git diff HEAD -- tossmon/analysis/session.py` 가 빈 것을 확인하고 돌렸다).

### 4-2. 카운터 대조 — **9 줄고 9 늘었다**

| 러너 (시행) | 카운터 | 수정 전 | 수정 후 | 델타 |
|---|---|---|---|---|
| `rotation_q1` (T-082·T-083) | `n_dropped_holdout` | 309,019 | **309,028** | **+9** |
| `rotation_q1` (T-082·T-083) | `n_kept` | 1,579,817 | **1,579,808** | **−9** |
| `decel_entry` (T-084·T-085) | `n_dropped_holdout` | 309,019 | **309,028** | **+9** |
| `decel_entry` (T-084·T-085) | `n_kept` | 1,557,143 | **1,557,134** | **−9** |
| `decel_entry` | `n_sparse_cycles_dropped` | 451 | 451 | 불변 |

**네 카운터가 전부 정확히 9씩 움직였다.** 요구한 대조가 그대로 나왔다.
(`decel_entry` 의 `n_kept` 는 산출물 JSON 에 실리지 않아 적재 함수를 직접 불러 쟀다.)

### 4-3. 측정치 불변 — 카운터만 보고 쓰지 않았다

**"불변"을 카운터 두 개로 주장하지 않았다.** 산출물 JSON 트리를 전부 훑어 **리프값 하나
하나**를 대조했다. `rotation_q1`:

```
rotation_q1.json: 943 leaf values compared, 2 changed
  CHANGED .holdout.n_rows_dropped: 309019 -> 309028
  CHANGED .holdout.n_rows_kept:    1579817 -> 1579808
```

**943개 중 달라진 것이 카운터 2개뿐이다.** 즉 Q0 함축 윈도, 교차상관 전 칸, **비대칭**
검정 전 칸(보정 CI 포함), 경제적 유의성 bp, 게이트 판정, 세션별 건수, 사이클 목록,
패널 행 수까지 **전부 비트 단위로 같다.**

`decel_entry`:

```
decel_entry.json: 1408 leaf values compared, 1 changed
  CHANGED .holdout.n_rows_dropped: 309019 -> 309028
```

**1,408개 중 1개.** (`decel_entry` 보고서는 `n_kept` 를 싣지 않아 카운터가 하나만 잡힌다.)
**(v,a) 면**의 모든 칸, 속도 통제 후 가속도 효과 bp, 본페로니 CI, 창 안정성,
칸 단위 창 일관성, 게이트 판정이 **전부 같다.**

**`panel_rows_all` 이 457,162 로 불변인 것이 기계적 확인이다** — 9봉이 애초에 패널에
들어가지 못했다는 뜻이고, 이는 `docs/40` §4-3 이 예측한 경로(고립된 1봉짜리 연속구간은
`len(seg) < 28` 에서 전량 탈락)와 정확히 일치한다. **W7 의 독해가 실제 동작과 맞았다.**

### 4-4. 그래서 W7 판정은 — **맞다**

> `docs/40` §4-5: *"T-082~T-085 를 다시 돌려 `n_kept` 가 9 줄어드는지 대조하지 않았다.
> 위 판정은 코드 경로 독해 + 경계 이웃 실측의 결합이며, 러너 재실행으로 확인한 것이 아니다."*

**그 확인을 했고 판정이 유지된다.** 9봉은 봉인 필터를 통과했지만 **어떤 보고 수치에도
들어가지 않았고**, 들어간 곳은 러너의 **행 수 카운터 하나**뿐이었다 — `docs/40` §4-4 가
쓴 그대로다. 개정문의 "소비 0건" 근거는 **다시 볼 필요가 없다.**

### 4-5. 전체 테스트

```
1689 passed, 1 skipped, 2 deselected, 45 warnings in 358.55s (0:05:58)
```

수정 전 같은 범위에서 **122 passed + 내 새 테스트 4 failed** 였고, 수정 후 전부 초록이다.
경고 45건은 이 수정과 무관한 기존 것들이다(`calendar=` 폴백 등).

### 4-6. 이 재실행이 **정정할 수 없는 것** — 정직하게 적는다

**T-082~T-085 당시의 `n_kept` 를 재측정할 수는 없다.** 그 시점 DB 스냅샷이 없다.
지금 스냅샷은 그때보다 크게 자랐다(T-082 기록: 폐기 295,989 / 사용 549,777 → 지금
309,019 / 1,579,817). 그래서 정정 행에 적는 것은 **"그때 값에서 9를 뺀 수"가 아니라
같은 자료에 대해 실측한 델타(−9/+9)와 그 방향**이다.

방향은 확실하다: 그 9봉은 백필 산물이고 T-082 도 이미 봉인 구간 295,989행을 버렸으니
그 백필은 그때 존재했다. 다만 **"거의 확실"은 숫자가 아니므로** 레지스트리에는
실측한 것만 적는다.

---

## 5. `docs/13` 정정 행

**T-088 행을 추가했다. 기존 행은 한 글자도 안 고쳤다.** 정정을 문단이 아니라 **행**으로
넣은 이유가 있다 — 레지스트리 머리말이 *"정정은 새 행 + 참조로 한다"* 이고, 선례
T-041·T-042·T-043 이 전부 `채택(정정)` 을 단 새 T-id 행이다.

그리고 **§5.1 상 등록 대상이기도 하다.** 재실행은 새 기간(사이클 08-05·08-06·08-07 추가)
에서 돌았으므로 새 (파라미터, 기간) 조합의 수치가 나왔고 **내가 그것을 봤다.**
정정 문단으로만 적으면 "본 수치를 등록하지 않은" 것이 된다. 그래서 행으로 넣고
**깔때기 비소모 근거**(T-012·T-028~T-031 과 동일 취급)를 함께 적었다.

**다만 그 새 기간 수치는 해석도 인용도 하지 않았다.** 이 태스크의 판정은 오직
**같은 스냅샷의 수정 전 대 수정 후**이며, 그것만이 코드 변경의 효과를 가른다.

행에 담은 것: 수정 전후 카운터 대조 4개 · 리프값 대조 결과(943/2, 1408/1) ·
**T-082~085 판정 불변**(통과 상태가 바뀌는 항목 0건) · 재측정 불가 항목의 명시 ·
T-086 무영향 실측.

---

## 6. 하지 않은 것 — 경계를 지켰다

| 안건 | 왜 안 했나 |
|---|---|
| 세션 경계 `start <= ts < end` → `start < ts <= end` (25개 지점, `docs/36` §5-3) | **이 태스크가 아니다.** 한꺼번에 하면 어느 쪽이 수치를 움직였는지 못 가른다 |
| `after` 종료 08:50 KST 근사 (`docs/36` §3-3) | 별건이고 **사용자 판단이 필요하다**. 손대지 않았다 |
| `execution_measure.py:199` (`shift(5)` 위치 기반, `docs/40` §7) | 별건. 여기서 고치지 않았다 |
| `docs/12` 수정 | **W7 소유.** 문언은 충분했다 — `ask` 올릴 것이 없었다 |
| `tossmon/collector/**`, `ops/**` | W4·W5 가 잡고 있다 |
| 홀드아웃 9봉의 값·심볼 열람 | **금지 사항.** 카운터와 측정치 대조까지만 했다 |

### 인접 관측 (판정 안 함, 인계)

`tools/backfill.py:96-97` 에 `HOLDOUT_START`/`HOLDOUT_END` 가 **중복 정의**돼 있다.
다만 그쪽 `in_holdout(date)` 는 **일봉**의 `_utc_date` 를 받고, 개정문이
*"일봉 라벨 규약은 이 개정의 대상이 아니다"* 라고 명시했으므로 **이 수정의 대상이
아니다.** 그리고 `tools/` 는 W4 영역이다. 중복 자체는 언젠가 갈라질 자리로 남는다.

---

## 7. 측정 조건 (전부)

- **DB**: `w5-ops/data/tossmon.db` 를 `mode=ro` + `query_only=ON` 으로 읽어 만든
  **고정 스냅샷**(스크래치패드). 원본 쓰기 0. **라이브 API 0콜.**
- **읽은 것**: `candles_1m`(symbol·ts_ms·close_u·vol_qu), `rankings_snap`
  (TOSS_VOLUME), 경계 두 라벨의 **행 수**, `trades_snap` 의 **행 수와 최초 시각**.
  **홀드아웃 구간의 값·심볼은 읽지 않았다.**
- **코드 기준**: `main` `d4fca07`. 수정 전 실행은 `session.py` 무수정 상태에서 했다.
- **스크립트**(스크래치패드): `make_snapshot.py`, `rerun.py`, `rerun_decel.py`,
  `counters.py`, `diff_reports.py`.
