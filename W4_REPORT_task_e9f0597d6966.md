# W4 — 예산 여유 확보 + 개장 축출 차단 (task_e9f0597d6966)

브랜치 `w4-collector`, 커밋 **63e788a**. 전체 1452 passed / 1 skipped.
라이브 호출 0건, 가동 중 수집기 무접촉 (진단은 로그·DB 읽기 전용).

---

## 1. 예산 기준 일치

전에는 두 판정이 서로 다른 천장을 봤다:

| 판정 | 예전 기준 | 지금 기준 |
|---|---|---|
| `validate_plan` (설정 검증) | target 7.000 | **plan_ceiling** = target × (0.95 − 0.10) = **5.950** (경보) / 하드 위반은 shrink_ceiling |
| `should_shrink` (실측 축소) | target × 0.95 = 6.650 | **shrink_ceiling** = target × 0.95 = **6.650** |

`PLAN_RESERVE_FRAC = 0.10` 을 명시 상수로 두었다. 계획 밖 호출(재시도, 승격 직후 백필,
세션 전환 후 베이스라인 갱신, tier2 호가)이 전부 이 여유에서 나간다.

**출하 설정 실측 (MARKET_DATA)**

```
target                     7.000 req/s
shrink ceiling (x0.95)     6.650
plan ceiling (reserve 10%) 5.950
planned = tier1 0.178 + trades 5.000 + book 1.250 = 6.428
  -> 축소 트리거까지 여유   +0.222 req/s   (코디네이터가 본 "0.233")
  -> 계획 여유 부족          -0.478 req/s   (경보 대상, 하드 위반은 아님)
```

즉 **지금 설정은 여유가 없다.** 기동 시 축소를 유발하지 않기 위해 하드 위반으로는
분류하지 않고 `budget: plan leaves no reserve (...)` 경고를 낸다.
CHART 는 계획 2.727 < 천장 2.975 로 여유가 있다 (문제 없음).

## 2. 429 그룹 귀속

`client.counters["http_429"]` 는 전역이라 호출한 쪽 그룹에 귀속하면 **엉뚱한 티어가**
깎인다(CHART 가 맞았는데 tier3 축소). limiter 는 429 를 정확한 그룹으로 받으므로
(`blocked_for_s`, `backoff`) 그 흔적으로 판별한다. 후보가 정확히 하나일 때만 귀속하고,
애매하면 호출자 그룹으로 두되 `http_429_unattributed` 카운터로 **귀속 불가를 명시**한다.
`limiter.py`(W1 소유)는 읽기만 했다. 이중 청구는 고수위 비교로 이미 차단돼 있고
테스트로 고정했다.

## 3. 개장마다 tier3 가 깎이던 경로 (★)

실측 3건(07-31 22:52, 08-03 22:34, 08-04 02:48) 모두 개장 직후이고 **앞에 429 라인이 없다** —
순수 measured-overshoot 분기였다. 08-03 건은 개장 32초 만에 **점수 0.65** 짜리를 축출했다.

- 세션 전환 후 `MEASURED_WARMUP_S = 180s` 동안 **측정 기반 축소를 하지 않는다.**
- 측정 초과가 `MEASURED_SUSTAIN_S = 60s` **연속 유지될 때만** 축소한다
  (건수가 아니라 경과 시간 기준 — 감사 H-4 와 같은 이유).
- **429(진짜 사고)와 계획 초과(설정 오류)는 워밍업·지속성과 무관하게 즉시 반응한다.**

회귀 테스트는 양방향 모두 고정했다: 개장 버스트는 축소를 부르지 않고,
워밍업이 끝난 뒤의 지속 과부하는 여전히 축소된다.

---

## 4. 남는 여유와 티어3 호가 선택지

트레이드오프의 핵심: **tier3 종목은 체결과 호가를 둘 다 받는다.** 종목 수를 줄이면
체결 예산도 같이 풀린다. `tier1` 0.178 req/s 고정.

| tier3 종목 | 체결 주기 | 호가 주기 | 체결 r/s | 호가 r/s | 합계 | 축소천장 대비 | 계획천장 대비 | 판정 |
|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 20 | 4s | **16s** (현재) | 5.000 | 1.250 | 6.428 | +0.222 | −0.478 | 여유 없음 |
| 20 | 4s | 8s | 5.000 | 2.500 | 7.678 | −1.028 | −1.728 | **축소** |
| 20 | 6s | 8s | 3.333 | 2.500 | 6.011 | +0.639 | −0.061 | 아슬 |
| 15 | 4s | **8s** | 3.750 | 1.875 | 5.803 | +0.847 | **+0.147** | **OK** |
| 12 | 4s | 4s | 3.000 | 3.000 | 6.178 | +0.472 | −0.228 | 아슬 |
| 10 | 4s | **4s** | 2.500 | 2.500 | 5.178 | +1.472 | **+0.772** | **OK** |
| 8 | 4s | 2s | 2.000 | 4.000 | 6.178 | +0.472 | −0.228 | 아슬 |
| 5 | 4s | **2s** | 1.250 | 2.500 | 3.928 | +2.722 | **+2.022** | **OK** |
| 5 | 2s | 2s | 2.500 | 2.500 | 5.178 | +1.472 | +0.772 | OK |

**여유를 남기면서 호가를 촘촘하게 하는 조합 (권장 후보)**
- **15종목 × 8초** — 폭을 조금만 줄이고 호가를 2배 촘촘하게. 여유 +0.147
- **10종목 × 4초** — 호가 4배 촘촘. 여유 +0.772 로 넉넉
- **5종목 × 2초** — 틱 분석용 최대 밀도. 여유 +2.022, 대신 관측 폭이 좁다

**결정은 사용자 몫이다.** 폴링 값은 이 태스크에서 바꾸지 않았다.
`polling.tier3_orderbook_s` 와 `universe.tier3_max` 두 값만 바꾸면 되고,
`validate_plan`/`reserve_deficit` 가 기동 시 검증한다.

## 5. 주의

- 20종목을 유지한 채 호가만 8초로 당기는 것은 **불가능하다**(7.678 > 6.650, 즉시 축소).
  20종목을 지키려면 체결 주기를 6초로 늦춰야 하고, 그러면 틱 데이터가 성겨진다.
- 위 숫자는 MARKET_DATA 그룹만 본 것이다. tier2 호가(별도 루프, 의도적으로 계획 밖)는
  같은 그룹에서 최대 0.5 req/s 를 기회적으로 쓰며, 사용률이 목표의 90% 를 넘으면
  스스로 물러난다. 표의 "계획천장 대비" 여유가 그 몫을 흡수한다.
