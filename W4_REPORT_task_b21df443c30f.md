# W4 — 예산 모델을 1초 고정 창 위에 다시 지음 (task_b21df443c30f)

브랜치 `w4-collector`. 라이브 호출 0건, 가동 중 수집기 무접촉. `api/` 무수정.

> ## ⚠ 먼저: W1 1단계가 저장소에 없다 (에스컬레이션 msg_3525f4194d84)
>
> 지시문은 "W1 계약 확정·머지 완료"를 전제하지만 리베이스한 main 에 산출물이 없다:
> `docs/06` 은 §9-2 에서 끝나고(§9-3~9-9 부재), `api/limiter.py` 는 **예전 토큰버킷 그대로**이며
> (`usage_ratio=0.7`, `BURST_FRACTION=0.3`, 연속 리필, **슬라이딩 하드캡 없음**),
> `under_own_limit` 는 git grep 0건, `w1/core-api` 는 e3cf184(Wave 0 직후 WIP)에 멈춰 있다.
>
> **1·2·4번은 W1 과 무관하게 옳은 작업이라 그대로 했다. 3번(usage_ratio 인상)만 보류했다** —
> 근거는 §3 에 수치로 있고, 결론부터 말하면 **하드캡 없이 올리면 산술적으로 한도를 깬다.**

---

## (a) 무엇을 바꿨나

| # | 항목 | 전 | 후 |
|---|---|---|---|
| 1 | 사용률 측정 | 60초 **평균** | **초당 첨두**(슬라이딩 1초 최대) + p95 + 평균(참고용) |
| 2 | 축소·회복 판정 | 평균 > 천장 | **첨두 > 천장**(지속 60s) 또는 429. 워밍업 유지 |
| 3 | `usage_ratio` | 0.70 | **0.70 유지** (인상 보류 — §3) |
| — | tier1 배치 | 8콜 **연속** | 1초 간격으로 분산 (버스트 평탄화) |
| — | 신규 안전장치 | 없음 | `check_usage_ratio()` — 리미터가 못 지키는 값이면 기동 시 경보 |

## (b) ★ 핵심 발견 — "한도의 1/5 인데 429" 의 정체

**tier1 스윕이 1500종목/배치200 = 8콜을 연속으로 쐈다.**

```
평균 기여    8콜 / 45s = 0.178 req/s   <- 예전 모델이 본 값. 무시할 만해 보인다.
실제 순간    8콜이 1초 안에            <- 서버가 보는 값. 한도 10 의 80% 를 혼자 먹는다.
```

같은 초에 도는 tier3 폴링(계획 5.0 req/s)과 겹치면 **한 초에 10회를 넘는다.** 60초 평균은
5.2 req/s 로 "한도의 절반" 이라 아무 문제가 없어 보인다. **평균과 첨두가 다른 것을 재고 있었고,
서버는 첨두만 본다.** 이것이 오늘 아침 착시의 기계적 원인이다.

`tier1_sweep_once` 가 배치 사이를 1초씩 벌리도록 고쳤다 — 주기(45s) 절반 안에 다 펴므로
**수집량·주기는 그대로이고 첨두만 사라진다**(예산 중립). 남은 연속 발사 지점은 §5 에 적었다.

## (c) 초당 측정 (요구 1)

- `peak_1s(group)` — **슬라이딩** 1초 최대. 판정의 근거다.
  정렬 버킷이 아니라 슬라이딩인 이유: 우리 시계와 서버 창의 **위상을 모른다.** 어떤 위상의
  고정 창이든 그 안의 호출 수는 슬라이딩 최대 이하이므로, 이 값은 위상과 무관한 **안전한 상계**다.
  (테스트로 고정: 0.9~1.1초에 걸친 8콜을 정렬 버킷은 4+4 로 쪼개 놓치고, 슬라이딩은 8 로 잡는다.)
- `p95_1s`, `per_second_counts` — 분포. 빈 초를 0 으로 채운다(빼면 p95 가 부풀어 오른다).
- `measured_rate`(60초 평균)는 **남겼지만 판정에 쓰지 않는다** — 텔레메트리 참고값이다.

**텔레메트리 신규 키**: `md_peak_1s`, `md_p95_1s`, `chart_peak_1s`, `rank_peak_1s`,
`over_limit_1s`, `http_429_under_own_limit`.
`describe()` 도 `peak/p95/avg/tgt` 순으로 바꿨다 — 첨두가 먼저 보이도록.

## (d) 판정 전환 (요구 2)

축소는 이제 **첨두 > `shrink_ceiling`(target×0.95)이 60초 지속** 또는 **429** 일 때만 한다.
계획 초과(설정 오류)는 그대로 즉시 반응하고, **개장 워밍업 180초·지속 60초 조건은 유지**했다.
회복(`should_grow`)도 평균이 아니라 첨두를 본다 — 평균만 보면 버스트 도중에 정원을 되돌린다.

**한도 자체를 넘은 초**(`peak_1s > limit`)는 축소가 아니라 **경보**로 보낸다. 정원을 깎아도
같은 버스트는 또 나기 때문이다 — 그건 리미터 문제다. 경보는 **에피소드당 1회**만 낸다
(첨두는 60초 지평에 남아 있어서 매 평가마다 세면 한 번의 버스트가 수십 건으로 부푼다.
가짜 반복 경보가 경보 무시를 만드는 것은 이 프로젝트에서 이미 겪은 실패다).

> **"깎을 일이 크게 줄어야 정상" 에 대한 답**: 하드캡이 없으므로 아직 줄지 않는다.
> 다만 **깎는 이유가 바뀌었다** — 예전에는 평균이 천장을 스치기만 해도 깎았고(개장 축출의 원인),
> 이제는 실제 1초 위반이 지속될 때만 깎는다. 하드캡이 들어오면 그때 위반 자체가 사라진다.

## (e) usage_ratio 결정 (요구 3) — **올리지 않는다. 0.70 유지.**

숫자가 결론을 강제한다. 토큰버킷은 유휴 직후 1초에 `capacity + rate` 를 통과시키고,
현재 정책은 `capacity = rate × BURST_FRACTION(0.3)` 이므로:

```
1초 최대 통과량 = rate × (1 + 0.3) = limit × usage_ratio × 1.3 ≤ limit
                → usage_ratio ≤ 1 / 1.3 = 0.7692
```

| usage_ratio | MARKET_DATA rate | 버킷 용량 | **유휴 직후 1초 최대** | 한도 10 대비 |
|---:|---:|---:|---:|---|
| 0.70 (현재) | 7.00 | 2.10 | **9.10** | OK |
| **0.769** | 7.69 | 2.31 | **10.00** | 딱 경계 |
| 0.80 | 8.00 | 2.40 | **10.40** | ❌ 초과 |
| 0.85 | 8.50 | 2.55 | **11.05** | ❌ 초과 |
| 0.87 | 8.70 | 2.61 | **11.31** | ❌ 초과 |

**지시문의 "예전 안전 상한 0.769" 가 정확히 1/1.3 이다.** 즉 그 값은 이 버스트 항에서 나온
것이고, **0.87 은 버스트 항이 제거된 뒤에만(=W1 하드캡이 있어야만) 성립한다.** 하드캡이 없는
지금 0.85 로 올리면 정원을 아무리 줄여도 유휴 직후 한 번의 버스트로 한도를 넘는다 —
예산이 아니라 리미터가 깨는 것이라 내 쪽에서 막을 방법이 없다.

이 계산을 코드에 넣었다: `max_safe_usage_ratio()` = `1/(1+BURST_FRACTION)`, 그리고
`check_usage_ratio()` 가 기동 시 초과를 경보한다. **하드캡이 머지되면 이 상한이 자동으로
따라 올라가지 않는다** — BURST_FRACTION 이 남아 있으면 여전히 경보하므로, W1 이 버스트
정책을 바꾸면 그때 값이 맞물린다.

**W1 하드캡 머지 후 권고: 0.85** (0.87 아님). 0.87 은 실측 *상한*이고, 상한에 걸터앉으면
측정오차·서버 변동에 남는 것이 없다. 0.85 면 약 2% 여유를 두고도 MARKET_DATA 가 **+21%** 다.

### 늘어난 여유를 어디에 쓸지 (usage_ratio 0.85 기준, plan_ceiling 5.950 → 7.225)

**결정은 사용자다.** tier1 0.178 고정, 현재 계획 5.178.

| 안 | tier3 종목 | 체결 | 호가 | 합계 req/s | 여유 | 무엇을 사는가 |
|---|---:|---:|---:|---:|---:|---|
| 현행 | 10 | 4s | 4s | 5.178 | +2.047 | — |
| **A. 폭** | **14** | 4s | 4s | 7.178 | +0.047 | 관측 종목 **+40%**. 여유가 거의 안 남는다 |
| A-. 폭(보수) | 12 | 4s | 4s | 6.178 | +1.047 | +20%, 여유도 유지 |
| **B. 호가 밀도** | 10 | 4s | **2.5s** | 6.678 | +0.547 | 호가 나이 2.0s → **1.25s** |
| B-. 호가(보수) | 10 | 4s | 3s | 6.011 | +1.214 | 호가 나이 → 1.5s |
| **C. 체결 밀도** | 10 | **2.5s** | 4s | 6.678 | +0.547 | 체결 결손 구간 축소 |
| D. 혼합 | 12 | 4s | 3s | 7.178 | +0.047 | 폭 +20% & 호가 1.5s |

내 의견: **A- 또는 D**. 지금 tier3 10종목은 관측 폭이 좁아 놓치는 종목이 생기고, 호가는 이미
4s 로 충분히 촘촘하다(직전 태스크에서 8s→2s 의 실익 곡선을 냈다). 다만 A(14종목)는 여유
+0.047 이라 계획 밖 호출 한 번에 트리거를 건드리므로 권하지 않는다.

## (f) 전수 순회 (요구 4) — `collector/` 의 레이트·창·정원 상수 전부

`docs/06` 형식(이름 / 현재값 / 근거 / 계약 일치)에 맞췄다. **계약 = 서버의 1초 고정 창.**

### 창·측정

| 이름 | 값 | 근거 | 1초 창 계약과 일치? |
|---|---|---|---|
| `SERVER_WINDOW_S` | 1.0 | docs/06 §9-2 (MARKET_INFO 3/s 에서 4번째 429) | ✅ **계약 그 자체** |
| `WINDOW_S` | 60.0 | 분포 관측 지평 | ✅ 판정 근거 아님으로 격하됨 |
| `PEAK_SLIDING` | True | 서버 창 위상 미상 → 슬라이딩이 안전 상계 | ✅ |
| `HEADROOM` | 0.95 | 천장 여유 | ✅ 첨두에 적용 |
| `PLAN_RESERVE_FRAC` | 0.10 | 계획 밖 호출(재시도·백필·베이스라인·tier2호가) | ✅ |

### 판정·회복

| 이름 | 값 | 근거 | 일치? |
|---|---|---|---|
| `MEASURED_WARMUP_S` | 180.0 | 개장 직후 축출 3건 실측(07-31, 08-03, 08-04) | ✅ 유지 |
| `MEASURED_SUSTAIN_S` | 60.0 | 한 번 튀는 것으로 안 깎음. 경과시간 기준(감사 H-4) | ✅ 유지 |
| `SHRINK_TO` | 0.9 | 경계에 붙이면 곧바로 재초과 | ✅ |
| `SHRINK_COOLDOWN_S` | 30.0 | 플래핑 방지 | ✅ |
| `RATE_LIMITED_SHRINK_FRAC` | 0.10 | 0.2 는 과했다(300→76 실측) | ✅ |
| `RECOVER_AFTER_S` / `_STEP_FRAC` / `_USAGE_MAX` | 300.0 / 0.25 / 0.70 | 일방통행 래칫 방지 | ✅ `_USAGE_MAX` 를 첨두 기준으로 전환 |

### 호출량을 만드는 값 (버스트의 원천)

| 이름 | 값 | 1초 창 계약과 일치? |
|---|---|---|
| `BATCH_MAX` | 200 | ✅ (배치는 1콜) |
| **`TIER1_BATCH_SPACING_S`** | **1.0 (신규)** | ✅ **8콜 연속 발사를 폄 — 이번 수정** |
| `MAX_BACKFILL_PAGES` | 3 | ⚠️ **연속 발사** — 승격 1건당 CHART 3콜이 몰린다 |
| `RESUME_BACKFILL_MAX_PAGES` | 12 | ⚠️ **연속 발사** — 재시작 직후 심볼당 최대 12콜 |
| `RANKING_TYPES` | 2종 | ⚠️ 연속 2콜 (RANKING 한도 5 대비 작아 실害 낮음) |
| `CANDLE_PAGE` / `TRADES_COUNT` | 200 / 50 | ✅ 응답 크기이지 호출 수 아님 |
| `TAPE_FAST_LANE_MAX` | 4 | ✅ 예산 중립 재배분 |
| `TIER2_ORDERBOOK_HEADROOM` | 0.90 | ✅ **첨두 기준으로 전환함** |
| `TIER2_ORDERBOOK_COOLDOWN_MS` | 5분 | ✅ |
| `SESSION_TIER_SCALE` | 1.0/0.8/0.5/0.4 | ✅ 정원 배수 |
| `BASELINE_REFRESH_SPREAD_S` | 900 | ✅ **이미 분산 적용됨** (좋은 선례) |
| `IDLE_SLEEP_S` / `STATE_SAVE_S` / `TELEMETRY_EVERY_S` | 5 / 60 / 300 | ✅ 호출량 무관 |

`api/` 쪽 표는 W1 몫이라 비워 뒀다 — 합쳐야 전체 그림이 된다.

## (g) CHART 429 (요구 5) — **못 풀었다. 두 가설로 좁혔고, 판별 계측을 심었다.**

**리미터 산술로 "CHART 혼자 자기 한도를 넘었다"는 배제된다**: CHART rate 3.5, 용량 1.05 →
유휴 직후 1초 최대 4.55 < 한도 5. 백필 12콜 연속도 리미터가 3.5/s 로 페이싱하므로
1초 창에서는 4를 넘지 않는다. 즉 **CHART 는 자기 창을 깨지 않았다.** 그렇다면 둘 중 하나다:

- **(A) 귀속 오류** — 진짜 주인은 MARKET_DATA 인데 CHART 로 기록됐다. 내 `_attribute_429` 는
  후보가 애매하면 **호출자 그룹**으로 떨어지므로, MARKET_DATA 429 가 CHART 호출 중에 관측되면
  CHART 로 찍힐 수 있다. (내 코드의 결함 가능성이다.)
- **(B) 서버가 그룹 한도를 공유** — MARKET_DATA(10)와 CHART(5)가 같은 버킷이면, MARKET_DATA
  첨두 8~9 에 CHART 3~4 가 겹쳐 한 초에 12를 넘는다. 이때 429 는 CHART 호출에서 터질 수 있다.

**판별 계측을 넣었다** (`under_own_limit` 이 없으므로 직접 만들었다). 429 순간에 남긴다:

```
HTTP-429-DETAIL group=... caller=... attributed=... peak1s={MARKET_DATA:n, MARKET_DATA_CHART:m, RANKING:k}
                own_within_limit=<bool> md_plus_chart=<n+m> headers={...}
```

**다음 관측에서의 판정 규칙** (그대로 읽으면 된다):

| 관측 | 결론 |
|---|---|
| `own_within_limit=false` | 그 그룹이 진짜 넘겼다 → 리미터 결함 |
| `own_within_limit=true` **이고** `md_plus_chart > 10` | **(B) 공유 한도** 유력 |
| `own_within_limit=true` **이고** `md_plus_chart <= 10` | **(A) 귀속 오류** 유력 |

텔레메트리에 `http_429_under_own_limit` 를 실었다 — 0 이 아니면 우리 한도 모델이 틀린 것이다.
(B)로 판명되면 예산 분할을 그룹별이 아니라 **패밀리 합산**으로 바꿔야 하고, 그것은 이 모듈의
`rates()` 구조 변경이라 별도 작업이다.

## (h) 회귀 테스트 (신규 12건)

| 테스트 | 무엇을 막나 |
|---|---|
| **`test_a_low_average_can_hide_a_one_second_burst`** | **★ 필수 — 평균 7.5 뒤에 숨은 초당 15회를 드러낸다** |
| `test_an_evenly_spread_stream_is_not_treated_as_a_violation` | 반대 방향 — 고르게 퍼진 트래픽을 위반으로 오인하는 것 |
| `test_peak_is_measured_sliding_so_a_burst_across_the_boundary_still_counts` | 정렬 버킷이 경계 버스트를 반으로 쪼개 숨기는 것 |
| `test_shrink_now_follows_the_one_second_peak_not_the_average` | 판정 근거가 평균으로 되돌아가는 것 |
| `test_a_single_burst_does_not_shrink_without_persistence` | 버스트 한 번에 정원을 깎는 것 |
| `test_open_warmup_still_suppresses_peak_based_shrink` | 개장 워밍업이 전환 과정에서 사라지는 것 |
| `test_exceeding_the_declared_limit_alerts_once_per_episode` | 한 버스트가 수십 건 경보로 부풀어 경보 무시를 만드는 것 |
| `test_grow_is_blocked_by_a_high_peak_even_when_the_average_is_low` | 버스트 도중에 정원을 되돌리는 것 |
| **`test_usage_ratio_above_what_the_limiter_can_honour_is_flagged`** | **★ 0.769 의 정체를 산술로 고정 — 하드캡 없이 인상되는 것** |
| **`test_tier1_batches_are_spread_so_they_do_not_burst_in_one_second`** | **★ 8콜 연속 발사의 부활** |
| `test_spreading_does_not_overrun_the_sweep_period` | 분산이 주기를 잡아먹는 것 |
| (기존 `test_tier2_orderbook_yields_first_under_budget_pressure` 갱신) | tier2 호가 양보가 평균 기준으로 되돌아가는 것 |

## (i) 남은 것 / 넘길 것

1. **W1 1단계 실물** — 하드캡이 들어오면 usage_ratio 0.85 로 올리는 것은 상수 한 줄이다
   (`check_usage_ratio` 가 자동 검증한다). 그 전에는 올리면 안 된다.
2. **백필 연속 발사** (`MAX_BACKFILL_PAGES=3`, `RESUME_BACKFILL_MAX_PAGES=12`) — tier1 과 같은
   방식으로 펼 수 있으나, 백필은 **승격 직후 빨리 끝나야 값어치가 있는** 호출이라 분산이
   곧 지연이다. tier1(45초 주기, 급하지 않음)과 성격이 다르므로 이번엔 손대지 않았다.
   하드캡이 들어오면 리미터가 알아서 페이싱하므로 그때 재평가하는 것이 맞다.
3. **CHART 429 판정** — 다음 라이브 세션의 `HTTP-429-DETAIL` 한 줄이면 (A)/(B)가 갈린다.
