# W4 보고 — 대량 백필 러너 (task_e99983f09016)

브랜치 `w4-collector`, main(0ec56f5) 위 단일 커밋. mock 전용 검증 (`TOSS_LIVE=0`),
라이브 호출·토큰 발급·`api_keys` 읽기·W5 DB 접근 전부 0건.

## (a) 무엇을 만들었나

`tools/backfill.py` — 사전등록 §2.8 을 그대로 집행하는 대량 과거 백필 러너.

- **스크린 (얼린 값)**: 일봉(수정주가, `adjusted=true`) 직전 20 매매일 통계로
  ① 종가 수익률 ≥ +15% ② 로그거래량 z ≥ 3 ③ 레인지(H/L−1) ≥ 20%.
  비율 판정은 마이크로달러 **정수 교차곱**으로 한다 — float 나눗셈은 정확히 +15.0% 인 날을
  1 ULP 오차로 떨어뜨린다(경계 테스트가 실제로 이 결함을 잡아서 고쳤다).
- **매매일 창**: `/market-calendar/US` 를 역·순방향으로 걸어(캐시, 비진행 가드)
  [D−25, D+2] **매매일** 경계를 세션 ms 로 확정. 겹치는 창은 병합. 달력이 모자라면
  클램프 사유(`calendar_short_back/fwd`)가 매니페스트에 남는다.
- **백필**: 후보 창 1분봉을 **원주가(`adjusted=false`) 로만** 조회(W1 client+limiter
  경유, 우회 없음), `before` inclusive(함정3)는 −1ms 로 처리, 무체결 분 부재(함정5)는
  오류가 아니다. 보관 깊이는 가정하지 않고 **프로브** — 페이지네이션이 실제로 닿은
  가장 오래된 봉과 `retention_exhausted` 여부를 기록.
- **매니페스트 (§8 산출물 1)**: JSON+CSV. 심볼별 실보관 깊이(프로브), 요청·수집 구간,
  **`adjusted=false` 호출 파라미터 증거(§7-e 안전망 ①)**, 디렉토리 스냅샷 날짜
  (symbols 테이블 updated_ms), 스크린 판정 근거 카운트, 실패·스킵 사유별 카운트,
  실행 커밋 해시.
- **멱등 재시작**: checkpoint.json 에 스크린 결과·창 진행·달력 캐시. 재실행 시 완료
  스크린/창은 호출 0회로 건너뛰고, partial 창은 받은 지점 아래부터 잇는다.
- **홀드아웃 (§6.2)**: 수집·저장은 전 구간, 그러나 홀드아웃(2026-05-01~07-29) 매매일의
  판정 근거·수치는 stdout·매니페스트 어디에도 없다 — 후보 엔트리는 {date, withheld}
  뿐이고 근거 카운트는 훈련·검증만 집계, 홀드아웃은 일수만 센다.
- **`--estimate`**: 호출 0회로 총 호출수·예산(한도×usage_ratio) 하 소요 시간 산출.
  스크린 체크포인트가 있으면 **실측 창 기준(정확)**, 없으면 가정(가정값 명시) 기준.
  일봉 스크린·달력 걷기 호출량도 계상(요구 7).
- **`--screen-only`**: 스크린+달력+창 계획까지만(1분봉 0회) — 이후 `--estimate` 가
  정확해진다. 내일 아침 2단계 실행 절차의 핵심.
- **라이브 이중 잠금**: 설정이 `live:true` 여도 `--allow-live` 없이는 기동 거부.

## (b) 검증 (오늘 밤, 전부 mock)

- 신규 테스트 16건 (`tests/test_backfill_runner.py` 15 + `tests/test_backfill_e2e.py` 1):
  - 스크린 경계값 3종이 **정확히 그 값에서** 갈림(+15%/z3/20% — 경계 포함).
  - [D−25, D+2] 매매일 산정: 주말·연말 걸침 28 매매일, 겨울 UTC 자정 넘김은 세션 ms 로
    판정, 달력 부족 클램프.
  - `adjusted=false` 강제: 상수를 수정주가로 바꾸면 **한 호출도 나가기 전에 기동 거부**
    (RuntimeError), FakeClient·전선(wire) 양쪽에서 파라미터 확인.
  - 멱등 재실행(2차 실행 호출 0회·행 수 불변), before inclusive 경계 이중 계상 없음
    (매니페스트 봉 수 == DB 행 수), 보관 부족 시 partial+프로브 기록, 홀드아웃 마스킹.
  - E2E: 실제 HTTP(tools/mock_server.py, 픽스처 런타임 주입) 로 스크린→후보→백필→
    매니페스트 전 과정 + 쿼리스트링 `adjusted=false` 전선 검증 + `--estimate` 가
    HTTP 트래픽 0건임을 스파이로 확인.
- 전체 스위트 811 passed, 1 skipped (main 795+1 대비 신규 16건 전부 포함).

## (c) 이견·해석 확정 (사전등록 문언이 비운 세부 — 코드가 정본)

1. z 통계 표본: 직전 20 매매일 중 **양의 거래량** 일봉의 로그. 표본 <2 또는 표준편차 0
   이면 z 판정 불가(통과 아님)로 세고 카운트 보고 — "데이터 없을수록 보수적" 관례.
2. 스크린의 ret 는 직전 **일봉** 종가 대비(그 심볼의 직전 거래일). 첫 봉은 판정 불가 카운트.
3. 창 시작에 달력이 못 닿으면(보관 초과) 창 자체는 §2.8 그대로 두고 커버리지 갭+클램프
   사유로 기록 — 문언 변경 없음.
4. 페이지 중 창 밖(더 옛날) 봉도 실데이터이므로 저장은 하되(멱등 upsert), 매니페스트
   봉 수는 창 내부만 센다.
5. 홀드아웃 후보의 존재(날짜)는 창 정의상 감출 수 없다 — 근거·수치만 차단(커버리지 허용
   문언에 부합). 이상이 §6.2 위반이라고 판단되면 알려달라, 날짜까지 마스킹으로 바꾼다.

## (d) 파일·커밋

- 신규: `tools/backfill.py`, `tests/test_backfill_runner.py`, `tests/test_backfill_e2e.py`
  (이번 태스크로 부여된 소유권 안). 기존 파일 수정 0건.
- diff 증명: `git diff --name-only $(git merge-base main HEAD)..HEAD` → 위 3개 + 보고서.

## (e) 내일 라이브 실행 절차 (08:50 W5 종료 후 — 코디네이터 승인 필요)

리스 취득: 별도 절차 없음 — W1 TokenManager 가 자격증명별 OS 파일락으로 리스를
자동 취득한다. W5 가 아직 안 놓았으면 "token lease already held" 로 **큰 소리로 실패**
하므로 그냥 실행해 보면 된다(성공=리스 확보).

명령 (w4 워크트리, 저장 DB 는 코디네이터가 지정 — W5 수집 DB 와 분리 권장):

    1) .venv\Scripts\python tools/backfill.py --config <라이브 config.yaml> --allow-live
         --db data/backfill.db --screen-only
       (env: TOSS_LIVE=1. 스크린+달력만. 유니버스는 symbols 테이블 기본,
        비면 --symbols CSV)
    2) .venv\Scripts\python tools/backfill.py --config <...> --allow-live
         --db data/backfill.db --estimate
       (호출 0회 — 실측 창 기준 정확한 1분봉 호출수·시간표 출력 → go/no-go)
    3) .venv\Scripts\python tools/backfill.py --config <...> --allow-live --db data/backfill.db
       (본실행. 중단돼도 재실행하면 이어받는다. 완료 시 backfill_manifest.json/csv)

예상 호출량 (예산: CHART 5×0.7=3.5 req/s, MARKET_INFO 3×0.7=2.1 req/s 기준 공식):

- 달력 걷기: 매매일당 ~1콜, 550 매매일 ≈ 580콜 ≈ **5분** (1회, 캐시됨).
- 일봉 스크린: 심볼당 ceil(보관일/200) ≈ 3콜. 유니버스 1,000심볼이면 3,000콜 ≈ **15분**,
  3,000심볼이면 9,000콜 ≈ **43분**.
- 1분봉 백필: 후보 창(28 매매일)당 — 봉밀도 150/400/1140 봉·일 시나리오로
  21/56/160콜 ≈ 6/16/46초. 후보 500창이면 **50분/2.2시간/6.3시간**.
  정확한 값은 2)의 --estimate 가 실측 창으로 낸다 — **1)~2)에 먼저 ~20-50분을 쓰고
  그 수치로 판단하는 것이 절차다.** 소형주 실보관(~320일)이 창을 자르므로 상한
  시나리오보다 실제는 작게 나온다.

주의: ① 스크린 종료일 상한이 필요하면 --to YYYY-MM-DD (기본: 일봉 전체).
② 실행 로그·매니페스트는 홀드아웃 수치를 내지 않는다 — 열람으로 치지 않는다(§6.2).
③ 중간 429/오류는 limiter+재시도가 처리, 창 단위 partial 은 재실행이 잇는다.
④ 완료 후 매니페스트(json/csv)와 checkpoint.json 을 분석 계층(W3/W7)에 인계.
