# toss_trade

> **독자는 이 레포에 새로 들어온 에이전트 세션이다.** 이 파일은 3 분짜리 정문이다.
> 갱신: 2026-08-13 · 기준 리비전 `a868e1a`

---

## 이게 뭔가 — 한 문장

> **"토스 커뮤니티/급상승 랭킹으로 인해 작전/동전/급등주로의 개미 유동성 몰림이 매일
> 발생하고, 우리는 스크립트 레벨에서 인간보다 빠르게 행동해 그 유동성을 발라먹는다."**
>
> — 사용자, 전략 골격. **정본이다.** 워커 명세는 이 문장을 인용한다.
> 출처: `coordination/USER-INPUT-QUEUE.md` D-6 (2026-08-04)

대상 시장은 **미국 소형주·동전주**, 매매 창구는 **토스증권 Open API**.
`docs/16_strategy_design_notes.md` 가 전략 방향의 정본이다.

## 지금 어느 단계인가

**실매매 코드는 존재하지 않는다.** 주문 계열은 래퍼조차 없고 HTTP 레벨에서 차단돼 있다
(계약 C-11). 지금 하는 일은 **고해상도 실시간 수집을 믿을 만하게 만드는 것** 하나다.

~~**분석은 멈춰 있다.**~~ → **2026-08-13 에 다시 열렸다** (사용자 지시). 가설이 고갈된
게 아니라 **재시작의 토대를 쌓는 중**이었다 — 모든 전략 후보가 죽은 원인이 *1 분봉이라는
낮은 해상도로 판단했기 때문*이라고 사용자가 판단했고(D-6), 그래서 처음부터 다시
시작하기로 했다.

> ### ★ 그런데 첫 삽에서 **잴 상대가 없다**는 것이 나왔다 (`docs/59`, 2026-08-13)
>
> 랭킹 사건 **1,437,699 건 중 300 초 안에 체결이 있는 것은 1.78%** 뿐이다. 원인은
> 시장이 아니라 **우리 수집 범위**다 — **티어 승격이 랭킹과 연동이 없어서 랭킹 top100 에
> 오른 종목의 71~99% 가 테이프에 아예 없다.** 표적 층($0~5)에서 잴 수 있는 사건은
> **최대 166 건**이다.
>
> **그래서 지금 하는 일은 "알파를 재는 것" 이 아니라 "잴 수 있게 만드는 것" 이다** —
> 사용자 결정 **D-21**(랭킹 진입을 승격 사유로 추가). 범위를 넓힌 뒤 `docs/59` 를 다시
> 돌려 확인하고, **그 다음에야** 전방 수익·대조군·비용 차감으로 넘어간다.

착수 관문은 `docs/58_gate.md` — **1 번 관문이 "우리가 정말 인간보다 빠른가" 다.**
(`docs/03` §3 의 옛 검증 질문 6 개는 2026-08-13 에 폐기됐다. 인용하지 마라.)

## 처음 3 분에 읽을 것 (이 순서로)

1. **`docs/INDEX.md`** — `docs/` **60 개** 전수 목록. **어느 문서가 현행이고 어느 것이
   그날의 기록인지** 여기서만 알 수 있다
2. **`coordination/COORDINATOR-STATE.md`** — 지금 무엇이 돌고 있고 무엇이 깨져 있나
3. **`coordination/USER-INPUT-QUEUE.md`** — 사용자 결정 대기. **추측으로 메우지 말고
   여기 쌓는다**
4. **`docs/04_contracts.md`** — 계약. 상충하면 이게 이긴다

## 절대 하면 안 되는 것

**운영**
- **라이브 API 호출은 수집기만.** 확인하려고 요청 쏘는 것도 금지 — 그 자체가 한도를 먹는다
- **가동 중 수집기를 직접 죽이거나 띄우지 마라.** `STOP` 파일 + 워치독 경로만
- 무인 프로세스는 **schtasks 로만**. `schtasks /run` 을 에이전트 세션에서 실행 금지
  (Ctrl+C 가 전파돼 같이 죽는다)
- **라이브 워크트리는 `w5-ops` 다** (`C:\Users\dongh\orca\workspaces\toss_trade\w5-ops`).
  **main 병합만으로는 배포가 아니다** — fast-forward 하고 **파일로 확인**해라
- **되돌릴 수 없는 수집 변경은 사용자 결정.** 지나간 랭킹·호가는 다시 못 받는다

**데이터·판정**
- 홀드아웃 **2026-05-01 ~ 07-29 열람 금지**
- 검증 카드 5 장 소각 금지 (현재 **0/5**)
- **과거 판정 재실행 금지** (D-10, 사용자: *"고치고 다시 돌리진 마"*)
- 사전등록 개정·카드 소각·홀드아웃 열람·전략 방향 전환은 **코디네이터 단독 금지**

**코드**
- **`monotonic` 값이 로그·경보·DB 로 새어나가면 안 된다** (원점이 프로세스마다 다르고
  재시작마다 리셋된다)
- **원인 모른 채 경보를 끄거나 문턱을 올리는 것 금지**
- **콘솔 출력에 비 ASCII 를 넣지 마라.** cp949 콘솔에서 `UnicodeEncodeError` 가 나고,
  실제로 그것 때문에 supervisor 가 죽어 collector 가 고아 프로세스로 남은 적이 있다

## 문서를 쓸 때의 규칙 (넷 다 비싸게 배웠다)

1. **문서 번호를 바꾸지 마라.** `docs/NN` 인용이 **코드에 822 줄 · 문서에 2,190 건**이고
   대부분 파일명 없이 번호만 적는다
2. **요약표를 근거로 훑지 마라. 원문 파일을 열어라.** 표가 틀린 물건일 수 있다 —
   실제로 그런 일이 있었고 표를 받아 파일을 실제로 연 워커가 잡았다
3. **부재 주장에는 리비전을 함께 적어라.** *"~라고 안 적혀 있다"* 는 읽은 판에
   전적으로 의존한다. 인용은 낡은 체크아웃에서도 살아남지만 부재는 아니다
4. **취소선 + 정정 병기.** 원문을 지우면 *"이 절이 무엇에 대한 반응이었는지"* 가 사라진다.
   **다만 정정 절 위에 정정 절을 쌓지 마라** — 어느 것이 현행인지 못 고르게 된다

## 확인된 현재 상태

이 절의 수치는 **이 세션에서 직접 실행해 본 것만** 적는다.

| 항목 | 값 | 확인 방법 |
|---|---|---|
| 테스트 | **2,113 passed · 1 skipped · 4 deselected** (366.24s, exit 0) | `.venv/Scripts/python.exe -m pytest -q`, 2026-08-13 저녁 `7b91ff9` 에서 직접 실행 |
| 수집 중인 랭킹 | 거래량 2 종 + `TOP_GAINERS`(1d) — **금액 2 종은 2026-08-04 11:22 KST 이후 안 들어옴** | `w5-ops/data/tossmon.db` `mode=ro` 질의 |
| 마지막 수집 | 2026-08-13 12:59 KST | 같은 질의 |

## 알려진 열린 결함

| | 누가 고칠 수 있나 |
|---|---|
| **스케줄러 `LogonType=Interactive`** — 콘솔 피살 **+ 부팅 후 로그인 전까지 수집이 안 산다** | **사용자만** — `Set-ScheduledTask` 가 관리자 권한을 요구한다. **2026-08-14 에 처음 비용이 측정됐다 (아래)** |
| ~~텔레메트리 `limit_header_clamped` 가 로그와 어긋남~~ | ✅ **닫힘 (2026-08-13, `cbc3b08`)** — 어긋난 적이 없었다. 진짜 결함은 `counter_scope` 오선언 (`docs/56` §9) |
| ~~`srv_s_unknown` 이 0 이 아니다~~ | ✅ **닫힘 (2026-08-13, `7b91ff9`)** — **사각지대는 429 다.** 크기는 프로세스당 최대 23·중앙 12, 정산 서버 초의 0.18%. **0 으로 안 만들었다** — 429 잔량 불신은 옳은 규칙이고 그 대가다. 다만 **무작위가 아니다**: 서버가 우리를 거절한 초에 몰려 있고 그 초에 `foreign` 이 구조적으로 0 이라 경보 넷이 조용해진다 (`docs/52` §13) |
| `rankings_snap` 보존정책 없음 (단조 증가) | 미결. **DB 바이트의 79.8%** (`docs/60` 실측). `ranking_type`·`duration` 정수화로 41.7% 회수 가능(비가역 아님) |
| 서버 20/s 의 정체 (사양 상향인가 헤더 의미 변경인가) | 미규명 — 모르면 천장을 못 올린다. **`MARKET_DATA` 도 15>10 으로 잘리는 중**이고 분당 쿼터 가설과 안 맞는다 (`docs/56` §9) |
| **랭킹 종목의 71~99% 가 테이프에 없다** | **원인 규명됨** — 티어 승격이 랭킹과 연동이 없다 (`docs/59`). 고치는 중 (D-21) |
| **29% 유실·16.1 초 지연의 원인** | **미규명.** 둘 다 **1 초 팔**에서 잰 값이라 우리 폴 주기의 함수가 아니다 (`docs/58` §1-1 정정). 정규장 재측정 대기 |

## ★ 2026-08-14 — `LogonType=Interactive` 의 값이 처음 측정됐다 (3 시간 38 분)

**윈도우 업데이트가 기계를 재부팅했고, 기계는 살아났는데 수집기는 3 시간 38 분 동안
죽어 있었다.** 소프트웨어 결함이 아니다 — **작업이 로그인 전에는 안 돈다.**

```
05:19:06  MoUsoCoreWorker.exe (Windows Update) 가 재시작 개시   [이벤트 1074]
05:22~25  TrustedInstaller 재시작 3 회
05:25:06  최종 부팅 — 이후 기계는 계속 켜져 있었다 (uptime 확인)
05:20~09:03  워치독 주기 0 회.  ← 로그인 전이라 예약작업이 안 돈다
09:03:03  [sentinel] ALERT[CRIT] watchdog_silent      ← 사용자 로그인 직후
09:03:07  [watchdog] watch_process_dead → RESTART outcome=OK
09:03:20  수집기 재개
```

**자가 복구 3 층은 설계대로 작동했다** — 로그인한 뒤 **1 분 안에** 센티널이 워치독 침묵을
잡고, 워치독이 죽은 프로세스를 잡아 되살렸다. 문제는 **그 층이 로그인 전에는 안 돈다**는 것.

**오늘은 운이 좋았다.** 재부팅이 **05:19**, 미국 정규장 마감(05:00 KST) **19 분 뒤**였다.
2026-08-13 정규장은 온전하다 (22:30:04~04:59:51 KST, 랭킹 스냅 5,511 개, **2 분 넘는 공백 0 건**).
**다음 윈도우 업데이트가 정규장 한가운데 떨어지면 그 세션을 통째로 잃는다.**

**고치는 법 (관리자 PowerShell 필요 — 코디네이터는 실행 불가)**

```powershell
# 로그인 여부와 무관하게 돌게 한다 (S4U = 비밀번호 저장 없이)
foreach ($t in 'tossmon-watchdog','tossmon-sentinel','tossmon-logrotate','tossmon-dailyhealth') {
  Set-ScheduledTask -TaskName $t -Principal (New-ScheduledTaskPrincipal `
    -UserId $env:USERNAME -LogonType S4U -RunLevel Limited)
}
Get-ScheduledTask | ? TaskName -like '*toss*' |
  % { [PSCustomObject]@{ Name=$_.TaskName; Logon=$_.Principal.LogonType } }
```

## 디렉터리

```
docs/          번호가 곧 식별자다. 목록은 docs/INDEX.md
coordination/  코디네이터 상태·사용자 결정 큐·워커 명세
tossmon/       api / store / universe / analysis / collector
ops/           supervisor · healthcheck · watchdog · 스케줄러 등록
tools/         mock 서버 · 라이브 프로브 · 리플레이 · 각종 계측
tests/         2,061건
```
