# W4 — usage_ratio 0.85 적용 + 여유 배분안 + tape_gaps 규명 (task_7363b99a3be2)

브랜치 `w4-collector`. 라이브 호출 0건, 가동 중 수집기 무접촉(로그 **읽기만** 함). `api/` 무수정.

---

## (a) 적용할 값 — 문자 그대로

```yaml
api:
  usage_ratio: 0.85          # 0.7 -> 0.85
```

**예상 plan 합계 (변경 없음 — 정원·주기는 그대로 두었다)**

| 그룹 | 계획 req/s | target | 축소 천장 | 계획 천장 | 계획 여유 |
|---|---:|---:|---:|---:|---:|
| MARKET_DATA | **5.178** | 8.500 | 8.075 | 7.225 | **+2.047** |
| MARKET_DATA_CHART | **2.727** | 4.250 | 4.037 | 3.612 | **+0.885** |
| RANKING | **0.167** | 4.250 | 4.037 | 3.612 | +3.446 |

`config.example.yaml` 의 두 예산 주석 블록도 같은 숫자로 갱신했다.

### 검증이 실제로 통과하는가 — **통과한다**

```
has_window_hardcap()      True            (limiter.WINDOW_HORIZON_S 존재)
max_safe_usage_ratio()    0.8696          (= 1 / 1.15)
check_usage_ratio()       None            <- 경보 없이 통과
worst_case_1s(MARKET_DATA) 10.0 <= 한도 10  <- 최악의 초가 한도를 안 넘는다
```

**`max_safe_usage_ratio()` 의 의미가 하드캡 전후로 바뀌었다.** 이걸 고치지 않았으면 0.85 가
내 경보에 막혔을 것이다:

- 하드캡 **전** — 상한은 **429 안전선**이었다. `1/(1+BURST_FRACTION)` = **0.769**.
- 하드캡 **후** — 한도 초과가 구조적으로 불가능해졌으므로 상한의 의미가 **"리미터가 낼 수 있는
  지속 속도"** 로 바뀐다. `window_cap / WINDOW_HORIZON_S` = `1/1.15` = **0.870**.
  이 위로 올리면 429 가 아니라 **하드캡이 병목이 되어 호출이 큐에 밀린다** — 폴링 주기가 조용히
  늘어나 tier3 4초가 4초가 아니게 된다. 경보 문구도 두 경우를 구분해 쓴다.

즉 **0.85 가 안전한 이유는 버스트가 작아서가 아니라 하드캡이 창을 지키기 때문**이고,
하드캡이 빠지면 이 값은 자동으로 0.769 로 되돌아가 0.85 를 도로 막는다.

## (b) 하드캡 의존성을 테스트가 드러낸다 (요구사항)

`test_usage_ratio_085_is_safe_ONLY_because_the_hardcap_exists`

| | 상한 | 최악의 초 | 0.85 판정 |
|---|---:|---:|---|
| 하드캡 **있음**(지금) | 0.870 | **10.0** ≤ 10 | 통과 |
| 하드캡 **없음**(모사) | 0.769 | **11.05** > 10 | **차단 + 경보** |

테스트가 `limiter.WINDOW_HORIZON_S` 를 실제로 제거해 두 번째 절반을 실행한다.
**하드캡이 사라지면 이 테스트가 실패한다** — 의존성이 코드가 아니라 테스트에 박혀 있다.

## (c) ★ tape_gaps=622 — **호가 때문이 아니다. 전제가 성립하지 않는다.**

내가 심어둔 `config_sig` 로 경계를 특정했다: **호가 16s → 4s 는 2026-08-04 11:22:42**.

```
2026-08-04 11:22:42 COLLECTION-CONFIG start sig=...,t3max10,tr4s,ob4s,...
```

**`tape_gaps` 는 07-31 14:25 에 0 에서 시작한 누적 카운터이고, 재시작해도 리셋되지 않는다**
(상태 파일에서 복원된다 — 08-01 07:11 재시작 전후 모두 137 로 확인). 622 는 "지금 새는 양"이
아니라 **나흘치 총합**이다. 오늘 하루 증가분은 11건이다.

| 구간 | 호가 | 결손 | 시간 | **분당** | tier3 평균 |
|---|---|---:|---:|---:|---:|
| 08-04 09:00–11:22 | 16s | 9 | 141분 | 0.064 | 3.6 |
| 08-04 11:22–13:20 | **4s** | 11 | 117분 | **0.094** | 3.2 |
| 08-03 09:00–13:25 (대조) | 16s | 4 | 262분 | 0.015 | 1.0 |
| **08-03 정규장** | **16s** | **176** | 387분 | **0.455** | **9.2** |
| 08-02 정규장 | 16s | 0 | 385분 | 0.000 | 0.0 |

**결정적 반증: 08-03 정규장은 호가가 16초인데도 분당 0.455 로 지금의 5배다.** 그때 tier3 평균이
9.2 였다. tier3 가 0 이던 08-02 정규장은 결손도 0 이다. 즉 **결손은 호가 주기가 아니라
(tier3 인원 × 장 활동)을 따라간다** — 폴링 횟수가 많을수록 결손을 관측할 기회도 많다.

경계 전후 0.064 → 0.094 는 있지만 (i) 같은 세션 안 시간대 차이, (ii) 대조군이 그 5배,
(iii) `tape_saturated=0` (50건 상한에 안 걸리고 있다) 를 고려하면 **호가 때문이라고 볼 수 없다.**
게다가 그 변경으로 MARKET_DATA 부하는 **줄었다**: 20종목/호가16s = 6.428 → 10종목/호가4s = 5.178.

**스케줄러 경합도 시뮬레이션으로 배제했다.** `run_tier3_micro` 를 그대로 모사(직렬 await +
완료시각 기준 재예약)해 보면 체결 실간격 중앙값이 호가 16s/4s/2.5s 전부 **4.071초**로 같다
(= 4초 + RTT 1회). 직렬 RTT 점유율이 현재 36.8% 라 여유가 충분해 큐가 밀리지 않는다.

> **따라서 배분표에 "호가를 당기면 체결이 샌다" 는 넣지 않았다 — 근거가 없다.**
> 대신 진짜 상충이 하나 있고, 그것은 반대 방향이다: **체결 주기를 당기면 결손이 실제로 준다.**
> `TRADES_COUNT=50` 이 폴당 상한이라 수용력이 `50/주기` 다 (4초 → 초당 12.5건).

## (d) 여유 배분안 — usage_ratio 0.85 기준

target 8.500 / 축소 천장 8.075 / **계획 천장 7.225**. tier1 0.178 고정.

| 안 | tier3 | 체결 | 호가 | req/s | 계획여유 | 체결수용 | 얻는 것 | 잃는 것 |
|---|---:|---:|---:|---:|---:|---:|---|---|
| 현행 | 10 | 4s | 4s | 5.178 | +2.047 | 12.5건/s | — | 여유가 놀고 있다 |
| **A 종목 14** | 14 | 4s | 4s | 7.178 | +0.047 | 12.5건/s | **(종목,날짜) 표본 +40%** — 지금 52개라 통계의 병목 | 여유가 거의 0. 계획 밖 호출 한 번에 트리거를 건드린다 |
| **A- 종목 12** | 12 | 4s | 4s | 6.178 | +1.047 | 12.5건/s | 표본 +20%, 여유도 유지 | 밀도는 그대로 |
| **B 호가 2.5s** | 10 | 4s | 2.5s | 6.678 | +0.547 | 12.5건/s | 매수/매도 판정오차 **0.604 를 더 축소** (호가 나이 2.0s → 1.25s) | 표본·틱밀도는 그대로 |
| **C 체결 2.5s** | 10 | 2.5s | 4s | 6.678 | +0.547 | **20.0건/s** | **틱 밀도 직접 상승**(사용자가 원하는 것) + **결손 수용력 +60%** | 호가가 상대적으로 낡는다(판정오차 소폭 악화) |
| **D 12종 + 호가 3s** | 12 | 4s | 3s | 7.178 | +0.047 | 12.5건/s | 표본 +20% & 판정오차 축소 | 여유 거의 0 |
| **E 12종 + 체결 3s** | 12 | 3s | 4s | 7.178 | +0.047 | 16.7건/s | 표본 +20% & 틱 밀도 상승 | 여유 거의 0 |

**내 의견 (결정은 사용자):** **C 또는 E**.
사용자가 원하는 것이 틱 밀도이고, 체결 주기 단축만이 `TRADES_COUNT=50` 수용력을 함께 올려
결손을 실제로 줄인다 — 다른 안은 결손에 아무 영향이 없다. 여유를 지키려면 **C**(+0.547),
표본까지 같이 늘리려면 **E**(+0.047, 다만 여유가 거의 없다).
**A(14종목)는 권하지 않는다** — 여유 +0.047 은 승격 직후 백필 한 번에 사라진다.

## (e) 부수 발견 — 이번에 고친 것과 넘길 것

### 고쳤다 — 내 계측이 만든 **허위 경보**

```
2026-08-04 13:10:06 ERROR budget: MARKET_DATA 1초에 13회 — 공시 한도 10 초과.
                           ... 리미터가 1초 창을 못 지키고 있다
```

하드캡 배포(12:49) **이후**인데도 났고, 같은 구간 `http_429=0` 이다 — **서버는 13회를 본 적이
없다.** 원인은 내 코드였다: `after_call` 이 재시도 시도들을 `for _ in range(n): on_request()`
로 **한 시각에 몰아** 계상했다. 재시도는 백오프로 떨어져 나가므로 첨두가 가짜로 치솟는다.
`on_requests(group, n)` 를 만들어 **직전 계상 이후 구간에 균등 분포**시켰다 (총량 보존).
가짜 경보는 진짜 경보를 묻는다 — 이 프로젝트에서 반복된 실패라 방치하지 않았다.

### 넘긴다 — 이번 작업 범위 밖이지만 **더 중요해 보인다**

오늘 13:20 텔레메트리:

```
tier3=2  tier3_cap=2   (설정은 tier3_max=10)   budget_shrinks=46
promotions=88792   loop_errors=36210   tier3_capacity_fills=557
```

1. **tier3 가 정원 10 중 2~4 로만 돈다.** `md_peak_1s=7` 인데 0.7 기준 축소 천장이 6.65 라
   내 첨두 기반 축소가 계속 걸린 것이다. **0.85 로 올리면 천장이 8.075 가 되어 첨두 7 은
   더 이상 축소를 부르지 않는다** — 이번 변경이 이 문제를 직접 푼다(그래서 값어치가 크다).
   다만 `budget_shrinks=46` 이 이미 쌓여 있어 **정원 복구는 `should_grow` 의 회복 스텝을
   기다려야 한다**(300초 간격, 25%씩). 배포 후 정원이 실제로 10 으로 돌아오는지 확인이 필요하다.
2. **`promotions=88792`, `loop_errors=36210`** — 승격 8.8만 건은 정상적인 수치가 아니고
   (티어 플래핑 의심), loop_errors 3.6만도 조용히 쌓이고 있다. 둘 다 이번 태스크 범위 밖이라
   손대지 않았지만 별도 진단이 필요하다고 본다.
