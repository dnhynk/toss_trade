# W1 — 레이트리밋 실제 계약 확정 + 리미터 정렬 (1단계)

커밋: `38dec1b` · 테스트: `1074 passed, 1 skipped` · 근거 원본: `docs/06_live_facts.md` §9-3~§9-9

---

## 0. 요약 — W4 가 예산 모델을 다시 지을 때 쓸 3줄

1. 서버는 **그룹마다**, **벽시계 초에 정렬된 고정 1초 창**으로, 창당 `limit` 개를 센다.
   그룹끼리는 독립이다. → **예산은 그룹 단위로 나눈다.**
2. 그 창은 경계에서 2×limit 이 통과한다(실측). 그래서 리미터에 **어떤 1초 구간에도 한도를
   넘지 않는 하드캡**을 넣었다. 이제 한 프로세스 안에서는 초과가 **구조적으로 불가능**하다.
3. `usage_ratio` 는 0.87 까지 안전해졌다(옛 구조의 상한은 0.769였다 — 0.7은 우연히 안전했다).
   0.85 면 MARKET_DATA 7.0 → 8.5 req/s(**+21%**). **값은 바꾸지 않았다** — 예산 결정은 W4 2단계.

---

## 1. 먼저 고친 것: 계측이 엉뚱한 응답을 찍고 있었다

지시대로 이 결함부터 잡았다. 지적이 정확했다.

```
11:10:12 WARNING HTTP-429-DETAIL group=MARKET_DATA status=200 headers={... remaining: 9 ...}
```

`client.last_headers` 는 "마지막 응답"이라, 429 는 client 안에서 1회 재시도되고 그 재시도가
성공하면 기록은 **항상 200 쪽**이 된다. 운영 로그 11줄 중 5줄이 그래서 `status=200` 이었다.

**정정**: 429 를 받은 **그 자리에서** 찍는다. 이후 어떤 응답도 덮어쓰지 못한다.

| 새 필드 | 무엇을 답하는가 |
|---|---|
| `client.last_429`, `recent_429s` | 그 429 의 상태·레이트리밋 헤더·본문 error code |
| `RateLimited.evidence` | 예외로 빠져나가는 경로에서도 근거가 함께 올라간다 |
| `own_requests_in_that_server_second` | **그 서버 초에 우리가 몇 발 쐈나** |
| `under_own_limit` | 위 값이 한도 미만인데 429 → **원인이 우리 밖** |
| `counters["http_429_no_retry_after"]` | Retry-After 없는 429 빈도 |

회귀 테스트 `tests/test_api_ratelimit_contract.py` 가 **429 다음에 200 을 연달아 주고**
기록된 것이 429 쪽인지 검증한다(지시한 그대로). 13개 테스트.

실증: 라이브 429 를 client 경로로 받아 기록을 확인했다
(`tests/fixtures/live/live_429_client_record.json`):

```
status=429  limit:3  remaining:0  reset:1  retry-after:1
error_code = rate-limit-exceeded
own_requests_in_that_server_second = 4  (한도 3 초과 → under_own_limit=false, 우리 탓)
```

---

## 2. 확정한 계약

| 질문 | 답 | 판정 |
|---|---|---|
| 고정 1초인가 슬라이딩인가 | **벽시계 초에 정렬된 고정 1초** | `확인됨` |
| 토큰버킷인가 | 아니다 | `확인됨` |
| 한도 범위 | **그룹 공유**, 그룹끼리 독립 | `확인됨` |
| Retry-After 는 오는가 | **올 때도 안 올 때도 있다** | `부분확인` |
| 우리가 지키는가 | 지킨다. 없으면 1.0초(고정 1초 창이라 반드시 다음 창) | `확인됨` |

**결정적 근거** — MARKET_INFO(한도 3/s)를 0.40초 간격 20발, 서버 초 안의 k 번째 호출이
정확히 `remaining = limit-k`, **20/20 (100%)**:

```
02:56:01  saw3 rem2  saw4 rem1  saw5 rem0
02:56:02  saw6 rem2  saw7 rem1
02:56:03  saw8 rem2  saw9 rem1  saw10 rem0
```

**그룹 공유** (같은 초, 다른 엔드포인트): `/exchange-rate rem2` → `/market-calendar/US rem1`
(순서를 뒤집어도 같음). **그룹 독립**: 직후 `/stocks` 는 자기 `limit5 rem4`.

전 그룹 `x-ratelimit-limit` 이 `SPEC_LIMITS` 와 일치했다 (6개 실측 확인, 불일치 0).
AUTH·ASSET·ORDER_INFO 는 프로브 불가라 **미실측으로 표기**했다(토큰 재발급 금지 / 호출 경로
없음 / 계좌 헤더 필요).

---

## 3. 리미터를 계약에 맞췄다

**경계 2배 통과는 실재한다** — 공시 **3 req/s** 그룹에 **6회를 0.561초 안에 전부 200**
(순간 10.7 req/s). 429 를 내지 않는 양성 대조로 확인했다:

```
02:58:02  pre0 rem2  pre1 rem1  pre2 rem0     (0.152초에 3발)
02:58:03  post0 rem2 post1 rem1 post2 rem0    (0.164초에 3발)
```

이건 기회가 아니라 위험이다 — 우리 버킷은 서버 창 위상을 모르므로 **의도치 않게** 이 상태에
빠질 수 있고, 반대편으로 가면 그게 429다.

**조치**: 버킷(평활화) 위에 **슬라이딩 하드캡**(어떤 1초 구간에도 공시 한도 초과 금지)을 얹었다.
슬라이딩 캡은 모든 1초 구간을 덮으므로 **창 위상을 몰라도 안전**하다.
구간 길이 1.15초 = 1.0 + 0.15 — 실측 RTT(min 54.6 / p50 71.0 / max 141.0 ms, n=47)의
편도 지연폭 43ms 의 약 3.5배 여유(우리는 *보낸* 시각을 재고 서버는 *도착* 시각을 센다).

**실서버 검증**: 리미터를 우회하지 않고 오히려 전속력으로 밀었다 —
8초 17콜, 한 서버 초 최대 **2회**(한도 3), 429 **0건**, 초별 `1,2,2,2,2,2,2,2,1,1`.

점검 요청 항목별 답:

| 항목 | 판정 |
|---|---|
| 버킷 창 vs 서버 창 | **어긋나 있었다.** 하드캡으로 막았다 |
| `BURST_FRACTION` 0.3 | 유지. 안전성은 이제 하드캡이 보장하므로 이 값은 평활화 취향이다. 숫자 자체는 근거 없음으로 표기 |
| 빈 버킷 시작 | 유지 (기동 스파이크 한 번 더 차단, 무해) |
| `usage_ratio` 0.7 | **옛 구조의 안전 상한은 1/1.3 = 0.769였다** — 0.8로만 올려도 설계상 한도 초과였고, `_capacity_for` 하한 1.0 때문에 MARKET_INFO(3.1>3)·ACCOUNT(1.7>1)는 0.7에서도 넘고 있었다. 하드캡 도입 후 **상한 0.87**. 0.85 → MARKET_DATA 8.5 req/s(+21%) |

**usage_ratio 는 올리지 않았다.** 수집 예산을 바꾸는 값이고 컬렉터가 가동 중이며, 예산 재설계는
W4 2단계다. 근거와 상한만 남겼다 (`docs/06` §9-8, config 주석).

**여러 루프가 같은 1초에 몰리는 문제 — 판정**:
한 프로세스 안에서는 **리미터가 흡수한다**(그룹별 `asyncio.Lock` + 하드캡이 직렬화).
**프로세스가 여럿이면 못 막는다** — 각자 자기 캡만 지킨다. → W4 요건으로 넘겼다.

---

## 4. 전수 순회 — `docs/06` §9-9

`SPEC_LIMITS` 9개 · 리미터 상수 11개 · 클라이언트 상수 8개 · 설정 4개 · 읽는 곳 7개를
표로 냈다(이름/현재값/근거/계약 일치 여부). 근거 없는 값은 **"근거 없음"으로 명시**했다
(`MAX_BACKOFF`, `RECOVER_FACTOR`, `RECOVER_INTERVAL_S`, `BURST_FRACTION` 등 — 전부 계약과
무관한 감속·평활화 값이라 안전에는 영향 없음).

**계약과 어긋난 곳 (W4 소유라 손대지 않음)**:

1. `collector/budget.py:38` `WINDOW_S = 60.0` — 예산을 **60초 평균**으로 잰다. 계약은 매 1초를
   센다. `CHART=0.50/3.50` 은 60초에 30콜이라는 뜻이고 그 30콜이 한 초에 5개 몰려도 이 지표는
   0.50 그대로다. **평균 예산은 1초 초과를 구조적으로 볼 수 없다.**
2. `collector/loops.py:549` — `last_headers` 를 읽는다. `client.last_429` 로 교체할 것.
3. 예산 분할 단위는 그룹 단위가 맞다(이미 그렇다). 이제 근거가 실측이다.

---

## 5. 아직 못 푼 것 — 정직하게

**2026-08-04 11:30 과 12:01 의 CHART 429 는 이 계약만으로 설명되지 않는다.**
CHART 사용률이 0.50/3.50 인데 429가 났고, 한 프로세스의 버킷만으로는 그 초에 5발을 낼 수 없다
(rate 3.5 + capacity 1.05 = 최대 4.55). 남은 가설 둘:

1. 같은 자격증명을 쓰는 **다른 발신자**가 서버 버킷을 공유했다.
2. 그룹별 말고 **다른 한도**(계정 전체 또는 엣지단 `edge-rate-limit-exceeded`)가 있다.

**추측으로 닫지 않고 판별 수단을 심었다**: 다음 429 부터 `under_own_limit` 이 답한다.
`true` → 원인이 우리 밖(1 또는 2), `false` → 우리가 실제로 그 초에 넘긴 것.
W4 가 `http_429_under_own_limit` 을 텔레메트리에 실으면 운영 중에 자동으로 결론이 난다.

---

## 6. 규율 준수

- **읽기 전용만.** 주문 계열 0건. `--probe all` 은 건드리지 않았고 위험 프로브는 opt-in 유지.
- **토큰 재발급 안 함.** `--reuse-token-state` 를 만들어 컬렉터의 토큰을 **읽어서만** 썼다.
  리스 획득·발급·무효화 전부 하지 않는다(상태파일도 건드리지 않는다 — 지우면 컬렉터가
  재발급하게 되어 결국 남의 토큰을 죽인 것과 같다).
- **컬렉터 방해 없음.** 프로브는 컬렉터가 쓰지 않는 MARKET_INFO(3/s)·STOCK(5/s) 에서만 돌았다.
  계약 프로브 4종은 429 를 내지 않도록 설계했고, 의도적 429 2건은 계측 실증용(MARKET_INFO)이다.
  프로브 중 컬렉터 429 0건 — 12:01 의 CHART 429 3건은 내 프로브 창(11:52–11:58, 12:10–12:16)
  **밖**이고 내가 한 번도 건드리지 않은 그룹이다.
- `collector/budget.py`·`loops.py` **미수정**.
- 배포 안 함. `w1-core-api` 커밋만.

## 7. 재현

```bash
TOSS_LIVE=1 python tools/live_probe.py --base-url https://openapi.tossinvest.com \
  --reuse-token-state <컬렉터의 token_state.json> \
  --probe ratelimit_contract,ratelimit_boundary,ratelimit_groups,limiter_holds
```
