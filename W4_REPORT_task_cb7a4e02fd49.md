# W4 — 그 둘을 갈랐다. 하나는 기각, 하나는 확정, 그리고 **세 번째가 있었다**

작성: W4, 2026-08-13. 브랜치 `w4-collector`. **배포 안 함 · 라이브 호출 0 · `w5-ops` 읽기 전용.**
전체 문서: **`docs/55_foreign_quota.md`**
도구: `tools/analyze_quota_partition.py`

---

## 0. 한 문단

코디네이터가 준 세 관측 중 **둘은 증거가 아니었다**. ①의 "같은 경로가 한도를 셋으로
보고한다" 는 동시가 아니라 **시간 순서**다 — 서버가 08-11 12:31~12:51 사이에
`/api/v1/candles` 한도를 5→15→20 으로 **올렸다**(같은 초·같은 분에 두 값이 섞인 적 0회).
②의 `group ≠ caller` 는 탐지 지점의 성질이다(`caller` 는 429 카운터 증가를 먼저 알아챈
루프). 대신 **훨씬 강한 것**이 나왔다: 332건 429 전부, 그 응답 스스로가 "이 창에서
1~4건 썼다(한도 5 또는 20)" 고 말한다 — **우리를 거절한 리미터는 헤더의 그 바구니가
아니다.** 그리고 `foreign > 0` 은 남의 소비가 **정확히 0** 인 열에서도 3 이 된다는 것을
프로덕션 코드 경로로 재현했다. 그래서 **(A) 는 기각, (B) 는 확정, 그러나 오늘 울린 그
경보가 (B) 인지 (C) 인지는 못 갈랐다.** 가르는 관측을 넣었다 — 배포 한 번이면 한 줄로 나온다.

---

## 1. 가설별 — 반증 관측을 먼저 진술하고, 그 관측을 했다

### (A) 같은 자격증명의 다른 발신자

**반증 관측**: (A) 의 소비는 우리 응답 도착 순서와 무관하다. 그러므로 초과 소비는
`own_in_server_s` 값에 관계없이 **같은 비율**로 나타나야 한다. 그렇지 않으면 (A) 가 아니다.

**결과 — 기각.** 같은 429 응답 하나에서 `consumed = limit - remaining`(서버가 센 것)과
`own_in_server_s`(우리가 센 우리 몫)를 나란히 뽑았다. 330건:

```
consumed == own : 285/330 (86.4%)   ← 그 바구니엔 우리뿐
consumed >  own :  43/330 (13.0%)
consumed <  own :   2/330 ( 0.6%)

own=1 → 초과율 15.3% (43/281)
own=2 → 초과율  0.0% ( 0/43)
own=3 → 초과율  0.0% ( 0/6)
(A) 가정하의 P(own>=2 에서 0건) = (1-0.153)^49 ≈ 2.9e-4
```

초과는 `own` 과 독립이 아니다. 이 모양은 **우리 응답 도착 순서**의 것이다 — 429 는 200
보다 빨리 돌아오므로(거절은 싸다) 형제 요청들의 응답이 안 온 상태에서 `own` 이
과소집계되고, 그 상태는 `own` 이 작을 때만 성립한다.
보조 관측: 332건 429 가 **100% `/api/v1/candles`** 다 (`/prices`·`/trades`·`/orderbook`·
`/rankings` 0건). 사람의 앱·브라우저 세션이 한 경로에만 정확히 몰릴 이유가 없다.
`consumed == 0` 인 줄이 0건이라 **서버는 거절한 요청도 카운터에 센다** — 그래서 86.4%
일치는 우연이 아니라 같은 요청 집합이다.

**한계(숨기지 않는다)**: 배제된 것은 "13% 전부가 남의 소비" 다. 작은 기여는 안 배제된다
(`own>=2` 의 0/49 → 95% 상한 약 **6%**). 그리고 이 표본은 **candles 바구니 하나**이고,
오늘 울린 경보는 MARKET_DATA 그룹이다 — 그 그룹의 (own, consumed) 원본은 로그에 없다.

### (B) 우리 구획이 서버 바구니와 다르다

**반증 관측**: (B) 가 참이면 429 시점에 그 경로의 자기 카운터가 한도에 닿아 있지
**않아야** 한다(다른 바구니가 거절했으므로). 닿아 있으면 (B) 가 아니다.

**결과 — 확정.** 332건 전부 소진량 1~4 (한도 5 또는 20). 한도 20 인 창에서 1건 쓰고
429 를 맞은 것이 74건이다. `Retry-After` 는 **전부 안 온다**(328/328). 즉 **서버 리미터가
최소 둘이고 헤더에 실리는 것은 하나뿐이며, 우리 모델에는 두 번째가 아예 없다.**
docs/06 §9-3 이 "429 의 잔량은 다음 창일 수 있다" 며 유보한 것도 이 자리에서 검증됐다 —
다음 창이면 소진량이 늘 0~1 이어야 하는데 우리 몫과 **함께 움직인다**.

**단, 그것이 오늘의 `foreign` 을 만들었는지는 별문제다** (§2).

### (C) 우리가 창을 잘못 붙였다 ← **새로 세운 후보**

**반증 관측**: 남의 소비를 0 으로 두고 합성했을 때 `foreign` 이 오르지 않으면 (C) 는 없다.

**결과 — 충분함이 증명됨.** 프로덕션 코드 경로로 재현했다 (`tests/test_server_second_watch.py`):

| 합성 (남의 소비 = 0) | 결과 |
|---|---|
| 초 0 에 5건, **마지막 응답만 `date=1`** 로 렌더(docs/06 §9-5 실측 조건) + 초 1 에 1건 | `own=2 consumed=5` → **`foreign=3`** |
| 초 0 에 4건, 3건 뒤 정산이 끝나고 4번째가 **늦게 도착**(원장 재생성) | `own=1 consumed=4` → **`foreign=3`** |
| (대조군) 어긋남 없는 5건 | `foreign=0` |

원인은 하나다: 원장 키가 **(그룹, 응답 `date` 초)** 이고 소진량을 `max(limit-remaining)`
으로 접는다 → **앞 창의 카운터가 이 창으로 넘어온다.**

---

## 2. 한도 헤더는 무엇의 한도인가

**경로별이고, 시각의 함수다.**

```
 limit  구간 시작              구간 끝                건수     (연속 구간 3개 / 332줄)
     5  08-10 09:00:22   08-11 12:31:11    203     같은 초에 다른 limit: 0/332
    15  08-11 12:43:37   08-11 12:44:01      2     같은 분에 다른 limit: 0/245
    20  08-11 12:51:08   08-13 11:01:19    127
```

`reset` 은 332건 전부 `1` (창 = 1초). 세션(day/pre/regular)과의 상관은 없다 —
전환 **시각**으로 갈린다.

**부수 발견 (질문 밖이지만 같은 데이터에서 나왔다).** `MARKET_DATA_CHART` 는 스펙·config
둘 다 5 이고, `limiter.update_from_headers` 는 서버 헤더를 `_spec_ceiling` 으로 자른다
(감사 B-2 의 옳은 규칙). 그래서 **08-11 12:51 부터 서버가 20/s 를 주는데 우리는 5/s 로
돈다.** `limit_header_clamped` 카운터가 텔레메트리에 없어 이 사실이 이틀 반 동안 어느
줄에도 안 남았다. `config.yaml`·`limiter.py` 는 **한 줄도 안 고쳤다** — 판단은 사용자 몫.

---

## 3. 못 갈랐다 — 무엇이 더 필요한가

**(B) 와 (C) 가 오늘의 경보에 대해 같은 크기대를 예측한다.**
관측: `md_foreign_s` 2~5 / 관측 39~58초, 경보 11건.

* (B) — candles 에 자기 한도가 있음이 확정됐으므로(§2) 남는 모양은 `{prices, trades,
  orderbook, price-limits} + {rankings}` 가 한도 10 을 공유하는 것이다. 그러면
  MARKET_DATA 의 `foreign` = 그 초의 RANKING 송신 → 초의 **8.3%**(`rk12s`, 3건 버스트),
  크기 **3**. 관측(5~9%, 최대 1~5)과 맞는다. (CHART 까지 한 바구니면 25% 는 나와야 하므로
  **관측보다 3배 크다** — CHART 는 그 바구니가 아니다.)
* (C) — 경계 렌더/늦은 도착이 60초에 3번이면 문턱을 넘고, 크기는 우리 송신 변동폭(첨두
  5~7)에 갇혀 1~5. 역시 맞는다.

**갈리는 자리는 하나다: RANKING 그룹의 `foreign` 크기.**
(B) 면 RANKING 원장이 MARKET_DATA 송신 전부를 남의 것으로 보므로 **4~7**, 그것도 관측한
초의 거의 전부에서. (C) 면 자기 버스트가 3건이라 **2 를 넘을 수 없다.**
**그 수가 로그에 없어서 못 갈랐다.** 그래서 그것을 싣게 했다:

1. **`budget.describe()`** 가 그룹마다 `srv{관측}/over/frn{foreign초}/frnmax{최대}` 를 싣는다.
   ```
   budget MARKET_DATA=peak0/.../tgt8.50/srv5/over0/frn5/frnmax3
          MARKET_DATA_CHART=.../srv5/over0/frn5/frnmax6
          RANKING=.../srv5/over0/frn5/frnmax7
   ```
   읽는 법: `RANKING frnmax >= 4 & frn ≈ srv` → **(B)** / `<= 2 & 산발` → **(C)** /
   세 그룹이 비슷 → **(A)** / `srv == 0` → 판정 불가.
2. **경보 문구**가 그룹별 `frn/관측·최대` 를 같이 싣고 원인 후보를 **셋**으로 적는다.
   **문턱·심각도·발화 조건은 그대로다.**
3. **유령 그룹 제거** — `snapshot()` 이 `self.counters` 를 그룹 목록으로 써서 전역
   카운터가 그룹처럼 렌더됐다. 운영 로그에 실제로
   `| budget … quota_not_ours=peak0/p95:0/avg0.00/tgt0.85` 로 찍혀 있다. 한도 1.0 짜리
   미지 그룹으로 보이므로 **"그 그룹은 깨끗하다" 로 읽힌다.** 내가 지난 태스크에서 넣은 결함이다.

**남은 것** (docs/55 §7): ⑴ `consumed` 를 최댓값이 아니라 **관측된 카운터 값의 집합**으로
세면 (C) 가 구조적으로 사라진다(한 창에서 우리 값은 `1..k` 연속이어야 하고 빈칸이 남의
소비다) — 추가 호출 0. 경보 술어가 바뀌므로 **사용자 판단 전에 안 했다.**
⑵ 그 집합을 넘길 길이 없다 — `loops.py:678` 이 `date` 를 버린다(내 소유 아님, 한 줄 변경).
⑶ MARKET_DATA 의 (own, consumed) 원본이 어느 로그에도 없다.
⑷ **두 번째 서버 리미터의 차원 미규명** — 429 간격 분포(중위 94/176초, 같은 초 2건 0회)는
"분 단위 바구니 소진" 모양이 아니라 고립된 거절이지만, 확정에는 경로를 갈라 보내는 능동
프로브가 필요하고 그것은 이번에 금지였다.

---

## 4. 내가 틀렸던 것

지난 태스크에서 `FOREIGN_SECONDS_MIN = 3` 의 근거를 "경계 렌더는 산발적이고 다른 발신자는
지속적이다" 로 적고 **값 3 은 관측이 아니라 선택이라고 자기 신고했다.** 이제 관측이 있고,
관측은 **문턱이 둘을 못 가른다**고 말한다 (`test_the_three_second_threshold_...`).
그래도 문턱을 올리지 않았다 — 원인을 모른 채 조용하게 만드는 것이 금지이고, 나는 아직 모른다.

---

## 5. 검증

* `python -m pytest -q` → **2,018 passed / 1 skipped** (4 deselected = 변이 마커).
  새 테스트 **9건** (`tests/test_server_second_watch.py` 24 → 33).
* `python -m tools.mutation_accounting` → **19/19 killed** (`all planted accounting
  defects were caught`). 내 변경으로 죽던 변이가 살아난 것은 없다.
* `tools/replay_gating.py` 의 `| budget` 파서는 `tgt` 뒤에 필드가 붙어도 깨지지 않는다
  (`[\d.]+` 가 `/` 에서 멈춘다) — 확인했다.

---

## 6. 혼자 내린 판단 (자기 신고)

1. **경보 문구를 고쳤다.** 문턱·심각도·발화 조건은 그대로 지켰다. 그러나 문구가 원인을
   **둘로 단정**했고 §1(C) 가 셋임을 보였으므로, 틀린 단정을 ERROR 로그에 남겨 두는 것이
   더 나쁘다고 판단했다.
2. **유령 그룹을 고쳤다** (`GLOBAL_COUNTERS`). 요청 범위 밖이지만 이 태스크가 읽어야 하는
   바로 그 줄을 오독하게 만드는, 내가 넣은 결함이다.
3. **`foreign` 계산은 안 고쳤다.** 정확한 방법을 알지만 경보 술어가 바뀐다.
4. **`describe()` 에 필드 4개를 더했다** — 텔레메트리 줄이 길어진다. 대안(새 로그 줄)은
   파서를 새로 만들어야 해서 안 골랐다.
5. **특성 테스트 둘을 남겼다** — 틀린 동작을 고정하는 테스트라 위험하지만, 지우면 이
   오탐이 조용히 돌아온다. 뒤집어야 할 자리를 주석으로 표시했다.

---

## 7. 안 한 것

* **배포 0.** 재시작 0, `STOP`/`PLANNED` 0, `schtasks` 0, 홀드아웃 0.
* **라이브 API 호출 0.** `w5-ops` 는 로그 읽기만 (DB 도 안 열었다).
* **경보 끄기·문턱 올리기 0.** `FOREIGN_SECONDS_MIN` = 3 그대로.
* `limiter.py` · `config.yaml` · `test_limiter_vs_counter.py` · `loops.py` **0줄.**
* **이중 기동 가설은 다시 세우지 않았다** (배제됨).
* "원인을 안다" 를 선언하지 않는다. §3 은 못 갈랐다.
