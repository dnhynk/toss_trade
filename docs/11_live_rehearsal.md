# 11 — 라이브 리허설 결과 (W5, 라이브 리스 보유 기간)

> 소유: W5(이번 문서 한정 신규 작성 권한). `docs/06_live_facts.md`(W1 소유)는 수정하지 않는다 —
> 여기 기록한 사실은 코디네이터가 검토 후 06에 반영한다.
> 각 항목은 `확인됨`/`부분확인`/`미확인` + 근거(마스킹된 스니펫 또는 수치)로 기록한다. 추측 금지.

## 0. 측정 조건

| 항목 | 값 |
|---|---|
| 브랜치 | `w5-ops`, 작업 B(야간 수집) 시작 시점 main 기준(rebase) `6b6fb4a` (A4+A5+정밀도 텔레메트리 전부 반영, pytest 614 passed) |
| 라이브 리스 부여 | 코디네이터, W1 반납 완료 확인 후 |
| Base URL | `https://openapi.tossinvest.com` |
| 도구 | 세션 무관 항목(A-2/A-3)은 임시 스크립트(scratchpad, 커밋 안 함 — `tossmon.api.*` frozen 레이어만 재사용, `tools/live_probe.py` 미수정), 세션 의존 항목(A-1)은 `tools/live_probe.py` 그대로 사용 |

---

## 1. (A-2) 1분봉 보관 경계 정밀 이분탐색 — `확인됨`

W1은 1024일 hit / 2048일 miss 까지만 좁혔다(docs/06 §2). 대형주 1 + 소형주 1을 하루 단위로
이분탐색했다.

### 1-1. AAPL (대형주)

- **경계: 1702일 전 = hit, 1703일 전 = miss** (앵커: 2026-07-30T20:50:00+09:00 기준 최신봉)
- 콜 수: 10 (이미 확인된 1024/2048 힌트를 재확인 없이 바로 재사용해 절약)
- 1702일 전 봉 timestamp: `2021-12-01T21:30:00.000+09:00`(ms=1638359400000) — 진짜 1분봉 그리드인지는
  W1이 이미 확인한 방식과 동일 소스이므로 별도 재검증 생략(비용 절감).
- **W1의 "2.8년 이상"이라는 보수적 결론보다 실제로는 약 4.66년(≈1702일)** — 백필 계획(docs/03 §1-A)의
  규모 산정을 상향 조정할 수 있는 근거.

### 1-2. SNTI (소형주) — ⚠️ 대형주와 크게 다름

- **경계: 320일 전 = hit, 321일 전 = miss** (앵커: SNTI 자체의 최신봉, 2026-07-30T14:32:00+09:00 —
  AAPL과 다른 시각인 이유: SNTI는 그 시각 이후 체결이 없어 최신봉이 더 과거임)
- 콜 수: 대괄호 탐색 2 + 이분 8 = 10
- **약 320일(≈10.5개월)로 AAPL의 1702일보다 훨씬 짧다.** 320일 전 봉: `2025-09-14` 근방
  (ms=1757721240000).
- **설계 함의 (중요)**: 백필 계획이 "1분봉은 1024일 이상 보관되니 전 종목 균일하게 백필 가능"을
  전제한다면 이 전제가 소형주에서 깨진다. 이유는 확인하지 못했다(신규상장/재상장, 혹은 유동성이
  낮은 종목 자체의 보관 정책 차이, 혹은 우연히 이 종목의 사정 — 추측 금지, 다른 소형주로 재검증
  필요). **백필 규모 산정 시 종목별로 실제 depth를 먼저 프로브하고 가정하지 말 것**을 권고한다.

### 1-3. 종합

| 종목 | 시총구분 | 경계(hit/miss, 일) | 비고 |
|---|---|---|---|
| AAPL | 대형 | 1702 / 1703 | W1 대비 훨씬 깊음 |
| SNTI | 소형 | 320 / 321 | AAPL의 1/5 수준 |

---

## 2. (A-3) 1분봉 timestamp 시작/끝 판정 — `부분확인` (AAPL 약한 신호, 소형주는 §3 이슈로 중단)

방법(코디네이터 지시, 2026-07-30): `/trades` 체결 시각을 `/candles` 각 봉의 `vol_qu`와 대조해
어느 시간창(`[T,T+60s)` 시작 가설 vs `(T-60s,T]` 끝 가설)의 체결량 합이 봉의 거래량과 더 가까운지로
판정.

### 2-1. AAPL — 약한 신호, 결론 보류

- `/trades count=50` 이 **단 110초**(20:47:32~20:49:22)만 커버했다 — AAPL이 현재 매우 활발히
  거래되고 있어(한 봉에 vol_qu가 최대 202,020 주 상당) 50건으로는 한 분(1분)도 온전히 못 덮는다.
  docs/06 §4의 "50건이 10분치"라는 프리마켓 관측과 달리, **유동성이 높으면 50건 샘플이 오히려
  분 단위 대조에 못 미친다** — 이 자체가 흥미로운 함의다(A2 §2 "테이프는 표본"이 세션·순간에
  따라 표본 크기가 크게 흔들린다는 뜻).
- 5개 봉 중 신호: `end` 가설 채택 2, `start` 가설 채택 1, 무승부(둘 다 오차 동일) 2.
  **다수결이지만 근거가 약하다** — 표본 부족 때문에 신뢰하기 어렵다.
- 결론을 소형주(저유동성, 50건이 여러 분을 덮음)로 재확인하려 했으나 §3의 가격 정밀도 문제로
  중단했다(에스컬레이션 진행 중 — 코디네이터 지시 대기).

### 2-2. 재확인 필요

이 항목은 **미완**이다. 저유동성 소형주(BTAI/CRKN 등)로 재시도가 필요하나, §3 이슈 때문에
`get_candles`가 그 종목들에서 SchemaMismatch로 실패한다. §3 해결(또는 회피 방법 확정) 후 재시도한다.

---

## 3. ⚠️ 설계 전제 위반 발견 — 계약 C-2 마이크로달러(1e-6) 정밀도가 CRKN(리버스 스플릿 종목)에서 깨짐 — `확인됨` (원인 확정), 계약 개정 A4 코디네이터 진행 중

A-3를 소형주로 재확인하려다 발견했다(원래 BTAI로 오인·보고했으나 **정정: 실제 종목은 CRKN**).
`client.get_candles("CRKN", "1m", count=6)` 호출이 다음 예외로 실패했다:

```
tossmon.api.errors.SchemaMismatch: candles.openPrice: precision finer than 1e-6: '0.10461614'
```

### 3-1. 원인 진단 (코디네이터 지시 P1~P3, 총 9콜)

**P1 — adjusted 가설 검증(확정)**: 문제의 CRKN 봉(`2025-03-05T08:00:00+09:00`)을
`adjusted=true`/`false` 로 각각 조회.

| adjusted | openPrice | 소수 자릿수 |
|---|---|---|
| true | `0.10461614` | 8 |
| false | `1.9889` | 4 (깨끗함) |

비율 `1.9889 / 0.10461614 ≈ 19.0114`(정수에 가까움) — **가설 확정**: `adjusted=true`(수정주가)가
과거 리버스 스플릿 비율(≈1:19, 배당 등 추가 보정이 섞여 있을 수 있어 정확히 19는 아님)을 곱해
파생값을 만들고, 이 나눗셈이 유한소수로 안 떨어지면서 임의 자릿수가 생긴다. `adjusted=false`
(원가)는 항상 깨끗하다.

**P2 — 엔드포인트 범위**: CRKN 외 `/trades`, `/orderbook`, `/prices`, `/candles(1d)` 및
BTAI·SNTI·DFNS 의 `/candles`(1m)는 전부 소수 4자리 이하로 정상이었다. **지금까지 관측 범위에서는
문제가 `/candles`(1m) 의 `adjusted=true` 경로, 그중에서도 과거 리버스 스플릿을 겪은 종목에
한정된다** (BTAI/SNTI/DFNS는 스플릿 이력이 없어 재현되지 않음).

**P3 — 종목 범위**: 소형주 4종 `/prices` 배치 1콜 — BTAI `0.7803`(4자리), SNTI `0.347`(3자리),
CRKN `0.0012`(4자리), DFNS `96.22`(2자리). 전부 정상.

### 3-2. 파급 범위와 설계 함의

- 계약 C-5 정책상 `SchemaMismatch`는 재시도 금지 + **해당 심볼 스킵**이다. 리버스 스플릿을 겪은
  저가 소형주는 정확히 이 프로젝트의 표적 종목군(docs/03 §1, 리버스 스플릿 이력 자체가 스윗스팟
  태그 중 하나 — docs/03 §1 Tier 0 보강 태그)과 겹친다 — **하필 노리는 종목군에서 과거 구간
  백필이 조용히 실패할 위험**이 실재한다.
- 범위는 "저가 종목 전반"이 아니라 **"adjusted=true + 과거에 스플릿을 겪은 종목의 스플릿 이전
  구간"으로 한정**되는 것으로 관측됐다(추측 아님 — P2 근거).
- 코디네이터 결정(권한 밖이라 자체 판단하지 않음): 단위는 마이크로달러 유지, 초과 정밀도는
  예외가 아니라 **반올림 처리**로 계약 개정(A4) 진행 중(W1이 CRKN 정정본 기준으로 구현·픽스처
  작성 중). 나노달러 전환은 가격×수량 누적의 int64 오버플로 위험(W3 VWAP 사례)과 sub-penny
  규정(Rule 612, $1 미만 최소호가 $0.0001) 대비 과도한 정밀도라 기각됐다.
- **계약 A5 신설(main 55c7d15, 코디네이터)**: 이 P1 결과가 더 근본적인 문제를 드러냈다 —
  수정주가(adjusted=true)는 **그 시점의 명목 가격을 지운다**. CRKN은 2025-03-05에 실제로는
  $1.99에 거래됐는데 수정주가로 보면 $0.10 동전주로 보인다. 이 프로젝트의 논지가 "동전주
  가격대에서 벌어지는 현상"인데, 수정주가로 과거를 보면 **당시엔 동전주가 아니었던 종목을
  동전주로 오분류**한다. sub-penny 호가·LULD 밴드($0.75/$3 경계) 등 미국 제도가 전부 명목
  가격 기준이라 미시구조 해석 전체가 틀어진다. **결정: 1분봉은 `adjusted=false`(원주가),
  일봉은 `adjusted=true`(수정주가)를 쓴다.** 분할은 장중에 일어나지 않으므로 일중 상대 변화
  (수익률·RVOL·VWAP 괴리)는 원주가로도 완전히 동일해 잃는 것이 없고, 다일 비교(베이스라인)만
  일봉(수정)으로 계산한다.
  **✅ 작업 B(야간 수집) 은 A5 적용 후(main `263a6ee`) 수집이다.** W4가 A5를 22:30 정규장
  개장 전에 `tossmon/collector/loops.py`에 반영·머지했고(`tier2_symbol_once`/`_backfill_1m`은
  `adjusted=False`, `_refresh_baseline`(일봉)은 `adjusted=True`를 명시적으로 호출 — 기본값을
  바꾸지 않고 호출부에서 명시하는 것이 계약이라는 코멘트가 코드에 남아 있다), 이 워커도 수집
  시작 전에 `git rebase main`으로 반영을 확인했다. 즉 §5의 1분봉은 원주가, 일봉 베이스라인은
  수정주가로 수집된 데이터다.

### 3-2b. A4 머지 후 검증 — 일봉(adjusted=true) 백필에서 `open_u`(등) 0u 뭉개짐 여부 — `부분확인`

코디네이터 요청(A4 머지 후, main `53578fb`): 극단적 리버스 스플릿이 누적된 종목의 아주 오래된
일봉은 수정주가가 1e-6 USD 미만으로 내려가 반올림(`ROUND_HALF_EVEN`)으로 `0u`가 될 수 있다는
우려 확인.

`main`을 rebase해 A4를 반영한 뒤, CRKN·BTAI·SNTI 세 종목의 **일봉(adjusted=true) 전체 이력**을
`nextBefore`로 끝까지 페이지네이션하며 `open_u/high_u/low_u/close_u` 전부를 검사했다(19콜).

| 종목 | 페이지 | 총 봉수 | 가장 오래된 봉 | 0u 뭉개짐 |
|---|---:|---:|---|---|
| CRKN | 1 | 41 | 2025-01-02 | 없음 |
| BTAI | 11 | 2,100 | 2018-03-08 | 없음 |
| SNTI | 7 | 1,294 | 2021-05-26 | 없음 |

- **A4는 "이론적 안전장치"가 아니라 실제 라이브 데이터에서 작동 중이다** — `precision_stats()`:
  `rounded=13/17179`, `max_digits=8`(최근 예 `0.28983126`). 즉 **13건이 실제로 발생했다.**
  A4 이전(예외 + 심볼 스킵) 코드였다면 이 13건은 전부 조용한 스킵으로 이어졌을 것이다 — "발생
  가능성이 있다"가 아니라 "이 리허설 세션에서만도 13번 발생했고, 전부 정상 처리됐다"로 기록한다.
  이 13건 중 `0`으로 내려간 사례는 없었다.
- **한계**: 3종목 표본이다. "관측 안 됨"이지 "발생 불가능함이 증명됨"은 아니다 — 더 극단적으로
  누적 리버스 스플릿을 겪은 종목(수차례 반복 스플릿)에서는 이론상 여전히 가능하다. 백필 실행 시
  `precision_stats()`를 주기적으로 점검하고, `rounded` 대비 `0`이 되는 사례가 실제 나타나면
  (`Store.upsert_candles_1d`에 들어가는 행에서 `open_u==0` 등을 직접 조회) 그 시점에 다시 보고할
  가치가 있다.
- **백필 계획 함의(신규)**: CRKN의 일봉 이력이 **2025-01부터 41봉뿐**이다(§3-2b 표) — 유니버스
  안에 **일봉 이력 자체가 짧아 20일 베이스라인(`compute_daily_baseline`)조차 못 만드는 종목**이
  섞여 있다는 뜻이다. 백필 계획(docs/03 §1-A)이나 유니버스 필터(docs/03 §1 Tier 0)에 "최소
  일봉 이력 길이" 조건이 필요한지는 코디네이터 판단 대상으로 남긴다(자체 판단 안 함).

### 3-3. 보너스 발견 (이번 이슈와 무관, 참고용) — CRKN 1분봉 정지 17개월+

CRKN은 `before` 없이(최신) 조회해도 1분봉 마지막이 `2025-03-05`다 — **17개월 넘게 1분봉
프린트가 없다**. `/prices.lastPrice` 는 `0.0012`로 응답은 오지만(A2 §3: 체결 없어도 lastPrice는
옴), 실질적으로 시간 단위가 아니라 **개월 단위**의 `no_print` 사례다. A2의 `first_print`/
`staleness_s` 설계가 다루려는 극단 사례의 실존 증거로 참고 가치가 있다(추가 조치·escalate는
불필요 — 설계 전제를 깨는 게 아니라 오히려 뒷받침하는 사례).

---

## 4. (A-1) 정규장/데이마켓 시세·호가·체결 거동 — 정규장 `확인됨`, 데이마켓 `미실행`

**일정 변경(코디네이터 지시, 2026-07-30 21:3x KST)**: 당초 22:30 개장 직후 실행 계획이었으나
취소했다. 이유: (a) `TokenManager`는 파일락 기반 단일 리스라 collector(작업 B)와
`live_probe.py`를 동시에 띄우면 후자가 즉시 `RuntimeError`로 죽는다. (b) 개장 직후 15분은
LULD 밴드 2배·HOD의 46.6% 형성 구간(docs/02 §2.4)이라 수집 데이터로서 가장 값진 구간이다 —
이 구간을 프로브로 끊지 않는다. **새 일정대로 23:37~23:43 KST(정규장, 장이 상대적으로 잠잠한
구간)에 계획된 정지·프로브·재기동을 실행했다.**

### 4-1. 실행 절차 및 계획된 정지·재기동 — `확인됨`

1. **정지**: `data/ops_state/STOP` 파일 생성 → 마지막 수집 로그 `2026-07-30T23:37:28+09:00`
   (tier ↑ TSLA). `supervisor_detached.out.log`: `stop file detected while running —
   terminating collector` → `stop file detected after child exit — exiting` — **크래시 없이
   정상 종료**(supervisor.py의 em-dash/cp949 수정이 실전에서 처음으로 의도한 대로 동작한
   순간이기도 하다).
2. **파일락 해제 확인**: `filelock.FileLock(...).acquire(timeout=0)` 로 직접 확인 — 즉시
   획득 성공(다른 프로세스 없음).
3. **rebase**: `git rebase main`(main=`e1ed22d`, W4 이벤트 재검출 억제/알림 등급 분리 +
   W2 events UNIQUE+UPSERT 마이그레이션 포함) — 성공.
4. **테스트**: `pytest -q` → **637 passed**(코디네이터 확인치 636과 오차 1, 문제 아님).
5. **라이브 DB 마이그레이션 적용** — §4-2 참고.
6. **live_probe A-1 실행** — §4-3 참고. 14콜, 65초.
7. **재기동**: `Start-Process`로 분리 실행(PID 45388). 로그: `resumed from
   collector_state.json: watch=74 tier2+=74 saved_ms=1785422203503` → `session closed →
   regular` — **워치리스트·티어 전량 복원 + 세션도 재시작 시점 기준으로 올바르게 재판정**
   (정지 전엔 세션이 "pre"로 마지막 기록됐었는데, 재시작 시 캘린더를 다시 읽어 "regular"로
   정확히 갱신했다 — 세션 인지가 상태 캐시가 아니라 라이브 판정임을 보여준다).
8. **정지·재기동 전체 소요**: 마지막 수집(23:37:28) → 재개(23:43:39) = **약 6분 11초**
   (마이그레이션·백업·probe·rebase 확인까지 포함한 계획된 정지였다 — 무계획 정지 2건
   (43초, 몇십 초)보다 훨씬 길지만, 이번엔 의도된 유지보수 창이었다).

### 4-2. 라이브 DB 마이그레이션(v1→v2, events UNIQUE) — `확인됨`

- **백업**: `sqlite3.Connection.backup()` API로 `data/tossmon.db.backup_pre_v2migration`
  생성 완료(WAL 상태와 무관하게 일관된 스냅샷 — 파일 복사보다 안전).
- **마이그레이션 전**: `events` 테이블 총 **171행**, `(symbol, t0_ms)` 고유 조합은 **16개** —
  즉 **평균 10.7배 중복**이 쌓여 있었다(검출기가 매 사이클 버퍼를 재스캔하며 같은 이벤트를
  반복 기록한 결과, W4가 고친 바로 그 버그).
- **적용**: `Store(db_path)` 생성만으로 `apply_migrations()`가 자동 실행됨(계약 C-6, 별도
  마이그레이션 스크립트 불필요) — `(symbol, t0_ms)` 그룹별 `MAX(id)` 행만 남기고 나머지 삭제,
  이후 `UNIQUE INDEX ix_events_symbol_t0` 생성.
- **마이그레이션 후**: `events` **16행**(고유 조합 수와 정확히 일치), `schema_version` 1→2,
  `PRAGMA integrity_check` = `ok`.
- **재기동 후 관찰**: 이후 새 이벤트는 W4의 애플리케이션 레벨 재검출 억제 + DB UNIQUE 제약
  이중 방어를 받으므로, 앞으로는 이런 규모의 중복이 재발하지 않을 것으로 기대된다(계속
  관찰 예정).

### 4-3. live_probe A-1 결과 (정규장, 23:42~23:43 KST) — `확인됨`

**quote_realtime** (6폴링×12s, 60.9초 소요):
- AAPL: 6회 모두 timestamp 진행, 지연 중앙값 **1.2s**(median, 프리마켓 W1 실측 12.8s보다도
  훨씬 빠름 — 정규장이 프리마켓보다 시세 반영이 더 실시간에 가깝다).
- SNTI: distinct_timestamps=2/6, 지연 5.5~229.4s(중앙 205s) — 활동은 있으나 드문드문.
- BTAI: distinct_timestamps=3/6, 지연 10.5~34.8s — SNTI보다는 활발.
- **CRKN: distinct_timestamps=0, timestamp_advanced=false** — §3-3에서 확인한 17개월+
  무프린트가 정규장에서도 그대로다(일관성 확인, 새로운 사실 아님).

**orderbook_depth** (QQQ/SOXL/MSFT/TSLA/NVDA, 5콜): **전부 1레벨** — docs/06 §3의
"정규장에서도 1레벨인지 미확인(부분확인)" 항목이 이제 **확인됨**으로 승격 가능하다
(프리마켓과 동일하게 유동성 최상위 종목까지 포함해 1레벨만 제공 — API 사양이지 세션 문제가
아니라는 W1의 추정이 확인됨).

**trades** (AAPL/SNTI/BTAI, 3콜): **유동성별 표본 커버리지 격차가 극단적으로 벌어졌다.**

| 종목 | 반환 | 커버 시간폭 |
|---|---:|---:|
| AAPL | 50건 | **3.0초** |
| BTAI | 50건 | 1,471초(≈24.5분) |
| SNTI | 50건 | 4,264초(≈71분) |

AAPL은 정규장에서 50건이 단 3초 분량이다(프리마켓 10분 대비 200배 조밀) — tier3 폴링
주기(`tier3_trades_s=4초`)로는 **AAPL 같은 초고유동성 종목의 테이프를 표본으로도 따라잡을 수
없다**는 뜻이다(4초마다 폴링해도 그사이 훨씬 많은 체결이 스쳐 지나간다). 반면 소형주는
정규장에서도 여전히 표본이 수십 분을 커버한다. §5의 tape_gaps 급증(36→262)이 바로 이
비대칭에서 나온다 — 자세한 수치는 §5-관측1 참고.

### 4-4. 데이마켓 — `미실행`

내일(2026-07-31) 09:00~10:00 KST(세션 초반, 최악 조건)와 세션 중반에 각각 실행 예정.

## 5. (작업 B) 야간 1세션 무인 수집 리허설 — `진행 중`

**일정 변경(코디네이터 지시)**: 원래 정규장(22:30) 시작 계획을 취소하고 **프리마켓부터 즉시
시작**했다. 근거: (1) 프리마켓 거래량·갭 크기가 정규장 셋업(스퀴즈/페이드)의 최대 사전 정보라
프리마켓 자체가 감시 대상 데이터다(docs/02 §2.5·§4.1, 프리마켓 대량갭퍼 페이드율 71.5%).
(2) 랭킹 스냅샷(토스 쏠림도 시계열)은 사후 조회가 불가능하다(계약 A2 §4) — 흘려보내는 시간은
영구 손실이다. (3) `scheduler.py`의 세션 전환 로직이 라이브에서 미검증이었다 — 프리마켓부터
시작하면 pre→regular(22:30)와 regular→after(05:00) 전환을 둘 다 실측할 수 있다(정규장부터
시작했다면 pre→regular 전환은 영영 관측 못 했을 것).

- **수집 시작**: `2026-07-30T21:35:30+09:00` (KST), `ops/supervisor.py` → `python -m
  tossmon.collector --config config/config.yaml` (main `6b6fb4a` 기준, A4+A5+정밀도 텔레메트리
  전부 반영).
- **세션 인지 확인**: 시작 로그에 `session closed → pre` — `/market-calendar/US` 기반 세션
  판정이 라이브에서 정상 동작함을 즉시 확인(계약 C-1, 하드코딩 없음).
- **초기 티어 승격 확인**: 기동 수 초 안에 랭킹 진입 트리거(`reason=ranking_entry`)로 SOXL,
  SOXS, NUWE, PN, DFNS, KORU, SNDQ, SKHY, SNXX, SNDU, SXTC, CYCU, GCTK, LBGJ, CISS, STKH 등이
  tier1→2로 승격 — 랭킹 기반 승격 경로가 라이브에서 즉시 동작함을 확인.
- **세션 전환 관측 예정**: pre→regular(22:30 KST), regular→after(05:00 KST 익일). 전환 시각과
  티어 재구성(`reconfigure_tiers`, `SESSION_TIER_SCALE`) 동작 여부를 여기 기록한다(미완).
- **계획된 정지·재시작(§4 A-1 프로브 겸용)**: 23:30~00:30 사이 실행 예정. 재시작 후 이어받기
  검증 결과는 여기 기록한다(미완).
- **뜻밖의(비계획) 정지·재시작 검증 — `확인됨`**: 정밀도 텔레메트리를 실사용해보다가
  `ops/healthcheck.py`(테이블별 staleness 게이트가 candles_1d/events/promotions처럼 사건성
  테이블에도 걸려 오탐 CRIT를 냄)와 `ops/supervisor.py`(상태 메시지의 em-dash가 Windows 콘솔
  기본 cp949에서 `UnicodeEncodeError`를 내는 버그 — 하필 STOP 감지 직후라 방치하면 재시작
  실험 자체가 깨질 뻔했다) 버그 2건을 발견해 수정했다(커밋 `5da390f`). 수정을 반영하려면
  이미 떠 있던 collector를 재기동해야 해서, 계획보다 이르게 **21:52 KST에 첫 정지·재시작을
  실행**했다:
  - `taskkill /PID <supervisor 최상위 PID> /T /F`로 안전하게 종료(파일락은 프로세스 종료 시
    OS가 자동 반납 — 종료 직후 `filelock.FileLock(...).acquire(timeout=0)`로 직접 확인해
    락이 실제로 풀렸음을 검증했다).
  - 재기동 로그: `resumed from data\collector_state.json: watch=32 tier2+=32
    saved_ms=1785415856886` — **워치리스트 32종목·tier2 승격 32건이 정확히 복원됐다.**
    `_resume_candle_ms`/`last_trade_ms`/`missing_streak`/`counters` 도 상태파일에 저장된
    그대로 이어받는 구조(계약 C-8, `CollectorContext.load_state`)이므로 이 결과는 무인 운영의
    핵심 요구사항(재시작 이어받기)이 라이브에서 실제로 동작함을 보여준다.
  - DB 무결성: 강제 종료(taskkill) 직후 healthcheck로 확인한 결과 손상 없음(WAL 모드,
    계약 C-6 그대로) — `candles_1m`/`rankings_snap`/`trades_snap` 카운트가 재기동 후에도
    끊김 없이 이어졌다.
  - **주의**: 이 정지·재시작은 §4의 계획된 A-1 프로브용 정지가 아니다(그건 23:30~00:30에 별도
    실행 예정). 이번엔 순수히 버그 수정 반영이 목적이었고, 결과적으로 재시작 이어받기 검증을
    한 번 더(그것도 실제 프로세스 킬로) 얻은 것이다.
- **두 번째 비계획 정지 — 22:07 KST, 원인 불명의 외부 종료(운영 도구 결함 아님)**: 이 워커의
  실행 환경(에이전트 세션)이 collector 프로세스를 백그라운드로 띄워둔 셸 작업 3개를 동시에
  "killed/stopped"로 표시했다 — 이 워커가 STOP 파일을 만들거나 taskkill을 실행한 적이 없다.
  즉 **운영 로직(supervisor/collector)의 결함이 아니라 이 워커를 구동하는 에이전트 세션 쪽의
  프로세스 관리 이벤트**로 보인다(정확한 원인은 이 워커의 관측 범위 밖). 대응:
  - 락 확인 → 자유(자동 반납 재확인) → 재기동. 로그: `resumed from collector_state.json:
    watch=40 tier2+=40 saved_ms=1785416778880` — **이번에도 이어받기가 정확히 동작**했고,
    끊긴 시간은 로그 타임스탬프 기준 약 43초(22:06:59 마지막 기록 → 22:07:41 재개)에 불과했다.
  - **완화 조치**: 이후로는 collector를 이 에이전트 세션의 셸 작업 트리에 묶이지 않는 진짜
    독립 프로세스로 띄운다(`Start-Process`로 분리 실행, PID로 직접 확인) — 셸 작업이 통째로
    정리되는 사고가 재발해도 collector 자체는 영향받지 않도록. 이 문서 갱신 시점 이후의
    collector 프로세스는 이 방식으로 실행 중이다.
  - **평가**: 두 번의 비계획 정지 모두 이어받기가 잘 동작했다는 점에서 무인 운영 요구사항
    자체는 강하게 검증됐지만, "몇 초~몇십 초의 수집 공백"이 실제로 두 차례 발생했다는 사실도
    같이 기록한다 — 완전 무손실은 아니다(계약 C-6 WAL 덕에 데이터 손상은 없었지만 공백 구간의
    체결/랭킹 스냅샷 자체는 놓쳤다).
- **세 번째 발견 — healthcheck.py 429 카운트 오탐 (22:30경, 정규장 진입 직후)**: `python -m
  ops.healthcheck` 를 다시 돌려보니 `logs: 429=6` 이 처음으로 찍혔다. 실제 429 인 줄 알고
  `collector.log` 를 grep 해봤더니 매칭된 라인 어디에도 실제 429 이벤트가 없었다 —
  `RE_429 = \b429\b` 단독 매칭이 밀리초 타임스탬프(`22:13:16,429`)와 epoch ms
  (`this_min=1785417429000`)에 우연히 등장하는 "429" 숫자를 전부 셌다. W4의 실제 429 로그
  형식(`budget: 429 on <group>`, `tossmon/collector/budget.py:on_429`)이 이제 확정돼 있으므로
  그 문맥으로 패턴을 좁혔다(커밋 `8874694`, 회귀 테스트 포함, 전체 622 passed). 수정 후
  재확인한 `logs: 429=0` 은 텔레메트리의 `api_errors=0`, budget MARKET_DATA 1.2~1.5/7.0(여유
  충분)과 일치한다 — 즉 오늘 밤 세션에서 **실제 429는 지금까지 0건**이다.
- 세션 종료(05:00 KST 이후) 후 `tools/dryrun_night.py`로 최종 리포트 생성 예정(미완).

### 5-1. 코디네이터 지시 관측 3건 (23:30 체크포인트)

**관측 1 — `tape_gaps` 세션별 급증 (36 → 262)**: 텔레메트리 누적치를 세션 경계로 잘라보면

| 구간 | tape_gaps | 구간 길이 | 분당 발생률 |
|---|---:|---:|---:|
| pre 시작(21:35)~pre 종료(22:30) | 0 → 36 | ≈55분 | ≈0.65/분 |
| regular 시작(22:30)~정지 직전(23:35) | 36 → 262 | ≈65분 | ≈3.47/분 |

**정규장이 프리마켓 대비 약 5.3배 빠른 속도로 tape gap을 만든다.** §4-3의 `/trades` 표본
커버리지(AAPL 50건=3.0초, 프리마켓 대비 200배 조밀)와 정확히 들어맞는다 — `tier3_trades_s=4초`
폴링 주기가 AAPL 같은 초고유동성 종목에서는 표본 사이 체결을 상시로 놓친다는 뜻이다. A2 §2가
전제한 "`/trades`는 최대 50건이라 표본이다"가 정규장 고유동성 종목에서 실측으로 뚜렷하게
확인된 셈이다. **함의**: tier3 폴링 주기를 더 좁히는 것은 예산상 불가능(MARKET_DATA 여유는
있지만 4초보다 빠르게 폴링해도 200배 격차를 메우기엔 역부족)하므로, 테이프 기반 지표(체결
버스트·소액 리테일 비중 등)는 애초에 "표본 통계"로만 해석해야 한다는 A2의 설계 전제를
그대로 유지하는 것이 맞다 — 폴링을 더 조인다고 해결되는 문제가 아니다.

**관측 2 — tier3 체류 이력 (승격 784건 중 tier3는 스냅샷 시점에 1개뿐인 이유)**: `promotions`
테이블에서 `to_tier=3`/`from_tier=3` 전체 11건을 재구성했다(23:43 시점까지).

| 종목 | tier3 진입 사유 | 진입 시각(KST) | 이탈 시각(KST) | 체류 시간 |
|---|---|---|---|---:|
| PN | precursor | 21:38:00 | 22:57:00 | 79분 |
| CYCU | precursor | 21:39:00 | 23:34:00 | 115분 |
| RKTO | precursor | 21:40:00 | 22:33:00 | 53분 |
| VSEE | confirm | 21:50:00 | 23:03:00 | 73분 |
| BBD | confirm | 22:36:00 | 23:20:00 | 44분 |
| YAAS | confirm | 23:24:00 | (진행 중, 23:43 기준) | 19분+ |

- **코디네이터가 관측한 "tier3=1"은 한 순간의 스냅샷일 뿐이다** — 실제로는 21:50~22:33 구간에
  PN·CYCU·RKTO·VSEE **4종목이 동시에** tier3에 있었다(정원 20 대비 여전히 낮지만 1은 아니다).
  다만 이후 시간이 지나며 하나씩 `score_decay`로 이탈하고 새 진입이 그만큼 빠르지 않아,
  23:43 스냅샷 시점엔 YAAS 1개만 남아 있었다.
  이탈 사유는 **11건 중 5건 전부 `score_decay`** — 강등이 너무 빨라서라기보다(체류 44~115분은
  짧지 않다), tier2→3 **진입 자체가 드물다**(74개 워치리스트 중 2시간 동안 단 6종목).
- **함의(검출기 튜닝 근거, 코디네이터/W4 판단 영역 — 자체 결론 내지 않음)**: tier3 정원(20)
  대비 실제 점유(최대 4)가 낮은 건 confirm/precursor 스코어 임계가 보수적이거나(의도된
  설계일 수 있음 — "집중 감시"는 소수 정예가 맞다), 혹은 이번 세션이 조용한 편이었을
  가능성도 있다. 급등 이벤트 자체가 드문 세션이라면 tier3 저점유가 정상일 수 있다 —
  §4-3의 이벤트 후보 수(§7 예정)와 함께 판단할 문제다.

**관측 3 — 예산 여유(0.32/7.00)와 만석 가정 산식(6.42)의 괴리**: `config/config.example.yaml`
하단 주석의 MARKET_DATA 산식(6.42 req/s)은 **tier3가 정원(20)을 다 채웠을 때**의 계산이다.
실측 `budget MARKET_DATA=0.32~1.27/7.00`은 tier3 실제 점유(1~4/20)를 반영한 값이라 산식과
크게 다르다 — **이건 모순이 아니라 "예산 산식은 최악 케이스(만석) 상한이고, 지금은 유니버스가
안 찼다"는 뜻이다.** `CollectorContext.refresh_plan()`이 애초에 "정원 기준"으로 계획을 세우는
이유가 바로 이것이다(주석: "현재 인원이 아니라 정원으로 계산 — 예산은 다 채우면 넘치나로
검증해야 사고를 미리 막는다"). 즉 설계는 의도대로 동작 중이며, 실사용률이 낮다고 정원(tier3_max)
을 늘리는 것은 **위험한 결론**이다 — tier3가 실제로 20개를 채우는 세션(더 활발한 밤)이 오면
그때 6.42 산식이 그대로 적용되기 때문이다.

## 6. 토큰 위생

리허설 종료 후 `data/token_state.json.lock` 잠김 여부를 확인해 이 절에 기록한다(미완).

---

# 재수집 (2026-07-31) — 유니버스 필터 적용

> 어제(§0~6) 15시간 수집은 **파이프라인이 무인으로 도는가**를 검증했지만, 감사 결과 유니버스
> 필터가 수집 경로에 전혀 적용되지 않아 NOK·AMD·ASML·META·GS 같은 대형주가 워치리스트를
> 채우고 tier3 진입을 봉쇄했다(§5-1 관측2 참고) — **표적 모집단이 아니었다.** W4가 tier0
> 게이트를 collector 에 연결했고, 이 절은 그 수정이 반영된 **분석에 쓸 수 있는 첫 데이터**의
> 수집 기록이다. 어제 DB는 `data/tossmon_20260730_polluted.db` 로 보존(삭제 안 함), 오늘은
> **새 DB**(`data/tossmon.db`)로 시작한다.

## 7. 블로커 — NASDAQ `$` 우선주 심볼이 `/stocks` 배치를 통째로 400 시킴 — `해결됨`

**증상**: `python -m tossmon.universe --config config/config.yaml` 라이브 실행 시 첫 청크
(200종목)에서 즉시 `SchemaMismatch: http-400 code=invalid-request` 로 죽음(`build_universe`가
예외를 잡지 않아 전체가 죽음).

**원인 진단(최소 호출로 확인, ~5콜)**: `tossmon/universe/seed.py`의 `_SYMBOL_RE`가 `$`를
유효 문자로 허용했다(NASDAQ Trader 는 우선주를 `ABR$D` 형식으로 표기). `/stocks`에 `$`
포함 심볼을 단 하나라도 섞어 배치 조회하면(청정 심볼과 섞어도) **배치 전체**가 400으로
거부된다 — 개별 무시가 아니라 요청 자체가 무효화. 참고로 `.` 포함 심볼(단위/워런트, 예
`AAC.U`)은 문제없음 — 200+빈결과로 조용히 필터됨(기존 미존재 심볼 처리와 동일).

**대응**: 코디네이터에게 즉시 escalate(정확한 원인 위치·1줄 수정안 포함), W2 재engage로
핫픽스(`$` 제외 + 배치 격리 + `rejected_charset`/`skipped_batches`/`skipped_symbols` 카운터
추가) → `main c5da76f`. 대기 중 **임시 지정 종목으로 먼저 수집을 시작할지**를 코디네이터와
논의했으나(한때 (b) 진행하려 했다가 코디네이터가 직접 철회), 최종적으로 **W2 수정을 기다렸다가
정식 유니버스로 한 번에 시작**하기로 결정했다(사전등록 프로토콜 오염 방지가 결정적 이유,
docs/12_preregistration.md — W5 소유 아님, 상세는 코디네이터 판단 참고).

## 8. 유니버스 빌드 결과 (2026-07-31T14:15~14:23 KST) — `확인됨`

```
tier0=1678 tier1=1500 former_runners=535 rejected_charset=0 skipped_batches=0 skipped_symbols=0
```

- **`rejected_charset=0` 은 정상이다(파싱 단계에서 이미 걸러짐)**: `parse_directory_file`이
  콘솔에 `"...: 387 symbol(s) rejected — outside Toss API charset..."`를 로그로 남겼다(파싱
  단계 1차 방어가 387개 전부 처리 → 배치 전송 전 2차 방어(`rejected_charset` 카운터)로 넘어올
  게 남지 않았다는 뜻 — 코디네이터가 미리 예고한 두 시나리오 중 "0일 수 있다" 쪽으로 확정).
  ⚠️ **부수 발견**: 이 로그 라인 자체가 Windows 콘솔 cp949 인코딩에서 em-dash(—) 때문에
  `UnicodeEncodeError`로 실패했다(`--- Logging error ---`, Python `logging` 모듈이 내부적으로
  잡아 프로세스는 안 죽었지만 그 줄은 유실됨) — 어제 `ops/supervisor.py`에서 고친 것과 **같은
  부류의 버그**가 `tossmon/universe/seed.py`(W2 소유)에도 있다. 치명적이지 않아 자체 수정하지
  않고 기록만 남긴다.
- **tier0=1678 / tier1=1500(정원 상한)**: 시드 7,478종목 중 22.4%가 tier0 통과 — "수십 개"도
  "수천 개"도 아닌 중간값이다. $0.1~$20 · $10M~$300M 은 미국 상장 마이크로/스몰캡 전반을
  포괄하는 넓은 대역이라(이 프로젝트가 표적으로 삼는 "동전주" 자체가 원래 개체수가 많은
  모집단), 이 정도 통과율은 필터가 헐겁다는 신호라기보다 **모집단 자체의 크기**로 해석한다
  (아래 가격·시총 표본이 이 해석을 뒷받침한다).
- **오염 사례 재확인 — 5종목 전부 `symbols` 테이블에 없음**:

  | 종목 | 결과 |
  |---|---|
  | NOK | NOT IN symbols table |
  | AMD | NOT IN symbols table |
  | ASML | NOT IN symbols table |
  | META | NOT IN symbols table |
  | GS | NOT IN symbols table |

  어제는 이 5종목이 전부 워치리스트를 채웠다 — 오늘은 유니버스 빌드 단계에서부터 존재하지
  않는다. 필터 작동의 직접 증거다.
- **가격·시총 분포 표본(tier1 무작위 15종목)**: 전부 `$0.90~$14.01` / `$24.5M~$275.3M` —
  설정 경계($0.10~$20, $10M~$300M) 안에 깔끔하게 들어온다. 단위 변환 버그(예: 마이크로달러
  스케일 오류로 경계를 우회하는 통과)의 흔적 없음.

  | symbol | price(USD) | mcap(USD M) |
  |---|---:|---:|
  | ISPR | 1.440 | 82.7 |
  | HOFT | 14.010 | 150.5 |
  | CLNN | 5.040 | 64.4 |
  | MHH | 7.950 | 95.4 |
  | INLX | 5.455 | 24.5 |
  | WTF | 2.680 | 129.3 |
  | TLYS | 3.710 | 113.1 |
  | OHAC | 9.930 | 224.7 |
  | GRO | 2.050 | 125.7 |
  | JABRU | 10.080 | 184.9 |
  | EXOD | 5.340 | 160.3 |
  | ZSQR | 5.220 | 275.3 |
  | KBSX | 0.900 | 40.3 |
  | BGSF | 5.250 | 56.3 |
  | VNMEU | 10.300 | 206.0 |

## 9. 수집 시작 (2026-07-31T14:25 KST, day 세션) — `진행 중`

- `Start-Process`로 완전히 분리된 프로세스로 기동(PID 29704 → 자식 collector PID 25444) —
  어제 배운 교훈(에이전트 세션 백그라운드 잡은 세션 정리 시 함께 죽음) 반영.
- 시작 로그: `universe: seeded 1500 tier1 symbols from the symbols table (watch=1500)` →
  `session closed → day` — 유니버스 시드가 정확히 소비됐고 세션도 즉시 올바르게 판정됨.
- 초기 텔레메트리에 신규 필드 확인: `watch_outside_universe=0 universe_rejected=0`(W4가 노출).
- 랭킹 진입 즉시 대형주 거부가 실측됨: MU, SNDK, SOXL, SKHY, KORU, EWY, DRAM, SOXS, TQQQ,
  NVDA, TSLA, QLD 등이 `universe: X rejected at tier0`으로 즉시 걸러짐. 반면 CYCU, MGRX(둘 다
  소형주, 어제도 tier3까지 올라갔던 종목)는 정상적으로 tier2 승격.
- 이후 30분 관측·중간 보고는 이 절 아래에 계속 추가한다(미완).

## 10. 30분 중간 점검 (14:25~14:48 KST) — `확인됨`

- **워치리스트 거부 카운터 실측**: 텔레메트리 신규 필드 `universe_rejected`가 0(14:25) →
  22(14:30) → 23(14:35) → 23(14:40) → 24(14:45)로 계속 증가 — 게이트가 살아서 계속 일하고
  있다는 직접 증거. `watch_outside_universe`는 시종 0(대형주가 워치리스트에 새는 사고 없음).
- **tier2/tier3 점유**: tier2 는 43~66 사이에서 유동적(day 세션은 `SESSION_TIER_SCALE`로
  정원이 300의 40%로 줄어 있어 원래도 좁다 — 정상 범위). **tier3 는 대부분 0이었다가
  14:46:23 에 `PLYX`가 `confirm` 경로(score=0.682)로 처음 진입** — 어제는 정원 20 중 최대
  4개였던 게 전부 대형주에 밀려난 자리싸움의 산물이었는데, 오늘은 유니버스가 깨끗해진 뒤
  **표적 소형주가 실제로 tier3 에 오른 첫 사례**다. 계속 관찰 필요(세션 종료 리포트에 tier3
  체류 이력을 어제와 나란히 비교할 것).
- **예산 실측**: `MARKET_DATA 0.15~0.33/7.00`, `MARKET_DATA_CHART 0.55~1.02/3.50`,
  `RANKING 0.28~0.32/3.50`, 신규 `STOCK 0~0.02/3.50`(tier0 실시간 판정용 `/stocks` 호출 —
  어제는 없던 그룹, W4가 `_resolve_universe`에서 추가). 전부 여유 큼.
- **429 실측(정상 처리 확인)**: 14:30:05 `budget: 429 on MARKET_DATA_CHART (count=1) —
  forcing tier shrink` 1건 발생·정상 대응 로그 확인. `api_errors=0` 유지. `ForbiddenEndpoint`
  미처리 예외(W4 미머지 항목) 관련 크래시 없음.
- **대형주 거부 실측 로그**: `MU, SNDK, SOXL, SKHY, KORU, EWY, DRAM, SOXS, TQQQ, NVDA, TSLA,
  QLD` 등이 랭킹 진입 즉시 `universe: X rejected at tier0`으로 걸러짐(가격·시총 로그까지 함께
  남아 근거 확인 가능).

## 11. 비계획 정지 #3·#4 — 수집 공백 2건 (18:07 트리 킬 8분 / 20:48 토큰만료×env 102분) — `해결됨` / docs/12 §7-(b) 갭 매니페스트 등재

**공백 구간 (docs/12 §7-(b) 매니페스트 항목)**:

| # | 정지 (KST) | 재개 (KST) | 길이 | 세션 | 영구 손실 | 복구 가능 |
|---|---|---|---|---|---|---|
| ③ | 2026-07-31 18:07:30 | 2026-07-31 18:15:36 | 8분 06초 | pre | 랭킹 스냅샷·테이프 | 1분봉(백필) |
| ④ | 2026-07-31 20:48:20 | 2026-07-31 22:30:59 | 1시간 42분 39초 | pre + regular 개장 첫 1분 | 랭킹 스냅샷·테이프 (**22:30:00~22:30:59 정규장 개장 순간 포함**) | 1분봉(백필) |
| ⑤ | 2026-07-31 22:31:19 | 2026-07-31 22:39:18 | 7분 59초 | regular 개장 직후 | 랭킹 스냅샷·테이프 (개장 2~9분 구간) | 1분봉(백필) |

- 처리 규칙은 docs/12 §7-(b) 그대로: 랭킹 의존 지표(Q2·쏠림도)에서 이벤트의
  [T0−120분, T0+120분]이 이 구간과 겹치면 Q2 표본 제외 + 제외 수 보고.

**사망 정황 (원인: 외부 종료로 판단)**:

- collector PID 29236·supervisor PID 29704 가 **동시에, 흔적 없이** 소멸. 마지막 로그는
  18:07:30,798 정상 INFO(`tier ↑ SLND: 2→3 confirm`) — traceback 없음, stderr 출력 없음,
  Windows Application 이벤트 로그(WER/Application Error)에도 항목 없음, supervisor 의
  재시작 시도 로그도 없음(부모까지 같이 죽었다는 뜻).
- `ForbiddenEndpoint` 미처리 크래시(교대 인수인계의 1차 용의자)라면 traceback 이 남고
  supervisor 가 자식을 재시작했어야 한다 — 정황 불일치. **부모+자식 동시·무음 소멸은 외부
  프로세스 트리 킬**과 일치하며, 같은 날 원 W5 에이전트 PTY 소실(14:2x)·본 교대 요원의
  분리 백그라운드 모니터 외부 종료(17:41)와 같은 계열(Orca 런타임의 PTY/잡 정리)로 추정.
  §9 의 `Start-Process` 분리 기동도 이 종료 경로 앞에서는 불충분했다.

**복구 절차 (코디네이터 지시 6단계, 18:1x KST)**:

1. `git rebase main` — old HEAD `8578dab` → new HEAD `67445d2` (main `fc3a9cc`, 795 테스트
   green). 재기동 프로세스에 `ForbiddenEndpoint` 종료 처리·정직한 RANKING 경보(`e512463`)·
   A7 상태파일 권한·W2 charset 수정이 포함됨.
2. 재기동은 **Windows 작업 스케줄러 일회성 작업**(`tossmon-collector-oneshot`, schtasks
   `/SC ONCE` + `/Run`)으로 — 프로세스 트리가 스케줄러 서비스(svchost) 소속이 되어 어떤
   Orca PTY/잡 오브젝트에도 속하지 않게 함. 기동 후 트리거 즉시 비활성화(Next Run: N/A,
   이중 기동 방지). supervisor stdout 은 `data\supervisor.stdout.log` 로 리다이렉트.
3. DB 그대로 재사용(WAL 은 사망 직후 18:07:36 플러시 확인, `symbols` 무손상). 유니버스
   재빌드 없음, collector 자체 외 추가 라이브 호출 없음. 상태 이어받기 정상:
   `resumed from data\collector_state.json: watch=1500 tier2+=95` (18:15:36).
4. 기동 검증 3종 통과: ① 부모 체인 `cmd.exe ← svchost(Schedule) ← services.exe` (Orca 밖),
   ② `collector.log` 18:15:36 부터 재흐름 + `session closed → pre` 정상 판정,
   ③ 재기동 후 가짜 `budget: RANKING predicted…` ERROR **0건** (구프로세스에서 13초 간격
   스팸 → 신코드 가동의 직접 증거).
5. 신프로세스 트리(Python 3.13 venv 런처가 실제 인터프리터를 자식으로 spawn): supervisor
   36084→48156, collector 1928→4544.

### 11-1. 정지 #4 — 토큰 만료 × 스케줄러 env에 `TOSS_BASE_URL` 부재 (20:48:20~22:30:59) — `해결됨`

**증상이 특이했다: 프로세스는 살아서 로그를 쓰는데 API 만 전면 사망.** 20:48:20 부터
`unexpected RuntimeError: TOSS_BASE_URL is not set (계약 C-9: 기본값 없음)` WARNING 이
35,000건+ 누적됐고, 텔레메트리는 계속 찍히되 budget 전 그룹 0.00 / `precision_parsed`·
`promotions` 등 카운터 완전 동결. **`api_errors` 는 끝까지 0** — RuntimeError 가 호출별
WARNING 으로 삼켜져 api_errors 카운터에도, 로그 신선도 기반 감시(모니터)에도 안 걸렸다.
22:25 개장 준비 체크포인트에서 연속 두 텔레메트리가 완전 동일한 것을 보고 발견.

- **원인 사슬**: 어제 20:49 발급 토큰이 오늘 20:48 만료(TTL 24h) → 재발급 경로
  `tossmon/api/tokens.py:371` 은 계약 C-9 에 따라 **기본값 없이 env `TOSS_BASE_URL` 만**
  읽음 → 18:15 스케줄러 재기동(§11 복구 2단계)의 cmd 환경에는 그 env 가 없었음(14:25 원
  프로세스는 원 셸에서 상속받아 정상) → 재발급 실패가 모든 호출로 전파. 기동 시
  `base_url` 은 config.yaml 에서 와서 **토큰이 살아 있는 동안(18:15~20:48)은 정상 수집** —
  잠복했다가 만료 순간 발병한 구조.
- **env 전수 감사(코디네이터 지시)**: tokens.py 가 읽는 env 는 `TOSS_BASE_URL`(필수·기본값
  없음), `TOSSMON_LEASE_DIR`(선택 재정의 — 미설정 시 LOCALAPPDATA 폴백, 기존 리스 위치
  유지를 위해 **의도적으로 미설정**), `LOCALAPPDATA`/`USERNAME`/`USERDOMAIN`(스케줄러
  사용자 세션에 존재 — 18:15~20:48 라이브 수집으로 실증). 추가 설정 필요분은
  `TOSS_BASE_URL` 하나로 확정.
- **복구(코디네이터 승인 RESTART-NOW-WITH-ENV, 22:29)**: ① 트리 kill(22:28:44, taskkill /T)
  ② 런처를 `.cmd` 파일로 교체 — `set "TOSS_BASE_URL=https://openapi.tossinvest.com"` 후
  supervisor 기동(값은 비밀 아님 — 기동 로그에 상시 노출되는 값) ③ 1차 재기동은 기동
  10초 만에 트리거 /DISABLE 을 호출했더니 인스턴스가 0xC000013A(STATUS_CONTROL_C_EXIT)로
  동반 종료되는 함정 발견 — **교훈: 일회성 작업 비활성화는 기동 안정화(수 분) 후에** ④ 2차
  재기동 22:30:59 성공.
- **검증**: 22:31:00 `collector start base_url=… live=True` → **22:31:01 collector 스스로
  토큰 재발급**(token_state.json 갱신 — 수동 발급 없음, 리스 보존) → 22:31:02
  `session closed → regular` 정상 판정 → 재기동 후 `TOSS_BASE_URL` 오류 0건, 상태 이어받기
  `watch=1500 tier2+=247`. 재기동 직후 tape gap WARNING 다수는 실명 구간(20:48~22:30)의
  테이프 불연속을 수집기가 올바르게 탐지·등록한 것(오류 아님).
- **후속 과제(수집 코드 소유자 앞, 관측만 기록)**: (i) 토큰 재발급 연속 실패는 지금처럼
  per-call WARNING 으로 삼키지 말고 `api_errors` 집계 또는 fail-fast 로 승격해야 감시가
  잡는다 — "api_errors=0 인데 API 전멸" 은 텔레메트리 사각. (ii) 기동 환경 재현성을 위해
  ops_config `collector_cmd` 에 필수 env 를 명시하는 방안 검토.

### 11-2. 재기동 불안정 — 1~3차 기동이 10~30초 만에 0xC000013A 동반 사망, 4차 성공 (공백 ⑤)

env 수정 자체는 2차 기동에서 즉시 검증됐으나(22:30:59 기동 → 22:31:01 토큰 자가 재발급 →
22:31:02 `session=regular`), 1차(22:29:51)·2차(22:30:56)·3차(22:35:10) 기동이 모두 기동
10~30초 뒤 **STATUS_CONTROL_C_EXIT(0xC000013A)** 로 cmd·supervisor·collector 가 동반
사망했다(콘솔 단위 Ctrl 이벤트 — supervisor stderr 무출력 = 자식 사망을 인지하기도 전에
같이 죽음). 배제한 용의자: STOP 파일(없음), supervisor 재시작 예산(메모리 전용이라 새
프로세스에 미승계), Defender(치료 이벤트 없음), 스케줄러 작업 설정(1~4차 동일), Temp 의
.cmd 런처(3차는 인라인 /TR 로도 사망). **4차(22:39:12)와의 유일한 재현 가능 차이는
"schtasks /Run 을 호출한 셸이 즉시 종료됐고, 같은 호출 안에서 sleep·폴링을 하지 않았다"는
것** — 에이전트 도구 호출(샌드박스)이 활성인 동안 태어난 프로세스 트리가 호출 정리 시
함께 걷히는 것으로 추정(확증은 못 함 — 스케줄러 운영 로그 비활성). 2초 간격 프로세스 트리
관찰자를 붙인 4차 기동은 사망 창(30초)의 4배를 넘겨 안정 확인.

**운영 규칙으로 채택: 스케줄러 작업 기동은 fire-and-forget** — `/Run` 만 하고 즉시 셸을
빠져나온 뒤, 검증은 별도 호출에서 로그·프로세스 카운트로 한다. 트리거 비활성화도 기동
안정화(5분+) 후 별도의 짧은 호출로만.

### 11-3. 애프터 세션 랭킹 스냅샷 전량 유실 — SQLite int64 OverflowError — `해결됨(세션 종료로 자연 종결)` / 부분 유실 기록

**05:00:36(애프터 전환 30초 뒤)부터** 랭킹 폴링마다
`rankings: unexpected OverflowError: Python int too large to convert to SQLite INTEGER`
WARNING 이 1건씩 발생 — 발생 간격이 애프터 세션 랭킹 폴링 주기(~47초)와 1:1 로 일치, 즉
**애프터 세션의 `rankings_snap` 기록이 사실상 전량 실패**. 06:55 체크포인트에서 발견하여
escalation(msg_05a84a75a156). 코디네이터 응답 대기 중 세션이 진행되어, 응답 지연 시
애프터 마감(09:00)으로 자연 종결되는 구조였다.

- **또 하나의 "api_errors=0 사각"**: DB 쓰기 실패가 per-poll WARNING 으로 삼켜져
  `api_errors` 에도, 텔레메트리 어디에도 집계되지 않는다 — §11-1 후속 과제 (i)과 동일
  부류(이번엔 API 가 아니라 DB 쓰기 경로). 감시 규칙에 "동일 WARNING 반복 폭주" 탐지를
  추가할 근거가 하나 더 생겼다.
- **원인 가설(미확정 — 심볼이 로그에 안 찍힘, DB 열람은 잠금 경합 회피 원칙상 미실시)**:
  애프터 세션 랭킹 페이로드의 마이크로 단위 정수 필드가 int64 상한(9.22e18)을 초과.
  정규장에서 INTC `mcap_u=490377680000000000`(4.9e17)이 실측된 바 있어, 초대형주
  (mcap ≥ ~$9.2조 상당) 또는 이상치 volume 계열이 유력. 재현 조건이 "애프터 세션
  랭킹에 해당 종목 등장"이므로 세션 의존적.
- **영향 범위**: 애프터 세션(05:00~09:00)의 랭킹 의존 지표만 실명. 티어 이동·1분봉·
  이벤트 파이프라인은 정상 동작(05시 이후에도 EVENT win: LHSW·MODD·BIOT·NRSN(precursor)·
  SLGB(confirm), day: ELOX). docs/12 §7-(b) 처리 규칙 적용 시 이 구간과 겹치는 Q2
  표본은 제외 대상.
- **소유권**: 랭킹 수집 루프는 W4 소유 코드 — W5 는 관측·보고만 수행(불변 규칙 준수).
