# docs 색인 — `docs/` 전수 목록

> **이 파일이 `docs/` 의 유일한 전수 목록이다.** 여기 없는 문서는 `docs/` 에 없다.
> 갱신: 2026-08-13 · 기준 리비전 `a868e1a`
>
> 이전까지 문서 지도는 `docs/00_HANDOFF.md` §9 하나뿐이었고 거기 적힌 것은
> `01`·`02`·`03`·`04`·`05`·`06`·`07`·`08`·`09`·`11` **열 개뿐**이었다.
> `docs/` 에는 **60 개**가 있었으므로 **50 개가 어느 색인에도 없었다.**
>
> 이 표가 전수인지는 기계로 대조한다 — 표의 행 수와 `docs/*.md` 실제 파일 목록을
> 맞춰 보고 **양쪽 차집합이 0** 이어야 한다. ~~2026-08-13 기준 60행 / 누락 0.~~
> → ~~**2026-08-13(늦게) 기준 63행 / 누락 0.**~~ (`58`·`59` 는 아래 W3 정정, `60` 은 같은
> 창에서 W1 이 신설.)
> → ~~**2026-08-13(더 늦게) 기준 64행 / 누락 0.** `61` 은 **존재하지 않는 번호**다 —
> 건너뛴 것이지 누락이 아니다.~~
> → **2026-08-13(제일 늦게) 기준 65행 / 누락 0.** **`61` 은 존재한다** — 같은 창에서
> W4 가 `61_ranking_promotion.md`(D-21)를, W1 이 `62_open_cadence_plan.md` 를 각자
> 신설했고 서로의 브랜치를 못 봤다. **번호를 건너뛴 것이 아니라 동시에 쓴 것이다.**
> 두 브랜치가 이 문단을 동시에 고쳤고 병합에서 **합집합**으로 폈다. 위 재현 명령을
> 실제로 돌려 확인했다: `MISSING: []`, 표 데이터 행 65 · `docs/*.md` 65.
>
> ### ★ 정정 — **누락 0 이 아니었다** (2026-08-13, W3)
>
> `docs/58_gate.md` 가 **전수 표에 없었다.** 이 색인의 §역할별 빠른 진입(아래)에는
> 적혀 있었지만 위의 전수 표에는 행이 없었고, 그래서 *"60행 / 누락 0"* 이 거짓이었다.
> 기준 리비전 **`0c769e1`** 에서 확인했다 (`docs/*.md` 61 개 vs 전수 표 60 행).
> **`58` 과 `59` 두 행을 더해 62 행으로 맞췄다.**
>
> 이 색인이 스스로에 대해 틀린 첫 사례다. **표의 행 수를 손으로 세지 마라** — 위
> 대조를 실제로 돌려라. 재현:
> ```
> python -c "import re,pathlib; idx=pathlib.Path('docs/INDEX.md').read_text(encoding='utf-8'); \
> sec=idx.split(chr(35)*2+' 전수 표 (번호순)')[1].split(chr(10)+'---')[0]; \
> rows=set(re.findall(r'\`([^\`]+\.md)\`', sec)); \
> files={p.name for p in pathlib.Path('docs').glob('*.md')}-{'INDEX.md'}; \
> print('MISSING:', sorted(files-rows))"
> ```
> **`files - rows` 방향만 본다.** 반대 방향(`rows - files`)은 오탐이 난다 —
> 행 **본문**에 옛 파일명을 인용한 칸이 있기 때문이다(57 행이 `51_detector_windows.md`
> 를 언급한다). 오탐을 아는 채로 두는 것이 정규식을 조이는 것보다 낫다:
> 본문 인용을 지우면 번호 충돌 이력이 사라진다.

---

## 이 색인을 쓰는 규칙 (넷 다 비싸게 배운 것이다)

1. **번호는 바꾸지 않는다.** `docs/NN` 인용이 **코드에 822 줄**
   (`tossmon/**`, `ops/**`, `tools/**`, `tests/**`), **문서에 2,190 건**
   (`docs/**` + `coordination/**`) 이다. 대부분 파일명 없이 **번호만** 적는다.
   번호를 옮기면 그 전부가 조용히 틀린 곳을 가리킨다.
   재현: `grep -rn "docs/[0-9]" --include=*.py --include=*.ps1 tossmon/ tools/ ops/ tests/ | wc -l`
2. **요약표를 근거로 훑지 마라. 원문 파일을 열어라.** 이 표도 요약표다.
   실제로 W7 의 훑기표가 없는 주장을 원본에 있다고 적었고, **그 표를 받아 파일을 실제로 연
   W3 이** 잡았다 (`docs/48` §12-7a, 전사 커밋 `7c6258e`).
3. **부재 주장에는 리비전을 함께 적어라.** *"~라고 안 적혀 있다"* 는 읽은 판에 전적으로
   의존한다. 인용은 낡은 체크아웃에서도 살아남지만 부재는 아니다.
4. **번호만 떼어 인용하지 마라.** `docs/44` 의 D-17·D-18 과 `USER-INPUT-QUEUE` 의
   D-17·D-18 은 **서로 다른 안건**이다. 어느 문서의 번호인지 함께 적는다.

## 상태 표기의 뜻

| 표기 | 뜻 | 인용할 때 |
|---|---|---|
| **현행** | 지금도 그대로 구속하거나 참이다 | 그대로 인용 가능 |
| **기록** | **그날은 참이었다.** 지금 참인지는 이 표가 보증하지 않는다 | **어떤 자로 쟀는지 병기 의무** |
| **폐기** | 이제 거짓이다 | **인용 금지.** 후속 문서를 대신 본다 |
| **[미판정]** | 판정할 근거가 없다 | 사용자 확인 대기 — §미판정 참조 |

> **"기록" 은 "틀렸다" 가 아니다.** 이 프로젝트에서 가장 비싼 실패는 전부 *조용히 틀리는*
> 것이었고, 그중 가장 위험한 형태는 **낡은 진술을 현행으로 읽는 것**이다. 그래서 낡음을
> 지우지 않고 **표시**한다.

---

## 전수 표 (번호순)

| # | 문서 | 소유 | 장르 | 상태 | 한 줄 · 상태 근거 |
|---|---|---|---|---|---|
| 00 | `00_HANDOFF.md` | 코디 | **설계 근거** | **현행** | **2026-08-13 재작성.** *"왜 이렇게 지었는가"* 만 남겼다 — 논제 3 세대, API 실측 요약, 아키텍처, 계약 개정의 이유, **비싸게 배운 결함 18 건**. 낡은 상태 진술(613 tests·리허설 중)·옛 문서 지도·검증 질문 6 개는 걷어냈고 **행선지를 §7 에 적었다** |
| 01 | `01_api_analysis.md` | W1 | 스펙 | **현행** | OpenAPI v1.2.5 분석. 스펙 자체는 안 바뀐다. **실측 정정본은 `06`** — 둘이 다르면 06 이 이긴다 |
| 02 | `02_theory_background.md` | 코디 | 문헌 | **현행** | Barber 2022(로빈후드 어텐션), La Morgia 2020(P&D), 기저율. 논문은 낡지 않는다 |
| 03 | `03_phase1_monitor_design.md` | 코디 | 설계 | **부분 폐기** | 티어드 감시 구조(§1)·이벤트 정의(§3 앞부분)는 현행. **§3 말미 "핵심 검증 질문 6 개" 는 폐기**(2026-08-13 사용자) |
| **04** | `04_contracts.md` | 코디 | **계약** | **현행** | C-1~C-11 + 개정 A1~A6. **상충 시 이 문서가 이긴다** |
| 05 | `05_orchestration_plan.md` | 코디 | 운영 | **기록** | 2026-07-30 웨이브 배치표(W1~W6). 그 웨이브는 끝났다 |
| **06** | `06_live_facts.md` | W1 | **실측 정본** | **현행** | 라이브 호출로 확인한 것만. API 사실의 정본 |
| 07 | `07_analysis_spec.md` | 코디 | 스펙 | **기록** | 1 분봉 분석의 지표 정의식. **`12` 와 함께 동결**(2026-08-13 사용자) — 고해상도 재시작에는 새 명세가 필요하다 |
| 08 | `08_runbook.md` | W5 | 운영 | **현행** | 무인 운영 절차·장애 대응 |
| 09 | `09_secret_hygiene.md` | W5 | 운영 | **현행** | 시크릿 정책 |
| 10 | `10_audit.md` | W6 | 감사 | **기록** | 적대적 감사 1차. 기준 `e1ed22d`(636 tests) |
| 10-B | `10_audit_b.md` | W6b | 감사 | **기록** | 독립 감사자 B. 같은 기준 커밋, 반증 지향 |
| 11 | `11_live_rehearsal.md` | W5 | 측정 | **기록** | 2026-07-30~08-04 리허설. 발견 대부분은 `06` 에 흡수됨 |
| **12** | `12_preregistration.md` | W7 | **사전등록** | **기록** ★불가침 | **파일을 고치지 않는다.** 1 분봉 분석의 절차 정본. **고해상도 재시작은 덮지 않는다**(2026-08-13 사용자) — 새 사전등록이 필요하다 |
| **13** | `13_trial_registry.md` | W3 | **시행 기록** | **기록** ★불가침 | append-only. 행 삭제·수정 금지. `12` 와 한 쌍 |
| 14 | `14_audit_prereg_align.md` | W6 | 감사 | **기록** | 감사 3 차. 사전등록 정합 |
| 15 | `15_phase1c_report.md` | W3(작성)·W7(표기) | 분석 결과 | **기록** | Phase 1-C 본분석. **1 분봉 자로 쟀다** |
| 15-부록 | `15_phase1c_appendix_run_all.md` | W3·W7 | 분석 결과 | **기록** | 위 문서의 `run_all` 산출 표 |
| **16** | `16_strategy_design_notes.md` | 코디 | **전략 정본** | **현행** | 논제·수익 목표·설계 제약. **전략 방향의 정본** |
| 17 | `17_precursor_redesign.md` | W3 | 측정 도구 | **기록** | 회전 인지형 전조 측정식. **회전 논제는 사망**(D-6) |
| 18 | `18_execution_reality.md` | W3 | 측정 | **기록** | 스프레드·실효 왕복 비용 실측(3.22%~6.94%). **호가 스냅샷 추정이고 실체결 0 건** |
| 19 | `19_rule_search.md` | W3 | 분석 결과 | **기록** | 조건부 규칙 640 개 탐색. **전부 CI 하한 음수** |
| 20 | `20_audit4.md` | W6 | 감사 | **기록** | 감사 4 차 |
| 21 | `21_cost_amendment_memo.md` | W7 | 제안 | **현행** | 비용 전제 개정 권고(B안). **D-1 미결이라 살아 있는 안건** |
| 22 | `22_rotation_exit.md` | W3 | 분석 결과 | **기록** | 랭킹 기반 동적 이탈. **이탈 축 사망**(D-6) |
| 23 | `23_shooting_structure.md` | W3 | 분석 결과 | **기록** | 연속 슈팅 구조. 표본 = 랭킹 수집 2 매매일 |
| 24 | `24_validation_contamination_memo.md` | W7 | 제안 | **현행** | 검증 기간 오염 처리(D안 권고). **D-3 미결** |
| 25 | `25_audit5.md` | W6 | 감사 | **기록** | 감사 5 차. 첫 양수 결과를 공격해 기각시켰다 |
| 26 | `26_rotation_design_draft.md` | 코디 | 설계 초안 | **기록** | 회전 논제 시험 설계안. **논제 사망**(D-6) |
| 27 | `27_deceleration_entry_design.md` | 코디 | 설계 | **기록** | 감속 진입. **사망**(D-6) — 최대 효과 8.33bp vs 비용 66bp |
| 28 | `28_tick_instrument.md` | W3 | 측정 | **기록** | 틱 해상도 계측기. 2026-08-04 스냅샷 |
| 29 | `29_tick_tasting.md` | W3 | 측정 | **기록** | 닫힌 후보 맛보기. **(라) 1 건 열려 있음** — 수집기 3 번째 경계 재확인, 라이브 관측이라 문서만으로 못 가름 |
| 30 | `30_infra_20260804.md` | W5 | 사고·인프라 | **기록** | 그날 배포 4 회의 전체 기록. 데이터 경계 문서 |
| 31 | `31_gap_audit.md` | W5 | 도구 | **현행** | 결손 감사. `ops/gap_audit.py` 가동 중 |
| 32 | `32_429_group_contract.md` | W1 | 측정 | **기록** | 1 초 한도 계측 불신. → `37` → `45` → `46` → `52` → `56` 사슬의 시작 |
| 33 | `33_budget_governor_open_collapse.md` | W4 | 사고 | **기록** | 2026-08-04 개장 사고 |
| **34** | `34_alert_grades.md` | W5 | **계약** | **현행** | 경보 등급 4 종. 범위 제한은 `53` |
| 35 | `35_ranking_cadence.md` | W1 | 측정 | **현행** | **서버 10 초 격자 · 29% 유실 · 중앙 16.1 초 늙음.** 폴 주기 결정 미결(D-8). ★ **§7 에 정규장(08-18) 재측정** — 같은 정의로 **33.3%**, **별칭 성분 ~0**(같은 응답열을 8 배 굵게 읽어도 안 움직인다). 다만 그 창의 `rankedAt` **초 자리가 넷뿐**이라 **분모 가정이 프리마켓과 다르다**(§7-4). ★ 29% 를 *"폴 12.4 초 탓"* 으로 읽는 오인이 `docs/00_HANDOFF` 106 행에 **아직 살아 있다**(§7-5) |
| 36 | `36_candle_label_blast_radius.md` | W3 | 측정 | **기록** | 1 분봉 라벨 어긋남 84 지점. 수정은 `47` |
| 37 | `37_quiet_counters.md` | W4 | 측정 | **기록** | `api_errors`·정원 격차. §3.5 해석은 `45` §176 이 정정 |
| 38 | `38_dispatch_sweep.md` | W5 | 도구 | **현행** | 완료 보고를 산출물과 대조 |
| 39 | `39_top_gainers.md` | W4 | 변경 기록 | **기록** ⚠ | **제목이 지금 거짓이다** — *"배포는 안 했다"* 인데 **배포됐고 수집 중**. 아래 §정정 대기 1 |
| 40 | `40_d12_amendment.md` | W7 | 사전등록 개정 | **기록** | 홀드아웃 경계 봉 라벨. `12` 부속 |
| 41 | `41_tick_observation.md` | W3 | 측정 | **현행** | 0 단계 관문 — 우리 스트림이 무엇을 볼 수 있나. **고해상도 재시작의 자** |
| 42 | `42_intra_second_order_blast_radius.md` | W3 | 측정 | **현행** | 초 안 순서 의존의 범위 |
| 43 | `43_tape_saturation.md` | W4 | 측정 | **현행** | 테이프 50 건 상한 포화 계측 |
| 44 | `44_tick_stages.md` | W3 | 측정 | **현행** | 초 막대 위의 슈팅. **D-19·D-20 열려 있음**(이 문서 안 번호는 D-17·D-18) |
| 45 | `45_limiter_contract_gap.md` | W1 | 측정 | **기록** | *"리미터가 아니라 계측이 거짓말한다"*. 근본 수정은 `52` |
| 46 | `46_double_billing.md` | W4 | 수정 | **기록** | 이중 계상 수정. 잔여 첨두의 주인은 `52` 가 찾았다 |
| 47 | `47_label_fix.md` | W3 | 수정 | **기록** | `36` 이 센 49 지점 수정. **과거 판정 재실행 안 함**(D-10) |
| 48 | `48_cutoff_amendment.md` | W7 | 사전등록 개정 | **기록** | 컷오프 관측가능성. **§12 가 `docs/**` 55 개 전수 훑기표** — 오염 지도의 원본 |
| 49 | `49_cutoff_code.md` | W3 | 수정 | **기록** | 나-컷 3 곳 산술 수정 |
| 50 | `50_a2_alignment.md` | W4 | 정합 | **기록** | 실시간 검출기 인용 정정. **동작 코드 0 줄** |
| 51 | `51_anchor_boundary.md` | W3 | 측정 | **기록** | 앵커 경계 실측(2/60). **`docs/51` 이라는 인용은 이 문서를 뜻한다** |
| 52 | `52_send_time_accounting.md` | W4 | 수정 | **현행** | 계상을 완료→송신 시각으로. **잔여 첨두 11~13 의 주인은 시계였다**. **§13(2026-08-13): 사각지대 `srv_s_unknown` 은 429 였다** — 프로세스당 최대 23·중앙 12, 정산된 서버 초의 0.18%. §12.5·§12.6 을 정정한다 |
| 53 | `53_planned_scope.md` | W5 | **계약** | **현행** | 계획 표식은 창이 아니라 **키**를 덮는다 |
| 54 | `54_stale_claims_code.md` | W3 | 훑기 | **기록** | `tossmon/analysis/**` 주석 훑기 17 지점 |
| 55 | `55_foreign_quota.md` | W4 | 측정 | **현행** | `foreign` 의 정체. ~~**(B)/(C) 판정 열려 있음**~~ → **§11 에서 판정됐다**(2026-08-14): 정규장 **1 세션 78 창 전수**(08-13 22:30:08~08-14 04:56:48, **프로브 없음**)에서 RANKING `frnmax` **최대 2** → 사전 규칙(`COORDINATOR-STATE` §1-1, 측정 **전**: 4~7→(B) / ≤2→(C))상 **(C) 창 어긋남**. **"(C) 로 확정" 이 아니다 — 정규장 하나다.** **§3 의 (B)(두 번째 서버 리미터)는 그대로 확정** — 갈린 것은 *"그 세션의 `foreign` 을 무엇이 만들었나"* 다. ★ **`frnmax` 는 0 아니면 정확히 2 이고 1 이 한 번도 안 나온다 — 이 양자화는 (C) 가 설명하지 못한다. 미해결**(§11-3). 탐지기는 **한 번도 교정된 적이 없고** 교정은 `docs/62` §8-3 프로브 창에서 **(나)** 로 한다 |
| 56 | `56_limit_clamp_visibility.md` | W1 | 측정 | **현행** | 서버가 한도를 올렸는데 우리가 잘랐다. ~~텔레메트리 어긋남 열려 있음~~ → **§9 에서 닫혔다**(2026-08-13, `cbc3b08`): **어긋난 적이 없고** 진짜 결함은 `counter_scope` 오선언이었다. `MARKET_DATA` 도 15>10 으로 잘리는 중이라는 라이브 사실이 §9 에 있다 |
| 57 | `57_detector_windows.md` | W5 | 수정 | **현행** | 계측이 거짓말하는 네 군데. **← 옛 `51_detector_windows.md`**, 번호 충돌로 이동 |
| **58** | `58_gate.md` | 코디 | **관문** | **현행** | Phase 2 착수 관문 G-0~G-5. **`docs/03` §3 의 검증 질문 6 개를 대체한다**(그쪽은 폐기). §1-4 의 고리 하나가 사용자 결정 대기 |
| 59 | `59_hires_event_inventory.md` | W3 | 측정 | **현행** | 고해상도 랭킹 사건의 **자**. E1·E2·E3 를 랭킹만으로 정의하고 **표본만 셌다**(결과 미측정). ★ **사건의 98.2% 는 300 초 안에 체결이 없어 결과를 잴 수 없다** — 원인은 시장이 아니라 수집 범위. **§11(08-18) D-21 경계 앞뒤를 따로 재실행** — 이전 10 세션 / 이후 2 세션, 격자 위 잴 수 있는 몫 1.85% -> 3.04%. ★ **D-21 판정(25.9->29.1%)과 분모가 달라 빼거나 나누면 안 된다**(그쪽은 종목 집합, 이쪽은 사건) |
| **60** | `60_poll5s_cost.md` | W1 | **측정** | **현행** | 폴 주기 5 초의 비용. **배율은 2.5 가 아니라 2.25×**, API 는 천장의 11% 로 무해, **디스크 수명 214 → 109 일**. §5-3 에 무손실 41.7% 감축안, §6 에 *"5 초로 못 사는 것"*. §10 이 §6 을 정정한다 |
| **61** | `61_ranking_promotion.md` | W4 | 측정·변경 | **현행** | D-21 랭킹 진입을 승격 사유로. ★ **`trades_snap` 은 tier3 에서만 채워진다**(tier3 종목 985 = 체결 있는 종목 985) — 그래서 기존 `ranking_entry` 1,011 건이 전부 tier2 라 **테이프를 한 건도 안 늘렸다**. 설계 3 안 + 9 정규장 시뮬(진동 0). ★ **좌석 하나 = 0.583 req/s, 순증 상한 +2, 그런데 `tier3_cap` 은 이미 표본의 72.8% 에서 10 아래다**. **기본값 꺼짐으로 병합** |
| **62** | `62_open_cadence_plan.md` | W1 | **계획** | **현행** | 정규장 재측정 발사 계획 **+ 라이브 결과**. ★ **2026-08-18 22:31:01~23:14:02 쐈고 완주**(D 안 · 529 콜 · `aborted null` · 우리 429 0, §10). 산출물 `coordination/daily/2026-08-18_probe_d_output.json`. ★ **세 팔이 4.0 회/분에서 만난다** — `order`·`volume` 은 `rankedAt` 과 **함께만** 바뀌고 매초 바뀌는 것은 **가격**이다. §9 가 **`.cmd` 발사 기전이 이 기계에서 10 초 만에 `0xC000013A` 로 죽는 것**을 실측하고 콘솔 없는 런처로 바꿨다. ★ **`open` 프로파일을 그대로 쏘면 분해가 안 된다** — 두 팔 다 300 초 = 30 슬롯이라 CI ±15.6pp, 갈리는 최소 격차 약 25pp. 5 초는 **솎기 stride 5 로 콜 0** 에 얻는다. §6 이 분석 계획을 사전에 못박았다 |
| **63** | `63_ranking_gap.md` | W4 | 측정 | **현행** | 개장 첫 70 분의 랭킹 수집 공백. ★ **멈춘 것은 랭킹만이 아니다** — 08-14 정규장에 랭킹·호가가 **같은 초에** 20 초 넘게 조용한 구간이 21 개, 그중 셋은 60~71 초다. 그리고 프로세스 안 계측기가 `md`·`chart`·`rank` `peak_1s` **셋 다 0** 인 창을 둘 보여준다(재기동 아님) — **캔들도 같이 멈췄다.** ★ **`candles_1m` 에는 폴 시각 컬럼이 없다**(`ts_ms` 는 봉 분) — *"캔들은 멀쩡했다"* 는 그 표로는 못 재는 것이다. 가설 2(중복 억제)·3(서버)은 죽었고, 남은 것은 그룹과 무관한 송신 정지 하나뿐인데 **그 경로를 세는 카운터가 없어서 못 갈랐다** — 그 셋을 텔레메트리에 실었다(동작 0 줄). ★ **§12 (08-18): 토큰 락 가설은 죽었다** — 락의 연속 보유 상한은 63.5 초가 아니라 15 초이고(코드), 08-17 창에는 토큰 POST 자체가 없었다(상태파일 mtime). *"밀림이 공백의 절반"* 은 기전이 아니라 **정지가 텔레메트리 마감을 가로지른 만큼**이라는 산수다(오차 1 초 이내). 그리고 **개장 첫 70 분은 성질이 아니었다** — 08-17 은 23% 만 앞에 있다. 남은 후보 12 개를 반증 설계와 함께 표로 세웠고, `loop_lag_max_ms`·`db_write_max_ms` 게이지 둘을 넣었다(동작 0 줄, 미배포) |
| **64** | `64_ranking_forward_path.md` | W3 | 측정 | **현행** | 랭킹 사건의 **전방 가격 경로** (G-2 **탐색 · 판정 아님**). `docs/59` 격자 위에 결과를 얹었다. ★ **변동성·테이프 밀도를 같은 종목·같은 세션에서 맞춘 위약과 여섯 칸 전부 0 을 교차한다** — 보정 전에도 후에도. 본페로니 분모(18)를 **러너가 스스로 센다**(§4.4-F 재발 방지). ★ **`max_ret` 은 기계적으로 양수**라 실제 팔 수치 단독 인용 금지. ★ 앵커가 붙는 사건이 1~5% 이고 그중 **36~50% 는 직전 60 초 체결이 2 건 이하라 정합 자체가 불가**(D-20 이 13.8% 가 아니라 여기서는 절반). ★ **전방 창의 테이프 깊이는 못 맞췄다 — 스스로 신고**(§9-1) → `docs/65` 가 다뤘고 **못 닫았다**. ★★ **이 문서의 10 세션에는 08-13(확증 팔)이 들어 있다** — 탐색 팔 9 세션으로 다시 재면 `TOP_GAINERS` 는 군집 4 라 **CI 가 통째로 없어진다**(`docs/65` §7 · §11). §6-2 의 2 번을 인용할 때 이 문장을 빼지 마라 |
| **65** | `65_forward_depth_and_tier.md` | W3 | 측정 | **현행** | `docs/64` 의 **자기 신고 두 개**를 정면으로 친 것 (G-2 **탐색 · 판정 아님**, D-27 = (가)). ★ **전방 창 깊이는 이번에도 못 맞췄다** — 앵커 **이전** 300 초 막대 수(`nbar300`)를 정합 축에 넣었더니 밴드는 물렸지만(254 vs 241) **앵커 뒤** 깊이 격차의 |중앙값| 이 **19.5 → 39.5 막대로 오히려 커졌다**. 깊이 밴드는 정합이 아니라 **표본 교체**로 작동한다(짝 잃음 62.6% → 80.9%). ★ **깊이가 결과를 끌어올린다는 전제 자체는 참**(3 분위에서 `max_ret` +0.32% → +4.55%)이라 이 축은 덮을 수 없다. ★★ **탐색 팔 9 세션으로 좁히면 `TOP_GAINERS` 는 거래일이 4 개뿐이라 통합 CI 가 없다** — `docs/64` 의 유일한 보정 후 0 제외 칸(−0.579%)은 **08-13(확증 팔)을 5 번째 군집으로 썼다**. ★ 표적 층 `$0~5` 가 잴 수 있는 것의 **83~88%** 이고, `$5 이상` 은 짝 **1~15 건이라 못 잰다**. 보정 전 0 을 제외하는 칸은 `TOSS_..._VOLUME E2 K10` 변동성+밀도 **하나**(+1.857%, 하한 +0.033%)이고 **보정(48) 후에는 교차한다**. ★ §11 이 `G2G3-PREREG` §5-1 에 걸리는 사실 하나를 코디네이터에게 넘긴다 |

---

## ★ 번호 충돌 이력 (인용할 때 반드시 볼 것)

**`docs/51` 이 두 개였다.** `51_anchor_boundary.md`(W3, 2026-08-09)와
`51_detector_windows.md`(W5, 같은 날)가 서로 아무 상관없는 문서인데 같은 번호를 썼다.
W5 의 스펙(`coordination/archive/specs/w5_detector_windows.md:186`)이 *"51 은 전 브랜치에서 비어
있음을 확인했다"* 고 적었고, 같은 시각 W3 도 같은 판단을 했다. **두 워커가 각자 옳았고
조합이 틀렸다.**

- W3 이 `docs/54` §179 에서 발견했고, 코드(`tossmon/analysis/evaluate.py:172`)는
  이미 파일명을 박아 피해 갔다.
- **2026-08-13 해소**: 인용이 적은 쪽(`detector_windows`, 5 곳·전부 파일명)을
  **`57_`** 로 옮겼다. 반대쪽은 42 곳이고 그중 다수가 `docs/51` 로만 적는다.
- **따라서 `docs/51` 이라는 맨 번호 인용은 전부 `51_anchor_boundary` 를 뜻한다.**

---

## 미판정 — 사용자 확인 대기

여기 있는 것은 **판정할 근거가 없어서** 비워 둔 자리다. 추측으로 채우지 않았다.

**1. ~~`docs/07_analysis_spec.md` 의 지위~~ → 해소 (2026-08-13)**
사용자 결정: **`12` 와 함께 동결**. 고해상도 재시작에는 **새 명세가 필요하다.**
(같은 날 결정된 *"`12` 는 고해상도를 안 덮는다 — 새 사전등록이 필요하다"* 와 한 쌍)

**2. `docs/15` 의 소유자**
문서 자신은 *"소유: W3"*(3 행)이라 적고, `coordination/archive/HANDOFF-20260813.md` 소유표는
**W7** 로 적는다. 이력을 보면 **작성은 W3**(08-02~03, 6 커밋), **표기 개정은 W7**(08-11·08-12,
2 커밋)이다. 둘 다 참이라 이 표는 `W3(작성)·W7(표기)` 로 적어 두었다 — 확정 필요.

---

## ★ 재발 감시 대상 — 한 번 잡힌 **(다)** 네 건

W7 이 `docs/**` 55 개를 전수로 훑어 상태 진술 152 줄을 판정했다
(`docs/48` §12): **(가) 137 / (나) 8 / (다) 4 / (라) 3.**
W3 이 `tossmon/analysis/**` 주석을 같은 축으로 훑었다(`docs/54`): 17 지점 중
**(가) 12 / (나) 4 / (다) 1 / (라) 1.**

> **분류축은 "많이/적게" 가 아니라 "안전/위험" 이다.**
> **(다) = 이제 거짓이면서 우리에게 유리해 보이게 만드는 것.**
> 음성 결과를 가리는 것도, 없는 근거를 있다고 적는 것도 같은 (다) 다.

**이미 고쳐졌다. 같은 모양이 다시 나오는지 보라.**

| 어디 | 무엇이라고 적혀 있었나 | 왜 위험했나 |
|---|---|---|
| `docs/15` 2 곳 | *"조건부 규칙 탐색 아직 착수 안 함"* | **다음 날 했고 자격 규칙 640 개 전부 CI 하한 음수**(`docs/19`). 읽는 사람은 *"마지막 희망이 남아 있다"* 로 읽는다 |
| `docs/48`·`docs/12` 이력 2 곳 | *"`docs/07` §4.1 문언이 실측으로 틀린 것이 확정"* | `obs_le` 값으로 **엄격 모드 한정** 문언을 반박한 **모드 혼합** |
| `exit_value.py:10` | *"이탈 축은 아직 제대로 안 쟀다"* | **쟀고 음성이다**(`docs/23` §10-P: 관측 가능 20 종 전부 CI 0 교차, 회수율 상한의 12%) |
| `docs/12` §1.8 | *"`vol_surge_lead_min` 미측정"* | W3 이 08-09 에 실측했다(2/60 에서 갈림) |

**(라) 판정 불가로 남은 것** — `docs/29`+`docs/13`(수집기 3 번째 경계 재확인) 1 건은
**라이브 관측이라 문서만으로 못 가른다. 열려 있다.**

---

## 불가침 문서에 가한 변경 — **전량 여기에 기록한다**

`docs/12`·`13` 은 불가침이다. 그래도 손댈 일이 생기면 **그 사실이 문서 밖에 남아야**
나중에 "기록 없는 편집"으로 보이지 않는다. 이 절이 그 자리다.

| 일자 | 문서 | 무엇 | 승인 |
|---|---|---|---|
| 2026-08-13 | `12_preregistration.md:1119` | **렌더링 버그 수정만.** 개정 이력 9 번 행 안의 조건부확률 막대 `\|` 2 개를 `\\\|` 로 이스케이프했다 (`Pr[X≥5 \| X≥3]`, `Pr[rvol_at_t0 ≥ 5 \| 게이트 통과]`). 인라인 코드 안이어도 마크다운은 표 칸을 가르기 때문에 **그 행이 3 열이 아니라 5 열로 렌더링되어 내용 일부가 엉뚱한 칸에 실려 읽을 수 없었다.** 글자·수치·판정 **무수정** | **사용자** (*"고쳐라 — 렌더링 버그는 불가침 밖"*) |

검증: 이력 표 1100~1123 행 전부 이스케이프 안 된 파이프가 정확히 4 개(3 열)임을
스크립트로 재확인했다.

---

## 정정 — 실측으로 잡힌 거짓 진술

**1. ~~`docs/39_top_gainers.md` — 제목이 거짓이다~~ → 고쳤다 (2026-08-13)**

제목이 *"`TOP_GAINERS` 를 수집에 추가 — 만들었고, **배포는 안 했다**"* 였는데,
수집 DB 를 읽으니 **배포됐고 6 일째 수집 중**이었다.
제목에 취소선 + 정정을 병기하고 머리에 정정 상자를 넣었다. **본문은 안 지웠다** —
그 절들이 무엇에 대한 반응이었는지가 사라지기 때문이다.

```
$ python -c "... sqlite3 'file:.../w5-ops/data/tossmon.db?mode=ro' ..."
TOP_GAINERS   1d   2,140,182 행   최초 2026-08-07 22:26 KST   최종 2026-08-13 12:59 KST
```

문서 작성일이 2026-08-07 이므로 **작성 당시엔 참이었고 같은 날 저녁 정규장 개장
직전(22:26)에 거짓이 됐다.** 6 일간 아무도 안 고쳤다.

같은 질의가 D-8(랭킹 4 종→2 종 축소)도 확인해 준다 — 금액 계열 2 종은
**2026-08-04 11:22 KST 에 멈춘 뒤 다시 안 들어온다.**

| 랭킹 타입 | duration | 행 수 | 최종 수집 (KST) |
|---|---|---:|---|
| `MARKET_TRADING_VOLUME` | realtime | 5,467,304 | 2026-08-13 12:59 |
| `TOSS_SECURITIES_TRADING_VOLUME` | realtime | 5,466,466 | 2026-08-13 12:59 |
| `TOP_GAINERS` | **1d** | 2,140,182 | 2026-08-13 12:59 |
| `MARKET_TRADING_AMOUNT` | realtime | 1,136,600 | **2026-08-04 11:22** |
| `TOSS_SECURITIES_TRADING_AMOUNT` | realtime | 1,136,271 | **2026-08-04 11:22** |

---

## 지운 것 — 루트 워커 리포트 16 개 (2026-08-13)

레포 루트에 `W4_REPORT_task_2989b7ebc8b7.md` 같은 **해시 이름 파일 16 개**가 있었다.
전부 **대응 docs 문서가 확인돼서 지웠다**(사용자 결정). 아래가 그 대조표다 —
**옛 참조를 따라온 사람이 빈손이 되지 않도록** 남긴다. 원본은 git 이력에 있다
(`git show <커밋>:W4_REPORT_task_2989b7ebc8b7.md`).

| 지운 파일 | 언제 | 무엇 | 지금 어디 |
|---|---|---|---|
| `W1_REPORT_task_804c0fbcb976.md` | 08-13 | 클램프 가시성 | **`docs/56`** (리포트가 "근거 원본" 으로 지목) |
| `W1_REPORT_task_c0b073db57eb.md` | 08-04 | 레이트리밋 실제 계약 확정 | **`docs/06` §9-3~§9-9** (리포트가 지목) |
| `W3_REPORT_task_9cba80a70f6f.md` | 08-07 | 홀드아웃 봉인을 종료 라벨로 | **`docs/40` §6** 요구 1·2 |
| `W4_REPORT_task_2989b7ebc8b7.md` | 08-12 | 예산 타임라인 monotonic 분리 | **`docs/52` §5.5** (리포트가 지목) |
| `W4_REPORT_task_59b9b6e505d3.md` | 07-31 | 유니버스 미적용 + 룩어헤드 | `docs/10` · `coordination/daily/2026-07-31.md` |
| `W4_REPORT_task_6a6bc5a53927.md` | 07-31 | `ForbiddenEndpoint` 미처리 | `docs/04`(A6·A7) · `docs/10` |
| `W4_REPORT_task_7363b99a3be2.md` | 08-04 | `usage_ratio` 0.85 | **`docs/30` §1-3**(배포 4) |
| `W4_REPORT_task_7e950bd90a40.md` | 08-04 | 검출기 랭킹 타입 명시 | `docs/30` §1-1 |
| `W4_REPORT_task_8280709b754a.md` | 08-04 | `symbol_not_found` 분리 | `docs/11` |
| `W4_REPORT_task_8cab8f2af043.md` | 08-04 | 랭킹 2 종 + tier3 10×4 초 | **`docs/30` §1-1**(배포 1) |
| `W4_REPORT_task_b21df443c30f.md` | 08-04 | 예산 모델을 1 초 창 위에 | **`docs/30` §1-2**(배포 2·3) |
| `W4_REPORT_task_cb7a4e02fd49.md` | 08-13 | `foreign` 세 후보 판정 | **`docs/55`** (리포트가 "전체 문서" 로 지목) |
| `W4_REPORT_task_d3207dc2562c.md` | 08-12 | 감시점 셋을 서버 초 위로 | **`docs/52` §12** (리포트가 지목) |
| `W4_REPORT_task_e99983f09016.md` | 08-01 | 대량 백필 러너 | `docs/07` · `docs/12` §2.8 |
| `W4_REPORT_task_e9f0597d6966.md` | 08-04 | 예산 여유 확보 + 개장 축출 차단 | **`docs/33`**(개장 사고) · `docs/30` |
| `W5_REPORT_task_4c7447a2597a.md` | 08-07 | 아침 리포트 따라잡기 | **`docs/34`** |

**아직 이 파일들을 가리키는 두 곳** — 둘 다 이력 문서라 고치지 않았다:
`coordination/daily/2026-07-31.md:95` · `coordination/archive/HANDOFF-W4.md:28`.

> **판정 근거**: 각 리포트의 고유 토큰(커밋 해시·3 자리 이상 수치·백틱 식별자)이
> `docs/**` + `coordination/**` 안에 있는지 기계로 대조했다. 덮임률은 66.7%~100%
> 였고, **안 덮인 것은 전부 테스트 함수명·내부 private 함수명·브랜치 커밋 해시**였다
> — 코드와 테스트에 사는 것이지 문서에서 사라진 사실이 아니다.

---

## 역할별 빠른 진입

> **빈 컨텍스트로 들어왔다면 아래 [0] 을 먼저 읽고, 자기가 할 일에 해당하는 층만 더 읽어라.**
> **전부 읽지 마라.** `docs/` 는 다 합쳐 3 만 줄이 넘고, 그중 다수는 **1 분봉 시절의
> 기록**이라 지금 판단에 쓰면 안 된다.

### [0] 누구나 — 약 1,300 줄

| 순서 | 문서 | 왜 |
|---|---|---|
| 1 | `README.md` (104) | 골격 한 문장 · 금지사항 · 이 세션에서 확인된 수치 |
| 2 | **이 색인** (269) | 나머지 문서를 **언제 열지** 정하는 근거. 상태 표기를 여기서 배운다 |
| 3 | `coordination/COORDINATOR-STATE.md` (355) | 지금 뭐가 도는지 · 열린 결함 · **§3-2 다음 할 일의 순서** |
| 4 | `coordination/USER-INPUT-QUEUE.md` (579) | 사용자가 정한 것과 아직 안 정한 것. **여기 없는 건 묻지 말고 여기 쌓아라** |

### [1] 전략을 검증하려는 세션 — 여기부터 추가 761 줄

| 순서 | 문서 | 왜 |
|---|---|---|
| 5 | **`docs/58_gate.md`** (292) | **관문. 지금 어느 관문 앞에 서 있는지가 여기 있다.** §1-4 에 아직 안 풀린 고리 하나 |
| 6 | `docs/16_strategy_design_notes.md` (159) | 전략 방향의 **정본**. 수익 목표가 왜 설계 제약인지 |
| 7 | **`coordination/STRATEGY-VERDICTS.md`** (310) | **후보 5 개가 어떻게 죽었나.** 새 후보를 제안하기 전에 읽어라 — 같은 벽이다 |

### [2] 고해상도 데이터를 직접 만질 때 — 추가 3,000 줄

`41`(0 단계 관문 — 우리 스트림이 무엇을 볼 수 있나) → `42`(초 안 순서) →
`43`(테이프 포화) → `44`(초 막대 위의 슈팅, **1,902 줄** — 목차부터 보고 필요한 절만)

### [3] 비용이 걸린 판단을 할 때 — 추가 507 줄

`18`(실효 왕복 비용 실측) · `21`(비용 전제 개정 권고, **D-1 미결**)
그리고 `docs/27` §7 — **최대 효과 8.33 bp vs 비용 66 bp.** 후보들이 죽은 자리다.

### [4] 수집기를 건드릴 때

`06`(API 실측 **정본**) → `08`(런북) → `35`(랭킹 주기 — **지금 1 번 관문**) →
**`60`(폴 주기 5 초의 비용 — 배포 전 필독)** →
`52`(계상 시각) · `56`(클램프) · `55`(`foreign`) · `57`(계측 창) → `34`+`53`(경보 등급)
그리고 `30`(2026-08-04 배포 4 회 — **데이터 경계 문서**)

### ★ 안 읽어도 되는 것 (약 5,400 줄) — **컨텍스트를 여기 태우지 마라**

`15`·`17`·`19`·`22`·`23`·`26`·`27` 은 **1 분봉 자로 잰 분석 결과**다.
사용자가 2026-08-04 에 **그 해상도 자체가 틀렸다**고 판단했고(D-6) 과거 판정은
재실행하지 않기로 했다(D-10). **결론만 필요하면 `STRATEGY-VERDICTS.md` 로 충분하다.**
원문은 그 결론을 **인용해야 할 때만** 열고, 열면 *"1 분봉 자로 쟀다"* 를 병기해라.

감사 5 개(`10`·`10_audit_b`·`14`·`20`·`25`)도 **기준 커밋 시점의 코드**에 대한 것이다.
`coordination/archive/**` 는 전부 끝난 것이다.

### ★ 먼저 알아야 할 함정 다섯

1. **`docs/44` 의 D-17·D-18 과 큐의 D-17·D-18 은 다른 안건이다.** 번호만 떼어 인용하지 마라
2. **맨 `docs/51` 인용은 `51_anchor_boundary` 단독**을 뜻한다 (W5 것은 `57` 로 옮겼다)
3. **`docs/12`·`13` 은 불가침이다.** 읽기만 해라. 손댈 일이 생기면 이 색인 §불가침 절에 기록
4. **요약표를 근거로 훑지 마라. 원문 파일을 열어라** (이 색인도 요약표다)
5. **부재 주장("~라고 안 적혀 있다")에는 리비전을 함께 적어라**

---

**처음 들어온 세션 (3 분)**
`README.md` → 이 색인 → `coordination/COORDINATOR-STATE.md`(현재 상태) →
`coordination/USER-INPUT-QUEUE.md`(사용자 결정 대기)

**계약을 어길까 봐 확인하는 세션**
`04`(계약) · `34`+`53`(경보 등급과 범위) · `12`·`13`(불가침, 읽기만)

**수집기를 건드리는 세션**
`06`(API 실측) → `08`(런북) → `30`(그날 인프라 경계) →
`52`(계상 시각) · `56`(클램프) · `55`(`foreign`) · `57`(계측 창)

**분석을 다시 여는 세션**
먼저 **왜 멈췄는지**: `USER-INPUT-QUEUE` D-6 → 그다음 **닫힌 후보들**:
`23`(슈팅) · `27`(감속 진입) · `19`(규칙 640 개) · `22`(이탈) · `17`·`26`(회전) →
그다음 **새 자**: `41`(0 단계) · `42` · `44`
**주의**: 이 판정들은 전부 **1 분봉 자로 잰 것**이고, 사용자는 그 해상도 자체가
틀렸다고 판단했다(D-6). 인용할 때 그 사실을 함께 적는다.

**한도·레이트리밋을 파는 세션**
`32` → `37` → `45` → `46` → `52` → `56` **이 순서로 읽어라.** 뒤가 앞을 정정한다.
