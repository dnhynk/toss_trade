# 63. 랭킹 공백 — **멈춘 것은 랭킹이 아니라 송신 전체다**

> **[현행]** 소유 W4 · 2026-08-15 · 명세 `coordination/specs/w4_ranking_gap.md` (`cb7bdd7`)
> **· 2026-08-18 §12 추가** · 명세 `coordination/specs/w4_gap_token_lock_falsified.md` (`91accee`)
> 상태 표기의 뜻과 전수 목록: [`docs/INDEX.md`](INDEX.md)
>
> ⚠️ **§1~§11 은 08-15 시점 그대로다.** 그 뒤 08-17 정규장 관측으로 **토큰 락 가설이
> 죽었고**(§12-3) §7-가·§8-1·§8-2·§8-5·§7-라 가 정정됐다. **정정 목록은 §12-7.**

작성: W4, 2026-08-15. 브랜치 `feat/collector`.
**배포 0 · 라이브 API 호출 0 · 수집기 미접촉 · `w5-ops` 읽기 전용 · `config` 무수정.**
자는 둘뿐이다 — `w5-ops/data/tossmon.db` (`mode=ro`) 와 `w5-ops/data/collector.log` (읽기).

**판정하지 않는다.** 아래는 숫자와 측정 조건이다. 못 가른 것은 §8 에 적었다.

---

## 0. 한 문단 요약

**대조표의 `candle` 열은 우리 폴 시각을 잴 수 없다** — `candles_1m` 에는 폴 시각 컬럼이
아예 없고 `ts_ms` 는 **거래소 봉 분**이다(§2). 그래서 *"캔들은 멀쩡했다"* 는 관측이
아니라 계측기의 한계다. 폴 시각을 싣는 계측기(`rankings_snap.snap_ms` ·
`orderbook_snap.snap_ms`)로 다시 재면 **랭킹과 호가는 같은 초에 같이 멈춘다** — 08-14
정규장에 둘 다 20 초 넘게 조용한 구간이 **21 개**, 전부 13:43~14:40 UTC 안이다(§3).
그리고 프로세스 **안**의 계측기가 그것을 독립으로 확인한다: 08-14 23:11:54 · 23:27:21
KST 두 창에서 `md_peak_1s`·`chart_peak_1s`·`rank_peak_1s` 가 **셋 다 0** 이다 —
60 초 지평 안에 **어느 그룹도 한 건도 안 보냈다**(§4). **캔들도 같이 멈췄다.**
그래서 가설 2(중복 억제)는 코드에서 죽고(§6-2), 가설 3(서버가 랭킹을 안 줌)은 세 그룹
동시 정지를 설명 못 해 죽는다(§6-3). 가설 1 은 *"랭킹이 자기 그룹 예산에서 밀렸다"*
형태로는 죽고(§6-1), *"그룹과 무관하게 송신 전체가 선다"* 형태만 남는다 — 그 형태를 낼
수 있는 코드 경로는 **하나뿐이고**(`TokenManager._alock` 을 쥔 채 도는 전송 재시도,
§7-가), **그 경로를 세는 카운터가 텔레메트리에 한 번도 나온 적이 없다.** 그래서
**못 갈랐다.** 그 셋을 텔레메트리에 실었다(§9) — 다음 창은 갈린다.

---

## 1. 측정 조건 (먼저 적는다)

| | |
|---|---|
| 창 정의 | 정규장 **13:30~20:00 UTC** (= 22:30~05:00 KST). 명세 §1 정의 그대로 |
| 랭킹 폴 시각 | `rankings_snap.snap_ms` distinct. **우리 관측 시각**이다 (`loops.py:1698` = `ctx.clock.now_ms()`) |
| 호가 폴 시각 | `orderbook_snap.snap_ms` distinct. 역시 우리 폴 시각 |
| 캔들 | **폴 시각 컬럼이 없다** — §2. 폴 타이밍 판단에 쓰지 않았다 |
| 프로세스 안 계측 | `collector.log` 의 `telemetry` 줄. `*_peak_1s` 는 **60 초 슬라이딩 지평**의 1 초 최대 송신 수 (`budget.py:53` `WINDOW_S=60.0`, `budget.py:617`) |
| 표본 | 정규장 **3 개** (08-12·08-13·08-14) 전수. 표본추출 아님 |
| 못 잰 것 | 라이브 재현 0 (배포 금지). 프로파일링 0. **어느 코루틴이 언제 깼는지는 로그에 없다** |

**세 밤의 전수 수치** (내가 직접 질의했다. 명세 §1-1 의 `5179` 와 §1-3 의 33 건이 재현된다)

```
session      rank_snaps   ob_snaps   rank_gaps>20s   first(UTC)   last(UTC)    max
2026-08-12         5571      62391               0          -           -         -
2026-08-13         5511      48919               3    13:31:42    15:55:28     37.7s
2026-08-14         5179      44691              33    13:43:18    19:38:40     74.1s
```

> **명세 §1-4 와 한 건 다르다.** 명세는 *"13:43~14:40 에 32 건, 그 뒤 1 건(16:06:43)"*
> 이라 적었다. 내 경계(`< 14:41:00 UTC`)로는 **31 건 + 2 건**(16:06:43 · 19:38:40)이다.
> 경계 한 칸 차이이고 몰림 자체는 같다. **고치지 않고 내 경계를 밝힌다.**

---

## 2. ★ 먼저 계측기부터 — **`candle` 열은 폴 시각을 못 잰다**

명세 §1-2 의 대조표에서 *"캔들은 그대로다"* 가 *"일부 폴 루프만 멈췄다"* 의 근거였다.
그 열이 무엇으로 만들어지는지 스키마를 열었다.

```
CREATE TABLE candles_1m (
    symbol TEXT NOT NULL,
    ts_ms  INTEGER NOT NULL,      <- 거래소 1분봉의 봉 시각. 우리 폴 시각이 아니다
    open_u ..., high_u ..., low_u ..., close_u ..., vol_qu ...,
    PRIMARY KEY (symbol, ts_ms)
) WITHOUT ROWID
```

**폴 시각을 담는 컬럼이 없다.** `orderbook_snap` 에는 `snap_ms`(우리 시각)와
`ts_ms`(서버 시각)가 **둘 다** 있는데 `candles_1m` 에는 `ts_ms` 뿐이다.
그러므로 *"공백 구간 안에 봉 596 행이 있다"* 는 **그 구간에 폴을 했다는 뜻이 아니다** —
공백이 끝난 뒤의 폴 한 번이 그 구간을 덮는 봉들을 가져와 쓰면 같은 결과가 나온다.
캔들 폴 주기는 **110 초**(`config_sig` 의 `t2c110s`)로 공백(62~74 초)보다 **길다**.

명세가 `trades_snap.ts_ms` 에 대해 이미 같은 주의를 적었다(*"거래소 체결 시각이지 우리
폴 시각이 아니다"*). **같은 주의가 `candles_1m` 에도 그대로 걸린다.** 대조표의
`candle` 열은 지우지 말고 **"이 열로는 폴 타이밍을 못 읽는다"** 를 병기하면 된다.

**그리고 캔들은 실제로 멈췄다** — §4 의 프로세스 안 계측기가 그것을 직접 보여준다.

---

## 3. 랭킹과 호가는 **같은 초에** 멈춘다 (08-14 전수)

`rankings_snap.snap_ms` 와 `orderbook_snap.snap_ms` 를 합집합으로 놓고 20 초 넘는
침묵을 세면 **21 개**다. **전부 13:43:31~14:40:39 UTC 안**이다.

명세 §1-1 의 60 초 초과 4 건 안에서 호가가 얼마나 조용했나:

```
랭킹 공백                       호가 폴 수   그 안의 최장 호가 침묵
14:03:33 -> 14:04:36  62.7s          0          62.7s  (14:03:33->14:04:36)
14:10:49 -> 14:11:54  65.3s          9          60.2s  (14:10:54->14:11:54)
14:26:10 -> 14:27:22  71.6s          2          71.1s  (14:26:10->14:27:21)
14:32:41 -> 14:33:55  74.1s         18          39.4s  (14:32:53->14:33:33)
```

**1·2·3 번은 호가도 60~71 초 동안 완전히 조용하다.** 4 번만 부분적이다.
호가 쪽에서 거꾸로 세도 같은 자리가 나온다 — 호가 침묵 최장 3 건이
`14:03:20(76.1s)` · `14:26:10(71.1s)` · `14:10:54(60.2s)` 이고 그 안의 랭킹 폴은
각각 3 · **0** · **0** 건이다.

### 3-1. 느려진 것이 아니라 **섰다가 돈다**

같은 계측기로 창을 셋 비교했다.

```
창                              ob 폴 수   gaps>5s   >20s    최장    중앙 간격
08-14 14:00-14:40 (공백 구간)      2217        74     23    76.1s      0.32s
08-14 15:00-15:40 (같은 날 조용)   5146         2      0     5.8s      0.31s
08-13 14:00-14:40 (전날 같은 시각) 5826         0      0     3.6s      0.29s
```

**중앙 간격이 셋 다 같다(0.29~0.32 초).** 돌 때는 정상 속도로 돈다. 폴 수가 43% 로
준 것은 **정지 구간의 길이** 때문이지 매 폴이 느려서가 아니다.

---

## 4. ★ 프로세스 **안**의 계측기 — 세 그룹이 동시에 0 이었다

DB 는 "쓴 것" 만 보여준다. 송신 자체를 세는 것은 텔레메트리의 `*_peak_1s` 다 —
**60 초 지평 안 어느 1 초의 최대 송신 수**(`budget.py:617`). 이 값이 0 이면
**그 60 초 안에 그 그룹이 한 건도 안 보냈다**는 뜻이다.

2026-08-12 22:30 ~ 2026-08-15 05:00 KST 전 구간에서 셋 중 하나라도 0 인 줄은 **10 개**다.

```
2026-08-13 08:55:08  {md:0, chart:0, rank:0}   <- 재기동 직후 첫 줄
2026-08-13 10:11:03  {md:0, chart:0, rank:0}   <- 재기동 직후 첫 줄
2026-08-13 10:26:00  {md:0, chart:0, rank:0}   <- 재기동 직후 첫 줄
2026-08-13 11:41:01  {md:0, chart:0, rank:0}   <- 재기동 직후 첫 줄
2026-08-13 12:11:00  {md:0, chart:0, rank:0}   <- 재기동 직후 첫 줄
2026-08-14 00:56:02  {md:0, chart:0, rank:0}   <- 재기동 직후 첫 줄
2026-08-14 09:03:21  {md:0, chart:0, rank:0}   <- 재기동 직후 첫 줄
2026-08-14 09:16:03  {md:0, chart:0, rank:0}   <- 재기동 직후 첫 줄
2026-08-14 23:11:54  {md:0, chart:0, rank:0}   <- ★ 재기동 없음
2026-08-14 23:27:21  {md:0, chart:0, rank:0}   <- ★ 재기동 없음
```

여덟은 `collector start` 직후 첫 줄이다(코디네이터가 `daily/2026-08-14.md` §9 에서 같은
성질을 이미 짚었다 — *"재기동 0.6 초 뒤 텔레메트리"*). **나머지 둘은 재기동이 없다.**
23:11:54 KST = 14:11:54 UTC, 23:27:21 KST = 14:27:21 UTC — **공백 2 번과 3 번이 끝나는
바로 그 초**다.

**이것이 이 태스크의 중심 관측이다.**

* 세 그룹은 리미터 버킷도 락도 **따로**다 (`limiter.py:190` `_bucket`, `:100` 버킷당
  `asyncio.Lock`, `:312-313` `acquire` 는 자기 버킷 락만 잡는다). 한 그룹이 굶는 것으로는
  셋이 같이 0 이 되지 않는다.
* `chart_peak_1s` 도 0 이다 → **캔들도 같이 멈췄다.** §2 가 예고한 그대로다.
* 08-12 정규장에는 이런 줄이 **하나도 없다** (그 밤 `md_peak_1s` 최솟값 9,
  `rank_peak_1s` 최솟값 3, `chart_peak_1s` 최솟값 2 — 어느 그룹도 0 에 안 닿는다).

### 4-1. 프로세스는 살아 있었다

공백 안에서 DB 쓰기가 일어난다. 공백 3 번(14:26:10~14:27:22)에 `promotions` 32 행이
들어갔다 — 다만 **전부 `14:26:10` 한 초에** 몰려 있고 그 뒤 71 초는 비어 있다.

```
gap2  promotions 쓰인 초: {14:10:49: 2, 14:10:55: 32}
gap3  promotions 쓰인 초: {14:26:10: 32}
gap4  promotions 쓰인 초: {14:32:41: 2, 14:32:51: 2, 14:33:53: 30}
```

그리고 텔레메트리 줄은 계속 나왔지만 **늦었다**. 공칭 300 초인데 실제 간격이
`335s · 345s · 315s` 다(23:06:09 · 23:11:54 · 23:27:21 KST). **10 초 틱 루프도
15~45 초 지연됐다.** 송신 정지(60~71 초)보다는 짧다.

---

## 5. 08-13 의 3 건은 08-14 의 33 건과 **모양이 다르다**

명세 §1-3 이 *"08-13 에 이미 3 건이 샜다 → D-21 이 유일한 원인일 수 없다"* 로 읽었다.
그 3 건 안에서 호가가 뭘 했는지 보면 **08-14 와 다르다**.

```
2026-08-13 랭킹 공백>20s        길이     그 안의 호가 폴
13:31:42 -> 13:32:07          24.1s          22
13:33:24 -> 13:33:47          22.7s          13
15:55:28 -> 15:56:06          37.7s           7
```

**셋 다 호가는 계속 돌았다.** 08-14 의 1·2·3 번(호가 0·9·2, 그중 60~71 초 완전 침묵)과
같은 현상이라고 말할 근거가 이 표에는 없다. 그리고 세 번째(15:55:28 UTC = 00:55:28 KST)는
**프로세스 사망·재기동 구간**과 겹친다 (`ALERT_20260814_005558_watch_process_dead`,
`collector start` 2026-08-14 00:56:01 KST).

> **그래서 "08-13 에 이미 있었다" 는 진술은 살아 있되 뜻이 좁아진다** — *"20 초 넘는
> 랭킹 공백"* 은 08-13 에 있었다. *"세 그룹이 동시에 60 초 서는 현상"* 은 **08-13 정규장
> 전수에 0 건**이다(§4 의 목록에 그 밤 항목이 재기동 줄뿐이다). **판정은 안 한다.**

### 5-1. 세 밤 사이에 **안 달라진 것**

| | 08-12 (공백 0) | 08-13 (3) | 08-14 (33) |
|---|---|---|---|
| `tier2_cap` / `tier3_cap` | 300 / 10 | 300 / 10 | 300 / 10 |
| `md_peak_1s` 가 천장 10 인 창 | 77/79 | 63/78 | 66/78 |
| `rank_peak_1s` 최대 | 3 | 3 | 3 |
| `promotions` 총계(정규장) | 19,042 | 19,087 | 17,825 |
| `config_sig` | A | **A (동일)** | A + `rkpTVOLUME@10/k2/h300s/rotate/cd600s` |

**정원도, 포화도, 티어 전이량도 세 밤이 같다.** 08-14 만 티어 전이가 오히려 **적다**.
`config_sig` 는 **08-12 와 08-13 이 완전히 동일**하다 — 그러니 08-13 의 3 건은
설정 변화로 설명되지 않는다. 08-14 만 D-21 이 붙어 있다.

---

## 6. 가설 셋 — 살았나 죽었나

### 6-1. 가설 1 "개장 폭주에 랭킹 폴이 밀려 굶었다" — **좁은 형태는 죽고, 넓은 형태만 남는다**

* **죽은 형태**: *"랭킹이 자기 그룹 예산·리미터에서 밀렸다."* RANKING 은 자기 버킷과
  자기 락을 가진다(`limiter.py:190`,`:100`). `rank_peak_1s` 는 세 밤 다 최대 3 이고
  RANKING 한도 근처에 간 적이 없다. 그리고 이 형태는 **호가·캔들이 같이 선 것**을
  설명하지 못한다.
* **죽은 형태**: *"MARKET_DATA 포화가 원인."* 포화도는 08-12(천장 77/79 창)가 08-14
  (66/78)보다 **더 높다**. 그 밤 공백은 0 이다.
* **남은 형태**: *"그룹과 무관하게 송신 경로 전체가 선다."* §4 가 그 모양이다.
  코드에 그런 경로가 있다 — §7-가.

### 6-2. 가설 2 "응답은 왔는데 내용이 같아 스냅을 안 남겼다" — **죽었다 (코드)**

명세가 요구한 *"내용 동일 시 건너뛰는 분기"* 를 쓰기 경로 전체에서 찾았다. **없다.**

* `loops.py:1709` — `stored += ctx.store.insert_rankings(snap_ms, page)` 를 **조건 없이**
  부른다. 앞에 내용 비교가 없다.
* `store/writer.py:140-170` — `INSERT ... ON CONFLICT(snap_ms, ranking_type, duration, rank)
  DO UPDATE SET ...`. 충돌 키에 **`snap_ms` 가 들어 있고** 그 값은 폴마다 새로 찍는
  우리 시계다(`loops.py:1698`). 그러므로 **같은 내용이어도 새 `snap_ms` 로 새 행이 된다.**
* 실패로 유실된 것도 아니다 — `rankings_write_failures` 가 08-14 정규장 전 구간 **0**.

> 즉 `distinct snap_ms` 의 공백은 **"폴을 안 했다"** 이지 **"폴했는데 안 남겼다"** 가 아니다.
> 이 문장이 §3·§4 전체의 전제다.

### 6-3. 가설 3 "서버가 그 초에 랭킹을 안 줬다" — **죽었다 (범위)**

* 서버가 랭킹만 안 준 것으로는 **호가와 캔들이 같이 선 것**을 설명 못 한다(§3·§4).
* 그리고 우리 쪽 오류 카운터가 **전부 평평하다**. 08-14 22:21~23:47 KST 의 텔레메트리
  18 창 전수에서 창별 증가분:

```
loop_errors  Δ = 0  (전 구간 36210 고정)
api_errors   Δ = 0
http_429     = 0
```

  서버가 거절했거나 응답이 깨졌으면 `api_errors` 나 `http_429` 가 움직인다. 안 움직였다.
  **다만** 아래를 함께 읽어야 한다 — 전송 계층 재시도는 이 셋 어디에도 안 세어졌다(§9).

---

## 7. 명세가 물은 네 가지

### (가) 랭킹 폴과 호가 폴은 같은 리미터/예산/락을 공유하는가? 캔들은 왜 다른가?

**리미터·예산은 안 나눠 쓴다. 그런데 랭킹 루프는 다른 그룹으로도 나간다.**

| 경계 | 파일:줄 |
|---|---|
| 그룹 매핑 | `api/endpoints.py:32-46` — `/rankings`→`RANKING`, `/orderbook`·`/prices`·`/trades`→`MARKET_DATA`, `/candles`→`MARKET_DATA_CHART`, `/stocks`→`STOCK` |
| 버킷·락이 **그룹당 하나** | `api/limiter.py:190` `_bucket`, `:100` `self.lock = asyncio.Lock()`, `:312-313` `acquire` 는 `b.lock` 만 잡는다 |
| 락을 **대기 중에도 쥔다** | `api/limiter.py:308-310` 주석 + `:313-328` — 같은 그룹 안에서는 직렬화된다. **다른 그룹은 못 막는다** |
| 랭킹 루프가 STOCK 으로 나감 | `collector/loops.py:1751-1755` `_resolve_universe` → `get_stocks` |
| ★ 랭킹 루프가 **MARKET_DATA** 로 나감 | `collector/loops.py:1794-1799` `_resolve_watchlist_unknowns` → `get_prices` → `after_call(GROUP_MARKET_DATA)` |
| 캔들 루프 | `collector/loops.py:2016`·`2025` — `GROUP_CHART` 하나뿐 |

그래서 **랭킹 루프는 MARKET_DATA 버킷 락에 걸릴 수 있고**(위 다섯째 줄), 그 반대도
성립한다 — 이것이 랭킹↔호가를 잇는 **그룹 경계 안의** 유일한 고리다.
**그러나 이 고리로는 캔들이 같이 선 것을 설명 못 한다** — 캔들은 CHART 하나뿐이다.

★ **그룹과 무관하게 전부를 세우는 경로는 하나다.**

```
client.py:296   token = await self.tokens.get()        <- 모든 송신이 무조건 지난다
tokens.py:170       async with self._alock:            <- 그룹 구분 없는 단일 락
tokens.py:173           self._acquire_lease()
tokens.py:180           self._token, ... = await self._issue()
tokens.py:381               httpx.AsyncClient(timeout=15.0)   <- 락을 쥔 채 최대 15초
client.py:285-292   except TransientHTTP: 재시도 (MAX_TRANSIENT_RETRIES=3,
                    백오프 TRANSIENT_BACKOFF_S=(0.5,1.0,2.0) + 지터)
```

**최악값**: 4 회 시도 × 15 초 + 백오프 3.5 초 ≈ **63.5 초**, 그동안 `_alock` 이 잡혀
있으므로 **RANKING·MARKET_DATA·CHART 가 전부 선다.** 관측된 정지는 60.2 · 62.7 ·
71.1 · 76.1 초다. **크기대가 맞는다. 그러나 맞는다는 것이 그것이었다는 뜻은 아니다** — §8.

> ⚠️ **2026-08-18 정정 (§12-3 못 1).** *"그동안 `_alock` 이 잡혀 있으므로"* 가 **틀렸다.**
> `_issue()` 가 예외를 올리면 `async with` 가 풀려 **락이 놓이고**, 재시도는 락 **밖**의
> `client._request` 루프가 한다. 락의 연속 보유 상한은 `_issue()` 1 회 = **15 초**다.
> 그리고 08-17 창에는 토큰 POST 자체가 없었다(§12-3 못 3). **이 후보는 죽었다.**

### (나) `rankings_snap` 쓰기 경로에 내용 동일 시 건너뛰는 분기가 있는가?

**없다.** §6-2. 가설 2 는 그 자리에서 죽는다.

### (다) 08-13 의 3 건과 08-14 의 33 건 사이에 무엇이 달라졌나

* `config_sig` 로는 **08-12 와 08-13 이 동일**하고 08-14 만 D-21 이 붙는다(§5-1).
  그러므로 **08-13 의 3 건은 D-21 로도 설정으로도 설명되지 않는다.**
* 08-13 의 3 건과 08-14 의 33 건은 **호가가 같이 섰는가**에서 갈린다(§5).
  같은 현상으로 묶을 근거를 나는 못 찾았다.
* D-21 이 랭킹·호가 폴에 **구체적으로 무엇을 더 시키는가**: `_ranking_tier3_lane`
  (`loops.py:1828-1889`)이 켜지고, `rankings_once` 안에서 상위 `top_n=10` 후보에 대해
  `_resolve_universe` 를 **한 번 더** 부른다(`loops.py:1718-1724`). 그 호출은 캐시 미스일
  때만 `/stocks`(STOCK 그룹) 한 건을 낸다. 08-14 정규장 `universe_rejected` 증가분은
  창당 0~5 건이고 `rk_t3_promotions` 증가분은 창당 0~2 건이다 — **초당 송신을 눈에 띄게
  늘리는 크기가 아니다.** `rank_peak_1s` 도 세 밤 다 최대 3 으로 같다.
* **못 가른 것**: D-21 이 켜진 밤에만 세 그룹 동시 정지가 났다는 것은 사실이지만,
  **정규장 표본이 1 개**다. 인과를 세울 수 없다.

### (라) 개장 첫 70 분에만 몰리는 이유를 코드로 설명할 수 있는가

> ⚠️ **2026-08-18 정정 (§12-4).** 08-17 정규장에서 48 건 중 첫 70 분은 **11 건(23%)**,
> 마감 전 120 분이 **27 건(56%)** 이다. *"개장 첫 70 분"* 은 **성질이 아니었다.**

**못 한다.** 설명하지 못했다는 것을 그대로 적는다. 세운 가설 하나는 **검정하고 기각했다**:

> 가설: *"티어 대량 전이(`set_capacity` 등)가 정지를 촉발한다."*
> 반증 관측: 정지 시작 직전 6 초 구간(`[-5s, +1s]`)의 `promotions` 행 수가
> 정지 시작점에서 유의하게 크면 지지, 아니면 기각.
> 결과: **기각.**
> ```
> 정지 시작점 21 개 : 중앙 0, 평균 6.8, 최대 34
> 대조군 1,346 개   : 중앙 0, 평균 4.4, 최대 115
> 6 초 안에 10 행 이상: 정지 5/21 (23.8%)  vs  대조 221/1346 (16.4%)
> ```
> 가장 긴 두 정지(60.2s·71.1s)에는 34·32 행이 앞섰지만 62.7 초짜리에는 **0** 이다.
> 세 건만 보고 세운 인상이었고 전수로 재니 무너졌다.

측정된 모양만 남긴다: 21 개 전부 개장 +13 분 ~ +70 분(13:43:31~14:40:39 UTC)에 있고,
그 뒤 5.3 시간에 20 초 초과가 2 건(16:06:43 · 19:38:40)이다.

---

## 8. 못 가른 것 — **이게 이 보고서에서 제일 중요한 절이다**

1. ~~**§7-가 의 토큰·재시도 경로가 실제로 그 60~71 초를 만들었는지 못 갈랐다.**~~
   → **2026-08-18 에 갈렸다. 죽었다 (§12-3).** 아래는 08-15 시점의 기록이다.
   **§7-가 의 토큰·재시도 경로가 실제로 그 60~71 초를 만들었는지 못 갈랐다.**
   크기대는 맞고(≤63.5 초 vs 관측 60.2~76.1 초) 그룹 전체를 세우는 유일한 경로이지만,
   **그 경로가 돌았다는 관측이 하나도 없다.** 이유는 계측 부재다 —
   `client.counters["retries"]`·`["http_5xx"]`·`["auth_refresh"]` 는 **텔레메트리에도
   상태파일에도 나간 적이 없다.** 토큰 재발급은 성공하면 로그 한 줄도 안 남긴다.
   그래서 08-14 의 두 창을 사후에 못 읽는다. **§9 가 이것만 고친다.**
2. **76.1 초는 63.5 초보다 크다.** 최악값 계산이 관측 최댓값을 못 덮는다.
   *(2026-08-18: 천장은 63.5 초가 아니라 **15 초**였다 — 간극은 더 벌어진다. §12-3 못 1.)*
   덮으려면 `_request` 바깥의 추가 대기(예: 재시도가 두 번 겹침)를 가정해야 하는데
   **가정하지 않는다.**
3. **개장 첫 70 분 몰림을 설명 못 했다** (§7-라).
4. **08-13 의 3 건의 원인은 안 봤다.** 08-14 와 모양이 다르다는 것까지만 쟀다.
5. **이벤트루프가 막힌 것인지 송신만 막힌 것인지 못 갈랐다.** 텔레메트리 틱은
   15~45 초 늦었고 송신은 60~71 초 멈췄다 — **두 수가 다르다.** 다르다는 사실만 적는다.
   *(2026-08-18: **두 수가 다른 이유가 밝혀졌다** — 밀림은 정지가 텔레메트리 마감을
   가로지른 만큼이지 정지 길이가 아니다. §12-1. 코드에는 세션 틱과 폴 루프가 공유하는
   자원이 이벤트 루프뿐이다(§12-2). **관측으로 못 박는 게이지 둘을 §12-5 에 넣었다.**)*
6. **라이브 재현 0.** 배포 금지 구간이라 프로파일러도 계측 배포도 안 했다.
   §9 는 **PR 까지만**이다.

---

## 9. 넣은 것 — **계측 셋. 송신률은 한 건도 안 바뀐다**

`client.counters` 에 **이미 존재하지만 아무 데도 안 나가던** 세 값을 텔레메트리에 실었다.

| 파일 | 무엇 | 왜 |
|---|---|---|
| `tossmon/collector/loops.py` (`telemetry()`) | `retries` · `http_5xx` · `auth_refresh` 3 필드 추가 | §8-1 을 다음 창에서 가르려면 이 셋이 필요하다. 기존 `http_429` 와 **완전히 같은 관용구**(`getattr(self.client, "counters", {}).get(...)`) |
| 같은 파일 `PROC_SCOPED_COUNTERS` | 세 이름 등록 | 셋은 `client.counters` 라 **프로세스 수명**이다. 선언 안 하면 `retries=0` 이 *"재시도 없었다"* 로 읽히는데 실제로는 *"이 프로세스가 뜬 뒤로 없었다"* 이다 — **docs/56 §9 가 정확히 그 오독이었다** |
| `tests/test_transport_retry_visibility.py` (신규) | 5 건 | 딕셔너리·**실제 출력 줄**·미존재 카운터 대조군·수명 선언·선언 문자열 |

**동작 변화 0**: 읽기만 하는 필드 3 개다. 새 호출·새 로그 줄·새 분기 없다.

**빨강→초록을 실제로 확인했다** (`loops.py` 변경만 stash 하고 돌렸다):

```
--- RED (without the loops.py change) ---
5 failed in 1.94s
--- GREEN ---
5 passed in 1.66s
```

### 9-1. 안 한 것 / W1 안건으로 넘기는 것

* **배포 0.** 명세 §5 그대로. 오늘 밤 22:30 창에 이 변경은 **안 들어간다.**
* **`tossmon/api/limiter.py` 는 한 줄도 안 고쳤다** (W1 소유). 읽기만 했다.
  → **W1 안건**: `acquire()` 가 **대기 중에도 버킷 락을 쥔다**(`limiter.py:308-310`).
    한 프로세스 안 초과를 막는 옳은 설계지만, 그 그룹의 **모든** 대기가 직렬화된다.
* **`tokens.py` 도 안 고쳤다.** → **안건**: 토큰 재발급 소요 시간을 세는 카운터가 없다.
  §8-1 을 완전히 닫으려면 `_issue()` 의 소요 ms 최댓값이 필요하다. **이번에 안 넣었다** —
  §9 는 이미 있는 값을 내보내는 것뿐이고, 새 계측을 다는 것은 별개 판단이다.
* **`config.yaml`·`ops/**`·`tossmon/store/**`·`tossmon/analysis/**`·`coordination/**`
  ·`tools/d21_verdict.py` 무수정.**

---

## 10. 재현 (전부 읽기 전용, 라이브 0)

세 밤 전수 + 공백 세기:

```python
import sqlite3
from datetime import datetime, timezone
DB = "file:C:/Users/dongh/orca/workspaces/toss_trade/w5-ops/data/tossmon.db?mode=ro"
c = sqlite3.connect(DB, uri=True)
def utc(d, H, M):
    return int(datetime(2026, 8, d, H, M, tzinfo=timezone.utc).timestamp() * 1000)
for d in (12, 13, 14):
    rk = [r[0] for r in c.execute(
        "select distinct snap_ms from rankings_snap where snap_ms>=? and snap_ms<? order by 1",
        (utc(d, 13, 30), utc(d, 20, 0)))]
    ob = [r[0] for r in c.execute(
        "select distinct snap_ms from orderbook_snap where snap_ms>=? and snap_ms<? order by 1",
        (utc(d, 13, 30), utc(d, 20, 0)))]
    both = sorted(set(rk) | set(ob))          # 둘 다 조용한 구간
    g20 = [(a, b) for a, b in zip(rk, rk[1:]) if (b - a) / 1000 > 20]
    q20 = [(a, b) for a, b in zip(both, both[1:]) if (b - a) / 1000 > 20]
    print(d, "rank", len(rk), "ob", len(ob), "rank_gaps>20s", len(g20),
          "both_silent>20s", len(q20))
```

세 그룹 동시 0 창 (`collector.log`, 읽기):

```python
import re
LOG = r"C:/Users/dongh/orca/workspaces/toss_trade/w5-ops/data/collector.log"
for line in open(LOG, encoding="utf-8", errors="replace"):
    if " telemetry " not in line:
        continue
    g = {k: int(m.group(1))
         for k in ("md_peak_1s", "chart_peak_1s", "rank_peak_1s")
         if (m := re.search(r"\b" + k + r"=([0-9]+)", line))}
    if g and 0 in g.values():
        print(line[:19], g)          # 재기동 직후 첫 줄과 진짜 정지를 손으로 가른다
```

**함정 하나**: `*_peak_1s` 의 지평은 **60 초**다(`budget.py:53`). 0 은 *"그 순간 안 보냈다"*
가 아니라 *"직전 60 초에 한 건도 안 보냈다"* 이다. 그리고 **재기동 직후 첫 줄은 항상 0**
이므로(`proc_uptime_s` 로 가른다) 그것을 정지로 세면 08-13 이 08-14 처럼 보인다.

---

## 11. 게이트 출력

```
$ git merge --ff-only origin/main        # cb7bdd7
$ .venv/Scripts/python.exe -m pytest -q
2202 passed, 1 skipped, 4 deselected, 24 warnings in 267.75s (0:04:27)
```

명세 §6 의 기준선은 `main` 에서 **2,197 passed** 다. 내 수는 **2,202** = 2,197 + 이번에
추가한 **5 건**이다. 줄어든 것 없음.

**콘솔 출력 비 ASCII 검사 (바이트 단위, `grep -P` 안 씀)** — 실제로 방출되는 텔레메트리
줄을 만들어 바이트를 셌다:

```
telemetry line bytes = 1782
non-ASCII bytes      = 0
PROC_SCOPE_FIELD non-ASCII = 0
new keys present     = True
RESULT: console-emitted telemetry line is pure ASCII
```

**소유 위반 검사 (점 셋)** — 로컬 `main` 은 낡을 수 있으므로 `origin/main` 기준으로 잰다.

```
$ git diff origin/main...HEAD --stat
 docs/63_ranking_gap.md                   | 464 +++++
 docs/INDEX.md                            |   1 +
 tests/test_transport_retry_visibility.py | 127 +++++
 tossmon/collector/loops.py               |  16 ++
 4 files changed, 608 insertions(+)
```

**삭제 0 줄.** 셋은 명세 §4 의 "네 것" 안이다(`tossmon/collector/**` · `tests/**` 중
내가 만든 것 · `docs/63`).

> **네 번째 `docs/INDEX.md` 한 줄은 명세 §4 의 두 목록 어디에도 없다.** 그래서 왜
> 건드렸는지 적는다: 그 파일은 **`docs/` 의 유일한 전수 목록**이고 스스로
> *"여기 없는 문서는 `docs/` 에 없다"* 고 선언한다. `docs/63` 을 넣고 행을 안 더하면
> 그 선언이 **거짓이 되고**, 색인 자신의 대조 스크립트가 `MISSING: ['63_ranking_gap.md']`
> 를 낸다(실제로 냈다). 더한 뒤 다시 돌려 `MISSING: []` 를 확인했다.
> **더한 것은 내 문서의 행 하나뿐이고 다른 행은 안 건드렸다.** 이게 월권이면 되돌려라 —
> 되돌리는 쪽이 색인을 거짓으로 만든다는 것만 함께 적어 둔다.

---

# 12. **토큰 락 가설은 죽었다** — 그리고 늦은 텔레메트리는 두 번째 현상이 아니었다

> 작성: W4, 2026-08-18. 명세 `coordination/specs/w4_gap_token_lock_falsified.md` (`91accee`).
> **배포 0 · 라이브 API 호출 0 · 설정 무수정 · `w5-ops` 읽기 전용(`mode=ro` + 파일 stat).**
> **판정하지 않는다.** 아래는 창 정의·표본 수·오차와 함께 적은 관측이다.
> §1~§11 은 2026-08-15 시점 그대로 두고 여기서만 정정한다 (정정 목록은 §12-7).

## 12-0. 측정 조건

| | |
|---|---|
| 창 | **08-17 13:30 ~ 20:00 UTC** = 08-17 22:30 ~ 08-18 05:00 KST (명세 §1 그대로) |
| 로그 | `w5-ops/data/collector.log.1` — 창 전체가 이 파일 안에 있다 (회전은 08-18 06:44) |
| DB | `w5-ops/data/tossmon.db` (`mode=ro`). 1,163,282 페이지 x 4,096 B = **4.76 GB** |
| 프로세스 | 창 내내 **재기동 0**. 03:44:25 줄의 `proc_uptime_s=38874` → 기동 08-17 16:56:31 (`tossmon.db-shm` mtime 과 일치) |
| telemetry | 창 안 **77 줄**, `config_sig` **1 종** — 코디네이터 §1 재현됨 |
| 랭킹 공백 | `rankings_snap.snap_ms` distinct **5,079** 개, 20초 초과 **48** 건, 60초 초과 **5** 건 — 코디네이터의 5건 표가 시각까지 그대로 재현됐다 |

## 12-1. ★ **"밀림이 공백의 절반"은 기전이 아니라 산수다**

명세 §3-1 이 물은 것: *37/72 ~ 0.51, 65/122 ~ 0.53 — 우연인가 기전인가.*

**우연이다.** 그리고 우연인 이유가 이 절의 값이다.

### 무엇이 텔레메트리 시각을 정하는가 (코드)

`run_session_watch` (`loops.py:2637`)는 **10초 틱**이고, `report_telemetry`
(`loops.py:1210`)는 `now - _last_telemetry_ms < 300s` 면 **그냥 돌아간다**. 그래서
다음 줄이 나오는 시각은 *"직전 줄 + 300초(=마감) 이후 첫 틱"* 이다. 정지 D 가
그 마감을 **가로지르면** 줄은 정지가 끝나야 나온다:

```
지연 = (정지 끝) - (직전 줄 + 300초)
```

정지 시작이 마감 대비 어디에 떨어지는지는 무작위이므로, *"마감을 가로지른 정지"*
의 지연 기댓값은 **정지 길이의 절반**이다. 0.51 · 0.53 은 그 기댓값이다.

### 그 식이 실제로 맞는지 — **순환하지 않는** 검정

지연을 예측하는 쪽(정지 구간)과 예측되는 쪽(텔레메트리 줄)이 **다른 asyncio 태스크**
여야 검정이 성립한다. 그래서 정지 구간은 **DB 만으로** 만들었다 — 로그 줄은 한 개도
안 썼다 (텔레메트리 줄 자신이 섞이면 예측이 자기 자신이 된다).

```
관측 A ("폴 루프가 살아 있었다")  = orderbook_snap.snap_ms ∪ rankings_snap.snap_ms
                                   ∪ promotions.ts_ms ∪ tape_gaps.poll_ms
                                   = 53,204 점, 인접 간격 p50 0.25s, p99 2.08s
관측 B ("세션 틱이 살아 있었다")  = collector.log 의 telemetry 줄 시각 77 개
```

| 마감(직전 줄+300s) | 실제 줄 | 지연 | 마감을 가로지른 A 침묵 | 예측 | 오차 |
|---|---|---:|---|---:|---:|
| 03:37:43.49 | 03:38:20.58 | **37.1s** | 03:37:34.50 → 03:38:21.31 (46.8s) | 37.8s | **-0.7s** |
| 03:43:20.58 | 03:44:25.02 | **64.4s** | 03:42:33.27 → 03:44:25.60 (112.3s) | 65.0s | **-0.6s** |
| 04:14:52.93 | 04:15:04.51 | 11.6s | 04:14:34.57 → 04:15:00.00 (25.4s) | 7.1s | +4.5s |
| (작은 것 1건) | | 2.7s | (2.9s 짜리) | 2.9s | -0.2s |

**76 개 창 중 마감을 가로지른 침묵이 있는 것은 4 개**고, 큰 둘은 **1 초 이내**로 맞는다.
나머지 72 개는 지연 중앙값 **1.0초**, 최대 18.9초다.

### 그래서 코디네이터의 짝짓기가 한 칸 어긋나 있었다

명세 §1-1 은 **+37s 밀림**을 **72.1초 공백**(03:34:53→03:36:05)과 같은 줄에 놓았다.
그 공백은 **마감(03:37:43)보다 1분 38초 먼저 끝났다** — 밀림을 만들 수 없는 자리다.
그 마감을 실제로 가로지른 것은 바로 뒤의 **51.9초 공백**(03:37:29.797→03:38:21.677)이다.
버킷 단위로 짝지으면 그 차이가 안 보이고, 그때 37/72 라는 비율이 생긴다.

> **그러므로 "두 현상인가 하나인가" 의 답: 하나다.** 작은 공백들이 텔레메트리를 안 민
> 것은 성질이 달라서가 아니라 **마감을 안 가로질렀기 때문**이다. 가로지른 것은
> 크기와 무관하게 밀었다 (2.9초짜리도 밀었다 — 표 4행).

### ★ 표본 밖 재현 — **08-14 에도 그대로 맞는다** (같은 스크립트, 창만 바꿈)

08-17 만 보고 세운 식이면 그 밤에 맞춘 것일 수 있다. 그래서 **08-12·08-13·08-14** 에
같은 코드를 돌렸다.

```
08-14  probeA=52,441  5초 초과 침묵 98  telemetry 78줄
  마감 23:11:09.59  지연 45.3s   침묵 23:10:55.36->23:11:54.65 (59.3s)  예측 45.1  오차 +0.3
  마감 23:27:06.97  지연 14.7s   침묵 23:26:10.63->23:27:21.77 (71.1s)  예측 14.8  오차 -0.1
  마감 23:21:56.78  지연 10.2s   침묵 23:21:49.68->23:22:06.49 (16.8s)  예측  9.7  오차 +0.5
  (그 외 가로지른 것 3건: 오차 +0.1 / -0.3 / -3.3)
  가로지른 것 없는 71 창: 지연 중앙값 0.6s
08-13  5초 초과 침묵 12개, 가로지른 것 1건.  나머지 76 창 중앙값 0.6s, 최대 10.4s
08-12  5초 초과 침묵 **0개**.               77 창 중앙값 0.4s, 최대 10.5s
```

> **★ 여기가 "절반" 을 확실히 죽이는 자리다.** 08-14 의 **71.1초** 공백이 만든 밀림은
> **14.7초** 다 — 비율 **0.21**. 같은 밤 **59.3초** 공백은 **45.3초**(비율 **0.76**)를
> 만들었다. **비율은 0.2 에서 0.8 까지 흩어진다.** 흩어지는 그 값을 *"마감까지 남은
> 거리"* 하나로 바꾸면 **오차 0.5초 안**으로 모인다. 08-17 의 0.51·0.53 은 두 개를
> 뽑아 본 값이었다.

**부기 둘**: (1) 침묵이 없는 창에서 지연이 **정확히 10.4초** 로 여러 번 나온다 — 10초
틱 한 칸을 통째로 놓친 것이다(누적 드리프트가 10초를 넘으면 그렇게 된다). 모델과
어긋나지 않는다. (2) 지연이 −0.2 ~ −0.5 초로 **음수**인 창이 몇 개 있다.
`_last_telemetry_ms` 는 `clock.now_ms()`(서버 보정)이고 로그 타임스탬프는 로컬이라
그만큼 어긋난다. 결론에 영향 없는 크기다.

08-14 에도 못 가른 것이 남는다: 마감 23:05:34 의 **35.4초** 지연에 가로지른 침묵이
없다. 08-17 의 두 건(아래)과 같은 모양이고 **같이 설명 못 했다.**

### 갈리지 않은 둘

| 마감 | 실제 줄 | 지연 | A 침묵 | 왜 안 갈렸나 |
|---|---|---:|---|---|
| 03:54:26.26 | 03:54:45.13 | 18.9s | 마감을 가로지른 것 **없음** | 침묵(03:54:35.6→03:54:57.1)이 **마감보다 뒤에** 시작하고, 텔레메트리 줄이 그 침묵 **안**에서 나왔다 |
| 04:35:14.73 | 04:35:31.29 | 16.6s | 없음 | 같은 모양 (침묵 04:35:15.6→04:35:39.9) |

두 가지로 읽을 수 있고 **이 자료로는 못 고른다**: (a) 루프가 마감 직후부터 막혔고
줄이 나온 뒤 폴 파이프라인(리미터+HTTP+파싱)이 다시 차는 데 8~12초가 더 걸렸다,
(b) 세션 틱만 늦었고 폴 침묵은 별개다. **설명 못 했다고 적는다.**

## 12-2. 늦은 텔레메트리가 뜻하는 것 — **코드에는 공유 자원이 없다**

명세 §3-2: *"이벤트 루프가 통째로 막혔는지, telemetry 도 같은 자원을 기다린 것인지 코드로 갈라라."*

세션 감시는 **자기 태스크**다 (`run_all` 이 `asyncio.create_task(..., name="session")`).
그 태스크가 폴 루프와 공유하는 것을 전부 세면:

| 무엇 | 공유하는가 | 근거 |
|---|---|---|
| 리미터 버킷·락 | **아니오** | 세션 틱은 HTTP 를 거의 안 낸다 (아래) |
| 예산(BudgetGuard) | 읽기만 | `report_telemetry` → `budget.describe()` 는 동기 |
| 토큰 `_alock` | **아니오** | HTTP 를 안 내면 `tokens.get()` 에 들어가지도 않는다 |
| DB 연결 | **아니오** | `_session_tick` 에 `store.*` 호출이 없다 |
| **이벤트 루프** | **예 — 이것뿐이다** | |

세션 틱이 HTTP 를 내는 자리는 둘뿐이다 (`_session_tick`, `loops.py:2646`):

* `scheduler.ensure()` 가 `needs_refresh()` 일 때 — TTL 은 **`CALENDAR_TTL_S = 6시간`**
  (`scheduler.py:33`). 6.5 시간 창에서 최대 1~2 회다.
* `history_days` 가 비었을 때 `scheduler.history(HISTORY_DAYS=3)` — 최대 3 건을
  **순차로** 낸다. 이건 **세션 전환 직후 한 번**이다 (`reconfigure_tiers` 가 비운다).
  창 안의 전환은 시작점(22:30 pre→regular) 하나뿐이고, **못 가른 두 지연(03:54·04:35)은
  전환점이 아니다.** 그러니 이 경로로는 그 둘을 설명 못 한다 — 설명 못 한 채로 둔다.

나머지 ~2,300 번의 틱은 **await 없이** 텔레메트리 간격만 보고 돌아간다.

> **그러므로: 폴이 멈춘 그 초에 세션 틱까지 늦었다면, 늦춘 것은 공유 자원이 아니라
> 이벤트 루프 자신이다.** §12-1 에서 마감을 가로지른 창 **아홉**(08-17 넷 · 08-14 여섯
> 중 다섯이 오차 0.5 초 이내)이 정확히 그 모양이다. 오차가 1 초 이내라는 것은
> **서로 모르는 두 태스크가 같은 순간에 같이 풀렸다**는 뜻이다.

**독립 확인**: 03:38:20 → 03:44:25 창의 telemetry 줄은
`md_peak_1s=0 chart_peak_1s=0 rank_peak_1s=0` 이고 `ranking_snap_age_s=121` 이다.
정규장 telemetry 중 **세 그룹이 동시에 0 이면서 재기동이 아닌 줄**은 로그 전체에서
셋뿐이고 (`proc_uptime_s` 가 있는 구간 한정 — 08-04·08-08 의 4 줄은 그 필드가 없던
시절이라 판별 불가), 08-14 의 둘(§4)에 **이번이 셋째**다:

```
2026-08-14 23:11:54  uptime_s=50152  rank_age_s= 65
2026-08-14 23:27:21  uptime_s=51079  rank_age_s= 70
2026-08-18 03:44:25  uptime_s=38874  rank_age_s=121   <- 이번
```

### 12-2a. 동기 블로킹 후보 — **먼저 반증 설계, 그 다음 관측**

명세가 요구한 순서대로 적는다. "결과" 열이 비어 있으면 **안 쟀다는 뜻**이다.

| | 후보 | 코드 근거 | 어떻게 반증하는가 | 결과 |
|---|---|---|---|---|
| A | **동기 sqlite 쓰기** | `store/writer.py` 는 `sqlite3` 를 직접 부른다. 컬렉터 전체에 `to_thread`/`run_in_executor` 가 **0 건** (`universe/build.py` 한 곳만 있고 수집 경로가 아니다) | 그런 호출이 존재하는가 → grep | **존재 확인.** 한 번의 `COMMIT` 이 길면 루프 전체가 선다. **그 소요를 세는 계측이 없었다** → §12-5 가 이것만 만든다 |
| B | **WAL 자동 체크포인트** | 라이브 PRAGMA: `journal_mode=wal`, `wal_autocheckpoint=1000`(=4 MB), `page_size=4096`, 본체 4.76 GB. 체크포인트는 `COMMIT` **안에서** 돈다 | `-shm` 의 WAL-index 헤더(`mxFrame`/`nBackfill`)를 read-only 로 표본해 주기와 정지를 직접 본다. DB 를 열지 않으므로 리더 표시를 남기지 않는다 | **부분 반증** — 아래 |
| C | **로그 회전 (32 MB)** | `notifier.py` `MAX_BYTES=32*1024*1024` | 창 안에 회전이 있었나 | **반증.** `collector.log.1` mtime = 08-18 06:44, 창 밖. 창 안 회전 **0 회** |
| D | 로그 줄당 동기 쓰기 | `RotatingFileHandler` + `StreamHandler`, 매 줄 | 로그 줄 공백으로 정지를 잴 수 있나 | **잴 수 없다.** 창 안 1,812 줄 = 4.6 줄/분. 최대 줄 간격 135초인데 정지와 무관한 자리에도 100초급이 있다 — **해상도 부족** |
| E | 상태파일 쓰기 | `loops.py:1387` `_atomic_write_json`, `STATE_SAVE_S=60` | 크기·빈도 | 파일 35,870 B. mkstemp+write+`os.replace`. **안 쟀다** — `loop_lag` 큼 + `db_write` 작음 조합이 이 후보를 가리킨다 |
| F | GC gen2 정지 | CPython | 프로세스 **안**에서 `gc.callbacks` 를 달지 않는 한 사후 관측이 없다 | **안 쟀다.** 관측 수단 자체가 없다 |
| G | `time.sleep` | grep | 수집 경로에 있나 | **반증.** `tossmon/collector/**`·`api/**`·`store/**` 에 0 건 |
| H | 토큰 `_alock` | `tokens.py:169-182` | §12-3 (못 셋) | **반증** |
| I | 리미터 그룹 락 | `limiter.py:100`·`:190`·`:300` — 버킷당 락 | 세 그룹 동시 정지를 만들 수 있나 | **못 만든다** (§6-1 과 같은 근거) |
| J | 워치독(5분 주기 PowerShell) | Task Scheduler `tossmon-watchdog`, `PT5M`, 위상 = (unixtime mod 300) == **52** | 정지 시작이 그 위상 ±10초에 몰리는가 | **반증.** 08-14 **2/33** (우연 기대 2.3), 08-17 **4/48** (기대 3.4) |
| K | Defender 검사·설정변경 | Windows 이벤트 로그 | 정지와 겹치는가 / 검사가 정지를 만드는가 | **충분조건으로 반증** — 아래 |
| L | OS 레벨 정지 (절전·디스크 오류) | Windows System 로그 | Kernel-Power 41/42/107, 디스크 129/153 | **반증.** 창 안 **0 건**. 디스크 여유 **62.1 GiB**(=66.7 GB) — 코디네이터가 08-18 에 잰 62.29 GiB 와 같은 값이다 |

#### B — WAL 체크포인트를 라이브로 쟀다 (읽기 전용, 08-18 10:01~10:05, 240초)

`-shm` 헤더에서 `mxFrame`(WAL 안 프레임 수)을 50 ms 간격으로 읽었다. `mxFrame` 은
writer 가 커밋할 때만 오르므로, *"안 오르다가 줄어드는"* 구간이 체크포인트를 실은
커밋이다.

```
RESET  mxFrame 1108 -> 3     직전 정지    405 ms
RESET  mxFrame 1001 -> 3     직전 정지   3559 ms
RESET  mxFrame 1141 -> 142   직전 정지    203 ms
RESET  mxFrame 1014 -> 126   직전 정지    102 ms
RESET  mxFrame 1040 -> 145   직전 정지    152 ms
RESET  mxFrame 1062 -> 3     직전 정지     51 ms
RESET  mxFrame 1101 -> 153   직전 정지    152 ms
RESET  mxFrame 1100 -> 148   직전 정지    388 ms
RESET  mxFrame 1130 -> 6     직전 정지   1780 ms
240초에 리셋 9회 (~27초마다). 리셋 직전 피크는 전부 1001~1141 = 문턱 1000 바로 위.
커밋 사이 정지 전체: p50 558 ms · p90 1,878 ms · p99 3,559 ms · 최대 3,655 ms
```

읽는 법 두 가지, **둘 다 적는다**:

* **기전은 실재한다.** 자동 체크포인트가 **평상시 27초마다** 돌고, 커밋 사이가
  최대 **3.66초** 얼어붙는다. 이 값이 무엇이든 그동안 이벤트 루프는 다른 태스크를
  못 깨운다 (후보 A). 랭킹 폴 주기 12초에 3.66초는 작지 않다.
* **그러나 체크포인트가 범인이라는 근거는 아니다.** 체크포인트를 **실은** 커밋의
  직전 정지 중앙값은 **203 ms** 로, 평상 커밋 간격 중앙값 **558 ms** 보다 오히려
  **작다.** 이 표본에서 체크포인트는 지배적 정지가 아니다.
* **그리고 이 표본은 장 마감 뒤(`after`)다.** 정규장 쓰기량에서 다시 재야 한다 —
  같은 스크립트를 오늘 밤 창에 돌리면 된다 (§12-6).

> **함정**: `tossmon.db-wal` 파일 크기(34.7 MB = 8,469 프레임)는 **고수위**다.
> 체크포인트가 끝나면 SQLite 는 WAL 을 잘라내지 않고 앞으로 되감는다. 그러니
> *"WAL 이 34 MB 니까 체크포인트가 밀려 있다"* 로 읽으면 안 된다 — 위 표본에서
> 실제 `mxFrame` 은 1,000 근처에서만 돈다.

#### K — Defender 는 겹친 자리가 하나 있고, **그 하나로는 안 된다**

겹친 것: **08-18 03:14:43** 에 Defender 설정 변경(Id 5007) 5 건이 났고,
그날 첫 60초 초과 공백이 **03:15:03.443** 에 시작한다 — **20초 뒤**. 그리고 60초
초과 5건 중 넷이 그 뒤 30분 안에 몰려 있다.

**그런데 충분조건으로는 죽는다. 반증 관측 셋:**

```
08-12 창 안 04:44:28~04:47:16  Defender 전체검사 (2m48s)  -> 그 밤 20초 초과 공백 0건
08-17 창 안 02:37:19~02:41:06  Defender 검사 (3m47s)      -> 그 구간 20초 초과 공백 0건
08-17 창 안 04:25:28~04:27:25  Defender 검사 (1m57s)      -> 그 구간 20초 초과 공백 0건
08-14 정지 무리(22:43~23:40)   -> 그 창에 Defender 이벤트 자체가 0건
```

**검사는 정지를 안 만들었고, 정지 무리에는 Defender 가 없었다.** 03:14:43 의 겹침은
표본 1 이고 **설명 못 한 채로 남긴다.** (설정 변경 5007 은 검사가 아니라 ECS 구성
갱신이라 검사와 부하 성질이 다를 수 있다 — 그것도 안 쟀다.)

## 12-3. ★ **토큰 락 가설 — 못 셋으로 죽었다**

§7-가 가 남긴 유일한 단일 후보였다: *"`_alock` 을 쥔 채 15초 토큰 POST + 최대 3회
재시도(최악 63.5초)"*. 셋 다 독립이다.

### 못 1 — **코드의 천장이 63.5초가 아니라 15초다** (§7-가 정정)

```python
async def get(self):                      # tokens.py:169
    async with self._alock:               # <- 락은 여기서만
        ...
        self._token, ... = await self._issue()      # tokens.py:181
```

`_issue()` 가 예외를 올리면 **`async with` 가 풀리면서 락이 놓인다.** 재시도는
`_alock` 밖의 `client._request` while 루프가 하고(`client.py:263-292`), 백오프
`asyncio.sleep` 도 락 밖이다. 그러므로 **락의 연속 보유 상한은 `_issue()` 1 회
= httpx `timeout=15.0` 한 번**이다 (`tokens.py:381`).

> §7-가 의 *"4회 시도 x 15초 + 백오프 ~ 63.5초, 그동안 `_alock` 이 잡혀 있으므로"*
> 에서 **"그동안 잡혀 있으므로" 가 틀렸다.** 63.5초는 그 **호출 하나**가 걸릴 수 있는
> 최대 시간이지 락 보유 시간이 아니다. 15초로는 60.2~122.2초를 못 만든다.
> §8-2 가 *"76.1초는 63.5초보다 크다"* 를 미해결로 남겼는데, 천장이 15초라면
> 그 간극은 더 벌어진다 — **줄이는 쪽이 아니라 늘리는 쪽으로 정정된다.**

### 못 2 — 재시도 카운터가 안 움직였다 (코디네이터 §1-1 재현) — **다만 정확히 읽는다**

가장 큰 공백 셋(69.6s · 72.1s · 122.2s)을 품은 버킷에서
`Δretries = Δhttp_5xx = Δhttp_429 = Δauth_refresh = 0` 이다.

**무엇이 무엇을 세는지 코드로 못 박고 읽는다** (`client.py:263-292`):

| 카운터 | 언제 오르나 | 최종 실패에도 오르나 |
|---|---|---|
| `retries` | **실제로 다시 시도할 때만** | **아니오** |
| `http_5xx` | TransientHTTP 를 받을 때마다 | **예** |
| `http_429` | RateLimited 를 받을 때마다 | **예** |
| `auth_refresh` | 그 호출의 **첫** AuthExpired | 첫 건은 예 |

그래서 `Δretries=0` 하나만으로는 *"재시도 없었다"* 까지고, **최종 실패**까지 배제하려면
`http_5xx`·`http_429`·`auth_refresh` 가 함께 0 이어야 한다. **넷 다 0 이다.** 그러므로
그 셋의 구간에서는 전송 계층이 아무 일도 안 겪었다.

> **그런데 창의 첫 60초 초과 공백(22:31:19, 71.2s)은 다르다.** 그 버킷은
> `Δretries=+5 · Δhttp_5xx=+5` 다 (코디네이터 §1-1 표 1행). 그 자리에 대해서는
> **§7-가 의 (i) 전송 재시도 성분을 반증하지 않았다.** 다만 (i)은 **부르는 코루틴
> 하나만** 세운다 — 백오프는 `asyncio.sleep` 이고 그룹 버킷도 따로다. 같은 초에
> 호가까지 69.2초 조용했던 것(§12-0)을 (i)로는 못 설명한다. 그리고 (ii) 토큰 락은
> **못 1·못 3 으로 이미 죽어서** 그 구간에도 안 살아난다.
> **22:31 건은 따로 안 봤다.**

### 못 3 — ★ **창 안에 토큰 POST 가 아예 없었다** (직접 관측)

`_issue()` 가 성공하면 `_write_state()` 가 상태 파일을 **원자적으로 교체**한다
(`tokens.py:290-303`). 그 파일을 봤다 (토큰 값은 읽지도 출력하지도 않았다):

```
w5-ops/data/token_state.json  mtime      = 2026-08-17 11:41:16 KST
                              expires_at = 2026-08-18 11:41:15 KST   (24시간짜리)
창                                        = 08-17 22:30 ~ 08-18 05:00 KST
```

**창 전체가 이 토큰의 유효기간 안이고, 창 안에 파일이 다시 쓰인 적이 없다.**
선제 재발급 문턱은 `REFRESH_MARGIN_MS = 60_000` 이라 08-18 **11:40:15** 에나
걸린다 — 창이 끝나고 6.7시간 뒤다. 그러므로 창 안의 모든 `tokens.get()` 은
캐시 히트로 즉시 락을 놓았다.

### 12-3a. 다만 코디네이터의 **읽는 법**은 한 칸 고쳐야 한다

명세 §2 는 *"`auth_refresh` 가 0 이다 → 토큰 POST 자체가 없었다"* 로 읽었다.
**그 함의는 성립하지 않는다.** `auth_refresh` 는 `_request` 가 **401(AuthExpired)를
받았을 때만** 올린다(`client.py:270-273`). `_issue()` 자체는 **성공하면 어떤 카운터도
안 올린다** — 선제 재발급은 카운터로 흔적을 안 남긴다.

결론은 그대로 살아 있다. **다만 근거가 못 2 가 아니라 못 1 과 못 3 이다.**
이 구분이 중요한 이유: 다음에 `auth_refresh=0` 을 보고 *"토큰은 결백하다"* 로
읽으면 **성공한 재발급을 놓친다.**

## 12-4. 개장 몰림은 **성질이 아니었다** — 두 창을 같이 놓는다

명세 §3-3 이 맞다. 그리고 수치로 보면 더 강하게 무너진다.

```
                  20초 초과   개장 첫 70분   마감 전 120분   개장 이후 중앙값
2026-08-14           33         31 (94%)        1 ( 3%)         +46분
2026-08-17           48         11 (23%)       27 (56%)        +284분
```

**같은 현상이 08-14 엔 앞에, 08-17 엔 뒤에 몰렸다.** *"개장 첫 70분"* 도
*"마감 1~1.5시간 전"* 도 안정된 성질이 아니다. 둘 다 그날의 우연이다.

### 그러면 두 밤에 공통인 것은? — 폴 주기가 같은 밤 전수

`p50 폴 간격`이 0.26~0.34초로 같은 밤만 비교한다 (08-04~08-06 은 11.6초라 다른 설정).

```
날짜     개장 시점 rankings_snap 누적    20초초과   60초초과   최장     정지 총합
08-03            3,172,541 행               11        1     129.0s      470s
08-07            8,932,138 행                1        1     254.3s      254s
08-10           10,958,333 행                0        0      14.4s        0s
08-11           12,996,828 행                0        0      16.5s        0s
08-12           14,144,926 행                0        0      13.1s        0s
08-13           16,136,418 행                3        0      37.7s       85s
08-14           17,859,915 행               33        4      74.1s     1182s
08-17           19,357,255 행               48        5     122.2s     1759s
```

**08-10 부터 08-17 까지 여섯 밤은 단조 증가다** (0·0·0·3·33·48). 그런데:

* **08-03 이 그 단조를 깬다** — 320만 행에 11 건이다. 그 밤은 디스크 경보가
  세 번(`disk_warn`·`disk_reclaim`·`disk_critical`) 난 밤이라 다른 원인이 있다.
  **제외하지 않고 적는다.**
* **D-21 과 DB 크기가 완전히 얽혀 있다.** D-21 은 정확히 최악의 두 밤(08-14·08-17)
  에만 켜져 있다(§5-1). 표본이 **밤 4 개**다. **인과를 못 세운다.**
* 이 표는 *"DB 가 커져서"* 를 지지하지도 반증하지도 않는다. **다음 밤 하나로 갈리는
  것도 아니다** — 갈리려면 D-21 을 끈 밤이 하나 필요하고, 그건 수집 변경이라
  내 권한 밖이다 (명세 §4).

## 12-5. 넣은 것 — **게이지 둘. 송신은 한 건도 안 바뀐다**

§12-2 가 코드로 세운 것(*"공유 자원이 없으니 늦은 텔레메트리는 루프 정지다"*)은
**논증**이지 관측이 아니다. 다음 창에 그것을 **재는** 값 둘을 넣었다.

| 파일 | 무엇 | 왜 |
|---|---|---|
| `tossmon/collector/scheduler.py` (`Clock.sleep`) | `sleep(x)` 가 x 보다 얼마나 더 걸렸는지의 **창 최대치** → `loop_lag_max_ms` | 모든 수집 루프의 쉬는 시간이 이 한 곳을 지난다. 루프가 통째로 서면 그때 자던 태스크의 초과분에 정지 길이가 그대로 남는다. **대기로는 안 오른다** — 대기는 자는 게 아니라 `await` 이고 루프는 계속 돈다 |
| `tossmon/collector/loops.py` (`_timed`) | 동기 `store.*` 호출 1 건의 소요 **창 최대치** → `db_write_max_ms` | 후보 A·B 를 후보 E·F 와 가른다. `Store` 는 W2 소유라 **안 건드리고 호출측에서만** 잰다 |
| 같은 파일 `WINDOW_SCOPED_GAUGES` | 창 값 4 개 선언 (`promotions_delta`·`demotions_delta` + 위 둘) | `counter_scope` 의 `rest:install` 은 나머지가 전부 설치 수명 누적이라고 말한다. `loop_lag_max_ms=0` 이 *"한 번도 안 밀렸다"* 로 읽히면 정확히 docs/56 §9 다. **기존 두 `_delta` 도 원래 오선언이었다 — 같이 고쳤다** |
| `tests/test_loop_stall_visibility.py` (신규) | 15 건 | 진짜 이벤트 루프를 `time.sleep` 으로 막고 게이지가 오르는지 / 조용한 루프 대조군 / 예외 통과 / 창 리셋 / `telemetry()` 순수성 / **소스 AST 전수**(계측 안 지나는 `store.*` 직접 호출 0 건) / 기존 `proc:` 파서 회귀 |

**읽는 법 — 이 둘은 짝이다:**

```
lag 큼 + db 큼   -> 동기 sqlite 호출이 루프를 세웠다          (후보 A·B)
lag 큼 + db 작음 -> 루프는 섰는데 sqlite 가 아니다            (후보 E·F·L)
lag 작음         -> 루프는 돌았다. 폴이 멈췄다면 대기지 정지가 아니다
```

**동작 변화 0 의 증거** (08-15 와 같은 방법, `git diff -U0 -- tossmon/`):

```
추가 83 줄 / 삭제 14 줄
추가 줄의 제어흐름 키워드: {await:1, finally:1, if:2, return:2, try:1}
삭제 줄의 제어흐름 키워드: {await:1}
그 7 줄 전부:
        try:                                  <- 신규 함수 _timed 안
            return fn(*args, **kwargs)        <- 신규 함수 _timed 안
        finally:                              <- 신규 함수 _timed 안
            if ms > ctx._db_write_max_ms:     <- 신규 함수 _timed 안
            await asyncio.sleep(want)         <- 삭제된 await 1 개를 그대로 대체
            if lag_ms > self.sleep_lag_max_ms:   <- 최댓값 갱신 가드
            return value                      <- 신규 메서드 take_sleep_lag_max_ms 안
```

**기존 분기는 한 줄도 안 바뀌었다.** 새 HTTP 호출·새 로그 줄·새 asyncio 태스크 0.

**빨강→초록을 두 방식으로 확인했다:**

```
--- RED-1: 소스 변경을 stash (이름 자체가 없다) ---
E   ImportError: cannot import name 'WINDOW_SCOPED_GAUGES'
1 error in 0.95s        (15건 전부 수집 단계에서 죽는다)

--- RED-2: 이름은 두고 계측 **본체만** 병합 전 것으로 되돌림 ---
FAILED test_a_blocked_event_loop_shows_up_in_the_sleep_lag
FAILED test_the_gauge_keeps_the_worst_not_the_last
FAILED test_timed_records_the_duration_of_a_slow_store_call
FAILED test_timed_still_records_when_the_call_raises
4 failed, 11 passed in 1.72s

--- GREEN ---
15 passed in 2.15s
```

RED-2 를 따로 한 이유: RED-1 은 *"이름이 없다"* 만 보여 준다. 계측이 **동작을**
재는지는 본체를 되돌려야 보인다.

## 12-6. 안 한 것 / 다음에 할 것

* **배포 0.** 오늘 밤 22:30 창에 이 변경은 **안 들어간다.** 배포는 코디네이터다.
* **라이브 API 호출 0.** `w5-ops` 는 `mode=ro` 와 파일 `stat`/읽기만 썼다.
* **정규장에서 WAL 체크포인트를 안 쟀다.** §12-2a 의 표본은 마감 뒤 240초다.
  같은 방법(-shm 헤더 읽기, DB 를 안 연다)을 **정규장 창에 돌리면** 후보 B 가
  배포 없이 갈린다. 이건 오늘 밤에 할 수 있다.
* **GC 를 안 쟀다** (후보 F). 프로세스 안에 `gc.callbacks` 를 달지 않는 한 수단이 없다.
  달면 그건 계측 배포다 — 여기선 안 했다.
* **상태파일 쓰기를 안 쟀다** (후보 E). `loop_lag` 큼 + `db_write` 작음 이 나오면
  그때 재는 것이 순서다.
* **가로지른 침묵 없이 늦은 창 셋을 못 갈랐다** — 08-17 의 03:54(18.9s)·04:35(16.6s) 와 08-14 의 23:05(35.4s). 셋 다 같은 모양이다 (§12-1).
* **창의 첫 60초 초과 공백(22:31:19, 71.2s)을 따로 안 봤다** — 그 버킷만 `Δretries=+5·Δhttp_5xx=+5` 다. 전송 재시도가 그 자리에서 무엇을 했는지 **안 쟀다** (§12-3 못 2 의 인용문).
* **03:14:43 Defender 설정 변경과 20초 뒤 정지의 겹침을 설명 못 했다** (§12-2a K).
* **08-13 의 3 건은 이번에도 안 봤다** (§8-4 그대로).
* **`tossmon/api/**` · `tossmon/store/**` · `config.yaml` · `ops/**` ·
  `coordination/**` · `tools/**` 무수정.** `tokens.py`·`limiter.py`·`writer.py` 는
  **읽기만** 했다.
* **`docs/INDEX.md` 한 줄** — 명세 §4 의 소유 목록 밖이다. §11 과 같은 이유로 건드렸고
  (그 파일은 `docs/` 의 유일한 전수 목록이고 스스로 그렇게 선언한다), **내 문서의 행
  하나만** 고쳤다. 그 행의 옛 요약이 이제 거짓이기 때문이다 — *"개장 첫 70 분"* 과
  *"남은 것은 송신 정지 하나뿐인데 못 갈랐다"* 둘 다 §12 가 뒤집었다. 색인 자기 대조를
  다시 돌려 `MISSING: []` 를 확인했다. **월권이면 되돌려라** — 되돌리는 쪽이 색인을
  거짓으로 만든다는 것만 함께 적어 둔다.
* **동작 코드를 바꿔야 한다는 결론** — 명세 §4 가 시킨 대로 바꾸지 않고 적는다:
  `store/writer.py` 의 모든 쓰기는 이벤트 루프 스레드에서 돈다. 후보 A·B 가 확인되면
  고칠 곳은 그리로 **`asyncio.to_thread`** 이거나 `wal_autocheckpoint` 조정이다.
  **둘 다 W2 소유이고 수집 동작을 바꾼다. 내가 안 한다.**

## 12-7. §1~§11 에 대한 정정 목록 (본문은 안 지웠다)

| 어디 | 무엇이 틀렸나 | 어디서 고쳤나 |
|---|---|---|
| §7-가 | *"최악 4회 x 15초 + 백오프 ~ 63.5초 **동안 `_alock` 이 잡혀 있다**"* — 락은 `_issue()` 1 회마다 풀린다. 연속 보유 상한은 **15초** | §12-3 못 1 |
| §8-1 | *"토큰·재시도 경로를 못 갈랐다"* — **갈렸다. 죽었다** | §12-3 |
| §8-2 | *"76.1초 > 63.5초"* 의 간극 — 천장이 15초이므로 **더 벌어진다** | §12-3 못 1 |
| §8-5 | *"이벤트루프가 막힌 것인지 송신만 막힌 것인지 못 갈랐다"* — 코드로는 갈렸다(공유 자원 없음). **관측으로는 다음 창에 갈린다** | §12-2 · §12-5 |
| §7-라 | *"개장 첫 70분"* 은 안정된 성질이 아니다 (08-17 에 23%) | §12-4 |
| `loops.py` 주석 (텔레메트리 3필드) | *"토큰 재발급은 `_alock` 을 쥔 채 15초 POST"* — 이 문장은 **맞다**. 63.5초 주장은 이 주석에 없다 | 안 고쳤다 |

## 12-8. 재현 (전부 읽기 전용, 라이브 0)

§10 의 스크립트를 그대로 쓰되 창만 08-17 13:30~20:00 UTC 로 바꾼다. 이번에 새로 쓴 둘:

```python
# (1) 순환 없는 straddle 검정 (§12-1). 창만 바꾸면 어느 밤에나 그대로 돈다 (08-12~08-17 실행함)
#     관측 A 는 DB 4개 컬럼만, 관측 B 는 telemetry 줄만. 절대 섞지 말 것 --
#     A 에 로그 줄을 넣으면 "정지 끝 = 텔레메트리 줄" 이 되어 예측이 자기 자신이 된다.
A = (orderbook_snap.snap_ms | rankings_snap.snap_ms
     | promotions.ts_ms | tape_gaps.poll_ms)          # 창 안 distinct
SIL = [(a, b) for a, b in zip(A, A[1:]) if b - a > 5_000]
for prev_line, next_line in zip(tel, tel[1:]):        # telemetry 줄 시각
    due = prev_line + 300_000
    lag = (next_line - due) / 1000
    strad = [(a, b) for a, b in SIL if a <= due < b]
    pred = (strad[0][1] - due) / 1000 if strad else 0.0
    # lag ~ pred 이면 그 창의 지연은 "정지가 마감을 가로질렀다" 로 전부 설명된다
```

```python
# (2) WAL 자동 체크포인트를 DB 를 열지 않고 재는 법 (§12-2a B)
# -shm 의 WAL-index 헤더는 네이티브 바이트오더:
#   +14 u16 szPage  +16 u32 mxFrame  +20 u32 nPage        (WalIndexHdr)
#   +96 u32 nBackfill  +100 u32 aReadMark[5]  +128 u32 nBackfillAttempted
import struct, time
SHM = "C:/Users/dongh/orca/workspaces/toss_trade/w5-ops/data/tossmon.db-shm"
prev = None
while True:
    try:
        with open(SHM, "rb") as f:      # 쓰기가 잡고 있으면 PermissionError -- 건너뛴다
            b = f.read(160)
    except PermissionError:
        time.sleep(0.05); continue
    mx = struct.unpack_from("<I", b, 16)[0]
    if prev is not None and mx < prev:
        print("checkpoint reset", prev, "->", mx)   # 직전 정지 = 체크포인트 소요 상한
    prev = mx
    time.sleep(0.05)
```

```
# (3) Defender / OS 레벨 (§12-2a K·L) -- PowerShell, 읽기만
Get-WinEvent -FilterHashtable @{LogName='Microsoft-Windows-Windows Defender/Operational';
  StartTime=[datetime]'2026-08-17 22:00'; EndTime=[datetime]'2026-08-18 05:30'}
Get-WinEvent -FilterHashtable @{LogName='System'; Id=1,41,42,107,129,153,157; ...}
```

**함정 하나**: `tossmon.db-wal` 의 **파일 크기**로 체크포인트 밀림을 판단하면 안 된다.
그 값은 고수위이고 SQLite 는 체크포인트 뒤 파일을 자르지 않는다 (§12-2a B 의 함정).

## 12-9. 게이트 출력 (2026-08-18)

```
$ git fetch origin && git merge --ff-only origin/main
Updating b6696c7..91accee        # 1차 (명세를 읽은 리비전)
Updating 91accee..a1cdbc2        # 2차 — 작업 중 코디네이터가 PR #33 을 병합했다.
                                 #      충돌 없음(겹치는 파일 0), 이 base 에서 다시 돌렸다.

$ .venv/Scripts/python.exe -m pytest -q
2236 passed, 1 skipped, 4 deselected, 23 warnings in 355.12s (0:05:55)
```

**명세 §5 의 기준선 "2,202 passed" 는 08-15 값이라 이 base 와 안 맞는다.**
(코디네이터도 `daily/2026-08-18.md` §게이트에서 같은 결론을 냈다 — *"워커 주장이
옳았고 내 명세가 틀렸다"*.) 그래서 **수집 개수로 직접** 쟀다:

```
a1cdbc2 만 (내 변경 stash + 새 테스트 파일 치움) : 2222/2226 collected
내 변경 포함                                      : 2237/2241 collected
                                                    차이 = +15 = 이번 추가분 전부
```

2,237 = 2,236 passed + 1 skipped. **줄어든 것 없음.**

> 91accee base 에서도 한 번 돌렸다 (`2234 passed, 1 skipped`, 기준선 2,220 collected,
> 역시 +15). **두 base 모두 +15 이고 실패 0 이다.**

**콘솔 출력 비 ASCII 검사 (바이트 단위)** — §11 과 같은 방법으로 실제 방출 줄을 만들어 셌다:

```
telemetry line bytes = 1898
non-ASCII bytes      = 0
PROC_SCOPE_FIELD non-ASCII = 0
new keys present     = True
... counter_scope=proc:...;window:promotions_delta,demotions_delta,loop_lag_max_ms,db_write_max_ms;rest:install
    loop_lag_max_ms=122200 db_write_max_ms=3655
RESULT: console-emitted telemetry line is pure ASCII
```

**소유 위반 검사 (`git diff origin/main...HEAD --stat`)** — 병합 뒤 코디네이터가 잴 값.
`docs/63` 줄 수는 **이 블록을 쓰기 직전** 값이라 최종본보다 몇 줄 작다 (이 문서가 자기 자신을 세는 자리다). **파일 목록과 삭제 수가 검사 대상이다**:

```
  docs/63_ranking_gap.md              | 578 +++++++++++++++++++++++++++++++++++-
  docs/INDEX.md                       |   2 +-
  tests/test_loop_stall_visibility.py | 325 ++++++++++++++++++++
  tossmon/collector/loops.py          |  77 ++++-
  tossmon/collector/scheduler.py      |  20 +-
  5 files changed, 986 insertions(+), 16 deletions(-)
```

삭제 16 줄의 내역: `tossmon/**` **14 줄** (전부 내가 그 자리에서 다시 쓴 줄 —
§12-5 의 키워드 census 가 그 14 줄을 다 보여 준다), `docs/INDEX.md` **1 줄**
(내 문서 행 하나를 갈아 끼웠다), `docs/63` **1 줄**. **지운 기능 0.**

명세 §4 의 소유는 `tossmon/collector/**` · `tossmon/api/client.py` · `docs/63` 이다.
`api/client.py` 는 **안 건드렸다** (읽기만). `tests/**` 중 내가 만든 파일 하나와
`docs/INDEX.md` 한 줄에 대해서는 §12-6 에 이유를 적었다.
