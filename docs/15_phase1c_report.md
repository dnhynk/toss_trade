# 15 — Phase 1-C 본분석 리포트 (train + validation, 홀드아웃 봉인)

> 소유: W3. 실행일 2026-08-02. 정본 규약: `docs/12_preregistration.md`(개정 6건 반영본).
> 시행 기록: `docs/13_trial_registry.md`. 부록 표: `run_all_appendix.md`(스크래치패드 산출물).
>
> **홀드아웃(2026-05-01~07-29)은 열지 않았다.** 분석 상한은 스크립트 기본값 2026-04-30 이며
> 카탈로그의 `market_date` 최대값은 **2026-04-27** 이다. 본문에 등장하는 홀드아웃 수치는
> 매니페스트의 counts-only 계수(후보일 6,065건) 뿐이다.

---

## 0. 한 문단 요약 (결론 먼저)

사전등록 §8 열람 순서대로 오프라인 재유도를 수행해 **이벤트 51건**(train 21 / val 30)을
얻었다. 그러나 §1.7 기저율 5개가 **전부 허용 대역 밖**으로 나와 **표본 정의 감사가 의무
발동**했고, 감사 결과 원인이 확인됐다: 우리 §2.7 유니버스 필터가 통과시킨 종목군은
**정규장의 약 90%가 무체결(no-print)인 초저유동성 마이크로캡**이다(이벤트일 정규장 실제 봉
중앙값 **390분 중 40분**, T0 봉 거래량 중앙값 **600주**). 이런 테이프에서는 "30분 내 저점 대비
+15%"가 연속적인 급등이 아니라 **수 시간 떨어진 두 개의 희소한 체결** 사이의 가격차로 충족되며,
실제로 T0→피크 소요 중앙값은 **1분**, 피크 수익률 중앙값은 **+1.1%** 로 러너 동역학이 없다.
따라서 **Q1·Q4·Q5·Q6 에 accept/reject 판정을 내리지 않는다(판정 불가)**. Q3 는 데이마켓
이벤트 **n=0** 으로 표본 미달, Q2 는 §6.3 구조적 예외로 라이브 구간 한정이며 현 카탈로그와
시간대가 겹치지 않아 산출 불가다. 이것은 코드 결함이 아니라 **표본 정의와 문헌 표본의 불일치**
이며, 해소 방법은 §7 에 적었다(§2.7 에 유동성/체결률 기준 추가 — **W7 개정 절차 필요**).

---

## 1. 실행 조건과 무결성

| 항목 | 값 |
|---|---|
| 입력 1분봉·일봉 | `w4-collector/data/backfill.db` — **읽기 전용 연결**(`?mode=ro`) |
| 입력 캘린더 | `w4-collector/data/backfill/calendar_us.json` (2,107 매매일) |
| 입력 랭킹(Q2) | `w5-ops/data/tossmon.db` — **읽기 전용 연결** |
| 라이브 호출 | **0건** (전 과정 오프라인, 토큰 발급 없음) |
| 오염 DB | `tossmon_20260730_polluted.db` **미사용** |
| 날짜 상한 | 2026-04-30 (스크립트 기본값). 카탈로그 최대 `market_date` = 2026-04-27 |
| 통계 | Wilson 95%(비율), 부트스트랩 95% 백분위 10,000회, **시드 20260730** |

**러너 강제조항 이행** (docs/14 §VII + 리허설 권고):

| 조항 | 이행 |
|---|---|
| ① `calendar=` 항상 전달 | 전 호출에 전달. `day_grouping_calendar=0.0` 건수 **0** |
| ② 다일 프레임에 `prev_close_u` 명시 금지 | 명시 전달 없음 — §2.3 대체 사슬만 사용 |
| ③ `run_all` 에 `meta=`/`curve=`/`calendar=` | 3개 전부 전달 |
| ④ provenance 행 ≥1 → 시행 무효 | **최종 시행 provenance 행 0건.** 위반 2회를 잡아 무효 처리 후 재실행(레지스트리 T-002·T-004) |

조항 ④가 실제로 두 번 작동했다 — 이것이 이 규율의 값어치다:
1. `curve=None` 으로 호출 → `(half_day_check_not_run)` 발생 → 무효 → 캐리어 곡선으로 재실행.
2. `run_all` 에 빈 `df_1m` 전달 → `t0_price_unavailable` 로 **51건 전량 제외**(§2.7 필터가
   사실상 미적용) → 무효 → T0 봉 프레임을 실어 재실행. 이 조항이 없었다면 "표본 필터를
   통과했다"는 잘못된 리포트가 나갈 수 있었다.

---

## 2. 커버리지 매니페스트 요약 (§8-1)

| 항목 | 값 |
|---|---|
| 1분봉 | 1,795,441행 / 1,465심볼 |
| 일봉 | 3,347,898행 / 1,707심볼 |
| `symbols` 메타 | 1,707행 (§2.7 필터 입력으로 사용) |
| 매니페스트 후보일 | 162,289 (train+val). 스크린 사유: range 112,697 / logvol_z 48,068 / ret 41,663 |
| 홀드아웃 후보일 | **6,065 (counts-only 인용)** — 내용 미열람 |
| 1분봉 창 상태 | `done` 220 / **`partial` 3,782** |
| 창 skip 사유 | `retention_exhausted` 3,820 / 없음 182 |
| 보관한계 도달 심볼 | 1,355 / 1,465 |
| 심볼별 1분봉 일수 | 중앙값 **6일**, p75 15일, 최대 215일. ≥21일 보유 심볼 **279개** |

**캘린더 일수 불일치 확인**: `calendar_us.json` 은 2,107일, 매니페스트
`calendar.days_resolved` 는 6,781이다. 후자는 **달력 걷기 중 조회한 날 수**(주말·휴장 포함,
474회 walk 로 해소된 총 날짜)이고 전자는 **세션이 실재하는 매매일만** 담은 직렬화 결과다.
분석은 후자(2,107일)를 썼고, 그중 홀드아웃 이후를 잘라 사용했다. 모순이 아니라 단위 차이다.

---

## 3. 오염 처리 패스 결과 (§7 (a)~(h) — G3 조건)

| 항목 | 탐지·처리 | 결과 |
|---|---|---|
| (a) events 테이블 중복 | **collector `events` 테이블을 분석 입력으로 쓰지 않음.** `candles_1m` 에서 `detect_events` 로 전량 재유도 | 카탈로그 51건 전부 오프라인 재유도. 라이브 DB의 `events` 36행은 미사용 |
| (b) 비계획 재시작 공백 | 랭킹 의존 지표(Q2)에만 영향. Q2 자체가 산출 불가라 해당 제외 규칙 미발동 | §7-b 제외 0건(적용 대상 없음) |
| (c) tape_gaps (`/trades` 50건 상한) | Phase 1 피처는 테이프 미사용(docs/07 §7 편향 4) | 영향 없음 |
| (d) 유니버스 오염(대형주·ETF 승격) | §2.7 필터로 차단 — `not_common_stock` 0, `not_active` 0, `meta_missing` 0 | 제외 0건 |
| (e) A5 혼입 + 분할일 | **분할 파이프라인 가동**: `split_scan_report` → `split_dates` 를 `detect_events`/피처에 전달 | 분할 등록일 **348**, 보수 확대 `n_widened` **339**, `r` 미계산 (심볼,매매일) **24,742**, `split_excluded` **0** |
| (e) A5 혼입 검사 | **미실행 — 사유: 순환 참조.** 분할 탐지와 혼입 판정이 둘 다 `r` 연속성에 기대므로 외부 분할 이력 없이는 "분할인데 r 연속"을 판별할 수 없다(§7-e 강등 문언 그대로) | 대체 안전망 결과는 아래 별도 |
| (f) 봉 타임스탬프 미확정 | q6 ±1분 감도 **의무 병기** 산출 | 경계 민감 버킷 1개(`open_0_15`, 1건 이동) |
| (g) 생존 편향 | 제거 불가 — Q5 수치에 "생존 종목 조건부" 딱지 부착 | §5 Q5 항목에 명시 |
| (h) M-3 겨울 분할 / M-4 반일장 | 코드 수정으로 해소됨. 본 분석에서 재확인 | `day_grouping_calendar=0.0` **0건**, `half_day_length_sample` **0건** |

**A5 혼입 대체 안전망 (§7-e 문언대로 2종 보고)**

1. **매니페스트 출처 증거**: `call_params.candles_1m.adjusted = "false"`,
   `call_params.candles_1d.adjusted = "true"`. 창 단위로도 `adjusted="false"` 가 기록돼 있다.
   → 수집 시점 파라미터는 **원주가/수정주가 규약을 지켰다는 증거가 남아 있다.**
2. **r 분포 스팟체크**: 심볼별 `split_ratio_series` 가 상수 구간을 유지하다 점프하는 형태를
   보였고(분할 등록 348일), 전 구간 연속(=혼입 의심)으로 판정된 심볼은 **관측되지 않았다.**
   단 이 스팟체크는 §7-e 가 지적한 순환 참조를 우회하지 못하므로 **음성 증거로만** 취급한다.

---

## 4. 표본 정의 감사 (§1.7 의무 발동) — 이 리포트의 핵심

### 4.1 발동 사유 — 기저율 5개 전부 대역 밖

| 지표 | 문헌 | 허용 대역 | **실측(n=51)** | 판정 |
|---|---:|---:|---:|---|
| 페이드 비율(fade+dump) | 71.5% | 56.5~86.5% | **41.2%** | 대역 밖 |
| HOD 대비 −20% 붕괴 | 50% | 35~65% | **7.8%** | 대역 밖 |
| VWAP 아래 마감 | 73% | 58~88% | **31.4%** | 대역 밖 |
| HOD 10:00 ET 전 형성 | 63% | 48~78% | **13.7%** | 대역 밖 |
| HOD 개장 15분 내 형성 | 46.6% | 31.6~61.6% | **7.8%** | 대역 밖 |

5개 중 5개가 벗어났고 전부 **아래쪽**이다. §1.7 규정대로 "시장이 다르다"가 아니라
**우리 표본 정의를 먼저 의심**했다.

### 4.2 감사 결과 — 원인은 표본의 유동성 수준

| 진단 지표 | 실측 | 함의 |
|---|---:|---|
| 이벤트일 정규장 실제 봉 수 | **중앙값 40 / 390분** (체결률 10.3%, p75 16.8%) | 세션의 ~90%가 무체결 |
| T0 봉 거래량 | **중앙값 600주** (p25 267주, 최소 0) | 트리거 분의 체결이 수백 주 |
| T0 봉 가격 | 중앙값 $1.88 (15/51은 $1 미만) | §2.7 [$0.10,$20] 안 |
| 시총 | 중앙값 $30.2M | §2.7 [$10M,$300M] 안 |
| 홀트 프록시 `halt_gap_count` | **중앙값 12** (최대 27) | 정규장 5분 이상 공백이 상시 |
| T0→피크 소요 | **중앙값 1분** | 러너 동역학 없음 |
| 피크 수익률 | **중앙값 +1.1%** | T0 직후 상승 여지가 사실상 없음 |

**해석**: §2.7 필터는 설계대로 작동했다(가격·시총·보통주 조건 전부 통과, 제외는
`mcap_out_of_range` 2건뿐). 그러나 그 필터에는 **유동성 조건이 없다.** 그래서 통과한 종목군은
문헌 표본("프리마켓 5M주+ 갭퍼")과 **다른 모집단**이다. 이런 초희소 테이프에서
"30분 내 저점 대비 +15%"는 연속 급등이 아니라 **수 시간 떨어진 두 체결 사이의 가격차**로
충족될 수 있고, RVOL 분모(곡선)도 똑같이 희소한 이전 20일에서 만들어져 불안정하다.
T0→피크 1분·피크 +1.1% 가 그 증거다 — 진짜 러너라면 T0 이후에 상승 여지가 남아야 한다.

### 4.3 이것은 코드 결함이 아니다

- 무체결 분에 봉이 없는 것은 API 사양이며(docs/07 §1.4) 파이프라인은 이를 거래량 0 으로
  올바르게 처리한다.
- `retention_exhausted` 3,820창은 **API 보관 한계**이지 수집 실패가 아니다(docs/11 §1).
- M-3/M-4 재발 지표(`day_grouping_calendar=0.0`, `half_day_length_sample`)는 **0건**이다.
- 즉 파이프라인은 규약대로 동작했고, **규약이 겨냥한 모집단이 우리가 실제로 얻은 모집단과
  다르다**는 것이 발견이다.

---

## 5. 검증 질문별 판정 (§4 기준)

> 훈련 구간 수치는 §5.3-3 에 따라 **"선별용" 딱지**를 달고, 성능 주장으로 쓰지 않는다.
> 아래 표의 train 수치는 전부 선별용이다.

### Q1 — 거래량 선행 → **판정 불가**

| 지표 | train+val (n=51) |
|---|---|
| `vol_surge_lead_min` 검출률 | 0.902 (46/51), 중앙값 **91.5분** (부트 CI 40.5~195.0) |
| `rvol_first_cross_3_lead_min` 검출률 | 0.843 (43/51), 중앙값 **21.0분** (CI 12.0~52.0) |

표본 수 자체는 §4.1 최소선(n≥30)을 넘고, 형식적으로는 채택 조건(중앙값 ≥5분 & 검출률 ≥0.5)을
충족한다. **그럼에도 판정하지 않는다** — §4.2 의 감사 결과상 이 리드타임은 러너의 전조가
아니라 **희소 테이프에서 앞선 체결이 우연히 z 임계를 넘은 것**일 수 있고, 실제로 P1 예측
(검출률 ≤0.7, 중앙값 0~15분)과 정반대 방향으로 크게 벗어났다. §1.7 은 대역 이탈 시 해석보다
표본 감사를 우선하라고 규정하며, 감사가 표본 부적합을 지목했으므로 **보수 기본값**을 적용한다:
**Phase 2 는 확인-진입(confirm) 경로만 설계한다.**

### Q2 — 랭킹 리드/래그 → **산출 불가 (표본 0)**

랭킹 스냅샷은 **1,715,399행** 존재하나 구간이 **2026-07-31 14:25 ~ 2026-08-01 08:50** 로,
본 카탈로그(2025-09-04~2026-04-27)와 **시간대가 전혀 겹치지 않는다.** §4.2 자격 조건
([T0−120분, T0+120분] 랭킹 커버리지 80% 이상)을 충족하는 이벤트가 **0건**이므로 최소 표본
n≥20 미달 → **판정 불가**, 보수 기본값 적용: **랭킹은 Phase 2 에서 어떤 용도로도 신호로 쓰지
않는다.**

> **§6.3 의무 유보** — 이 결론은 단일 수집 기간의 관찰이며 홀드아웃 검증이 없다. 표본은 특정
> 시장 국면(2026-07~)에 조건화되어 있고, 수집 공백(§7-b)의 영향을 받았을 수 있다. 이 결론만을
> 근거로 Phase 2 진입 로직을 만들 수 없으며, 청산 타이밍 가설로만 사용하되 페이퍼 트레이딩에서
> 재검증해야 한다.

라이브 구간에 대한 Q2 산출은 **그 구간의 이벤트를 같은 방식으로 오프라인 재유도**해야 가능하다
(collector `events` 테이블은 §7-a 로 금지). 그 구간은 본 태스크의 분석 상한(2026-04-30) 밖이라
별도 태스크가 필요하다.

### Q3 — 데이마켓 지속성 → **판정 불가 (n=0)**

데이마켓(`session="day"`) T0 이벤트 **0건**. 세션 분포는 `regular` 45 / `after` 6 이며
`day`·`pre` 는 0이다. 최소 표본 n≥15 미달 → **판정 불가**, 보수 기본값 적용:
**데이마켓 이벤트는 Phase 2 진입 트리거에서 배제.**

원인 관찰(해석 아님): 백필 창은 후보일 중심 [D−25,D+2] 이고 데이마켓·프리마켓은 정규장보다
체결이 더 희박해, 그 세션에서 RVOL 게이트를 통과할 만큼의 봉이 남지 않았다.

### Q4 — 덤프 속도 → **추정치 사용 불가 → 보수 기본값 채택**

| 지표 | train+val |
|---|---|
| 분모 이벤트 n | 51 (§4.4 최소선 30 충족) |
| −20% 도달 | **8건 / reach_rate 0.157** (Wilson 95% 0.082~0.280) |
| 도달 소요 중앙값 | 22.0분 (부트 CI 6.0~46.0, **반폭 20.0분** ≤ 30분) |
| 우측 절단(미도달) | 43건 — 분포에서 분리 |
| 홀트 프록시 ≥1 비율 | **1.00** (전 이벤트) — 상한 추정으로만 |

CI 반폭 기준(≤30분)은 형식적으로 충족한다. 그러나 `reach_rate` 0.157 은 P4 예측 대역
[0.35,0.65] 과 Wilson CI 가 **분리**되며(상한 0.280 < 0.35), 이는 §4.2 감사가 지목한 표본
부적합의 직접 결과다(피크 수익률 중앙값 +1.1% 이면 −20% 붕괴가 나올 여지 자체가 작다).
따라서 추정치를 Phase 2 설계 입력으로 쓰지 않고 **§4.4 보수 기본값**(도달 표본 하위 사분위수
시간)을 채택한다 — 도달 8건의 하위 사분위수는 **6분**이다. 홀트 프록시가 100%인 것은 프록시
포화(무체결 공백과 구분 불가)이므로 **홀트 빈도 정보로 쓸 수 없다**.

### Q5 — 기대수익 → **무조건부만 산출, 조건부 탐색·검증 미수행 → Phase 2 착수 불가**

무조건부(비용 1.0% 차감 후):

| 구간 | 정책 | n | 평균 net | 중앙값 net | 부트 95% CI(평균) | 승률 |
|---|---|---:|---:|---:|---|---:|
| train *(선별용)* | `t0_to_30m` | 21 | −6.43% | −4.33% | [−9.91%, −3.25%] | 19.0% |
| train *(선별용)* | `t0_to_close` | 21 | −8.41% | −9.16% | [−11.75%, −5.07%] | 14.3% |
| train+val | `t0_to_30m` | 45 | **−7.56%** | −7.27% | **[−9.74%, −5.51%]** | 13.3% |
| train+val | `t0_to_close` | 51 | **−6.46%** | −5.85% | **[−8.91%, −4.23%]** | 19.6% |

**비용 분해(§2.6)**: 총 1.0% = 수수료 0.2%(US 0.1%/체결 × 2) + 환전 0.3% + 슬리피지 0.5%.
> 참고: 이 표본의 T0 봉 거래량 중앙값이 600주이므로 **슬리피지 0.5% 가정은 낙관적**이다.
> 실제 체결 가능성 자체가 의문이며, 이 수치는 "체결됐다면"의 상한이다.

**P5 예측(무조건부 음수)은 방향상 지지**되나, §4.2 감사 결과 이 표본이 문헌 모집단과 다르므로
**"예측 적중"으로 카운트하지 않는다.** 또한 `t0_to_30m` 은 n=45(6건은 T0 후 30분 구간에 봉이
없어 NaN)로, 그 자체가 희소 테이프의 증상이다.

- **조건부 규칙 탐색(§4.5-2)·검증(§4.5-3): 수행하지 않았다.** 표본이 부적합으로 판정된
  상태에서 규칙을 탐색하면 그 규칙은 아티팩트에 적합된다. 탐색을 시작하지 않았으므로
  검증 깔때기(≤5개)는 **아직 소모되지 않았다** — 표본 문제 해소 후 온전히 사용 가능하다.
- **§7-g 생존 편향 딱지**: 위 수치는 전부 **"생존 종목 조건부 상한"** 이다. 백필 유니버스는
  현재 시점 심볼 디렉토리 기반이라 상장폐지·심볼 변경 종목이 빠져 있고, 펌프 후 폐지된 최악
  결과가 표본에 없다.
- **Phase 2 착수 판정: 불가.** §4.5-4 홀드아웃 절차의 전제(검증 통과 규칙 1개)가 존재하지 않는다.

### Q6 — 시간대 효과 → **판정 불가 (버킷 최소 표본 미달)**

n=51 은 §4.6 최소선(n≥50)을 겨우 넘지만 **버킷별 최소 5건** 조건에서 해석 가능한 버킷이
제한된다. ±1분 감도(§7-f) 병기:

| 버킷 | n | 중앙 `peak_ret` | 중앙 `ret_close` | 경계 민감(±1분) |
|---|---:|---:|---:|---|
| open_0_15 | 6 | +0.02% | −3.03% | **예 (1건 이동)** |
| open_15_30 | 4 | +2.15% | −0.67% | 아니오 |
| mid_30_180 | 22 | +1.33% | −4.81% | 아니오 |
| late_180_330 | 10 | 0.00% | −4.20% | 아니오 |
| close_330_390 | 3 | +4.08% | +0.65% | 아니오 |
| after_hours | 6 | +4.54% | −14.05% | 아니오 |

`open_15_30`(n=4)·`close_330_390`(n=3)은 최소 5 미달로 **보고만 하고 해석 금지**.
`open_0_15` 는 ±1분 이동으로 소속이 바뀌는 이벤트가 있어 **판정 불가**로 다룬다(§7-f 규칙).
P6 예측("개장 15분에서 `peak_ret` 최대")은 **지지되지 않는다**(개장 15분 중앙 peak_ret 이
오히려 최저 수준). 다만 §1.7 감사 결과 표본 부적합이므로 이 역시 **기각으로 카운트하지 않는다.**

---

## 6. 표본 필터 결과 (§2.7, `scope` 컬럼 포함)

| reason | n | scope |
|---|---:|---|
| not_common_stock | 0 | event |
| not_active | 0 | event |
| meta_missing | 0 | event |
| t0_price_unavailable | 0 | event |
| price_out_of_range | 0 | event |
| **mcap_out_of_range** | **2** | event |
| half_day_length_sample | 0 | event |
| **r_uncomputable** | **24,742** | symbol_day |
| split_excluded | 0 | symbol_day |
| mcap_band_low_kept | 0 | mcap_band_sensitivity |
| **mcap_band_low_excluded** | **1** | mcap_band_sensitivity |
| mcap_band_high_kept | 0 | mcap_band_sensitivity |
| mcap_band_high_excluded | 0 | mcap_band_sensitivity |

**총 51건 중 49건 유지**(2건 시총 범위 밖). `scope` 가 다른 행은 **합산하면 안 된다** —
`r_uncomputable` 은 (심볼, 매매일) 단위이고 `mcap_band_*` 는 감도 보고 전용이다.
시총 하단 밴드($9M~$11M)에서 제외된 1건은 경계 근처 오분류 가능성을 보여주는 감도 수치이며,
**판정은 확정 경계 [$10M, $300M] 로만** 했다.

카탈로그 생성 단계의 제외(이벤트가 되지 못한 단위):

| 사유 | 건수 |
|---|---:|
| 평가된 (심볼, 매매일) 단위 | 4,849 |
| 직전 20매매일 중 데이터 보유 10일 미만 → 스킵 | **5,702** |
| 곡선 비어 있음(§2.2 min_days=10 미달) | 577 |
| 이벤트 생성 | 51 (전부 `rvol_gated=True`) |

---

## 7. Phase 2 착수 게이트 (§4.5) — 현 단계 판정

| 게이트 | 조건 | 현 상태 |
|---|---|---|
| **G1** | Q5 홀드아웃 승인 | **미충족** — 홀드아웃 미열람(정상). 그 전 단계인 조건부 규칙 검증이 시작되지 않았다 |
| **G2** | Q4 사용 가능 판정 또는 보수 기본값 명문화 | **충족** — 보수 기본값(하위 사분위수 6분) 채택을 §5 Q4 에 명문화 |
| **G3** | §7 오염 처리 전 항목 완료, 미해결 치명 0건 | **부분 충족** — (a)(b)(c)(d)(f)(g)(h) 완료. **(e) A5 혼입 검사는 순환 참조로 미실행**(§7-e 강등 문언에 따른 정당한 미실행이나 "완료"는 아님) |
| **G4** | 시행 레지스트리 완결·마감 + 홀드아웃 이전 커밋 해시 | **진행 중** — `docs/13` 생성·기록 완료, 마감(동결)은 홀드아웃 열람 직전 단계 |
| **G5** | W6 적대적 감사 완료 | **미실시** (본 리포트 대상 감사) |

---

## 8. 다음 단계의 전제조건 (홀드아웃 열람으로 가기 전에 반드시)

1. **표본 정의 문제 해소** — §2.7 에 **유동성/체결률 기준**을 추가해야 한다.
   예: "T0 매매일의 정규장 체결률(실제 봉/390) ≥ X" 또는 "직전 20일 ADV ≥ Y주".
   **이는 사전등록 개정이므로 W7 §9 절차(데이터 열람 후이므로 post hoc 딱지 + 새
   아웃오브샘플 재검정 조건)를 따라야 한다.** W3 가 단독으로 바꿀 수 없고, 바꿔서도 안 된다.
2. **1분봉 커버리지 확대** — 현재 심볼별 중앙값 6일, `partial` 창 3,782개. 보관 한계
   (`retention_exhausted` 1,355심볼)가 원인이라 **과거로는 더 못 간다**. 따라서 실질적
   해법은 (a) 실시간 수집을 계속해 앞으로의 표본을 쌓거나 (b) 유동성 기준을 올려 체결이
   촘촘한 종목군으로 모집단을 좁히는 것이다.
3. **Q2 라이브 구간 이벤트 재유도** — 랭킹 구간(2026-07-31~08-01)의 이벤트를 §7-a 준수
   방식으로 오프라인 재유도해야 Q2 를 답할 수 있다. 별도 태스크.
4. **검증 깔때기 미소모 확인** — 조건부 규칙 탐색을 시작하지 않았으므로 §5.3 의 검증 5개
   슬롯은 **온전히 남아 있다**. 표본 문제 해소 후 사용한다.
5. **홀드아웃은 여전히 봉인** — 본 태스크에서 열지 않았고, 열 이유도 없었다(열 전제인
   "검증 통과 규칙 1개"가 존재하지 않는다).

---

## 9. 한계·편향 정직 기재

1. **표본 정의 불일치(최대 한계)** — §4 전체. 우리 이벤트는 문헌의 러너와 다른 현상일 수 있다.
2. **생존 편향(§7-g)** — Q5 수치는 생존 종목 조건부 상한. 폐지 종목이 표본에 없다.
3. **슬리피지 가정 낙관** — T0 봉 거래량 중앙값 600주에서 0.5% 슬리피지는 낙관적이며,
   체결 가능성 자체가 검증되지 않았다.
4. **홀트 프록시 포화** — `halt_gap_count` ≥1 이 100%. 무체결·홀트·수집중단을 구분하지
   못하므로 홀트 정보로 사용 불가(상한 추정 전용).
5. **A5 혼입 검사 미실행** — 순환 참조(§3). 대체 안전망 2종은 음성이나 결정적 증거가 아니다.
6. **첫 매매일 분할 미탐지** — 비교 대상이 없어 원리상 불가(docs/07 §7 편향 23).
7. **보수 확대의 위양성** — `n_widened` 339. 결측 구간마다 분할일이 넓게 등록돼 그만큼
   당일 조건이 제외됐다(§7-e 승인된 트레이드오프).
8. **Q1 리드타임의 해석 불가** — 형식 조건은 충족하나 감사 결과 신뢰할 수 없다(§5 Q1).
9. **train/val 분리의 실효성 제한** — 이벤트가 51건뿐이라 구간별 비교의 검정력이 거의 없다.
