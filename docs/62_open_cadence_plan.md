# 62. 정규장 재측정 발사 계획 — **쏘기 전에, 이 팔로는 못 가른다는 것부터**

> **[현행]** 소유 W1 · 2026-08-13 · **계획서다. 발사 안 했다. 라이브 API 호출 0 건.**
> ★ **`--cadence-profile open` 을 그대로 쏘면 이 측정의 목적(분해)을 달성하지 못한다** — §2
> 상태 표기의 뜻과 전수 목록: [`docs/INDEX.md`](INDEX.md)

작성: W1, 2026-08-13
질문: `docs/35` §6 의 정규장 재측정을 **지금 그대로 쏠 수 있는가**, 그리고 **쏘면 무엇이
갈리는가.**

사용자 결정 (2026-08-13 저녁): **"보류 — 정규장 재측정을 먼저."**
폴 5 초 배포는 보류됐다. 근거였던 *"29% · 16.1 초가 우리 폴 주기 탓"* 이 원문과 다르다는
것이 잡혔기 때문이다 (`docs/60` §10, `docs/58` §1-1 정정).

> **이 문서는 판정하지 않는다.** *"쏠 가치가 있다/없다"* 는 사용자 결정이다.
> 여기에는 **계획과 숫자**, 그리고 **그 숫자가 안 나오는 경우**만 있다.

---

## 목차

| 절 | 무엇 |
|---|---|
| §0 | 측정 조건 |
| §1 | **지금도 발사되는가** — 코드로 확인 |
| §2 | ★ **표본** — 지금 팔로는 분해가 안 된다. CI 계산 |
| §3 | **5 초 팔** — 콜 0 으로 얻는 법이 있다 |
| §4 | 팔 구성 후보 넷과 콜 수 |
| §5 | **발사 계획서** (언제·몇 콜·수집기 영향·중단·되돌림·못 가르는 것) |
| §6 | **분석 계획 — 쏘기 전에 못박는다** |
| §7 | 코드 변경 2 곳 |

---

## 0. 측정 조건

| 항목 | 값 |
|---|---|
| 코드 | `feat/core-api` @ `origin/main` `7b91ff9` 병합 후 |
| **라이브 API 호출** | **0 건.** 드라이런은 `tools/mock_server.py`(로컬 127.0.0.1) 상대다 |
| **수집기** | **건드리지 않았다.** `config` 무수정. 로그는 읽기 전용 |
| DB | 이 문서는 DB 를 안 읽었다 |
| 도구 | `tools/cadence_power.py` **신설** (표본·CI 계산, 라이브 0) |
| 재현 | `python -m tools.cadence_power` |

---

## 1. 지금도 발사되는가 — **네 곳을 코드로 확인했다**

`docs/35` §6 은 *"코드 수정 없이 발사되게 준비돼 있다"* 고 적었다. 그 뒤로 리미터·
클라이언트·계상이 네 번 바뀌었다(`docs/45`·`46`·`52`·`56`, 그리고 방금 W4 의 서버 초 감사).
**그 문장이 지금도 참인지** 하나씩 봤다.

| 확인한 것 | 지금 상태 |
|---|---|
| **중단조건이 읽는 로그 줄** `budget: 429 on GROUP (count=N)` | **살아 있다.** `budget.py:586` 이 그대로 찍고, 라이브 로그에서 `CollectorWatch._EVT` 정규식이 **676 줄** 일치(최신 08-13 15:30) |
| **텔레메트리 줄 파싱** `_TEL` | **살아 있다.** **3,193 줄** 일치. 필드가 늘었지만(`counter_scope`·클램프·`srv`) `k=v` 파싱이라 안 깨진다 |
| **토큰 경로** | **안전하다.** mock 모드(`live=False`)는 `TokenManager.get()` 이 `MOCK_TOKEN` 을 즉시 돌려주고 **리스를 아예 안 잡는다**(`tokens.py:168`). `release()` 도 `_flock is None` 이라 no-op — **가동 중 수집기의 토큰을 못 죽인다** |
| **랭킹 그룹 매핑** | 그대로. 프로브의 `/api/v1/rankings` 는 `RANKING` 그룹이다 |

### 1-1. 그런데 **`planned_calls` 가 과소계상이었다**

출력의 `budget.planned_calls` 는 팔 밖 콜을 `+6` 으로 잡고 있었다. 실제로는 **9** 다:

| 팔 밖 콜 | 수 |
|---|---:|
| 단발 — `{TOP_GAINERS, TOP_LOSERS} × {realtime, 1d}` | 4 |
| 겹침 — 위 두 목록 + 거래량 realtime 한 번씩 | 3 |
| `get_stocks` — 세 목록 합집합 약 260 심볼 / `BATCH_MAX` 200 → 배치 | 2 |
| **합** | **9** |

**상한 여유를 읽는 필드가 여유를 실제보다 크게 보고하고 있었다.** 고쳤다 (§7).
`docs/35` §6 이 적은 **453** 도 정확히는 **454** 다.

### 1-2. 드라이런 — mock 상대로 `open` 프로파일을 **끝까지 돌렸다**

라이브가 아니라 `tools/mock_server.py`(127.0.0.1:8899, 픽스처 서빙) 상대다.
**목적은 숫자가 아니라 "코드가 끝까지 도는가"** 다 — mock 은 같은 응답을 돌려주므로
`rankedAt` 이 고정이고 주기 값은 의미가 없다.

```
profile           : open
arms_spec         : vol [1s 1.0s×300s, 12s 12.0s×300s]  gain [1s 1.0s×120s]
budget            : {'call_cap': 550, 'planned_calls': 451}   <- 과소계상 (§1-1)
calls_used        : 454      elapsed_s: 724.6
verdict / aborted : 확인됨 / None
stop_thresholds   : {'our_429': 1, 'collector_60s': 3, 'collector_480s': 5, 'call_cap': 550}
collector 429 during probe: 0
oneshot            : TOP_GAINERS_realtime 400 / TOP_GAINERS_1d 200(100행)
                     TOP_LOSERS_realtime 400 / TOP_LOSERS_1d 200(100행)
universe_overlap   : _symbols_resolved 300/300   (= get_stocks 배치 2 확인)
```

> ### **`open` 프로파일은 지금 코드에서 끝까지 돈다.** `verdict: 확인됨`, `aborted: null`.
> `docs/35` §6 의 *"코드 수정 없이 발사되게 준비돼 있다"* 는 **참이다.**

**그리고 이 실행이 §1-1 의 결함을 그대로 보여줬다** — `planned_calls: 451` 인데
`calls_used: 454`. 이 실행은 수정 **전** 코드였다(빨강). 수정 후 판은 §7-3.

> **드라이런에서도 지켰다**: `--state` 와 `--fixture-dir` 를 스크래치패드로 돌려
> 공용 토큰 상태파일과 `tests/fixtures/live/` 를 건드리지 않았다.
> 소요 **724.6 초** = 팔 300+300+120 + 팔 사이 5 초 × 2 + 단발·겹침.

---

## 2. ★ 표본 — **지금 팔 구성으로는 분해가 안 된다**

이 측정이 존재하는 이유는 하나다 (`docs/60` §10-2):

> 미수신 틱을 **(i) 서버·캐시 성분**(우리가 못 줄이는 것)과
> **(ii) 얼라이어싱 성분**(폴 주기를 줄이면 되찾는 것)으로 **가르는 것.**

### 2-1. 슬롯 수는 **폴 간격이 아니라 지속 시간**이 정한다

서버 격자가 10 초이므로 한 팔이 덮는 **격자 슬롯 = 지속시간 / 10초**다.
**간격을 반으로 줄이면 콜만 두 배가 되고 슬롯은 그대로다.**

`--cadence-profile open` 은 두 팔 다 **300 초**다 → **각각 30 슬롯**.

```
$ python -m tools.cadence_power
# the one observation we have
  docs/35 §5-1: 10/34 slots missed on the 1s arm, premarket
  point 29.4%   Wilson 95% CI [16.8%, 46.2%]   half-width +-14.7pp

arm         gap   dur_s   calls  slots            95% CI at p   +-pp  aliasing?
1s            1     300     300     30         [16.7%, 47.9%]   15.6  no (gap <= grid)
12s          12     300      25     30         [16.7%, 47.9%]   15.6  yes (gap > grid)
```

> ### 즉 **정규장 팔은 프리마켓(n=34)보다 오히려 좁지 않다** (30 슬롯 → ±15.6pp).
> 1 초 팔이 300 콜을 쓰는데 12 초 팔과 **같은 30 슬롯**을 산다. 슬롯당 **12 배** 비싸다.

### 2-2. 두 팔을 갈라낼 수 있는 최소 격차 — **약 25pp**

분해는 두 비율의 **차**를 보는 일이다. 각각 ±15.6pp 인 구간 둘로는:

| 참 격차 | 95% 반폭 | 0 과 갈리나 |
|---:|---:|---|
| 5pp | 23.6pp | **아니오** |
| 10pp | 23.9pp | **아니오** |
| 15pp | 24.1pp | **아니오** |
| 20pp | 24.2pp | **아니오** |
| 25pp | 24.2pp | 예 |

**얼라이어싱 성분이 20pp 여도 못 갈라낸다.** 참고로 5 초 배포가 되찾을 수 있는 최대치가
곧 이 성분이고, 그것이 20pp 면 매우 큰 값이다 — **큰 효과조차 이 표본에서는 안 보인다.**

### 2-3. 좁히는 것은 **지속 시간**이다

```
 target +-pp   slots   duration  calls @1s  calls @5s  calls @12s
          16      28       280s        280         56          23
          12      52       520s        520        104          43
          10      76       760s        760        152          63
           8     122      1220s       1220        244         101
           6     222      2220s       2220        444         185
           5     317      3170s       3170        634         264
```

**±10pp 를 1 초 팔로 사려면 760 콜**이다 — 상한 550 을 넘는다.
**같은 ±10pp 를 12 초 팔로 사면 63 콜**이다.

---

## 3. 5 초 팔 — **콜 0 으로 얻는 길이 있다**

내 앞 보고는 *"5 초 팔이 없어 보간이 된다 (+60 콜로 추가 가능)"* 이라고 적었다.
**더 나은 방법이 이미 프로브 안에 있었다.**

`probe_ranking_cadence` 는 1 초 팔의 응답열을 **솎아서**(`decimation`) 굵은 자로 다시
읽는다 (`CADENCE["strides"]`). 솎기는 **같은 응답열을 다시 읽는 것**이라:

| | 별도 5 초 팔 | **솎기 stride 5** |
|---|---|---|
| 콜 | +60 | **0** |
| 슬롯 | 그 팔의 지속시간 / 10 | **1 초 팔과 같은 슬롯** |
| 비교 방식 | 독립 두 표본 | **짝지어짐 (같은 슬롯)** |
| 시간대 교란 | **있다** — 팔이 순차 실행이라 20 분 뒤 시장은 다른 시장이다 | **없다** |
| 서버가 요청 속도에 반응하면 | 잡는다 | **못 잡는다** |

**둘은 대체재가 아니라 상보재다.** `docs/35` §3-2 가 정확히 이 이유로 장치를 둘 넣었다.

- **솎기 stride 5** → 1s·5s·12s 미수신율을 **같은 슬롯 위에서 짝지어** 비교. 분해의 본체.
- **진짜 5 초 팔** → 솎기와 값이 갈리면 *"서버가 우리 요청 속도에 반응한다"* 는 뜻
  (`docs/35` §5-4 가 프리마켓에서 **반응 없음**으로 관측한 것의 정규장 재확인).

**`strides` 에 5 를 넣었다** (§7). 기존 1·2·4·8·12 는 그대로 뒀다 — 빼면 프리마켓 표와
대조가 끊긴다.

> **짝지어진 비교는 독립 표본보다 세다.** 같은 슬롯에서 "1 초로는 봤는데 5 초로는 못 봤다"
> 를 직접 세므로, §2-2 의 ±24pp 는 짝지음에서 **적용되지 않는다**. 다만 그 검정력은
> 불일치 슬롯 수에 달렸고 그 수는 **쏘기 전에는 모른다** — §6-3 에 적었다.

---

## 4. 팔 구성 후보 넷 — 콜 수와 상한 여유

상한은 `CADENCE["call_cap"] = 550` (`open` 프로파일에 사전 고정). 팔 밖 콜 **9** 포함.

| 안 | 팔 구성 | 콜 | 상한 여유 | 슬롯 (1s/5s/12s) | 소요 |
|---|---|---:|---:|---|---:|
| **A. `open` 그대로** | 1s×300 · 12s×300 · gain 1s×120 | **454** | 96 | 30 / — / 30 | 약 12 분 |
| **B. A + 진짜 5 초 팔** | 위 + 5s×300 | **514** | 36 | 30 / 30 / 30 | 약 18 분 |
| **C. 지속시간 재배분** | 1s×300 · 5s×900 · 12s×600 (gain 제거) | **539** | 11 | 30 / 90 / 60 | 약 30 분 |
| **D. 싼 팔에 몰기** | 1s×180 · 5s×1200 · 12s×1200 (gain 제거) | **529** | 21 | 18 / 120 / 120 | 약 44 분 |

**독립 두 표본으로 5s vs 12s 를 갈라낼 수 있는 최소 격차:**

| 안 | 최소 격차 |
|---|---:|
| A | **약 25pp** (1s vs 12s) |
| B | **약 25pp** — 5 초 팔을 더해도 **안 좁아진다** |
| C | 약 20pp |
| D | **약 15pp** |

> **B 가 A 보다 60 콜을 더 쓰고도 CI 가 그대로**라는 것이 §2-1 의 요점이다.
> 5 초 팔의 값어치는 CI 가 아니라 **"서버가 요청 속도에 반응하는가" 의 대조군**에 있다
> (§3). 분해의 정밀도는 **솎기**와 **지속시간**에서 온다.

**어느 안을 고를지는 이 문서가 정하지 않는다.** C·D 는 `gain_arms` 를 버리는데,
그러면 `TOP_GAINERS` 의 세션 의존성(`docs/35` §5-7 1 번)은 이번에도 안 갈린다.

---

## 5. 발사 계획서

### 5-0. 소요 시간은 실측했다

mock 드라이런에서 `open` 프로파일이 **724.6 초**였다 (§1-2). 팔 지속시간 합
(300+300+120=720) + 팔 사이 5 초 × 2 + 단발·겹침이 거의 그대로 나온다. 즉 §4 표의
"소요" 는 **팔 지속시간 합 + 약 15 초**로 계산하면 맞는다.

### 5-1. 언제 — **개장 직후를 피하지 않는다. 다만 1 분 비킨다**

`docs/35` §6-1 이 이미 정한 것을 그대로 쓴다. 정규장 개장 **22:30 KST**, 착수 **22:31**.

- **알고 싶은 것이 바로 그 순간이다.** 10 초 격자가 개장 폭주에도 10 초면 **고정
  스케줄**이라는 강한 증거다.
- **개장 버스트는 우리 그룹이 아니다.** 승격 폭주가 먹는 것은 `MARKET_DATA`·
  `MARKET_DATA_CHART` 이고 프로브는 `RANKING` 이다. 서버 창은 그룹마다 독립이다
  (`docs/06` §9-4).
- **1 분 비키는 이유**: 22:30:00 에 수집기가 세션 상태를 넘기며 창을 다시 잡는다.

> **`docs/33` 의 개장 붕괴가 재현되는가 — 아니다.** 그 사고는 *"남의 그룹 송신이 CHART 로
> 잘못 계상돼"* 정원이 날아간 것이고(`docs/33` §2-1), 그 경로는 `docs/46`·`52` 에서
> 없어졌다. 그리고 **`RANKING` 은 축소 대상이 아니다** — 아래 §5-3.

**다만 C·D 안은 30~44 분짜리라 개장 창을 벗어난다.** 팔이 **순차** 실행이므로
마지막 팔은 개장 40 분 뒤 시장을 본다. 그 자체가 §6-4 의 한계다.

### 5-2. 몇 콜 — §4 표. **상한 550, 계획 454(A)~539(C)**

중단조건 콜 상한은 프로브 안에 박혀 있고 초과하면 **그 자리에서 멈춘다**.

### 5-3. 수집기에 무슨 일이 생기나

**같은 계정·같은 자격증명이다. 서버 버킷을 공유한다.** 그룹별로 본다.

| 그룹 | 수집기 실측(정규장, `docs/60` §3-1) | 프로브가 더하는 것 | 한 초 최악 합산 | 천장 |
|---|---:|---:|---:|---:|
| **`RANKING`** | 평균 0.25 / **첨두 3** | 1 초 팔에서 **1.0** | **4** | 5 |
| `MARKET_DATA` | 평균 6.07 / 첨두 10 | **0** | 10 | 10 |
| `MARKET_DATA_CHART` | 평균 1.85 / 첨두 5 | **0** | 5 | 5 |
| `STOCK` | 평균 0.00 | 겹침 구간 **2 콜** | 2 | 5 |

- **`RANKING` 한 초 최악 4/5.** 여유 1 이다. `docs/35` §4 가 적은 *"3/5, 여유 2"* 는
  수집기 첨두를 2 로 본 값이고, **지금 수집기 `RANKING` 첨두는 3 이다**(08-12 수정 이후
  실측). **여유가 하나 줄었다** — 이 계획의 유일한 빡빡한 자리다.
- **`MARKET_DATA`·`MARKET_DATA_CHART` 는 프로브가 한 건도 안 쓴다.**

#### **tier2 호가가 또 굶는가 — 예산 경로로는 아니다. 코드로 확인했다**

D-9 의 전례는 `MARKET_DATA` 예산이 tier3 에 먹혀 tier2 호가가 정규장 내내 1.2% 만
수집된 것이다. 프로브가 그 경로를 다시 열 수 있는지 셋을 확인했다:

1. **프로브는 `MARKET_DATA` 를 안 쓴다** (위 표). 예산 경쟁 자체가 없다.
2. **`RANKING` 429 는 정원을 안 깎는다.** `SHRINK_TIER = {MARKET_DATA: 3, CHART: 2}`
   (`budget.py:31`) 이고 `apply_budget` 은 `if group not in SHRINK_TIER: ... continue`
   로 빠진다 (`budget.py:853`). 랭킹 429 는 **경보만** 낸다.
3. **`RANKING` 429 가 tier 회복을 막지도 않는다.** `should_grow` 는
   `for group in (GROUP_MARKET_DATA, GROUP_CHART)` 이고 `_last_429_s` 를 **그룹별로**
   본다 (`budget.py:898-901`).

> **그래서 tier2 굶주림은 이 프로브의 위험 목록에 없다.** 다만 §5-6 의 `foreign` 오염은
> 별개이고 그쪽이 진짜 위험이다.

### 5-4. 중단 조건 — `docs/35` §4-1 이 **지금도 유효하다.** 배경은 실측했다

| 순위 | 조건 | 임계 | 지금 상태 |
|---|---|---|---|
| 1 차 | **우리 프로브의 429** (`client.counters["http_429"]` 증가) | **1 건** | 유효 |
| 2 차 | 컬렉터 배경 429, 60 초 창 | 실측 배경 최대 + 1 (하한 3) | **유효** — `_EVT` 정규식이 라이브 로그에서 **676 줄** 일치 |
| 2 차 | 컬렉터 배경 429, 480 초 누적 | 실측 배경 최대 + 3 (하한 5) | 유효 |
| 3 차 | 콜 상한 | 550 | 유효 (§1-1 에서 계상만 고쳤다) |
| 3 차 | `data/PROBE_STOP` 파일 | 존재 | 유효 |

**배경 429 를 로그 전수로 셌다** (2026-07-31 14:30 ~ 08-13 15:30, 676 건):

| 그룹 | 건수 |
|---|---:|
| `MARKET_DATA_CHART` | 651 |
| `MARKET_DATA` | 21 |
| **`RANKING`** | **4** |

> **우리가 실을 그룹은 14 일 동안 429 를 4 번 맞았다.** 프로브가 미는 자리는 배경이
> 거의 비어 있다.

**임계가 어떻게 잡히나** — 오늘 22:31 발사를 가정하고 `--baseline-min` 별로 계산:

| baseline 창 | 배경 n | max/60s | **thr60** | max/480s | **thr480** |
|---|---:|---:|---:|---:|---:|
| **180 분 (기본값)** | 0 | 0 | **3** | 0 | **5** |
| 720 분 | 52 | 6 | 7 | 6 | 9 |
| 1440 분 | 69 | 8 | 9 | 9 | 12 |

**그리고 직전 정규장(08-12 22:30~08-13 05:00)의 실제 배경은 `n=2`, max/60s = 1 이다**
(둘 다 CHART). 즉 **기본 180 분 창이 주는 하한 임계 3/5 로도 정규장 배경보다 위**다 —
조기 종료 위험은 낮다.

> **거꾸로가 위험하다.** 배경이 몰리는 시간대는 정규장이 아니라 **낮 세션**이다
> (오늘 10:30 에 60 초 안에 **8 건**). 낮에 쏘면 하한 임계 3 이 배경만으로 깨진다.
> **22:31 발사에는 기본값이 맞고, 다른 시간대면 `--baseline-min` 을 늘려야 한다.**
> 어느 쪽이든 **발사 직후 출력의 `collector_baseline_429` 와 `stop_thresholds` 를
> 눈으로 확인할 것** — 그 둘이 이 표와 다르면 배경이 바뀐 것이다.

### 5-5. 되돌릴 수 있나

| | |
|---|---|
| **되돌릴 수 있다** | 프로브는 **읽기만** 한다. DB 에 안 쓰고, 수집기·config 를 안 건드리고, 토큰을 재발급하지 않는다(`--reuse-token-state`) |
| **못 되돌리는 것 1** | **그 시각의 정규장은 한 번뿐이다.** 실패하면 다음 거래일이다 |
| **못 되돌리는 것 2** | **`foreign` 관측이 오염된다** — §5-6 |
| **못 되돌리는 것 3** | 프로브가 쓴 API 호출 자체 (한도 소비). 되돌아오지 않는다 |

### 5-6. ★ 발사 전에 코디네이터가 확인할 것 — **W4 의 `foreign` 측정과 충돌한다**

W4 가 방금 병합한 `budget.describe()` 는 `foreign` 의 세 후보를 **그룹 간 비대칭**으로
가른다고 적었다 (`budget.py:998-1013`). 그 셋 중 하나가 이것이다:

> *"**같은 자격증명의 다른 발신자** — 그의 송신은 우리 그룹 구성과 무관하므로 비대칭이
> 없다."*

**프로브가 정확히 그 "다른 발신자" 다.** 정규장에 `RANKING` 으로 1 req/s 를 쏘면
수집기의 `RANKING` `frn`·`frnmax` 가 오른다. 그리고 `RANKING` 은 W4 가 **판별자로
쓰고 있는 바로 그 그룹**이다 (`COORDINATOR-STATE` §1-1 의 (B)/(C) 판정).

두 가지로 쓸 수 있다. **어느 쪽인지는 발사 전에 정해야 한다:**

| | |
|---|---|
| **(가) 겹치지 않게 한다** | W4 의 `foreign` 관측 창과 프로브 창을 분리한다. 가장 안전하다 |
| **(나) 교정에 쓴다** | 프로브는 **송신 수를 정확히 아는** 외부 발신자다. 그 창의 수집기 `frn` 이 프로브 송신 수와 맞으면 **`foreign` 탐지기가 교정된다** — 지금까지 못 한 것이다. 단 **사전에 못박아야** 하고, 그 창의 `foreign` 은 (B)/(C) 판정에 **못 쓴다** |

**이 문서는 고르지 않는다. 코디네이터·W4 의 결정이다.**

### 5-7. 무엇을 **못 가르나** — 이 측정으로도

1. **`docs/35` §5-7 2 번은 안 닫힌다.** *"서버가 그 틱을 안 만들었나 / 우리 캐시 노드에
   안 실렸나"* 는 **단일 클라이언트로는 원리상 못 가른다.** 이 프로브는 (i)+(ii) 를
   가를 뿐, (i) **안**을 못 가른다.
2. **한 세션이다.** 정규장 하루가 정규장 일반이라는 보장이 없다 — 프리마켓 한 세션으로
   일반화했던 것이 이번 정정의 원인이었다.
3. **팔이 순차라 시간대가 섞인다** (§4 C·D 에서 30~44 분). 솎기는 이 교란이 없지만
   진짜 팔끼리의 비교에는 있다.
4. **`rankedAt` 은 여전히 DB 에 안 들어간다** (`docs/35` §2-1). 이 측정은 프로브
   메모리·JSON 에만 남고 수집기는 계속 버린다. **재현하려면 매번 다시 쏴야 한다.**
5. **16.1 초 지연은 이 측정으로 안 줄어든다.** 재확인은 되지만 (b) 통과선 문제는 그대로다
   (`docs/60` §10-3).

---

## 6. 분석 계획 — **쏘기 전에 못박는다**

> **사후에 기준을 만들지 않는다.** 아래 표와 문턱은 **발사 전** 판이다.

### 6-1. 미수신율은 이렇게 센다 (정의 고정)

`docs/35` §5-1 이 쓴 것과 **같은 식**이다:

```
관측 슬롯 수 = 서버 스탬프 델타의 합 / 10초        (= 델타가 덮은 격자 구간 수)
받은 슬롯 수 = 델타 개수
미수신율     = 1 - 받은/관측
```

프로브 JSON 의 `…rankedAt.server_stamp_delta_hist` 에서 그대로 나온다.
**역행 델타(음수)는 프리마켓에서 1 건 있었다** — 부호를 그대로 더해 슬롯 수를 센다
(§5-1 이 `15×(+1) + 10×(+2) + 1×(−1) = 34` 로 한 것과 동일).

### 6-2. 낼 표 — 이 셋만

**표 1. 같은 응답열 위의 미수신율 (짝지어짐, 콜 0 추가)**

| stride | 유효 주기 | 관측 슬롯 | 받은 슬롯 | 미수신율 | Wilson 95% CI |
|---|---|---|---|---|---|
| 1 | 1 s | | | | |
| 5 | 5 s | | | | |
| 12 | 12 s | | | | |

**표 2. 솎기 vs 진짜 팔** — 서버가 요청 속도에 반응하는가

| 유효 주기 | 솎기 미수신율 | 진짜 팔 미수신율 | 차 |
|---|---|---|---|

**표 3. 프리마켓 대조** — `docs/35` §5-1·§5-2 와 같은 칸

| | 프리마켓 (2026-08-07) | 정규장 (이번) |
|---|---|---|
| 격자 간격 | 10 초 정수배만 | |
| 미수신율 (1 초 팔) | 29.4% (10/34) | |
| `sent − rankedAt` p50 | 16.1 초 | |
| `order` 변화 /분 | 4.51 | |

### 6-3. 어떤 값이면 무엇인가 — **문턱을 미리 적는다**

| 관측 | 읽는 법 |
|---|---|
| **stride 1 과 stride 12 의 미수신율이 CI 안에서 겹친다** | **얼라이어싱 성분을 못 봤다.** "없다" 가 아니라 **"이 표본으로는 못 잰다"** 다. §2-2 의 최소 격차를 함께 적는다 |
| **stride 12 − stride 5 ≥ 최소 격차** | 5 초로 내리면 그만큼 되찾는다는 **직접 관측**. `docs/60` §10-3 의 3 번 항목이 미측정에서 측정으로 바뀐다 |
| **stride 1 의 미수신율이 0 에 가깝다** | 프리마켓 29.4% 가 **세션 의존**이었다는 뜻. `docs/35` §5-1 의 해석을 정규장에 못 쓴다 |
| **stride 1 의 미수신율이 29% 근처** | 서버·캐시 바닥이 실재한다는 재확인. **5 초로도 그만큼은 남는다** |
| **솎기와 진짜 팔이 갈린다** | 서버가 **요청 속도에 반응**한다 → 솎기로 한 모든 추론(프리마켓 표 포함)을 다시 봐야 한다 |
| **`sent − rankedAt` p50 이 10 초 아래** | G-1a(b) 가 **세션 의존**이었다는 뜻 |
| **`rankedAt` 초 자리가 여섯 개가 아니다** | 10 초 격자 자체가 정규장에서 다르다 → 위 전부의 분모가 바뀐다 |

**짝지어진 검정**: 표 1 은 같은 슬롯이므로 stride 간 비교는 **불일치 슬롯**(한쪽만 받은
슬롯)으로 센다(McNemar). **불일치 수가 10 미만이면 정확검정을 쓰고, 그래도 CI 가
0 을 걸치면 "못 갈랐다" 로 적는다.**

### 6-4. **미리 적는 실패 조건**

- 중단조건이 발동하면 **부분 데이터로 위 표를 만들지 않는다.** 팔이 잘린 채 비교하면
  슬롯 수가 달라져 §2 의 계산이 통째로 무효다. `aborted` 필드가 비어 있을 때만 §6-2 를 낸다.
- **CI 를 안 적은 미수신율은 이 문서 기준 미완성이다.** 29.4% 가 CI 없이 인용돼 온 것이
  이번 정정의 원인이다.
- 결과가 무엇이든 **폴 주기 결정을 이 문서가 내리지 않는다.**

---

## 7. 코드 변경 — **2 곳, 최소 diff**

| 파일 | 무엇 | 왜 |
|---|---|---|
| `tools/live_probe.py` | `CADENCE["strides"]` 에 **5 추가** (1·2·4·**5**·8·12) | 폴 주기 후보를 **콜 0** 으로, 같은 슬롯 위에서 읽는다 (§3). 기존 값은 안 뺐다 |
| `tools/live_probe.py` | `planned_calls` 의 `+6` → `CADENCE_FIXED_CALLS = 9` | 상한 여유를 **과대보고**하고 있었다 (§1-1) |
| `tools/cadence_power.py` | **신설** | 슬롯·Wilson CI·분해 가능성 계산. 라이브 0 |
| `tests/test_cadence_plan.py` | **신설 13 건** | 위 셋 + *"슬롯은 지속시간이 정한다"* 를 못박는다 |

**팔 구성(`vol_arms`·`gain_arms`)과 상한은 하나도 안 바꿨다** — §4 의 안들은 **제안**이고
고르는 것은 사용자·코디네이터다.

### 7-1. 회귀

```
$ .venv/Scripts/python.exe -m pytest -q
2126 passed, 1 skipped, 4 deselected, 23 warnings in 351.89s (0:05:51)
```

직전 main 기준 **2,113** 에서 **+13** — 전부 `tests/test_cadence_plan.py` 신규다.
`tossmon/**` 는 한 줄도 안 건드렸다.

### 7-2. 수정 전 → 수정 후 (같은 드라이런, mock, 라이브 0)

**수정 전** (§1-2 의 실행, `open` 프로파일):

```
budget            : {'call_cap': 550, 'planned_calls': 451}
calls_used        : 454                      <- 3 건 과소계상
decimated strides : stride_1, stride_2, stride_4, stride_8, stride_12
                                             <- 5 초를 같은 응답열에서 못 읽는다
```

**수정 후** (같은 명령 재실행):

```
budget            : {'call_cap': 550, 'planned_calls': 454}
calls_used        : 454      elapsed_s: 724.9
verdict / aborted : 확인됨 / None
planned == used?  : True
decimated strides : stride_1, stride_2, stride_4, stride_5, stride_8, stride_12
stride_1 polls    : 300 | stride_5 polls: 60 | stride_12 polls: 25
```

> **`stride_5` 가 300 폴을 60 으로 솎는다** — 진짜 5 초 팔(60 콜)이 낼 폴 수와 **같다.**
> `stride_12` 는 25 로, `open` 의 진짜 12 초 팔 콜 수와 **같다.**
> **콜을 하나도 더 안 쓰고 같은 슬롯 위에서 얻는다** (§3).

### 7-3. 안 한 것

- **발사하지 않았다.** 라이브 API 호출 **0 건**. 위 드라이런 두 번은 전부
  `http://127.0.0.1:8899` (로컬 mock) 상대다.
- **팔 구성·상한을 안 바꿨다.** §4 는 제안이고 선택은 사용자·코디네이터다.
- **`docs/35` 본문 수치 무수정.** 이 문서가 §6 을 인용만 한다.
- **`docs/58`·`coordination/**` 안 건드렸다** (코디네이터 소유).
- **판정 안 했다.**
