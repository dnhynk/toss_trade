# W5 — supervisor 기동 경로가 수집기를 먹통으로 만든다 (2026-08-19 장애)

> 배정: 코디네이터 2026-08-19 22:2x KST · 워크트리 `w5-ops` / 브랜치 `feat/ops`
> **오늘 밤은 측정 창이다. 배포 금지.** 다음 휴장 창에 고친다.
> 이 파일이 명세다. 코디네이터 워크트리에 있으므로 **읽기만** 해라.

## 0. 무엇이 일어났나 — 실측

```
20:48  랭킹 스냅 멈춤
21:19  로그 자체가 멎음. 텔레메트리 세 그룹 첨두 전부 0 (전역 송신 정지)
21:23  워치독 RESTART outcome=FAILED
       그 뒤 워치독이 매 주기 10분(ExecutionTimeLimit=PT10M) 행 -> Result=267014 강제 종료
       MultipleInstances=IgnoreNew 라 다음 트리거도 무시됨
22:08  워치독 RESTART outcome=OK sup=1 col=1
       -> 프로세스 넷은 떴는데 stdout 0 바이트, DB 쓰기 0 (먹통)
22:17  코디네이터가 supervisor 를 건너뛰고 수집기를 직접 앞단에서 띄움 -> 18초 만에 정상
22:23  사용자가 같은 방식으로 띄움 -> 수집 재개 (개장 6.5분 전)
```

## 1. ★ 갈린 것 — **수집기 코드는 멀쩡하다. 기동 경로가 문제다**

같은 워크트리·같은 config·같은 venv 로:

| 경로 | 결과 |
|---|---|
| `python -m tossmon.collector --config config/config.yaml` (직접) | **18 초 만에 정상.** `resumed` -> `collector start` -> `session closed->pre` -> 텔레메트리 -> 테이프 폴 |
| `ops/launch_collector.cmd` -> `python -m ops.supervisor` -> 수집기 | **프로세스는 살아 있는데 stdout 0 바이트, DB 쓰기 0.** 재현됨 (21:28, 22:08 두 번) |

**즉 `ops.supervisor` 가 자식을 띄우는 방식에 문제가 있다.**

## 2. 가설 — **반증할 관측을 먼저 정해라**

가장 유력: **자식 stdout/stderr 파이프가 안 빠져 자식이 write 에서 블록된다.**

정황:
- `collector.stdout.log` 가 **204 MB** 이고 계속 자란다
- `detector.py:787` 의 `RuntimeWarning` 이 **초당 여러 줄** 쏟아진다 (앞단 실행에서 눈으로 확인)
- Windows 파이프 버퍼는 기본 약 64 KB — 안 빼내면 자식이 즉시 막힌다
- 증상이 정확히 그 모양이다: **프로세스 생존 + 출력 0 + 진행 0**

**하지만 확정하지 마라.** `ops/supervisor.py` 가 자식을 어떻게 부르는지
(`stdout=PIPE` 인가 파일 핸들인가, 드레인 스레드가 있나)를 **먼저 읽고**,
가설을 반증할 관측을 진술한 뒤에 고쳐라.

**다른 후보도 열어 둬라**: 콘솔/핸들 상속, `creationflags`, cp949 인코딩 예외,
로그 핸들러 충돌, 파일 잠금.

## 2-1. ★ 파이프 가설은 **죽었다** (코디네이터, 2026-08-20 01:1x — 코드 정독)

**§2 가 "가장 유력" 이라고 적은 파이프 교착은 성립하지 않는다.** `ops/supervisor.py:79-88`:

```python
log_fh = open(self.log_path, "ab", buffering=0)
return subprocess.Popen(
    self.cfg.collector_cmd,
    stdout=log_fh or subprocess.DEVNULL,
    stderr=subprocess.STDOUT if log_fh else subprocess.DEVNULL,
)
```

**자식 출력이 파이프가 아니라 파일 핸들로 간다.** 채울 파이프가 없으므로 자식이
write 에서 막힐 수 없다. **§2 를 쫓지 마라.**

### 대신 정독에서 나온 것 둘 — **둘 다 아직 가설이다**

1. **`log_fh` 를 열고 안 닫는다.** `_spawn` 이 매 기동마다 새로 열고 어디서도 닫지 않는다.
   재기동이 쌓이면 핸들이 샌다. **그 자체로 먹통을 설명하지는 못한다** — 다만 대상 파일이
   **204.5 MB** 이고, `"ab"` + `buffering=0` 으로 연다
2. **환경·cwd 는 용의자가 아니다.** `launch_collector.cmd` 가 `TOSS_BASE_URL`·`TOSS_LIVE=1`
   을 set 하고 `cd /d "%~dp0.."` 하며, 코디네이터의 앞단 실행도 같은 값으로 했는데
   **그쪽은 18 초 만에 정상**이었다. `collector_cmd` 도 절대경로 venv 로 올바르다(실측)

### 관측 하나 더 — 먹통일 때 **stdout 에도 한 줄이 없었다**

`collector.stdout.log` 의 마지막 쓰기가 **2026-08-19 21:19** 이고, 22:08 재기동으로 뜬
프로세스 넷은 **그 파일에도 아무것도 안 썼다.** 즉 자식이 **로깅 설정 전에** 멈춘다.

> **그러니 가르는 관측은 하나다: 휴장 창에서 `launch_collector.cmd` 로 실제로 띄워
> 어디까지 가는지 보는 것.** 코디네이터는 라이브라 그것을 못 했다. 그게 네 게이트 3 이다.

## 3. 함께 볼 것 둘 (같은 사고의 다른 얼굴)

1. **워치독이 자기 restart 에서 10 분을 멈춘다.** `ExecutionTimeLimit=PT10M` +
   `MultipleInstances=IgnoreNew` 조합이라 **한 번 막히면 그 뒤 트리거가 전부 무시된다.**
   워치독이 launcher 를 **동기로 기다리는지** 확인하고, 기다린다면 분리해라.
   **자가 복구가 이것 때문에 통째로 멈췄다.**
2. **`RuntimeWarning` 홍수.** `detector.py:787` 이 `calendar=` 미전달로 매 호출 경고를 낸다.
   파이프를 채우는 연료다. **경고를 끄지 말고 원인(캘린더 전달)을 고쳐라** — 끄면 다음에
   같은 함정을 못 본다. 소유가 W3/W4 면 **고치지 말고 보고**해라.

## 4. 하지 말 것

- **오늘 밤 배포 금지.** 측정 창이고, 지금 수집기는 **사용자 콘솔에서 직접** 돌고 있다.
  **그 프로세스를 건드리지 마라**
- 예약작업 속성 변경은 **관리자 권한**이 필요하다 — 필요하면 **명령만 적어서 보고**해라
  (코디네이터가 사용자에게 넘긴다)
- 경보 문턱을 올려 조용하게 만들지 마라

## 5. 게이트

1. `pytest -q` 초록
2. **가설을 반증할 관측을 실제로 수행한 출력**
3. 고친 뒤 **`launch_collector.cmd` 경로로 기동해 로그가 붙는 것**을 보여라 (휴장 창에서)
4. `git diff --stat origin/main...HEAD` — `ops/**` 만

## 6. 규율

- 원인을 설명하지 못하는 수정 금지. 특수 케이스 분기로 증상만 없애지 마라
- 두 번 연속 실패하면 멈추고 *"배제된 것 / 남은 가설"* 을 보고
- 콘솔 출력 ASCII 전용

---

## 2-2. ★★ 2026-08-20 아침 실측 — **§2-1 의 "로깅 설정 전에 멈춘다" 도 틀렸다**

**어젯밤 그 프로세스들이 지금도 살아 있다.** 죽지 않았고, 영구히 멈춘 것도 아니었다.
아래는 전부 코디네이터가 09:3x~09:5x 에 직접 잰 것이다.

### (가) 두 사슬이 동시에 떠 있다 (`Get-CimInstance Win32_Process`)

```
사슬 A (스케줄러 경로 - 문제의 그것)
  46064 cmd.exe      2026-08-19 22:10:57   ppid=2156
  41076 python.exe   2026-08-19 22:11:12   <- ops.supervisor
  50476 python.exe   2026-08-19 22:11:15
  41104 python.exe   2026-08-19 22:11:20
  49284 python.exe   2026-08-19 22:11:20

사슬 B (사용자 콘솔 - 지금 실제로 수집하는 것)
  26096 powershell   2026-08-19 22:20:22
  28212 python.exe   2026-08-19 22:21:06
  50724 python.exe   2026-08-19 22:21:06   <- 토큰 리스 보유자
```

### (나) ★ 사슬 A 는 **3 시간 24 분 뒤에 정상 기동했다**

`collector.log` 원문 (내가 grep 했다):

```
2026-08-19 21:19:35  마지막 telemetry (죽기 전 프로세스)
      ... 로그에 사슬 A 의 줄이 단 하나도 없다 ...
2026-08-19 22:17:26  resumed / collector start   <- 코디네이터의 앞단 실행
2026-08-19 22:21:17  resumed / collector start   <- 사용자 콘솔 (사슬 B)
      ... 사슬 A 여전히 침묵 ...
2026-08-20 01:35:41  resumed from collector_state.json (saved_ms=1787157317280 = 01:35:17)
2026-08-20 01:36:03  collector start / COLLECTION-CONFIG start      <- ★ 사슬 A 다
2026-08-20 01:36:06  AUTH-FAILURE (token lease already held ... pid 50724)
      ... 그 뒤 약 10 초마다 반복. 09:32 까지 2,482 줄 ...
```

**프로세스 생성 22:11:20 → 첫 로그 줄 01:35:41 = 3 시간 24 분.**
새 프로세스가 뜬 것이 아니다(위 생성시각 표를 보라 — 재기동이 없다).

> **그러므로 §2-1 의 관측 *"자식이 로깅 설정 전에 멈춘다"* 는 살아 있지만, 그것을
> *"영구히 죽는다"* 로 읽으면 안 된다. 막히는 것에는 끝이 있고 그 길이가 3.4 시간이다.**
> 무엇이 3.4 시간 뒤에 풀렸는지가 이 결함의 정체다. 재기동 트리거가 없었으므로
> **자식 안에서 무언가가 그만큼 기다렸다**는 뜻이다.

### (다) 지금 사슬 A 가 못 도는 이유는 **다른 것**이다 — 토큰 리스

`tossmon/api/tokens.py:143-146`:

```python
def lease_path_for_client(client_id: str) -> Path:
    """client_id -> 리스 파일 경로. 같은 자격증명이면 어디서 실행하든 같은 파일."""
    digest = hashlib.sha256(client_id.encode("utf-8")).hexdigest()[:16]
    return lease_dir() / f"token-{digest}.lease"
```

**리스 키는 자격증명 하나로만 정해진다.** `state_path` 도 `cwd` 도 config 도 안 들어간다.
`_acquire_lease` 는 `timeout=0` 이라 **기다리지 않고 즉시 RuntimeError** 를 낸다.

→ **사용자 콘솔 수집기(pid 50724)가 살아 있는 한, 라이브 모드로 두 번째 수집기를
띄우는 것은 원리상 불가능하다.** 이것이 네 게이트 3 을 직접 때린다 (아래 §5-1).

### (라) 워치독은 이 상태를 **정상으로 읽는다**

`watchdog.log` 09:30:53: `sup=1 col=2 ... rank_age=1 fetch_pct=100`.
**`col=2` 인데 아무 경보도 없다.** 워치독은 프로세스를 세지 *수집하는* 프로세스를
세지 않는다. 08-19 §9-4 의 *"세 층이 전부 '프로세스가 있다' 를 성공으로 읽는다"* 가
지금 이 순간에도 참이다. **이것도 같은 사고의 얼굴이다 — §3 에 넣어 함께 봐라.**

---

## 5-1. ★ 게이트 3 을 고쳐 적는다 — **라이브로는 못 한다**

옛 게이트 3(*"고친 뒤 `launch_collector.cmd` 경로로 기동해 로그가 붙는 것을 보여라"*)은
§2-2 (다) 때문에 **라이브 모드로는 수행 불가능하다.** 대신 이렇게 한다:

1. **비라이브 재현이 먼저다.** `launch_collector.cmd` 경로를 **`TOSS_LIVE=0` + 임시
   scratch DB + 임시 `state_path`** 로 띄워라 (라이브 워크트리의 `data/` 를 쓰지 마라).
   - **재현되면** — 그 자리에서 마음껏 붙잡고 고칠 수 있다. 그게 최선이다
   - **재현 안 되면** — 그 사실 자체가 정보다. *"라이브 모드에서만 난다"* 를
     적고 **거기서 멈춰라.** 라이브로 재현하려 들지 마라
2. 고친 뒤 **같은 비라이브 구성**에서 자식이 **초 단위 안에** 로그를 쓰는 것을 보여라.
3. **라이브 전환(사용자 콘솔 → 스케줄러 경로)은 네가 하지 마라.** 코디네이터가
   사용자 큐(**D-29**)에 올렸다. 절차와 되돌리기가 거기 적힌다.

### 왜 라이브 전환을 워커에게 안 맡기나

08-19 밤에 **코디네이터가 정확히 그것을 하다 17 분 동안 자가 복구를 스스로 막았다**
(`daily/2026-08-19.md` §9-2). 그리고 실패했을 때 되살릴 수 있는 유일한 확실한 손이
**사용자 콘솔**이다 — 에이전트가 앞단으로 띄운 프로세스는 Orca PTY job object 가
턴이 끝날 때 트리째 죽인다(`COORDINATOR-STATE` §5-3). **되돌릴 손이 없는 조작은
무인으로 하지 않는다.**

## 5-2. 사슬 A 를 **먼저 죽이지 마라**

지금 살아 있는 사슬 A 는 **이 결함의 살아 있는 표본**이다. 08-19 이후 재현 기회가
한 번뿐이었다. **정지시키기 전에 먼저 관측을 다 뽑아라** — 어느 pid 가 어느 파일
핸들을 잡고 있나, 스택이 어디에 있나(`py-spy dump --pid <pid>` 가 있으면), 부모-자식
중 누가 로그를 쓰나. 다 뽑은 뒤에 정리해도 늦지 않다.

**사슬 A 를 정지시키는 것 자체는 수집에 위험이 없다** — 리스를 못 얻어 수집을 0 건
하고 있고, 사슬 B 가 실제 수집을 전담한다. 다만 **`STOP` 파일은 쓰지 마라. 둘 다 멈춘다.**

## 5-3. 기한

**오늘 20:00 KST 까지** 진단·수정·비라이브 증명을 끝내고 PR 을 올려라.
그때까지 원인을 못 잡으면 **멈추고 "배제된 것 / 남은 가설"을 보고**해라 (§6).
**증상만 없애는 우회는 금지다** — 그 상태로 라이브에 넣는 것이 이 결함보다 위험하다.

---

## 2-3. ★★ 2026-08-20 워커가 7 분 만에 죽기 전에 찾은 것 — **여기서 다시 시작해라**

**2026-08-20 09:55 에 이 명세로 워커를 붙였는데 10:03 에 `Conversation interrupted` 로
끊겼다.** 산출물은 **0 건**이다 (`w5-ops` 에 디스패치 이후 수정된 파일이 하나도 없음을
코디네이터가 `find -newermt` 로 확인했다. 미추적 둘은 08-07·08-08 파일이다).
**그런데 죽기 전 7 분에 쓸 만한 것을 냈고, 코디네이터가 트랜스크립트에서 건져 왔다.**
**다시 처음부터 파지 마라.**

### (가) 프로세스 사슬의 모양은 **수수께끼가 아니다** — 워커가 갈랐다

`Scripts/python.exe` 가 **255 KB = venv launcher**(105 KB 짜리 base python 사본이 아니다).
그래서:

```
사슬 A = cmd -> venvlauncher -> ops.supervisor -> venvlauncher -> collector   (4 python)
사슬 B =        venvlauncher -> collector                                     (2 python)
```

**층 수 차이는 정상이다.** `__main__.py` 는 자식을 안 띄운다. 여기 파지 마라.

### (나) 침묵 구간에 **아무 데도** 안 썼다

- `supervisor.stdout.log` 는 **21:37 에서 멈춰 있다** — 사슬 A 가 거기에도 안 썼다
- `collector.stdout.log` 에서 사슬 A 의 첫 줄은 **죽은 프로세스의 21:19:52 줄 바로 뒤**에
  붙어 있다. 그 사이에 아무것도 없다

### (다) ★★ 유력 가설 — **우선순위다.** 워커가 실측까지 했다

```
PID=46064 cmd        BasePriority=6      <- 사슬 A (스케줄러가 띄운 것)
PID=41076 python     BasePriority=6
PID=50476 python     BasePriority=6
PID=41104 python     BasePriority=6
PID=49284 python     BasePriority=6
PID=26096 powershell BasePriority=8      <- 사슬 B (사용자 콘솔)
PID=28212 python     BasePriority=8
PID=50724 python     BasePriority=8
```

**6 = BelowNormal, 8 = Normal.** 그리고 **코디네이터가 그 원인을 확인했다** — 세 작업
(`tossmon-collector-oneshot`·`tossmon-watchdog`·`tossmon-sentinel`) **전부 XML 에
`<Priority>` 요소가 없다.** 없으면 작업 스케줄러의 **기본값 7 = `BELOW_NORMAL_PRIORITY_CLASS`**
가 적용된다. 관측된 6 과 정확히 맞는다.

**같은 방향의 관측 하나 더 (워커)**: 사슬 A 는 **cmd → python 에만 15 초**, launcher →
supervisor 에 3 초가 걸렸다. **태어날 때부터 굼떴다.** 락이라면 특정 지점에서 멈추지
전 구간이 느려지지 않는다.

> **이 가설이 왜 그럴듯한가**: 같은 순간 사슬 B(Normal)가 **5.6 GB DB 에 초당 수십 번
> 쓰고 있었다.** BelowNormal 프로세스는 CPU 뿐 아니라 **I/O 도 뒤로 밀린다.**
> 3.4 시간은 "멈춤" 이 아니라 **"굶주림" 의 크기**일 수 있다.

### (라) 이 가설을 **반증할 관측** — 이것부터 해라

1. **같은 부하 아래에서 우선순위만 바꿔 재라.** `launch_collector.cmd` 경로를
   **`TOSS_LIVE=0` + scratch DB** 로 두 번 띄운다 — 한 번은 BelowNormal(스케줄러와 같게),
   한 번은 Normal. **첫 로그 줄까지의 시간**을 재라.
   - Normal 은 초 단위인데 BelowNormal 은 분·시간 단위면 → **가설 지지**
   - **둘 다 느리면 가설은 죽는다.** 그때는 (마)로 가라
2. **부하를 갈라라.** 사슬 B 를 못 멈추므로, 대신 **디스크 부하가 낮은 시각**(휴장 깊은
   밤이 아니라 지금처럼 장이 닫혀 랭킹 폴만 도는 때)과 **개장 직후**를 비교해라.
   굶주림이면 부하에 따라 크기가 변해야 한다.
3. **정말 I/O 인지 CPU 인지**: 침묵 중인 프로세스의 `IO Read/Write Bytes` 델타를 재라
   (`Get-Process | Select-Object -Property Name,Id,@{n='IORead';e={$_.ReadOperationCount}}`).
   **0 이면 굶주림이 아니라 대기다.**

### (마) 가설이 죽으면 남는 것

`resumed from collector_state.json` **앞**에서 실행되는 코드만 후보다. 워커가 이미
*"그 앞 코드 경로를 읽겠다"* 까지 갔다. 거기서부터 이어라.

## 5-4. ★ 우선순위를 고치는 것은 **네 손이 아닐 수 있다**

`<Priority>` 를 바꾸려면 XML 을 내보내 고치고 `schtasks /create /xml` 로 다시 등록해야
하는데, **S4U 작업 재등록에는 관리자 권한이 필요하다**(2026-08-19 에 두 번 다 사용자
손이 필요했다). **명령만 적어서 보고해라** — 코디네이터가 사용자에게 넘긴다.
**추측으로 작업을 지우고 다시 만들지 마라.** 지금 도는 워치독·센티널이 그 작업들이다.

---

## 7. ★★ 2026-08-21 19:1x — **이 명세의 표제가 반증됐다. 여기서 끝낸다**

**워커 보고 전문: `coordination/daily/2026-08-21_w5_report.md`** (코디네이터가 저장소에
보존했다). 아래는 코디네이터가 **직접 확인한 것**만 적는다.

### 죽은 것 셋

| # | 가설 | 어떻게 죽었나 |
|---|---|---|
| 1 | 자식 stdout 파이프 교착 (§2) | `supervisor.py:79-88` 이 파일 핸들로 넘긴다. 08-20 정독에서 이미 기각 |
| 2 | **우선순위 굶주림 (§2-3 다)** | **독립적으로 두 번.** (가) 18:45:55 워치독이 같은 런처를 **라이브·6.2 GB 실 DB·BasePriority 6** 으로 무인 기동 → 첫 로그 **11 초** (b) 통제된 비라이브 A/B → **BelowNormal 2.8s vs Normal 1.6s, 차이 1.2 초.** 가설이 요구한 크기는 3.4 시간이었다 |
| 3 | **§1 의 표제 — *"기동 경로가 문제다"*** | **오늘 그 경로가 라이브에서 무인으로 정상 작동했고 지금도 그것으로 수집 중이다** |

> **★ 그래서 §5-4 의 `<Priority>` 재등록은 하지 마라.** 근거가 사라졌다.
> 관리자 권한을 쓸 이유가 없고 S4U 작업을 건드리는 위험만 남는다.

### 대신 나온 것 — **결함은 기동 경로 밖에 있다**

**18:30:16 에 사슬 B 가 살아 있는 채로 멈췄다.** 사슬 B 는 **사용자 콘솔에서 띄운
Normal(8) 우선순위**이고 **44 시간 정상 가동 중**이었다. 스케줄러도 supervisor 도
BelowNormal 도 아니다.

```
18:30:53  col=2 t2book=119476  age_min=0.6    <- 정상
18:35:53  col=2 t2book=119478  age_min=5.4    <- 멈춤 (col_up 은 계속 증가)
18:45:55  col=2 t2book=119478  age_min=15.4   -> 워치독 RESTART
```

**`col_up` 은 늘고 `t2book` 은 고정** = 프로세스는 살아 있는데 일을 안 한다.
**08-19 21:19 과 같은 모양이다.** → *"08-19 의 3.4 시간 침묵은 같은 것의 기동 시점
판본"* 이 워커 가설이지만 **증거가 없다** — 표본이 사라졌다.

### 곁가지 정정 — 사슬 A 는 **"먹통" 이 아니었다**

§2-2 가 사슬 A 를 통틀어 먹통처럼 적었는데 틀렸다. **01:36 이후 41 시간은 정상적인
재시도 루프**였다(`AUTH-FAILURE` 총 14,176 줄, 10 초 간격, 마지막 18:45:59).
리스를 못 얻어 수집만 0 이었을 뿐이다. **진짜 먹통은 08-19 22:11:20 ~ 08-20 01:35:41 뿐이다.**

### ★★ 남은 결함 하나가 다음을 막고 있다 — **워치독이 매번 표본을 파괴한다**

08-19 과 08-21, **재현 기회가 두 번 있었고 두 번 다 워치독의 `RESTART` 가 5 분 안에
증거를 지웠다.** 오늘 워커는 착수 2 분 만에 표본을 잃었고 `py-spy dump`·핸들·IO 델타를
**하나도 못 뽑았다**(워커 명령은 전부 읽기 전용이었다 — 코디네이터가 확인했다).

**이것이 지금 가장 값싸고 가장 급한 수리다.** 상세는 `COORDINATOR-STATE` §3-2.

> ⚠ **선행 조건**: **`py-spy` 가 설치돼 있지 않다** (코디네이터 확인 — PATH 에도
> `w5-ops` venv 에도 없다). 표본 뜨기를 만들려면 그것부터다.

**이 명세는 여기서 닫는다.** 후속은 새 명세로 낸다 — 표제가 바뀌었기 때문이다
(*"기동 경로"* 가 아니라 *"돌던 수집기가 조용히 멈춘다"*).
