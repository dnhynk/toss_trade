# W3 — G-2 탐색 착수: `docs/59` 재실행 + 랭킹 사건의 전방 가격 경로

> 배정: 코디네이터 2026-08-18 14:2x KST · 워크트리 `w3-analyzer` / 브랜치 `feat/analyzer`
> 사용자 결정 **D-25**: *"D-25 부터 순차적으로 전부 진행"* — (나) 본 목표로 간다.
> 이 파일이 명세다. 코디네이터 워크트리에 있으므로 **읽기만** 해라.

## 0. 네가 일할 곳

```
C:/Users/dongh/orca/workspaces/toss_trade/w3-analyzer      브랜치 feat/analyzer
```

지금 그 워크트리는 `fix/w3-verdict-numbered-log-rotation`(PR #33, 병합됨)에 있다.
**먼저 `feat/analyzer` 로 돌아와서 main 을 흡수해라:**

```bash
git checkout feat/analyzer
git fetch origin
git merge --ff-only origin/main    # 안 되면 멈추고 보고
```

`C:/Users/dongh/orca/workspaces/toss_trade/w5-ops` 는 **라이브**다. DB·로그 **읽기만**
(`mode=ro`). 오늘 밤 22:30 에 정규장이 열리고 **프로브도 예정돼 있다**(W1) — 그 워크트리에
쓰기 금지.

## 1. 먼저 읽어라

1. `docs/58_gate.md` **§G-2 전문** — 이 일의 정의와 제약
2. `docs/59_hires_event_inventory.md` — 네가 만든 사건 격자. 재실행 대상
3. `coordination/USER-INPUT-QUEUE.md` 의 **D-19 · D-20** — 아직 안 갈린 둘
4. `coordination/STRATEGY-VERDICTS.md` — **후보 다섯이 어떻게 죽었나.** 같은 함정을 다시 밟지 마라

## 2. ★ 이 작업의 지위 — **탐색이다. 판정이 아니다**

`docs/58` §G-5 가 *"G-2 착수 전에 새 사전등록이 필요하다"* 를 **아직 살려두고 있다.**
그 사전등록은 코디네이터가 **지금 따로 쓰고 있다.** 네 산출물은 그것과 **독립**이고
**판정을 하지 않는다.**

**모든 산출물·표·요약에 이 넷을 반드시 병기해라** (사용자 결정, `docs/58` §G-2):

```
탐색 · 판정 아님
9 세션(07-31, 08-03~07, 08-10~12)은 앞으로 확증에 재사용 불가
이 9 세션은 폴 주기 12.4 초 아래 데이터다 — 서버 10 초 격자 틱의 29% 미수신,
받은 랭킹은 중앙 16.1 초 늙음 (docs/35, W1 실측)
확증은 2026-08-13 이후 데이터로만 하며, 08-18 현재 3 세션뿐이라 아직 못 연다
```

**"성립 안 함" 이라고 쓰지 마라.** *"N 초 창, 오차 X%, 표본 M 에서 관측 안 됨"* 이다.

## 3. 할 일 둘

### 3-1. `docs/59` 재실행 (큐 #4 — D-21 판정이 끝나 이제 열렸다)

```
.venv/Scripts/python.exe -m tossmon.analysis.hires_events [db] [--until-ms N] [--out DIR]
```

- **`config_sig` 경계(2026-08-14 09:16 KST)를 넘어 두 구간을 뭉치지 마라** (`docs/59` §4).
  **D-21 이전 / 이후를 따로 낸다.**
- `docs/59` 의 헤드라인(사건 **1,437,699 건 중 300 초 안에 체결이 있는 것 1.78%**,
  표적 층 **최대 166 건**)이 **D-21 이후에 얼마나 달라졌는지**가 이 재실행의 요점이다.
  D-21 판정은 테이프 커버리지가 **25.9% → 29.1%** 로 늘었다고 냈다 — **사건 격자 위에서도
  늘었는지는 아직 아무도 모른다.** 분모가 다르다는 것을 잊지 마라 (`docs/59` 는 사건,
  D-21 은 종목 집합)

### 3-2. 랭킹 사건 → **전방 가격 경로** (G-2 의 본체)

`docs/58` §G-2 통과 판정 초안: *"초 단위 자에서 몰림이 **종목 고유 변동성과 분리돼**
관측되고, 그 빈도가 '매일 발생한다' 는 골격 문언과 맞는다."*

- 사건 정의는 **네가 정하고 근거를 적어라.** 다만 `docs/44`·`docs/59` 가 이미 만든 격자를
  버리지 말고 그 위에 쌓아라
- **D-19 을 정면으로 다뤄라**: 발화가 몰리는 종목이 원래 더 출렁이는 종목일 수 있다.
  밀도 정합에서 위약 MFE 가 +1.85% → +1.69% 로 내려온 그 문제다. **분리 못 하면
  "분리 못 했다" 라고 적어라** — 분리한 척하지 마라
- **D-20**: 성긴 테이프 발화 **374 건(13.8%)** 은 정합으로 원리상 답이 안 나온다.
  **그 부분집합을 따로 세고 결론에서 분리해라**
- **비용을 아직 차감하지 마라.** 그건 G-3 이고 사전등록이 필요하다. 이 단계는
  *"몰림이 보이는가"* 까지다

## 4. 하지 말 것

- **판정하지 마라.** 통과/실패를 쓰지 마라
- 사전등록 문서(`coordination/D21-COVERAGE-PREREG.md`) 수정 금지
- 홀드아웃 **2026-05-01 ~ 07-29 열람 금지**
- 라이브 API 호출 0 건. `w5-ops` 쓰기 0 건
- 콘솔 출력 **ASCII 전용** (cp949)
- 검증 카드 소각 금지 (현재 0/5). **이 작업은 카드를 안 태운다**

## 5. 게이트 (PR 전에 실제 출력을 붙여라)

1. `.venv/Scripts/python.exe -m pytest -q` — 초록
2. `docs/59` 재실행 전체 출력 (D-21 전/후 각각)
3. 전방 경로 산출물의 표본 수·창 길이·오차 — **숫자마다 측정 조건**
4. `git diff --stat origin/main...HEAD` (점 셋) — 네 소유(`tossmon/analysis/**`, `docs/59` 등)만

## 6. PR

`gh pr create --base main --head feat/analyzer`. 본문에:
무엇을 쟀는지 · 각 숫자의 측정 조건 · **D-19/D-20 을 어떻게 다뤘는지** ·
**네가 확인하지 않은 것** · §2 의 라벨 넷.

## 7. 규율

- 확신 없으면 멈추고 물어라. 추측으로 메우지 마라
- 버그를 고치기 전에 **원인 가설과 그것을 반증할 관측**을 먼저 진술
- 최소 diff. 요청 안 한 리팩터링 금지
- **평균으로 뭉개지 마라** — 1 분봉 한 칸이 6 초짜리 슈팅을 삼킨 것이 이 프로젝트가
  후보 6 개를 잃은 이유다. 새 지표를 만들 때 **사건의 시간축과 측정 창을 먼저 대조**해라
