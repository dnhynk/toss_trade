# W1 — 프로브 발사 준비 (`docs/62` D 안) · **조건부 오늘 밤**

> 배정: 코디네이터 2026-08-18 14:2x KST · 워크트리 `w1-core-api` / 브랜치 `feat/core-api`
> 사용자 결정 **D-25**: *"순차적으로 전부 진행"*. `docs/62` 는 네 소유다.
> 이 파일이 명세다. 코디네이터 워크트리에 있으므로 **읽기만** 해라.

## 0. 시작

```bash
cd C:/Users/dongh/orca/workspaces/toss_trade/w1-core-api
git fetch origin && git merge --ff-only origin/main     # 안 되면 멈추고 보고
```

## 1. 먼저 읽어라 — **전문**

- `docs/62_open_cadence_plan.md` **전체.** 특히 §4(팔 구성) · §5-1~5-7 · **§8(결정 셋)**
- `tools/live_probe.py` 의 docstring — 안전 기본값과 `--reuse-token-state` 의 의미

**이미 정해진 것 셋** (`docs/62` §8, 다시 열지 마라):

| | |
|---|---|
| 팔 구성 | **D 안** (529 콜 · 44 분 · 슬롯 18/120/120) — 사용자 |
| 발사 시점 | **D-21 배포 뒤** — 사용자. **충족됐다** (배포 08-14, 판정 08-18 통과) |
| `foreign` 오염 | **(나) 교정에 쓴다 + 창 표시** — 코디네이터. §8-3 |

## 2. 여유는 이번 창에서 확인됐다 — 그 위에서 시작해라

`docs/62` §5-3 이 요구한 *"`RANKING` 첨두 + 프로브 1 콜 ≤ 천장 5"* 가 2026-08-17 정규장
실측에서 **첨두 3 → 3+1 = 4 ≤ 5** 로 성립했다 (판정 러너 출력, n=77 · p95 3).
**08-14 에 내가 "D-21 이 정규장을 안 겪어 여유를 모른다" 며 미뤘던 조건이 풀린 것이다.**

> **그래도 발사 직전에 다시 재라.** 오늘 창의 첨두는 오늘 재야 안다.

## 3. ★ 발사 기전 — **이 레포가 두 번 죽은 자리다**

- **`schtasks /run` 을 에이전트 세션에서 실행 금지.** 콘솔 정리가 Ctrl+C 를 전파해
  작업이 같이 죽는다 (`LastTaskResult=3221225786`, 실제로 두 번)
- **무인 프로세스는 작업 스케줄러로만.** 부모가 `svchost(Schedule)` 이 되게 해라.
  `Start-Process` 분리로는 **부족하다** — Orca PTY job object 가 트리째 죽인다
- 참고 구현이 이미 있다: `tools/coord_headless.cmd` + `tossmon-coord-d21verdict` 작업.
  **시간 트리거로 등록하고 그 시각에 저절로 뜨게 한다.** 그 방식은 스모크로 검증됐다
  (`Result=0`)
- 런처는 **ASCII 전용** (cp949 콘솔에서 비 ASCII 는 `UnicodeEncodeError`)

## 4. 오늘 밤 창

```
정규장 개장   2026-08-18 22:30 KST  (13:30 UTC)
프로브 창     docs/62 §5-1 = 22:31 착수, 44 분
```

## 5. ★★ 발사 조건 — **하나라도 안 되면 오늘 안 쏘고 다음 거래일로 미룬다**

1. **mock 드라이런이 초록**이어야 한다 (`TOSS_BASE_URL=http://127.0.0.1:8899`,
   `tools/mock_server.py`). `docs/62` §2-1 의 기대치(`open` 프로파일 454 콜 / 724.6 초 /
   `aborted null`)와 대조하고 **차이가 있으면 보고**해라
2. **발사 기전이 실제로 검증**돼야 한다 — 등록만 하지 말고, `schtasks` 로 뜨는 것을
   **스모크로 확인**해라 (코디네이터가 08-15 에 그렇게 했다. 등록≠작동)
3. **수집기가 건강**해야 한다 (`sup=1 col=1`, `STOP`/`PLANNED` 표식 없음)
4. **22:30 까지 60 분 이상 여유**를 남기고 위 셋이 끝나야 한다.
   **못 남기면 중단하고 등록을 취소한 뒤 보고해라.** 급하게 쏘지 마라 —
   `docs/62` §5-5 가 *"못 되돌리는 것: 그 시각의 정규장은 한 번뿐이다"* 라고 적었다

**너는 발사 스위치를 직접 누르지 않는다.** 시간 트리거를 걸어 두고, **걸었다는 사실과
조건 넷의 결과를 코디네이터에게 보고**해라. 코디네이터가 마지막으로 확인한다.

## 6. 하지 말 것

- **`force429`·`force429_client` 프로브 금지.** 수집기가 도는 중엔 같은 버킷 페널티를 공유한다
- **토큰 재발급 금지.** 반드시 `--reuse-token-state <컬렉터 token_state.json>` —
  이 API 는 client 당 유효 토큰이 1 개라 재발급이 곧 **가동 중 토큰 살해**다
- `w5-ops` 에 쓰기 금지 (`token_state.json` 도 **읽기만**)
- config·수집기·리미터 천장 변경 금지 (천장은 D-25 에서 **나중** 순서로 정해졌다)
- **판정하지 마라.** 숫자와 측정 조건을 가져와라

## 7. 게이트 (보고에 실제 출력을 붙여라)

1. `.venv/Scripts/python.exe -m pytest -q` — 초록
2. mock 드라이런 전체 출력 + §2-1 기대치와의 대조
3. 발사 기전 스모크 결과 (작업 이름 · `LastTaskResult` · 로그)
4. 등록된 작업의 되읽기 (`Execute` / `Argument` / `Trigger` / `Principal`)
5. 발사 직전 첨두 재측정 (`RANKING` peak, 3+1 ≤ 5 인가)
6. `git diff --stat origin/main...HEAD` — 네 소유만 (`docs/62`, `tools/live_probe.py` 등)

## 8. 보고

조건 넷 각각의 결과 · 걸어둔 작업 이름과 트리거 시각 · **오늘 쏠 수 있는가/없는가와 그 이유** ·
**네가 확인하지 않은 것**. PR 은 코드/문서 변경이 있을 때만.
