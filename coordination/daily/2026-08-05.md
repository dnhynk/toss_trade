# 코디네이터 일일 기록 — 2026-08-05

> **그날의 기록이다. 고치지 마라 — 갱신은 새 날짜 파일로 한다.**
> `coordination/COORDINATOR-STATE.md` 에서 잘라 옮겼다 (2026-08-13, 원문 무수정).
> 현재 상태는 그 파일을, 문서 목록은 `docs/INDEX.md` 를 봐라.

---

## 4.9-0 ★ 예고된 네트워크 유실 — 2026-08-05 07:25 ~ 08:55 KST (사고 아님)

**사용자 공지(06:55)**: 약 1시간 30분 네트워크 유실, **노트북 덮개 닫음**.
따라서 이 구간의 수집 공백은 **사고가 아니다.** 결손 감사·분석에서 그렇게 읽지 마라.

**코디네이터가 걸어둔 대비:**
- `data/ops_state/PLANNED` 에 **`until = 2026-08-05 09:00:00`** 으로 창을 걸었다.
  이 구간의 경보는 `ALERT_` 가 아니라 **`PLANNED_`** 로 떨어진다
  (*"`ALERT_` 파일이 있으면 진짜 문제"* 규칙 보존). **워치독 자신의 파서로 재생해 파싱 확인함.**
  마커는 09:00 에 **자동 삭제**된다 — 잊힌 마커가 경보를 영구히 죽이는 일은 없다.
- **`STOP` 파일은 만들지 않았다.** STOP 을 놓으면 워치독이 통째로 손을 놓는데,
  네트워크 복귀 시 그것을 치울 사람이 없다(코디네이터 세션도 같이 끊긴다).
  **무인 구간에 STOP 을 남기지 마라 — 이건 규칙으로 삼을 것.**
- **수집기를 미리 세우지 않았다.** §4.5 표준대로 자동 복구에 맡긴다.
  기계가 깨어 있으면 워치독이 5분 주기로 살리고, 잠들면 깨어난 뒤 살린다.

**창이 끝나는 지점이 미묘하다**: 애프터 세션은 **08:50** 에 끝나고 데이마켓은 **09:00** 에 연다.
그 사이 10분은 **원래 세션이 없는 구간**이다(어제 아침 거짓 CRIT 을 만든 바로 그 구간).
그래서 손실의 대부분은 가치가 낮은 애프터 꼬리이고, **09:00 데이마켓 개장은 온전히 받는다.**
창을 09:00 으로 끊은 이유도 그것이다 — 개장 이후 문제는 **진짜 `ALERT_` 로 떠야 한다.**

**진행 중이던 작업**: W5 `task_47a9fbbef96e`(경보 분류 규칙 명세화)가 **가동 중에 끊긴다.**
워크트리 파일은 디스크에 남고 턴·컨텍스트만 사라진다. 복귀 후 재디스패치하면 이어서 할 수 있다.
**실패로 처리하지 마라.** 커밋을 자주 하라고 터미널로 직접 일러뒀다.

## 4.9 아침에 할 일 (2026-08-05) — 밤사이 코디네이터가 남긴 것

**밤 상태 (사용자 지시로 배포 후 1시간 감시 완료 — 23:51~00:53)**: 수집기 정상
(23:36:00 기동, W4 수정 + `usage_ratio` 0.85 적용본). **배포 후 1시간 17분간 정원 축소 0건**,
`tier2 287~310 / tier3 10`(설정 상한) 유지, 429 0, 에러 0, 인증 실패 0, 수집 성공률 100%.
새 ALERT 파일 없음. `PLANNED` 마커 자동 만료 확인(잊힌 마커가 진짜 경보를 가리지 않는다).
main `1580 passed·1 skipped`. 워커 3명 전원 `worker_done`, 열린 태스크 0, 미읽음 메일 0.

**단 `over_limit_1s` 는 계속 오른다**(23:41 이후 4 → 27). 즉 **유령 판정 자체는 안 고쳐졌고**
이제 피해를 안 줄 뿐이다. 그것이 W1 2단계 프로브가 잴 대상이다 — 아래 2번.

**아침 리포트는 자동으로 뜬다** — `tossmon-dailyhealth` 08:52, 이번부터 **결손 감사 포함**
(`ops/gap_audit.py` 배선 완료). 어제와 형식이 다르니 놀라지 마라.

### 사용자와 논의할 것 (혼자 판정 금지)

1. **★ `candles_1m.ts_ms` 가 종료 시각 라벨인데 분석 코드가 시작 시각으로 읽고 있을 수 있다.**
   W5 실측(`docs/31` §2): 21:51 봉이 `[21:50,21:51)` 을 덮는다(99.1% presence,
   93.4% 정확 거래량 일치 at +1min). 그런데 `tossmon/analysis/features.py` 의 컷오프 근거는
   *"T0 봉의 종가·거래량은 그 분이 끝난 뒤에만 관측되므로"* — **시작 시각 라벨 전제의 문장**이다.
   테스트는 합성 시나리오라 자기 규약끼리 일관돼 이 불일치를 못 잡는다.
   **사실이면 1분봉 기반 특징 정렬이 한 칸 어긋나 있었다는 뜻**이고, 과거 판정 해석에 닿는다.
   방향은 **누출이 아니라 과보수** 쪽으로 보이지만(종료 라벨이면 T0 봉은 이미 완결)
   `_minute_volumes` 같은 창 계산은 통째로 1분 밀린다. **코디네이터가 확정하지 않았다.**
   W5 도 "확인하지 않았다, `tossmon/**` 소유자가 볼 일"이라고 정확히 넘겼다.

2. **W1 2단계 프로브는 승인됐다 — 그런데 누가 깨울지 정해야 한다.**
   실행 창이 **오늘 12:00~15:00 KST**(429 가 실제로 나는 시간대)다. W1 은 `worker_done` 후
   idle 이라 **누군가 다시 디스패치해야 돈다.** 120콜·버스트 0·중단조건 있음.

### 워커에게 바로 낼 수 있는 것 (판정 아님, 기계적)

3. **`SHRINK_MAX_STEP_FRAC` 이 시험되지 않는다** — 1.0(상한 없음)으로 바꿔도 132개 전부 통과.
   코디네이터가 직접 확인했다. W4 후속 테스트. **작지만 이 프로젝트가 이걸로 데인 적 있다.**
4. **`client.last_429` 가 `loops.py` 에서 한 번도 안 읽힌다** — `docs/30` §2 의 "고쳤다"가
   현재 거짓이다. W1 이 `docs/32` §5-1 에 한 줄 제안만 남겼다(파일은 W4 소유라 안 고침). W4 몫.
5. **`usage_ratio` 가 `config_sig` 에 없다** — 오늘 밤 0.85→0.70→0.85 가 **데이터에 안 남는다.**
   `DATA-QUALITY-PROGRAM` 규칙 3("경계를 데이터에 남긴다")이 지금 안 지켜지고 있다.
   `config/config.yaml` 자체가 gitignore 라 git 에도 안 남는다 — 지금 유일한 기록이 §4.8 이다.
6. **개장 승격 버스트 357건/5분** 자체를 아무도 안 봤다. W4 가 "자기 진단을 받을 만하다"고 지적했다.
   이 버스트가 오늘 밤 사고의 **입력**이었다.

### ★ 7. 우리 예산 숫자가 전부 무효일 수 있다 (00:20 발견, 사고 아님)

배포 후 `tier2_orderbook_snaps` 가 **23:40 부터 완전히 멈췄다**(`_skipped_rate` 만 5분에 147씩 증가,
`_429` 는 불변). 처음엔 회귀인 줄 알았으나 **아니다 — 수정이 든 결과다.**

- tier3 호가(`orderbook_snaps`)는 **정상**(22:11 87,640 → 00:20 96,474). 체결·분봉도 정상.
- 멈춘 것은 tier2 의 **넓은 훑기**(600초 주기 × ~300종목)이고 사유는 **예산 양보**다.
  워치독 문구대로 *"tier2 호가는 제일 먼저 희생된다"* — **설계된 희생 순서**다.
- 어제 같은 시각 tier2=308 에서는 이게 돌았다(5분에 135건). **왜 어제는 됐나?**
  **어제는 tier3 가 굶고 있었기 때문이다.** W5 측정(`docs/31`): *"tier3 는 설정된 10 에
  한 번도 도달한 적이 없다 — 모든 창에서 평균 2.86~2.89종목."*
  **오늘 밤이 tier3 가 10 을 채운 첫 밤**이고, 그 4초 호가·4초 체결이 예산을 제대로 쓰자
  tier2 훑기가 밀려났다.

> **함의: 우리가 가진 예산·여유 숫자는 전부 tier3 가 ~3종목으로 굶은 상태에서 잰 것이다.**
> `docs/30` §5-3 의 *"늘어난 예산 여유 +2.047 req/s 를 어디 쓸지 미결, W4 권고는 체결 2.5초
> 또는 12종목"* 은 **그 전제 위에 서 있어 지금 무효다.** 정원이 실제로 차면 여유가 없다.
> **재측정 전에는 어떤 예산 재배분도 하지 마라.** `DATA-QUALITY-PROGRAM` 축 2 의
> "예산 여유 재배분"은 이 재측정이 선행 조건이다.

**사용자 결정이 필요한 트레이드오프**: 지금 구도는 **tier3 10종목을 촘촘히 vs tier2 300종목을
600초마다 훑기** 중 전자를 택한 것이다. 자동으로 그렇게 된 것이지 누가 고른 게 아니다.
tier2 훑기가 승격 후보 발굴에 쓰이는 것이라면 이 100% 희생이 맞는지 물어야 한다.

### 오늘 밤 데이터를 분석에 쓸 때 (인공 구멍)

계획 정지 3회 — 22:39:44~22:41:02(78s) / 22:49:16~22:51:09(113s) / 23:34:46~23:36:10(84s).
그리고 **22:33~23:36 은 정원이 깎인 채 운영**(tier2 1~21종목, 정상은 ~310).
**23:36 이후만 정상 수집이다.** 이 밤을 하나로 뭉쳐 "정규장 결손"을 계산하면 틀린다.

## 4.10 일일 코디네이터 실행 기록 — 2026-08-05 09:30 (자동화)

> **오늘은 코디네이터가 둘이었다.** 대화형 세션(`term_35919dae`)이 사용자와 붙어 있었고,
> 그쪽이 *"COORDINATOR-STATE 쓰기를 보류하겠다"*고 먼저 양보했다. 그래서 **이 문서는 자동화가
> 쓰고, W1·W6 큐와 사용자 대화는 대화형 세션이 갖는다.** 자동화는 `w5-ops` 만 건드렸다.
> 분담 내용은 `terminal send` 로 그쪽에 직접 통보했다. **다음에도 세션이 겹치면 이렇게 갈라라.**

**수집: 정상. 단 60분 공백이 있었고, 그것을 아침 리포트가 놓쳤다.**

- 수집기 프로세스는 **죽지 않았다** — `col_up` 이 이어진다(어제 23:36 기동본 그대로).
  **기계가 잔 것**이고(배터리, 사용자 노트북 덮개) 깨어나서 스스로 재개했다.
  §4.5 자동 복구는 제대로 동작했다. 09:41 현재 `fetch_success_pct=100`, `auth_failures=0`,
  `rank_age=6s`, 여유 디스크 13.07 GB.
- 실제 공백은 **08:01:46 ~ 09:01**(약 60분). 예고 창은 07:25~08:55 였으니 **뒤로 밀렸다.**
  `PLANNED` 마커(~09:00)가 대부분을 덮어 거짓 CRIT 은 안 났다 — 마커는 09:00 자동 만료 확인.

### ★ 오늘의 발견 1 — 아침 리포트가 60분 구멍을 **3.4분**이라고 보고했다

`daily_health_20260805.txt` 는 `max_gap=3.4min` 이라고 적었다. 코디네이터 DB 실측:

```
창                : 08-04 09:00 ~ 08-05 08:50
창 안 마지막 랭킹 폴 : 08-05 08:01:46
=> 창 끝에 붙은 구멍  48.2분          <- 리포트에 안 나온다
```

결손 계산이 **연속한 두 폴 사이의 간격**만 본다. 구멍이 창 **가장자리**에 있으면 비교할
다음 폴이 창 밖이라 간격 자체가 만들어지지 않는다. 창 앞쪽도 같다.
**즉 수집이 창 끝에서 죽으면 리포트는 "이상 없음"이라고 말한다.**

> **"성공을 반환하는 조용한 실패" 다섯 번째 사례.** 앞선 넷: 룩어헤드 테스트가 죽은 설계만
> 지킴 / 분석 함수가 테스트에서만 호출됨 / 가드가 허용목록만 검사함 / pandas 중복 인덱스
> 브로드캐스트. **이번 것이 제일 위험한 자리에 있다** — 이 리포트는 매일 아침 자동화
> 코디네이터가 **가장 먼저 읽는 문서**다. 오늘 DB 를 직접 안 봤으면 "수집 정상"이라고 쓸 뻔했다.
> **규율: 리포트의 결손 숫자를 그대로 인용하지 말고 마지막 관측 시각을 직접 확인하라.**

→ W5 `task_cc6937a98aa5` / `ctx_09476e99af71` 디스패치 완료.
   스펙 `coordination/archive/specs/w5_gap_audit_edge_hole.md`.

### ★ 오늘의 발견 2 — `rankings_snap.amount_u` 는 달러가 아니라 **원화**로 보인다

`rankings_clamped` 가 **처음으로 발화**했다(08-05 05:00~05:30, **500건**).
`amount_u` 가 int64 max 를 넘어 클램프됐다. 전부 **EA·AAPL·NVDA·SPY·MSFT** —
**우리 유니버스(`symbols`)와 교집합 0.** **표적 데이터 오염 0행.** 급한 사고가 아니다.

원인은 단위 불일치다. 같은 스냅샷에서 `last_u` 는 정상 USD 마이크로인데
(EA $209.84 / INTC $100.91 / SXTC $0.148 / HTZ $1.52) `amount_u` 만
`last_u × vol_qu` 의 약 1/700, 즉 **달러 금액의 약 1,433배**다.
**그리고 그 배수가 날마다 움직인다** (일별 중앙값, n=40,000/일):

```
07-31  1441.10   08-01  1441.92   08-03  1433.34   08-04  1434.00   08-05  1429.83
```

**고정 스케일이면 이렇게 안 움직인다. 환율처럼 움직인다.** → 가설: 토스가 랭킹 `amount` 를 원화로 준다.

**현재 소비처 2군데는 안전하다 (코디네이터 확인):**
- `analysis/features.py:507-512` `toss_share = toss.amount_u / mkt.amount_u`
  — 같은 스냅샷의 비율이라 **환율이 정확히 상쇄된다.**
- `analysis/measure/rotation_pilot.py:59` — 상위 N 선별·순서용이라 **단조 변환**.
- `analysis/rotation.py` 는 `amount_u12 = close_u × vol_qu` 로 **직접 재계산**한다(감사 4차 B10) — 올바른 경로.

**그러나 이름이 함정이다.** `last_u`·`price_u`·`close_u`·`open_u`·`high_u`·`low_u` 는 전부
USD 마이크로인데 `amount_u` 만 통화가 다르면, **다음 사람이 `amount_u/1e6` 을 달러라고 부르는
순간 1,433배 틀린다.** 잠복 함정이므로 문서화가 본체다.

→ W1 `task_042a1162d545` **생성만 하고 디스패치 안 함(`ready`).**
   이유는 하나다: **W1 워크트리에 오늘 12:00~15:00 2단계 프로브가 예정**돼 있어
   §4.7-4(같은 워크트리에 겹쳐 보내지 마라)를 지켰다. **W1 큐는 대화형 세션 몫이다.**
   스펙 `coordination/archive/specs/w1_ranking_amount_currency.md`. 오프라인 태스크(라이브 호출 불요).

### 그 밖에 본 것 (조치 안 함, 사실만)

- **`tier2_orderbook_flat` ALERT 6건** (00:55~05:00). §4.9 §7 이 예측한 그대로다 —
  tier3 가 정원을 채우면 tier2 넓은 훑기가 예산을 양보한다. **05:00 정규장 종료 직후
  tier2 호가 종목수가 240→263→282 로 즉시 회복**하는 것이 기전을 확정한다.
  오늘 아침 W5 가 머지한 `TRADEOFF_` 등급(`2bc8dfb`)이 정확히 이걸 위한 것인데,
  **이 6건은 그 배포보다 앞서서 `ALERT_` 로 찍혔다.** 다음 밤부터 등급이 갈릴 것이다 —
  안 갈리면 그때는 진짜 결함이다.
- **예산 목표가 올라갔다**: `MARKET_DATA` 7.00 → **8.50**, 나머지 3.50 → 4.25,
  `over_limit_1s` 0.70 → 0.85. `config_sig` 에 `usage_ratio` 가 없어서 **이 변경은
  데이터에 안 남는다** — §4.9 항목 5 가 지적한 그 구멍이 **오늘 또 한 번 실현됐다.**
  지금 이 문단이 유일한 기록이다.
- `over_limit_1s` **111**(어제 밤 27) — 유령 판정은 여전히 안 고쳐졌다(피해만 없다).
  `http_429=13`, `http_429_under_own_limit=25`, `api_errors=1`(첫 비영).
  `tier3_cap=4`·`tier2_cap=88` 로 다시 깎여 있다 — 한국 낮 시간대 429 패턴 그대로.
- **랭킹 해상도가 절반으로 줄었다.** 08-04 결손 감사에는 랭킹 타입이 **4종**(타입별 p50 13.08초,
  풀링 0.33초 → 고유 스냅 약 3.3초)이었는데, 지금 `config_sig` 는
  `rank2:MVOLUME+TVOLUME,...,rk12s` — **2종만 남았고 고유 스냅이 약 6초**다.
  실측: 어제 시간당 폴 1,090~1,104 → 오늘 577~582.
  빠진 것은 `MARKET_TRADING_AMOUNT` 와 `TOSS_SECURITIES_TRADING_AMOUNT`(금액 계열)이고
  VOLUME 계열 2종이 남았다. **`toss_share`(토스/마켓 거래대금 비율)는 계속 계산된다** —
  `amount_u` 는 타입이 아니라 컬럼이라 남은 2종에도 들어 있다. 그 점은 안전하다.
  **다만 예산 절감 목적으로 보이는 이 변경이 사용자 지시 D-6("해상도가 낮아서 전략이 죽었으니
  고해상도로 재시작")과 방향이 반대다.** 자동화가 승인한 것이 아니고, `config/config.yaml` 은
  gitignore 라 **git 에도 안 남는다.** → **대화형 세션·사용자가 의도를 확인할 것.**

**워커 상태**: 열린 태스크 0, 미읽음 메일 0, 실패 0 (인수 시점). 오늘 자동화가 연 것은
W5 1건(디스패치)·W1 1건(ready). 머지한 브랜치 없음.
