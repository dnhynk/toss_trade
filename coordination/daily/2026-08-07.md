# 코디네이터 일일 기록 — 2026-08-07

> **그날의 기록이다. 고치지 마라 — 갱신은 새 날짜 파일로 한다.**
> `coordination/COORDINATOR-STATE.md` 에서 잘라 옮겼다 (2026-08-13, 원문 무수정).
> 현재 상태는 그 파일을, 문서 목록은 `docs/INDEX.md` 를 봐라.

---

## 아침 09:30 — 일일 실행 기록 (자동화)

*원본 `COORDINATOR-STATE` §4.11 — **번호가 중복이었던 두 절 중 첫 번째**. 원 제목:
"4.11 일일 코디네이터 실행 기록 — 2026-08-07 09:30 (자동화)"*

> **08-06 자동화 실행 기록이 없다.** 09:30 에 기계가 꺼져 있었기 때문이다(아래 참조).
> 그날 대화형 세션이 재부팅을 진단했으나 **커밋은 하나도 없다** — main 은 08-05 13:19 (`1ffa45d`)
> 이후 그대로다. 즉 **08-06 은 이 문서에 기록이 비어 있는 날**이고, 아래가 사후 복구분이다.

### 08-06 사고 — 기계가 5시간 55분 꺼져 있었다 (수집기 결함 아님)

DB·ALERT·워치독 로그 교차 확인:

```
08-06 03:46:22  Windows 비정상 종료          <- 마지막 랭킹 폴 03:54:05 (버퍼 잔여)
08-06 09:36:22  재부팅
08-06 09:40:32  ALERT watchdog_silent   (하트비트 20,975초 = 5.8h)
08-06 09:40:56  ALERT watch_process_dead (sup=0 col=0) -> launch_collector.cmd
08-06 09:41:03  수집 재개
```

**§4.5 자동 복구가 설계대로 동작했다** — 사람 개입 0. 기계가 깨어난 지 **4분 34초** 만에 수집이 돌아왔다.

### ★ 발견 1 — 어제 고친 가장자리 구멍 탐지가 첫 실전 사건에서 그대로 증명됐다

08-06 창(08-05 09:00 ~ 08-06 08:50)에는 **아침 리포트가 없다**(사유는 발견 2).
오늘 `gap_audit --date 20260806` 으로 수동 재실행했더니:

```
max_hole       : 295.5min  [trailing_hole]  (08-06 03:54:30 -> 08:50:00)
max_inter_poll :   1.3min                    <- 08-05 이전 지표는 여기까지만 봤다
```

**옛 지표로는 "최대 공백 1.3분", 즉 이상 없음이었다.** 실제로는 창 끝에서 4시간 55분을 잃었다.
§4.10 발견 1 에서 지적하고 W5 `task_cc6937a98aa5` 로 고친 바로 그 구조적 사각이며,
고친 다음 날 실제 사건에서 227배 차이로 갈렸다. 산출물: `w5-ops/data/gap_audit_20260806_recovered.txt`.

### ★ 발견 2 — 사고가 자기 자신을 감춘다: 리포트는 사고 난 날 아침에만 결번된다

`daily_health_20260806.txt` 가 **존재하지 않는다**(08-05·08-07 은 있다). 예약 실행 시각 08:52 에
기계가 꺼져 있었고 **작업 스케줄러는 놓친 실행을 따라잡지 않는다.**

> **구조가 고약하다**: 리포트를 죽이는 사건과 리포트가 드러내야 할 사건이 **같은 사건**이다.
> 결번은 무작위로 나는 게 아니라 **하필 최악의 창에** 난다. 그리고 결번은 조용하다 —
> 오늘 자동화가 파일 목록을 세지 않았으면 08-06 창은 아무도 안 본 채 지나갔다.
> **규율: 아침에 `daily_health_<어제>.txt` 가 있는지를 먼저 확인하라. 없는 것이 곧 신호다.**

→ W5 `task_4c7447a2597a` / `ctx_0c385ea88d18` 디스패치 완료(term_b36cb0a7).
   스펙 `coordination/specs/w5_report_catchup_and_restart_verdict.md`. 두 건이다:
   ① 결번된 날 따라잡기(멱등, 기존 파일 불변, 사후 생성 표시)
   ② **재시작 경보의 자기평가 문구가 정당한 재시작을 "아마 오작동"이라고 읽게 만든다** —
   `watchdog.ps1:1645-1651` 이 사유와 무관하게 같은 문구를 붙인다. 08-06 사례는 `sup=0 col=0`
   (프로세스가 실제로 부재)인데 첫 불릿("session NOT TRUSTED -> 재시작이 아마 틀렸고 워치독 결함")이
   발화한다. 프로세스 부재 계열(`process_dead`/`supervisor_dead`/`collector_dead`)은 세션 신선도와
   **무관**하다. 진행 정지 계열의 현 문구는 08-04 에 비싸게 얻은 것이라 그대로 둔다.

### 수집 상태 (08-06 09:41 재개 이후 지금까지 정상)

- 현재(09:30 워치독): `sup=1 col=1 fetch_pct=100 auth_fail=0 rank_age=3 free_gb=100.15`.
  **디스크 여유 100 GB** — D-7 시절 6.71 GB 에서 완전히 풀렸다.
- **정규장(08-06 22:30~08-07 05:00)은 온전했다**: tier2 271~281 / tier3 **10(정원 만석)**,
  `budget_shrinks` 가 23:15 이후 **237 에서 한 번도 안 움직였다.** 08-04 정원 붕괴 수정(`ff83d68`)이
  세 번째 밤도 버텼다.
- `loop_errors=36210` 은 **24시간 내내 불변**이다(재기동 전후 동일 = 영속 누계). 신규 발생 0.
- 한국 낮 시간대(09:46~16:49)에는 여전히 정원이 깎인다(`tier2_cap` 12~120, `tier3_cap` 4,
  이 구간에서만 shrink 60회). §4.6 이 짚은 "429 는 우리 사용량과 무관, 그러나 대응이 정원 축소"
  패턴 그대로다. **개장 시각에는 재발하지 않았다** — 그것이 §4.6 의 걱정이었으므로 이번엔 통과다.

### 그 밖에 본 것 (조치 안 함, 사실만)

- **`TRADEOFF_` 등급이 실전에서 제 몫을 했다.** 24시간에 14건 — 전부 `tier2_orderbook_yield`.
  옛 등급이었으면 `ALERT_` 14건으로 떠서 아침에 사고로 읽혔을 것이다. 08-05 사용자 결정(`b34a60a`)이
  정확히 이걸 위한 것이었고, 이번 창이 첫 전면 검증이다.
- **그런데 그 트레이드오프의 크기가 사용자 결정을 요구한다** — 정규장 6시간 연속 tier2 호가
  **0건**, 정규장 전체 **117/9,431 = 1.2%**. 장 마감 후에는 곧바로 회복. → 큐 **D-9** 로 올렸다.
- **1분봉 라벨 오프셋이 이틀 독립으로 +1분(구간 종료 라벨) 확정**(정확일치 91.4%/89.3%).
  `tossmon/analysis/features.py:8-10` 은 여전히 시작 라벨 전제로 쓰여 있고 `1e9c92b` 이후
  손대지 않았다. §4.9 항목 1 이 "혼자 판정 금지"로 남긴 그대로 → 큐 **D-10** 으로 올렸다.
- W1 `task_042a1162d545`(랭킹 `amount_u` 가 원화로 보인다, 08-05 생성)이 **이틀간 `ready` 로
  방치**돼 있었다. 08-05 에 대화형 세션 몫으로 넘겼는데 그 세션이 집지 않았다.
  W1 유휴·겹치는 태스크 없음을 확인하고 오늘 디스패치했다(`ctx_21aba1a25750`, term_9d3735fd).
  **교훈: 세션 간에 큐를 넘길 때는 넘겼다는 사실 자체가 어디에도 안 남는다.**

**워커 상태**: 인수 시점 열린 태스크 1건(`ready`, 위 W1), 미읽음 메일 0, escalation 0, 실패 0.
오늘 자동화가 디스패치한 것 2건(W1·W5), 생성한 태스크 1건, **머지한 브랜치 0** (완료 보고가 없었다).

---

## 밤 — 미배포 대기 + 순서가 걸린 변경

*원본 `COORDINATOR-STATE` §4.11 — **번호가 중복이었던 두 절 중 두 번째**. 원 제목:
"4.11 미배포 대기 + 순서가 걸린 변경 (2026-08-07 밤)". 두 절이 같은 번호를 달고 파일
안에서 240 줄 떨어져 있었다 — 이번 분리로 갈랐다.*

> **★ 이 절의 "배포 대기: `TOP_GAINERS`" 는 지금 거짓이다.** 같은 날 밤 22:26 KST 에
> 배포됐고 계속 수집 중이다(2026-08-13 실측 2,140,182 행). `docs/INDEX.md` §정정 대기.

### ★ 배포 대기: `TOP_GAINERS` (main 머지 완료, 재시작만 남음)

`96971fe` → main. **설정 변경 불필요, 재시작만 하면 적용된다.**
22:13 에 도착했고 정규장 개장이 22:30 이라 **마진 5분으로는 안 했다** —
08-04 에 급한 배포가 tier3 를 고사시킨 그 모양이다.

**배포 창**: 정규장 종료(05:00 KST) 이후. 애프터 세션이라 공백 비용이 싸다.
절차는 §4.7-3 (`PLANNED` → `STOP` → 종료 확인 → `STOP` 제거 → 워치독이 되살림).
**`STOP` 을 무인 구간에 남기지 마라.**

배포되면 `config_sig` 가 바뀐다:
`rank2:MVOLUME+TVOLUME,...` → **`rank3:GAIN/1d+MVOLUME/rt+TVOLUME/rt@100,...`**
이 경계 앞뒤 데이터를 뭉치지 마라.

**디스크 주의**: +141.5 MB/일 (랭킹 전체 283 → 425 MB/일). 여유 97.9 GB 기준 **잔여 346일 → 231일.**
그리고 W4 가 짚었다 — **`rankings_snap` 에 보존 정책이 아예 없다. 단조 증가한다.**
231일은 "아무것도 안 지울 때"의 숫자다. W2 안건으로 올라가 있다.

### 순서가 걸린 것: 애프터 종료 경계 08:50 → 09:00 (사용자 (가) 선택)

**사용자 결정 (2026-08-07)**: 애프터 세션 종료를 **09:00 KST 로 고친다.**

근거 — `session.py` 가 스스로 *"고정 KST 오프셋 근사"* 라고 적어두었고, EDT 실제 애프터장
종료는 **09:00 KST**(20:00 ET)인데 우리 표는 08:50 이다. 백필 실측: 08:50~08:59 에도
**하루 평균 29.5봉**이 실제로 찍힌다(직전 10분은 92.0봉, 데이 정상 구간은 51.9봉).
**거래는 있으나 얇다** — 수집 가치보다 **정의 정확성**이 이 결정의 이유다.

> **코디네이터 정정 기록**: 처음에 이 창을 *"분당 1,500종목"* 으로 보고했는데
> **초 단위로 묶으면서 날짜를 안 나눈 실수**였다(백필 526일 합계였다). 실제는 분당 2.95봉이다.
> 그리고 08-04 에 이 구간을 *"원래 세션이 없는 구간"* 이라고 설명한 것도 틀렸다 —
> 토스가 정한 게 아니라 **우리 코드가 정한 것**이었다.

**★ 순서를 지켜라. W3 의 D-12 봉인 경계 수정(`task_9cba80a70f6f`)이 끝난 뒤에 착수한다.**
같은 파일(`session.py`)이고, 한꺼번에 바꾸면 **T-082~085 재실행에서 어느 변경이 수치를
움직였는지 못 가른다.** 그 대조가 W7 의 "소비 0건" 판정을 실증하는 유일한 수단이다.

**그리고 이쪽이 파급이 더 크다**: `session_date`(사이클 날짜)가 바뀌면
**과거 전 구간의 사이클 배정이 재계산된다.** 봉인 경계 9봉과는 규모가 다르다.
착수 전에 파급범위부터 재게 할 것.
