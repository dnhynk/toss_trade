# 2026-08-28 (금) — **워치독이 45 분간 조용히 죽어 있었다**

> 기준 `main` `6385830`. 확증 팔 2 세션 완료(08-26, 08-27), 오늘 밤이 3 번째.
> 측정 현황 전체는 `docs/79`.

## 0. 한 줄

디스크가 0 GB 까지 차면서 `watchdog_state.json` 이 BOM 3 바이트로 잘렸고, 그것을 읽는
`Load-State` 가 `$null` 을 돌려주면서 워치독이 매 사이클 로그 한 줄도 못 쓰고 죽었다.
**수집기는 무사했지만 약 45 분간 감시가 없었다.**

## 1. 사고 — 조용한 워치독 정지

### 1-1. 원인 사슬 (재현 완료)

```
1. 디스크 0 GB
2. Save-State 의 WriteAllText 가 BOM 3 바이트만 쓰고 실패
     -> watchdog_state.json = EF BB BF
3. Get-Content -Raw 가 BOM-only 파일에 아무것도 안 내놓는다
4. ConvertFrom-Json 이 그것을 받고 예외 없이 아무것도 안 낸다   <- 227행 try/catch 무력
5. Load-State 가 $null 반환
6. Set-Prop $null -> "null 배열에 인덱스 불가" (244행)
7. $ErrorActionPreference='Stop' -> 종료성 -> 로그 없이 exit 1
```

**4~6 단계를 실제 파일로 그대로 재현했다.**

### 1-2. 왜 아무도 못 잡았나

**하트비트는 죽기 전에 써진다.** 워치독은 매 사이클 `watchdog_heartbeat.txt` 를 쓰고
그 뒤에 죽었다. 센티넬은 *"하트비트 신선 → OK"* 만 보므로 **16:02:54 에도 정상이라고
보고했다.** 감시의 감시가 같은 신호를 봤다.

마지막 워치독 로그는 **15:40:53**, 복구는 **16:35:53**.

### 1-3. 조치

3 바이트 파일을 백업하고 제거했다. `Test-Path` 가 거짓이 되어 `Load-State` 가 정상
경로(`New-Object PSObject`)를 타고, 상태 파일이 7,093 바이트로 재생성됐다.
**잃은 것은 스트라이크 카운터와 경보 중복제거 상태뿐이고 한 사이클이면 다시 쌓인다.**

복구 확인: `2026-08-28 16:35:53 [watchdog] sup=1 col=1 ... col_up=84297s free_gb=24.55
fetch_pct=100 auth_fail=0`. **`col_up=84297s`(23.4 h) 이므로 수집기는 재기동된 적 없다.**

### 1-4. 아직 안 고친 결함 둘

| | 무엇 | 상태 |
|---|---|---|
| **A** | `Load-State` 의 `try/catch` 는 **파싱 예외만** 막고 **빈 반환은 못 막는다.** 디스크가 또 차면 똑같이 재발한다 | **미수정** — 라이브 코드라 배포 창 필요 |
| **B** | 센티넬이 **하트비트 신선도**만 본다. 하트비트는 죽기 전에 써지므로 이 침묵을 원리상 못 잡는다 | **미수정** |

A 의 형태:
```powershell
try { $s = Get-Content $StateFile -Raw | ConvertFrom-Json
      if ($null -ne $s) { return $s } } catch { }
```

### 1-5. 코디네이터 오독 — `procs=0`

수동으로 워치독을 돌렸더니 `procs=0 process_dead` 가 나와 *"다음 사이클이 멀쩡한
수집기를 재기동한다"* 고 판단했다. **틀렸다.**

양성 대조: 같은 질의로 `node.exe` 의 `ExecutablePath` 는 읽히는데 **`python.exe` 넷은
`ExecutablePath` 와 `CommandLine` 이 전부 빈 값**이다. 수집기는 예약 작업의 **S4U 세션**
에서 떴으므로 내 세션에서는 못 읽고 워치독 자기 세션에서는 읽힌다.
**`procs=0` 은 내 실행의 산물이다.** 16:35:53 정규 사이클이 `sup=1 col=1` 로 확인했다.

## 2. 디스크 실태

```
C: 사용 373.5 / 400 GB,  여유 26.5 GB (16:31 기준, 24.55 GB @16:35)
워치독 free_gb 추이: 12.6 -> 10.3 -> 8.9 -> 3.5 -> 2.5 -> 0.02 -> 1.84 -> 2.59 -> 0

AppData\Local\wsl    103.9 GB   ext4.vhdx, 16:26 에도 쓰는 중
AppData\Local\Temp    69.5 GB   t0xx-* — 다른 프로젝트의 에이전트 작업 디렉터리
orca\targets          79.3 GB
orca\workspaces       19.3 GB
우리 data/             8.26 GB   tossmon.db 8.03 GB
```

**우리 수집기는 사용량 373.5 GB 중 8.26 GB 다.** 압박의 출처가 아니다.
`ext4.vhdx` 와 남의 `Temp` 는 이 레포의 관할 밖이다.

## 3. 측정 현황

`docs/79` 로 분리했다. 이 일지에 중복해 적지 않는다.

## 4. ★★★ 결정 — **매매 논제를 닫았다** (사용자 D-34)

사용자 요청으로 코디네이터(Fable 5)가 레포 전체와 라이브 DB(`mode=ro`, 탐색 B 6 세션만)를 다시 봤다.
결과는 `docs/80`. 한 줄: **몰림은 실재하고 그 폭도 실재하지만 파는 쪽에만 열려 있다** — 데이→프리
전환 −5.1%/일(6/6), 롱온리 24 시간 주기에 양수 구간 없음, 데이마켓 지정가 공급 시뮬 음수, 숏은
17:00 전환 뒤 −1.1% 에 최대 역행 +11%.

사용자: *"수집기는 좀 더 가공해서 계속 띄워놓고, 향후를 도모하자. 나머지는 끝내줘."*

집행: `docs/80` · INDEX 행 80 · README 상자 · PREREG 개정 10 · STRATEGY-VERDICTS §4.4-G ·
COORDINATOR-STATE 머리 행 · `tools/closure_20260828/`. 수집기 축소안은 **D-35** 로 사용자 대기 —
라이브 변경은 배포하지 않았다(`config_sig` 불변). 라이브 API 호출 0. 확증 팔·홀드아웃 미열람.

## 5. D-35 답 — **(나) 관측 모드**. 코드는 브랜치에, 라이브는 아직

사용자가 (나)를 골랐다. 같은 브랜치(`coord/20260828-closure`)에 넣은 것: 예시 설정(랭킹 60 s ·
tier-3 10 s/10 s)과 그 값을 고정하는 테스트, 워치독 결함 둘의 수정과 자가시험 W1·W2. **라이브는
건드리지 않았다** — `config_sig` 불변, 수집기 무접촉. 배포 순서와 검증 항목은 `USER-INPUT-QUEUE`
D-35 답 블록. `rankings_snap` 보존정책은 일주일 실측 뒤로 보류했다.

