# 2026-08-20 (목) — **커버리지가 두 번째로 늘었고, 어젯밤 먹통 프로세스가 아직 살아 있다**

> 일일 코디네이터 무인 실행. 09:3x ~ 1x:xx KST.
> 현재 상태는 `COORDINATOR-STATE.md`. 사용자 결정 대기는 `USER-INPUT-QUEUE.md`
> **D-26(다) · D-28 · D-29**.

## 0. 한 줄

**08-19 창 판정 `exit 0` — B top-10 41.9%, A top-10 40.3%.** 둘 다 08-17 보다 크게 올랐고
**A 가 시뮬 55.1% 쪽으로 절반 이상 좁혀졌다.** 그리고 **어젯밤 먹통이던 프로세스 넷이
아직 살아서 3 시간 24 분 만에 깨어났다는 것을 찾았다** — W5 에 넘겼다.

---

## 1. 수집 건강 — **창은 지금까지 중 가장 깨끗했다. 그런데 판 위엔 수집기가 둘이다**

### 1-1. 어젯밤 창 (08-19 22:30 ~ 08-20 05:00 KST)

| | 값 |
|---|---|
| `rankings_snap` distinct `snap_ms` | **5,130** |
| 60 초 초과 공백 | **0 건** |
| head / tail | 7.5s / 1.3s |
| **미관측 합계** | **8.8 초 = 0.037%** (예산 117 초) — **08-18 의 0.041% 보다도 낮다** |
| `orderbook_snap` | 42,724 행 (distinct 42,466), 13:30:00 ~ 19:59:59Z 연속 |
| `config_sig` | distinct **1** (telemetry 78 줄) |

**개장 6.5 분 전에 살린 것이 그대로 값을 했다** — `head_s` 가 예산을 안 먹었다.

### 1-2. ★ 그런데 `col=2` 다 — 수집기가 둘이고 워치독은 그것을 정상으로 읽는다

```
사슬 A (스케줄러 경로)   cmd 46064(22:10:57) -> py 41076 -> 50476 -> 41104 -> 49284
사슬 B (사용자 콘솔)     ps  26096(22:20:22) -> py 28212 -> 50724   <- 토큰 리스 보유자
```

**사슬 A 는 어젯밤 "먹통" 으로 판정했던 바로 그 넷이다.** 죽지 않았다.

- **22:11:20 에 떠서 01:35:41 까지 로그를 한 줄도 안 썼다 = 3 시간 24 분.**
  그 뒤 `resumed` -> `collector start` -> `COLLECTION-CONFIG start` 로 **정상 기동**했다.
  **재기동이 아니다** — `Win32_Process` 생성시각이 22:11:20 그대로다.
- 01:36:06 부터는 **토큰 리스를 못 얻어** 10 초마다 `AUTH-FAILURE` 를 쓴다
  (09:32 까지 **2,482 줄**). 보유자는 사슬 B 의 pid 50724.
- **telemetry 는 한 줄도 안 낸다** -> 판정 러너가 읽는 계열은 **오염되지 않았다**
  (`proc_uptime_s` 가 단조 증가하는 단일 계열임을 확인했다).
- 워치독 09:30:53 `sup=1 col=2 rank_age=1 fetch_pct=100` — **경보 없음.**
  08-19 §9-4 의 *"세 층이 전부 '프로세스가 있다' 를 성공으로 읽는다"* 가 지금도 참이다.

> **이것이 어제 세운 두 가설을 다 죽인다.** 파이프 교착은 어젯밤 정독이 죽였고,
> *"자식이 로깅 설정 전에 멈춘다 = 영구 사망"* 은 이 관측이 죽였다.
> **막히는 것에는 끝이 있고 그 길이가 3.4 시간이다.**

### 1-3. ★ 토큰 리스가 계획 하나를 무효로 만든다

`tossmon/api/tokens.py:143-146` — 리스 파일 경로는 **`client_id` 해시 하나**로 정해진다
(`state_path`·`cwd`·config 안 들어감). `_acquire_lease` 는 `timeout=0`.

-> **사용자 콘솔 수집기가 사는 한 라이브로 두 번째 수집기를 못 띄운다.**
어제 아침 인계의 *"새 경로가 로그를 쓰는 것을 확인한 뒤에 갈아탄다"* 는
**라이브에서 원리상 불가능**하다. 확인은 **비라이브로만** 된다. -> W5 게이트 3 을 고쳐 적었다.

### 1-4. 오늘의 `ALERT_`

| 파일 | 무엇 |
|---|---|
| `ALERT_*_log_auth_failure.txt` (01:41 이후 매 시각) | **위 사슬 A 다.** 새 결함이 아니라 같은 사건 |
| `ALERT_20260820_055056_task_result_..._collector_oneshot` | 08-19 22:09:31 실행이 `result=1`. 어젯밤 장애 때의 것 |
| `ALERT_20260820_013608_task_result_tossmon_watchdog` (`0x800710E0`) · `013317_watchdog_silent` | 01:33 에 하트비트 2,068 초 노후 -> 재킥. **그 뒤로는 5 분 주기가 정상** |

**조치 안 함** — 전부 어젯밤 장애의 꼬리이고, 수리는 W5 태스크 하나로 묶었다.

---

## 2. ★★ D-21 판정 (08-19 창) — **두 번째 통과. 그리고 A 가 크게 움직였다**

`tools/d21_verdict.py --session 2026-08-19` -> **exit 0**.
전문: `coordination/daily/2026-08-20_d21_verdict_output.txt`.
**기준선 자가검사 10 행 + 요약 넷 전부 OK.**

| | 08-17 (첫 판정) | **08-19 (오늘)** | 밴드 |
|---|---:|---:|---|
| **B (테이프) top-10** | 29.1% | **41.9%** (62 중 26) | **> 25.9% -> 늘었다** |
| **A (좌석) top-10** | 21.8% | **40.3%** (62 중 25) | **HALF MATCHED (25~45%)** |
| B / A top-100 | 5.9% / 2.0% | **5.8% / 4.1%** | |
| 비용 `capacity_fill` | 106 | **149** | 기준선 최소 155 보다 **여전히 낮다** |
| `ranking_tier3` / `hold_expired` / 쿨다운 위반 | 58 / 35 / 0 | **128 / 104 / 0** | 사전등록 §5 (4) |
| `rank_peak_1s` (n=78) | max 3 | **max 3, p95 3** | `docs/62` §5-3 의 3+1=4 <= 5 성립 |
| `md_peak_1s` (n=78) | — | **max 10 · p95 10** · hist `{6:1,7:1,8:8,9:19,10:49}` | **78 중 49 가 천장 10 에 닿는다** |

**A 가 21.8% -> 40.3% 로 뛰었다.** 08-18 판정이 남긴 열린 안건(*"A 가 시뮬 예측
55.1% `[미재현]` 과 크게 어긋난다"*)이 **절반 이상 좁혀졌다.** 아직 밴드는
`HALF MATCHED` 이고 **왜 좁혀졌는지 아무도 안 봤다** — 세션이 둘뿐이다.

> **여전히 "돈이 된다" 가 아니다.** 사전등록 §4 제한이 그대로 붙는다 —
> **6 세션 기준선에 두 세션**이고 교환가능성 논증은 신뢰구간이 아니다.
> **"확인됐다" 라고 쓰지 마라.**

### 2-1. 무효 조건 (3) 프로브 — **러너가 걸었고 내가 갈랐다. 오탐이다**

러너가 창 안 mtime 을 가진 fixture **둘**을 신고했다
(`tests/fixtures/live/live_rankings_top_{gainers,losers}_1d_d.json`, mtime 08-19 23:51:19).

**셋을 대조해 오탐으로 판정했다:**

| 관측 | 값 |
|---|---|
| 파일 안 `captured_at_ms` | **1787059862172 = 2026-08-18 22:31:02 KST** — **08-18 프로브** 때 캡처 |
| 그 파일이 들어온 커밋 | `4ccce74` (W1), **PR #40 병합 `ebe2a3c` = 08-19 23:50:38** |
| mtime 과의 차 | **41 초** -> `git merge` 가 작업 트리에 쓴 시각이다 |

**전 워크트리 훑기**: 창 안 mtime 을 가진 JSON 은 루트의 그 둘과 `w3-analyzer` 의
넷(01:09:59 = git 체크아웃, 01:51:04 = W3 자신의 분석 산출물)뿐. **`probe_*` 산출물은
08-18 것 하나뿐이고 08-19 것은 없다.** -> **조건 (3) 미발동. 창은 유효하다.**

> **mtime 은 프로브의 증거가 아니다.** 러너가 쓸 수 있는 것이 그것뿐이라 그렇게 적혀
> 있고, 그래서 *"OPERATOR MUST CONFIRM"* 이 붙어 있다. **그 확인이 이 절이다.**

### 2-2. 확증이 열렸다

08-13 이후 정규장이 **08-13 · 14 · 17 · 18 · 19 = 5 개**. 거래일 군집 5 를 채웠다.
**다만 그것을 쓰기 전에 `USER-INPUT-QUEUE` D-28 이 먼저 답해져야 한다** (아래 §4).

---

## 3. 워커 수거 — **PR #45 병합 (게이트 다섯 전부 내가 재실행)**

| 게이트 | 결과 |
|---|---|
| 1. `main` 흡수 | 3 커밋 뒤짐 — **그 셋이 전부 `coordination/`** 이고 `tossmon/ tools/ ops/ config/ tests/` diff **빈 출력**. 회귀 대표성 영향 0 |
| 2. merge-base 소유권 | `docs/64`·`docs/65`·`docs/INDEX.md`·`tests/`·`tossmon/analysis/measure/` **5 파일**. `coordination/`·사전등록·`ops/`·`config/` **무접촉** |
| 3. 전체 회귀 (w3-analyzer 워크트리, 그 venv) | **2,318 passed · 1 skipped · 4 deselected (380.01s, exit 0)** |
| | 산수: `main` 수집 **2,303** -> 브랜치 **2,319**, **+16** = 그 파일 하나의 증가분(37 -> 53)과 정확히 일치 |
| 4. 비 ASCII **증분** | 새 비 ASCII 줄 140 개, 그중 **`print()` 안 0 개** (전부 docstring·주석) |
| 5. 병합 후 스모크 | import OK · 그 파일 **53 passed** · **판정 러너 여전히 exit 0** |

**병합 확인은 내용으로**: `docs/65_forward_depth_and_tier.md` 존재(36,061 B),
`nbar300` 이 모듈에 **14 회**. `main` = **`d087ecf`**.

> **`main` 회귀 기준선이 낡아 있었다.** 상태 문서의 **2,287** 은 PR #39 시점이고
> **PR #40 병합(08-19 23:50) 뒤 아무도 다시 안 셌다.** 수집 기준으로 지금 `main` 은
> **2,303** 이다. **나는 `main` 단독 전체 회귀를 돌리지 않았다** — 수집(collect-only)
> 으로만 셌다. 그렇게 적는다.

**W1 태스크 `task_c2b64e01a0ec` 를 `completed` 로 닫았다** — PR #40 이 08-19 밤에
병합됐는데 태스크가 `ready` 로 남아 있었다.

**`dispatch_sweep`: 조치(ALERT) 0 건.** CHECK 1 건은 `task_0b343c9c2a66`(W4, 이미
PR #39 로 병합됨)의 워크트리 경로를 도구가 못 읽은 것이다.

---

## 4. 사용자 결정 — **큐가 낡아 있어서 세 개를 갱신하고 둘을 새로 세웠다**

어젯밤 세션이 사용자 답(**"가 가 가"**)을 받아 **집행은 했는데 큐 파일을 안 고쳤다.**
큐가 결정의 정본이므로 오늘 맞췄다.

| 번호 | 상태 |
|---|---|
| **D-17** | (가) 집행 완료. **다만 새는 곳은 RDP 가 아니라 Docker 였다** (§5) |
| **D-26** | **부분 답.** (가) 닫힘 · (나) 이미 나와 있었다 · **(다) G-1a 관문 판정은 아직 열려 있다** |
| **D-27** | (가) 집행 완료 = PR #45 |
| **D-28** 신규 | **사전등록 §5-1 이 요구하는 확인 문장을 지금 그대로는 못 쓴다.** 사전등록 안건이라 내가 안 정한다 |
| **D-29** 신규 | **수집기 라이브 전환.** 실패하면 되살릴 손이 사용자 콘솔뿐이라 무인으로 안 한다 |

### 4-1. ★ D-26 (나)에서 **내가 틀렸고 W1 이 잡았다**

내가 큐와 W1 명세에 *"그 29% 는 **12.4 초 폴로 잰 것**"* 이라고 썼는데 **거짓이다.**
29.4% 는 애초에 **1 초 팔**(08-07 프리마켓)에서 나왔고, `docs/35` §0-1 이 바로 그
인용을 막으려고 붙여 둔 표식이었다. **내가 그 표식을 넘어서 썼다.**

정규장 재측정(`docs/35` §7-2)은 **1 초 33.3% · 5 초 32.8% · 12 초 33.9%** — **셋이 같다.**
**빠지는 것은 우리가 놓친 것이 아니라 서버가 자기 격자를 건너뛰는 것이다.**

**`docs/00_HANDOFF.md` 106 행의 같은 오인을 오늘 정정했다** (취소선 + 근거).
`docs/58`·`59`·`62`·`64` 에 남은 자리는 **손대지 않았다** — 남의 소유이고 목록은
`docs/35` §7-5 에 있다.

### 4-2. D-28 이 왜 급하지 않고, 왜 그래도 필요한가

`E` 를 **지금 고를 수 없다.** PR #45 가 *"깊이 격차는 여전히 열려 있다"* 로 끝났고
분리가 안 된 정의를 얼릴 수는 없다. **다만 `E` 를 고르는 날 이 답이 먼저 필요하다.**

---

## 5. 디스크 — **어제 16 GB 를 벌었는데 오늘 다시 빠지고 있다**

```
08-20 01:0x  RDP 추적 3,634 파일 16.01 GB 삭제  ->  여유 26.78 -> 42.75 GB
08-20 08:55  45.87 GB
08-20 09:15  45.86        <- 여기까지 평평
08-20 09:20  44.22  (-1.64)
08-20 09:25  43.53  (-0.69)
08-20 09:30  43.21  (-0.32)
08-20 09:5x  43.19        <- 멈췄다
```

**09:15~09:30 에 2.67 GB 가 빠졌고 원인을 못 붙였다.**

- **우리 것이 아니다** — `tossmon.db` 는 08-19 4.85 -> 오늘 **5.22 GB** (하루 +0.37),
  `collector.stdout.log` 는 **205.7 MB 로 안 자란다**
- 그 창에 크게 쓰인 파일은 `docker_data.vhdx` **하나**뿐(08-19 23.53 -> 오늘 **24.56 GB**).
  **vhdx 는 한 번 커지면 저절로 안 줄어든다**
- **내 세션 탓이 아니다** — 감소가 시작된 09:20 워치독 표본은 내 첫 명령보다 앞선다

**아무것도 안 지웠다.** Docker 는 D-17 의 (나)이고 사용자가 안 골랐다.

---

## 6. 새로 시킨 일 — **W5 `task_5e2073cc2256`** (opus/high, 기한 오늘 20:00 KST)

명세 `coordination/specs/w5_supervisor_pipe.md` 에 **§2-2 · §5-1 · §5-2 · §5-3 을 추가**했다.

- **게이트 3 을 고쳐 적었다**: 라이브로는 리스 때문에 불가능하므로
  **`TOSS_LIVE=0` + scratch DB 로 재현 -> 고침 -> 같은 구성에서 초 단위 기동 증명**
- **라이브 전환은 워커에서 뺐다** (D-29)
- **사슬 A 를 먼저 죽이지 마라** — 이 결함의 살아 있는 표본이다. 관측을 다 뽑은 뒤에
- `STOP` 파일 금지(둘 다 멈춘다), 사용자 콘솔 프로세스 금지

> **08-19 의 프로세스 구멍을 고쳤다** — 오케스트레이션 태스크를 **먼저 만들고**
> `worker-start` 로 붙였다. 터미널 메시지로만 지시하지 않았다.
> 그리고 **w5-ops 의 `coordination/` 사본이 낡았으므로** 명세를 루트 절대경로로
> 읽으라고 별도 메시지를 보냈다.

**모델을 sonnet/high -> opus/high 로 올렸다** — 3.4 시간짜리 미규명 지연이고
라이브 운영에 닿는다. 상태 문서 §4 의 W5 행에 적는다.
