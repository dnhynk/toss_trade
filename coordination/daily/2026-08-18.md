# 2026-08-18 (화) — **D-21 판정이 났다.** 그리고 그 판정을 하마터면 못 낼 뻔했다

> 일일 코디네이터 무인 실행. 09:3x ~ 10:2x KST.
> 이 파일은 **그날의 기록**이다. 현재 상태는 `COORDINATOR-STATE.md` 를 봐라.

## 0. 한 줄

**D-21 은 통과했다** (B top-10 **29.1%** > 25.9%). 판정 러너가 처음엔 `exit 4` 를 냈는데,
**데이터가 아니라 러너가 로그 파일을 못 찾은 것**이었다.

---

## 1. 인수

- 내 핸들 `term_9537dfec-f055-4d73-9b85-063bccba2e99`, Run `run_92948a1f80a5` **바인딩 성공**
  (`consumer_generation: 22`). 08-12 에 우려했던 런타임 교체는 이번엔 문제 없었다.
- **08-16(일)·08-17(월) 아침 세션의 커밋이 없다.** `main` 은 08-15 의 `e684c8a` 에서 멈춰
  있었고 `daily/` 에도 그 이틀 파일이 없다. 그 이틀에 정규장이 없었던 것은 맞지만
  (08-16 은 일요일), **세션이 돌았는지 자체를 나는 모른다.** 08-17(월) 아침 세션은
  돌았어야 한다 — §5 에 적었다.

## 2. 수집 건강

### 2-1. 측정 창은 깨끗했다

창 = **08-17 13:30 ~ 20:00 UTC** (= 08-17 22:30 ~ 08-18 05:00 KST).

| | |
|---|---|
| `rankings_snap` distinct `snap_ms` | **5,079** (첫 스냅 창 시작 +4.5 초, 마지막 창 끝 −2.3 초) |
| 미관측 시간 (§6-2) | **106.2 초 = 0.454%** — 예산 117 초(0.5%) **안** |
| `config_sig` | **1 종** (telemetry 77 줄) |
| 워치독 | 창 안에서 **91 회** 정상 실행, 최대 구멍 **5.3 분** |
| 프로브 | **안 쐈다** — 전 워크트리에서 창 안 mtime 파일 **0 개** |

### 2-2. ★ **기계가 잤다** — 어제 두 번

워치독은 schtasks 로 5 분마다 도는데 그 로그에 구멍이 둘 있었다. **워치독이 안 돈 것이
아니라 기계가 안 깨어 있었다.**

```
08-17 00:32:54 -> 11:41:02    668.1 분 (11.1 시간)
08-17 16:05:54 -> 16:39:25     33.5 분
```

**둘 다 휴장이라 잃은 수집은 0 이다.** 그런데 **대가는 이미 치렀다**: 16:39 에 깨어나자
랭킹이 **2,028 초** 늙어 있었고 워치독이 그것을 *"랭킹 루프가 조용히 죽었다"* 로 읽어
**멀쩡한 수집기를 재시작**했다. 잠든 2,011 초와 **17 초 차이**다 — 같은 사건이다.

→ **워치독 결함**: 자기 하트비트 구멍을 안 본다. 큐 **D-24** (전원 설정은 사용자 손).

### 2-3. 그 밖

- **08-16 에 프로세스 사망 4 회** (04:00·13:56·14:06·23:50). 이건 잠이 아니다 —
  그 시각들엔 워치독 구멍이 없다. `supervisor.stdout.log` 는 여전히 **줄에 시각이 없어서**
  원인을 못 가른다 (§2 의 열린 결함이 또 값을 치렀다).
- **`ALERT_20260817_231556_tier2_orderbook_flat`** (창 **안**, 23:15). 경보문은
  *"skip 카운터가 안 움직였으니 스윕이 도는 게 아니다"* 라고 단언하는데,
  **텔레메트리를 보면 `tier2_orderbook_skipped` 는 계속 늘었다** (106,352 → 106,942,
  25 분간 +590). `tier2_orderbook_snaps` 만 90,584 에 멈춰 있다가 23:25 에 재개했다.
  → **경보의 근거가 관측과 어긋난다.** 세 번째 스킵 사유가 있는데 경보가 그것을 안 센다.
  호가 자체는 창 전체에서 정상이었다(distinct `snap_ms` 45,415, 60 초 초과 공백 4 건).

---

## 3. ★ D-21 판정 — 났다. 통과다

### 3-1. 처음엔 `exit 4` 였다 — **그리고 그건 데이터 문제가 아니었다**

```
(2) config_sig invariance : distinct=0, telemetry lines=0      TRIGGERED
      no config_sig in window -- cannot show the config was unchanged
      log files read: collector.log(552 lines)
```

`collector.log` 가 **552 줄**이고 첫 줄이 **08-18 06:44:10** 이었다. 창이 통째로 없다.

**원인**: 08-18 06:44 에 수집기 자신의 `RotatingFileHandler` 가 32 MiB 에 닿아
`collector.log` → **`collector.log.1`** 로 회전시켰다. 창(08-17 22:30 ~ 08-18 05:00)은
**전부 `.1` 에 들어갔다.**

**러너는 그 이름을 모른다.** `tools/d21_verdict.py:215` 의 `log_sources()` 는
`collector.<stamp>.log.gz` — 즉 **`ops/rotate_logs.py` 방식만** 찾는다.
그리고 **그 회전은 이 프로젝트에서 한 번도 성공한 적이 없다**(§2-3 의 그 결함:
Windows 에서 수집기가 파일을 잡고 있어 `rename` 이 죽는다).

> **네 번째로 같은 모양이다** — 계측기가 자기 자신에 대해 거짓말했다.
> 러너는 *"config 가 바뀌었다"* 가 아니라 *"못 보겠다"* 를 말한 건데
> **종료코드가 둘을 구분하지 않는다.**

### 3-2. 손으로 먼저 확인하고 나서 다시 돌렸다

**순서를 지켰다** — 러너를 다시 돌리기 **전에** `collector.log.1` 을 직접 grep 해서
창 안 telemetry **77 줄**, `config_sig` **정확히 1 종**임을 확인했다. 즉 §3-4.2 가 묻는
사실(*"config 가 안 바뀌었나"*)의 답은 **러너와 무관하게 참**이다.

**자를 바꾼 것이 아니다.** 규칙·문턱·기대값 상수는 하나도 안 건드렸고,
러너에게 **창을 담고 있는 파일을 준 것**뿐이다 (`--log` 는 원래 있는 인자다).

```
.venv/Scripts/python.exe tools/d21_verdict.py --session 2026-08-17 \
    --log C:/Users/dongh/orca/workspaces/toss_trade/w5-ops/data/collector.log.1
```

전문은 `coordination/daily/2026-08-18_d21_verdict_output.txt`.

### 3-3. 결과

자가검사 **10 행 전부 OK**, 요약 넷 전부 OK → `SELF-CHECK: PASS`.

| | 값 | 밴드 |
|---|---:|---|
| **B (테이프) top-10** | **29.1%** (55 중 16) | **> 25.9% → 늘었다** ✅ |
| A (좌석) top-10 | 21.8% (55 중 12) | 시뮬 예측 **55.1%** `[미재현]` 과 어긋남 |
| B / A top-100 | 5.9% / 2.0% | |
| 비용 `capacity_fill` | **106** | 기준선 최소 155 보다 **낮다** |
| `ranking_tier3` 승격 / `hold_expired` 해제 / 쿨다운 위반 | 58 / 35 / **0** | |
| `RANKING` 첨두 | **3** (n=77, p95 3) | `docs/62` §5-3 의 3+1=4 ≤ 5 **성립** |

**무효 조건 넷 전부 통과** (0 forward-only / 1 미관측시간 / 2 config_sig / 3 프로브 —
(3)은 내가 전 워크트리를 훑어 확인했다).

> **이것은 *"돈이 된다"* 가 아니다.** *"잴 수 있게 됐다"* 다. G-2 · G-3 는 아직 안 열었다.
> 사전등록 §4 제한이 그대로 따라붙는다 — **6 세션에 한 세션이다. *"확인됐다"* 라고 쓰지 마라.**

### 3-4. ★ 개정 1(D-23)이 없었으면 오늘도 판정이 없었다

이 창엔 60 초 넘는 공백이 **5 건**(최대 122.2 초) 있었다.
**옛 규칙(단일 공백 > 60 초)이면 또 무효였다.** 사용자가 08-15 에 고른 (다)가
실제로 일을 했다.

---

## 4. ★ 08-15 에 배포한 계측이 첫 정규장에서 답을 냈다 — **토큰 락 가설은 죽었다**

`docs/63` 이 남긴 **유일한 단일 후보**는 *"`TokenManager._alock` 을 쥔 채 15 초 토큰 POST
+ 최대 3 회 재시도(최악 63.5 초)"* 였다. W4 는 *"확인 못 했다 — `retries`·`http_5xx`·
`auth_refresh` 가 텔레메트리에 안 나가서"* 라고 적었고, 그 셋을 내보내는 계측만 넣었다.

**그 계측을 실은 첫 정규장이 08-17 밤이다. 결과:**

| 공백 시작 (KST) | 길이 | 품은 telemetry 버킷 | 간격 | Δretries | Δhttp_5xx | Δauth_refresh |
|---|---:|---|---:|---:|---:|---:|
| 08-17 22:31:19 | 71.2s | 22:30:00 → 22:35:02 | 302s | +5 | +5 | 0 |
| 08-18 03:15:03 | 69.6s | 03:12:28 → 03:17:34 | 306s | **0** | 0 | 0 |
| 08-18 03:20:56 | 64.2s | 03:17:34 → 03:22:40 | 306s | +1 | +1 | 0 |
| 08-18 03:34:53 | 72.1s | 03:32:43 → 03:38:20 | **337s** | **0** | 0 | 0 |
| 08-18 03:42:23 | **122.2s** | 03:38:20 → 03:44:25 | **365s** | **0** | 0 | 0 |

**`auth_refresh` 는 창 전체(6.5 시간) 0 → 0 이다.** 프로세스 수명 16.7 시간 내내 0.

→ **가장 큰 공백 셋에서 재시도가 한 번도 안 일어났다.** 122.2 초짜리가 특히 결정적이다.
**재시도가 0 인데 재시도로 설명할 수 없다.**

### 4-1. 대신 새 사실이 하나 나왔다 — **telemetry 자신이 늦었다**

창 안 76 개 버킷 중 300 초 대비 **20 초 넘게 밀린 것은 정확히 둘**이고, 그 둘이
**가장 큰 공백 둘과 같은 구간**이다 (+37 초 / +65 초). 작은 공백들(64~70 초)은
telemetry 를 **안 밀었다**(+2·+6·+6 초).

**주기 태스크가 같이 늦었다**는 뜻이다 — 랭킹 루프만의 문제가 아니다.
밀림이 공백 길이의 **약 절반**이다(0.51, 0.53). **기전은 모른다.** W4 에 넘겼다.

### 4-2. 그리고 *"개장 첫 70 분 몰림"* 은 재현 안 됐다

08-14 는 넷 다 개장 첫 30 분이었는데, **08-17 은 다섯 중 넷이 마감 1~1.5 시간 전**
(18:15~18:42 UTC)이고 하나만 개장 직후다. → **안정된 성질이 아니다.**

---

## 5. 워커

- `dispatch_sweep`: **★ 조치(ALERT) 0 건** (창 밖 126 건). 손댈 것 없음.
- 태스크 126 개 전부 종결 (125 completed + 1 은 07-31 의 `probe after run bind` 스모크).
- **브랜치 다섯 전부 `main` 대비 ahead 0**, 열린 PR **0 개**.
- 라이브 워크트리(`w5-ops`, `e7c9437`)는 `main` 보다 6 커밋 뒤지지만
  **그 여섯은 전부 `coordination/`** 이고 **런타임 코드 diff 는 0** 이다 (직접 확인).
- 워커 터미널이 **하나도 없었다** → 새로 띄웠다.

### 새로 시킨 것 둘

| 태스크 | 워커 | 무엇 |
|---|---|---|
| `task_9ccdb6b764d5` | W3 (`feat/analyzer`) | 러너가 `collector.log.N` 을 보게 고쳐라. 합격 조건은 `--log` 없이 **exit 0 + 29.1/21.8/106** |
| `task_0b343c9c2a66` | W4 (`feat/collector`) | 토큰 락 반증을 받아 공백 진단 계속. **배포 금지·판정 금지** |

명세는 `coordination/specs/w3_verdict_log_rotation.md` · `w4_gap_token_lock_falsified.md`.
**디스패치 직후 화면을 읽어 둘 다 실제로 일을 시작한 것을 확인했다** (입력창에 앉아
있지 않다 — §0 의 그 함정).

> **실수 하나 기록**: `task-create --title` 로 두 번 호출했는데 그 플래그가 없어서
> (`--task-title` 이다) 조용히 실패했고, 내 파서가 그 오류를 삼켜 `None` 만 찍었다.
> **task-list 로 세어 보고서야 알았다.** 만들어졌다고 보고할 뻔했다.

---

## 6. 사용자 결정 대기 (큐에 적었다)

- **D-24** — 컴퓨터가 잔다. 전원 설정은 사용자만 바꾼다.
- **D-25** — 중간 관문을 넘었다. 다음이 (가) 프로브 / (나) G-2·G-3 / (다) 세션 더 쌓기.
  같이 볼 관측: **우리가 우리 천장에 붙어 있다** (표본 90.9% 에서 초당 최대가 천장 10,
  `docs/61` 의 48.4% `[미재현]` 의 거의 두 배. 서버는 15 를 준다).
- **D-23 은 닫았다** — 08-15 에 사용자가 답했는데 큐에 표시가 안 돼 있었다.
