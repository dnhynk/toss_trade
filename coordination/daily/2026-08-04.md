# 코디네이터 일일 기록 — 2026-08-04

> **그날의 기록이다. 고치지 마라 — 갱신은 새 날짜 파일로 한다.**
> `coordination/COORDINATOR-STATE.md` 에서 잘라 옮겼다 (2026-08-13, 원문 무수정).
> 현재 상태는 그 파일을, 문서 목록은 `docs/INDEX.md` 를 봐라.

---

## 4.6 일일 코디네이터 실행 기록 — 2026-08-04 09:30 (자동화)

**수집: 정상.** 지난 24h 랭킹 폴 25,723회(중앙 간격 0.3s), 90초 초과 공백 **단 2건**
(00:08→00:11 배포 2.1분, 08:49→09:00 아래 참조). 캔들 259,654 / 체결 107,130 /
호가 48,214 / 이벤트 42. `api_errors=0`, `auth_failures=0`, `fetch_success_pct=100.0`,
`watch_outside_universe=0`.

**★ 어젯밤 tier3 수정이 실제로 들었다 (DB 직접 확인, 워커 보고 아님):**

| 시각(KST) | tier3 승격/시간 | 체결 수집 종목 |
|---|---:|---:|
| 00:00 (고사 중) | 5 | 47 |
| 01:00 (최저) | **1** | 47 |
| 02:00 (`fill_to_capacity` 배포 후) | 39 | 84 |
| 03:00 | 67 | **101** |
| 04:00 | 90 | **107** |

사고 전(08-03 23:00) 78종목보다 **높다.** `tier3_capacity_fills` 가 계속 오른다(393).
**정원을 상대 순위로 채우는 방식이 맞았다.**

**오늘 아침 발견한 결함 2건 — 둘 다 "거짓 CRIT" 계열이다** (이 프로젝트가 세 번 데인
"성공을 반환하는 조용한 실패"의 거울상: **정상을 사고로 보고하는 시끄러운 실패**):

1. **워치독이 멀쩡한 수집기를 재시작했다** (08:55, 실제 발생). 근본 원인 확정:
   `closed` 세션에는 랭킹 루프가 쉬고(설계대로) 활동이 없어 `collector_state.json` 이
   **`session="after"` 인 채로 얼어붙는다**(마지막 저장 08:49:44). 어제 W5 가 넣은
   `c5a3a5f`("상태 파일을 1차 소스로")가 그 언 값을 믿어 `$openSession` 가드를 통과하고,
   얼어붙은 `last_ranking_snap_ms` 로 계산한 age 382s 로 CRIT + 재시작.
   **08:50~09:00 KST 는 토스 캘린더에 세션이 없는 구간**(after 05:00–08:50 / day 09:00–17:00)
   이라 **매일 재발한다.** DB 실측: 폴이 08:49:59 에 끊기고 09:00:00 에 정확히 재개(그 사이 0건).
   → **어제의 실명 수정이 오늘의 거짓 경보를 만들었다.** W5 `task_0c77aa33b2fc` 발행.
2. **`schema_mismatch` CRIT 5건이 전부 `http-404 code=stock-not-found`** (AVAT·BLRK·CABR·
   MACI·JSM = 없는/상폐 종목). 경보 문구는 "API 응답 모양이 바뀌었다, 재시작으로 안 고쳐진다,
   **오늘 데이터를 믿기 전에 계약을 검사하라**" — 멀쩡한 하루를 의심하게 만든다.
   `_guarded` 가 `SchemaMismatch` 를 통째로 한 카운터에 넣는 것이 원인. W4 `task_8280709b754a`.

**부수 발견 — 429 는 우리 사용량과 무관하다.** 오늘 09시 7건, 사용량은 한도의 15~20%
(`MARKET_DATA 1.05~1.40/7.00`, `api_errors=0`). 발생 시각이 **전부 KST 09~15시**(미국장 마감 중)
이고 **정규장·프리·애프터에는 3일간 0건**이다. 그런데 대응이 **티어 정원 축소**라
오늘 tier3 정원이 6→4→3→2 로 깎였다. 장이 닫혀 무해했지만 **같은 일이 22:30 개장 직후에
나면 어제 우리를 태운 tier3 고사가 재발한다.** W4 태스크 (2)번으로 조사 지시(수정 의무 아님).

**워커 상태 정정**: `task_7aad9a516fa0`(W6 룩어헤드 테스트 게이트)는 08-03 14:32 KST 에
dispatched 였으나 **워커에 도달한 적이 없다** — W6 터미널은 프롬프트에서 놀고 있었고
`w6-audit` 브랜치는 감사5 문서 이후 움직이지 않았다. **`ready` 로 되돌렸다**(추적기가
"진행 중"이라고 거짓말하지 않게). 오늘 재디스패치하지 않은 이유는 **디스크뿐**이다 —
큐 D-7 참조. 디스크가 풀리면 바로 올려라. 급하지 않지만 가치는 그대로다.

**미해결 (내가 못 하는 것)**: 디스크 여유 6.71 GB, 시간당 0.7 GB 하락, 상위 소비자가
WSL 10.6 GB / Docker 10.0 GB / Claude VM 8.7 GB — **전부 사용자 소유**. 수집 데이터는
1.0 GB(전체의 3%)뿐이라 **내가 지워서 벌 수 있는 게 없다.** 큐 D-7.

**운영 메모**: `orchestration` 바인딩이 이 세션에서 **두 번 풀렸다**(`consumer_generation`
4→6). `run_required` 가 나오면 조용히 실패하니 — `task-create` 한 건이 그렇게 유실됐다 —
**`--json` 출력의 `"ok"` 를 매번 확인하고, 실패하면 `run-use` 로 재바인딩 후 재시도하라.**

---

## 4.8 ★ 개장 사고 — 예산 거버너가 정원을 통째로 날렸다 (2026-08-04 22:30, 실시간 관측)

**사용자가 "개장을 지켜봐"라고 지시해서 코디네이터가 붙어 있다가 5분 만에 잡았다.**
자동 감시(워치독)는 **이 사고를 못 잡는다** — 프로세스도 API 도 멀쩡했고
`api_errors=0 auth_failures=0 fetch=100%` 였다. **에러 없이 수집량만 사라지는** 종류다.

```
22:30:09  개장. 정원 리셋 tier2_cap=300 tier3_cap=10, tier2=18 tier3=3      <- 정상
22:30:38  ERROR CHART 1초에 6회(한도 5) + "정원이 아니라 리미터가 1초 창을 못 지킨다"
22:31:22  ERROR MARKET_DATA 1초에 11회(한도 10), 같은 문구
22:33:14  budget shrink {MD:9, CHART:299} -> caps={tier2:1, tier3:1} demoted=316
22:35:11  tier2=3 tier3=1 tier2_cap=2 tier3_cap=2
```

**크기**: 어제(08-03) 같은 시간대 tier2 는 **307~314 종목**. 오늘은 **3** 이었다.

**핵심은 코드가 옳은 진단을 찍고 틀린 행동을 한다는 것이다.**
경보 문구가 *"티어를 깎아도 같은 버스트는 또 난다"* 라고 말해놓고 티어를 깎았다.
1초짜리 **버스트**(타이밍 문제)에 **정원**(지속 속도 손잡이)으로 대응했다.
> **"성공을 반환하는 조용한 실패" 다섯 번째 — 이번엔 사촌이다: 정확한 진단을 출력하고 무관한 조치를 취함.**

**비대칭이 치명적이다**: 내려갈 땐 한 번에 **299**, 올라올 땐 **+1**(5분에 한 번).
tier3 복구 ~40분, **tier2 복구 ~25시간**. 즉 **한 번 깎이면 그날 밤은 끝난다.**

**조치 (사용자 승인 후 코디네이터 집행, 22:39~22:41)**: §4.7-3 절차 그대로 —
`PLANNED` 마커 -> `STOP` -> 종료 확인 -> `STOP` 제거 -> **워치독이 되살림**.
`schtasks /run` 은 쓰지 않았다. **공백 78초**(22:39:44~22:41:02, 예상 5~6분보다 짧았다 —
워치독 5분 주기가 마침 맞았다). 재기동 후 `tier2_cap=300 tier3_cap=10 over_limit_1s=0`,
축소 재발 없음.

**원인 미확정 — 오늘 배포가 둘이라 못 가른다.** ① 초당 첨두 판정(13:09) ② `usage_ratio` 0.85(13:39).
②가 원인처럼 보이지만, ①이 없었으면 **어제도 똑같이 넘었는데 못 봤을 뿐**일 수 있다.
그래서 `usage_ratio` 를 임의로 되돌리지 않았다. W4 `task_cc850c75a2fd` 가 이걸 가른다.

**교훈**: 이날 아침 §4.6 이 *"같은 일이 22:30 개장 직후에 나면 tier3 고사가 재발한다"* 고
정확히 예측했다. **예측해놓고 막지 않았다.** 알려진 재발 위험은 관찰 항목이 아니라 작업 항목이다.

### 4.8-1 해결 (같은 날 23:36 배포, main `ff83d68`)

**원인은 `usage_ratio` 가 아니라 13:09 배포(초당 첨두 판정)였다.** W4 가 시뮬로 갈랐다 —
사고 당시 코드에 첨두 7 을 넣으면 **0.70 에서도 0.85 에서도 `drop=299` 로 동일**하고,
13:09 이전 평균 기반 규칙이었으면 429 바닥값인 30 만 나왔다. 코디네이터의 라이브 실험
(22:49 롤백 -> 5분 만에 재발)과 독립적으로 같은 답에 닿았다.

**산수 전모**: `ceil(excess/per_symbol_cost)`, tier2 한 종목 = 1/110 = **0.00909 req/s**.
CHART 첨두 7 -> 원 지시 **350** -> `min(n, have-1)` 에 잘려 299.
**첨두가 정확히 한도(5)일 때조차 130종목 제거를 지시한다** — 넘지 않아도 깎는다.

**진짜 결함은 계측이었다** (W4 자기 회귀로 인정): `_unaccounted_attempts` 가 **전역**
요청 카운터 델타를 통째로 한 그룹에 얹었다. 평균을 보던 시절엔 "총량 정확·방향 보수적"
이라 넘어갔고 **판정이 초당 첨두로 바뀐 순간 사고가 됐다.** 붉은 테스트가 실물로 보여준다:
**CHART 가 실제 2콜을 내고 11 로 계상**됐다.

**수정 3건**: ① 축소 판정·크기를 지속률로(첨두는 경보·텔레메트리 계속) ② 귀속을
`호출+재시도`로 자르고 나머지는 `budget_unattributed_attempts` 로 명시 ③ `SHRINK_MAX_STEP_FRAC=0.25`
로 축소를 회복과 대칭으로.

**코디네이터 독립 검증 (직접 재실행·직접 파괴):**
- 전체 1544 passed·1 skipped 재현.
- 주 수정(첨두->지속률) 되돌리면 **빨감**. 귀속 자르기 되돌리면 **빨감**(`assert 11 <= 2`).
- 축소를 통째로 끄면 **13개 빨감** — `진짜 지속 과부하는 여전히 축소`, `실제 429 는 워밍업
  중에도 즉시 축소` 포함. **경보를 그냥 끈 게 아니라는 증거.**
- **구멍 1개 발견·기록**: `SHRINK_MAX_STEP_FRAC` 을 1.0(상한 없음)으로 바꿔도 **132개 전부 통과**.
  2차 방어선이 시험되지 않는다. **후속 테스트 필요.**

**배포 후 실측 (23:41)**: `tier2=310 tier3=10 cap2=300 cap3=10` — 어제 수준 완전 복구.
그리고 **같은 버스트가 여전히 난다**(배포 후 `CHART 1초에 6회`·`MD 1초에 12회` 4건)
그런데 **축소 지시 0건**. 탐지는 그대로, 반응만 바뀌었다.

**남은 수수께끼 (W1 이 내일 잰다)**: 그 첨두 경보가 뜰 때 **`http_429` 는 0** 이다.
낮에는 반대로 `over_limit_1s=0` 인데 429 12건이었다. **계측기가 서버와 양방향으로 어긋난다.**
`docs/32` 참조. 그리고 `docs/30` §2 가 "고쳤다"고 적은 429 헤더 계측은 **안 고쳐져 있다** —
`client.last_429` 를 `loops.py` 가 한 번도 읽지 않는다(W1 발견, 다섯 번째 조용한 실패).

**오늘 밤 정규장의 인공 구멍 3개** (분석 시 사고로 읽지 말 것):
22:39:44~22:41:02(78s) / 22:49:16~22:51:09(113s) / 23:34:46~23:36:10(84s) — 전부 계획 정지.
그리고 **22:33~23:36 은 정원이 깎인 채 운영**돼 커버리지가 정상이 아니다(tier2 1~21종목).

> **교훈 — 하루에 배포를 넷 하면 사고의 원인을 못 가른다.**
> 오늘 밤 원인 규명이 오래 걸린 유일한 이유가 그것이다. 13:09 와 13:39 가 붙어 있어서
> 라이브 롤백 실험을 한 번 태워야 했다. **배포는 하나씩, 그리고 개장 같은 부하 전환을 한 번 넘겨보고.**
