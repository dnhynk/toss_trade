# 2026-08-21 (금) — **세 번째 통과, 그리고 자가 복구가 처음으로 제 몫을 했다**

> 일일 코디네이터 무인 실행. 09:3x KST 시작 → 09:4x 에 세션이 끊겼다가 **19:0x 에 재개**
> (사용자가 코디네이터와 워커를 함께 continue 시켰다).
> 현재 상태는 `COORDINATOR-STATE.md`. 사용자 결정 대기는 `USER-INPUT-QUEUE.md`
> **D-26(다) · D-28 · D-30**.

## 0. 한 줄

**08-20 창 판정 `exit 0` — B top-10 46.3%, 세 번 연속 통과.**
그리고 저녁 18:30 에 **네 번째 조용한 정지**가 났는데, **워치독이 스스로 잡아
13 초 만에 되살렸다.** 그 부작용으로 **D-29(전환)가 저절로 풀렸고 우선순위 가설이 죽었다.**

---

## 1. 어제 세션이 끊긴 자리부터

08-20 세션은 상태 문서를 갱신하던 중 끊겼다. 커밋 안 된 것 넷이 남아 있었다 —
`daily/2026-08-20.md`, 큐 갱신, `docs/00` 정정, 상태 문서 절반. **먼저 저장하고**
(`cda3cb6`) 나머지를 오늘 마저 고쳤다.

> **교훈으로 적어 둔다**: 어제 나는 큰 편집을 다 끝낸 뒤 한 번에 커밋하려 했다.
> 끊기면 그게 통째로 날아간다. **오늘은 발견을 찾자마자 먼저 커밋했다.**

---

## 2. 수집 건강

### 2-1. 08-20 창 (08-20 22:30 ~ 08-21 05:00 KST) — 유효

| | 값 |
|---|---|
| `rankings_snap` distinct `snap_ms` | **5,545** |
| 60 초 초과 공백 | **0 건** |
| head / tail | 2.1s / 10.1s |
| **미관측 합계** | **12.2 초 = 0.052%** (예산 117 초) |
| `orderbook_snap` | 51,401 행 (distinct 51,150) |
| `config_sig` | distinct **1** (telemetry 78 줄) |
| 프로브 | **0 건** — 러너 기계 증거도 0, 전 워크트리 훑기도 0 |

### 2-2. ★★ 오늘 저녁 18:30 — 네 번째 조용한 정지

```
18:30:53  수집기가 멎었다 (프로세스 생존, 로그·DB 동시 정지)
18:35:53  워치독 session=unknown(raw=pre, stale=321s)
18:45:55  ALERT[CRIT] watch_log_stale (15.4 분)
18:45:55  RESTART reason=log_stale launcher=ops/launch_collector.cmd
18:46:08  프로세스 넷 생성   cmd 52812 -> py 44592 -> 22584 -> 40348 -> 24956
18:46:21  collector.log 첫 줄 (resumed from ...)          <- 13 초
18:46:27  DB 랭킹 스냅 재개
18:50:59  sup=1 col=1 col_up=291s rank_age=9 fetch_pct=100
```

**공백 934.1 초(15.6 분), 전부 프리장** — DB 직접 질의로 확인했다(18:00~19:05 KST 에
60 초 초과 공백 **1 건**). **정규장 손실 0.**

**이 재기동이 세 가지를 한꺼번에 했다:**

1. **D-29 가 저절로 풀렸다** — 수집기가 08-19 이후 처음으로 **감독 경로 위**에 있다.
   내가 무인으로 하기를 거부했던 그 전환이, 사고로 일어나 성공했다.
2. **사용자 콘솔 사슬과 먹통 사슬 A 가 같이 정리됐다** (`col=2` → `col=1`).
3. **자가 복구 3 층이 실제로 작동하는 것을 처음 눈으로 봤다.** 08-19 §9-4 가
   *"세 층이 전부 프로세스가 있다를 성공으로 읽는다"* 고 적었는데, **로그 신선도**
   라는 다른 축으로는 제대로 잡았다.

### 2-3. 디스크

**39.67 GB (19:00).** 08-20 01:0x 에 RDP 16.01 GB 를 지워 42.75 로 벌었는데 **이틀 만에
그만큼 다시 빠졌다.** 흐름: 08-20 09:30 **43.21** → 08-21 09:2x **39.84** →
18:40 **36.87** → **재기동 직후 39.43 (+2.7 GB)**.

> ★ **재기동이 2.7 GB 를 되돌려 줬다.** 멎은 수집기가 무언가를 붙들고 있었다는 뜻이다.
> **08-20 아침에 원인을 못 붙였던 *"09:15~09:30 에 2.67 GB 증발"* 과 크기가 같다.**
> **우연일 수 있다 — 안 갈랐다.** `tossmon.db` 는 6.24 GB, `docker_data.vhdx` 24.75 GB.

---

## 3. ★★ D-21 판정 (08-20 창) — 세 번 연속 통과

`--session 2026-08-20` → **exit 0**. 전문 `daily/2026-08-21_d21_verdict_output.txt`.
기준선 자가검사 10 행 + 요약 넷 전부 OK.

| | 08-17 | 08-19 | **08-20** |
|---|---:|---:|---:|
| **B (테이프) top-10** | 29.1% | 41.9% | **46.3%** (54 중 25) |
| **A (좌석) top-10** | 21.8% | 40.3% | **40.7%** (54 중 22) |
| B / A top-100 | 5.9/2.0% | 5.8/4.1% | **7.4 / 3.6%** |
| 비용 `capacity_fill` | 106 | 149 | **93** |
| `ranking_tier3` / `hold_expired` / 쿨다운 위반 | 58/35/0 | 128/104/0 | **80 / 48 / 0** |
| `md_peak_1s` hist (n=78) | — | `10:49` | **`9:5 10:73`** |
| `rank_peak_1s` (n=78) | max 3 | max 3 | **`3:78`** |

> **세 세션이 다 통과선 위다. 그래도 "확인됐다" 가 아니다** — 6 세션 기준선이고
> 사전등록 §4 제한이 그대로 붙는다. **그리고 커버리지는 알파가 아니다.**

★ **`md_peak_1s` 가 78 표본 중 73 에서 천장 10 에 닿는다** (08-19 은 49/78).
서버는 15 를 준다. **우리가 우리 천장에 눌려 있고 그 정도가 세션마다 커진다.**
천장 재검토는 D-25 가 *"프로브·확증 표본 확보 뒤"* 로 미뤄 둔 안건이다 — 안 열었다.

---

## 4. 워커 — **W5 가 두 번 죽었다. 발견은 건졌다**

### 4-1. 08-20 디스패치는 7 분 만에 죽었다

09:55 배정 → **10:03 `Conversation interrupted`. 산출물 0 건.**
`dispatch_sweep` 이 `STALE_UNCOMMITTED` 로 걸었지만 **미추적 둘은 08-07·08-08 파일**이라
워커 것이 아니다(`find -newermt` 로 디스패치 이후 수정 파일 **0 개** 확인). 재디스패치해도
덮일 것이 없었다.

**죽기 전 7 분에 낸 것을 트랜스크립트에서 건져 명세 §2-3 에 옮겼다:**

- 사슬 A 의 python 층이 하나 더 많은 것은 **venv launcher**(255 KB)다. 수수께끼 아님
- 침묵 구간에 `supervisor.stdout.log` 에도 안 썼다
- **사슬 A `BasePriority=6`, 콘솔 사슬 B `8`.** 그리고 내가 원인을 확인했다 —
  세 예약작업 XML 에 **`<Priority>` 요소가 아예 없어** 스케줄러 기본값 **7 = BelowNormal**
- 사슬 A 는 **cmd → python 에만 15 초** 걸렸다 (태어날 때부터 굼떴다)

### 4-2. ★ 그런데 그 가설이 같은 날 저녁에 반증됐다

18:46 재기동으로 뜬 **새 사슬도 `BasePriority=6` 인데 13 초 만에 떴다.**
08-19 의 3.4 시간 사슬도 6 이었다. **같은 경로·같은 우선순위인데 13 초 vs 3.4 시간이다.**

그리고 **굶주림 가설과 방향이 반대다** — 08-19 22:11 에는 다른 수집기가 아예 안 돌고
있었으므로(21:37 정지) 그때가 디스크 부하가 **더 낮았다.**

**W5 에 이 관측을 보냈다.** 워커는 같은 시각 독립적으로 같은 자리에 도달해 있었다
(19:02 에 새 사슬 우선순위를 직접 재고 있었다).

### 4-3. 지금 W5

`task_5e2073cc2256` / `ctx_7f7d3af3d7f1`, opus/high, **활동 중**(19:04 실측).
옛 디스패치 `ctx_1783049b0650` 은 `worker-abandon` 으로 정리했다 — 그 터미널이 아직
낡은 하트비트를 흘리지만 트랜스크립트는 08-20 10:04 에서 멈춰 있다. **겹쳐 돌지 않는다.**

**★ 명세 §5-2(*"사슬 A 를 먼저 죽이지 마라 — 살아 있는 표본이다"*)는 무효가 됐다.**
18:46 재기동이 그 표본을 정리했다. 이제 **재현으로만** 잡을 수 있다.

### 4-4. 그 밖

`dispatch_sweep` **조치 1 건**(위 4-1, 오탐으로 판정) · 열린 PR **0** ·
`orchestration check` 에 escalation·question **0**.

---

## 5. 사용자 결정

| 번호 | 상태 |
|---|---|
| **D-29** | ✅ **저절로 풀렸다.** 묻지 않아도 됐다 — 다만 그 답을 얻은 방식이 또 한 번의 조용한 정지였다 |
| **D-30** 신규 | **워치독 문턱 15.4 분 vs 사전등록 예산 117 초.** 정규장에 같은 일이 나면 그 밤이 통째로 무효다. **얼마를 감수하시겠습니까** |
| **D-26 (다)** | 여전히 열려 있다 — **G-1a 를 통과로 볼지**. 관문 판정이라 코디네이터 단독 금지 |
| **D-28** | 여전히 열려 있다 — 사전등록 §5-1 확인 문장. `E` 를 고르는 날 먼저 필요하다 |

**D-30 에 대한 내 권고는 (가) 그대로 두고 원인을 계속 판다** 이다.
네 번 중 정규장에 난 것은 아직 없고, **원인을 모른 채 문턱을 조이면 오탐 재시작이
늘어나는데 그 자체가 2~3 분 공백**이기 때문이다. 다만 **다음에 정규장에서 나면 그때는
이미 늦다** — 그래서 물었다.

---

## 6. 오늘 밤

**08-21 22:30 ~ 08-22 05:00 KST 가 금요일 정규장이고, 그 뒤 주말 이틀은 휴장이다.**

**08-19 이후 처음으로 복구 경로가 있는 상태로 들어간다.** 수집기는
`launch_collector.cmd` → supervisor → collector 로 돌고 워치독이 실제로 되살리는 것을
오늘 저녁에 확인했다.

> **주말이 기회다.** 토·일은 정규장이 없어 **배포·실험의 대가가 0** 이다.
> W5 가 재현을 시도하기에 가장 싼 창이다.
