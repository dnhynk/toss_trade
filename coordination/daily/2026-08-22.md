# 2026-08-22 (토) — **이동한 밤이 가장 깨끗했다. 판정은 네 번째로 통과했고 추이가 꺾였다**

> 기준 `main` `d98f865` + PR **#49**. 이동 기록은 `daily/2026-08-21.md` §8·§9.

## 0. 한 줄

**08-21 창 `exit 0` — 네 번 연속 통과.** 커버리지는 일곱 세션 중 가장 깨끗했는데
**B top-10 은 46.3% → 35.2% 로 꺾였다.** 분자는 그대로고 **분모가 늘었다.**

---

## 1. 08-21 창 (22:30 ~ 08-22 05:00) — 유효

| | 값 | |
|---|---|---|
| `rankings_snap` distinct `snap_ms` | **5,577** | 일곱 세션 **최다** |
| 60 초 초과 공백 | **0 건** | |
| head / tail | 0.8s / 7.5s | |
| 최대 간격 | **14.7s** | 일곱 세션 **최소** |
| `orderbook_snap` | 53,529 행 (distinct 53,197) | |
| `config_sig` | distinct **1** | |
| `RESTART` · `ALERT` | **0 · 0** | `col_up` 65,672s = 18.2 시간 연속 |

**이동한 밤에 조용한 정지가 안 났다.** 다섯 번째 재현 기회는 오지 않았다.

### 1-1. ⚠ 미관측 수치는 **판정 러너의 것만 인용해라**

러너의 규칙은 **6-2: `unobserved_frac > 0.005`(= 117.0s) 이면 INVALID** 이고,
러너가 세는 방식은 *"간격에서 폴 주기를 뺀 합"* 과 **다르다.** 후자로 재면 08-20 이
383.7s 로 나와 러너의 12.2s 와 31 배 어긋난다. **두 수를 섞어 추이를 그리면 없는
회귀를 본다.** 창 비교가 필요하면 `tools/d21_verdict.py` 출력만 써라.

---

## 2. ★ 판정 (08-21 창) — 네 번 연속 통과, 그러나 꺾였다

`--session 2026-08-21` → **`SELF-CHECK: PASS`**, **`VERDICT EMITTED` exit 0**.
전문 `daily/2026-08-22_d21_verdict_output.txt`.

| | 08-19 | 08-20 | **08-21** |
|---|---:|---:|---:|
| **B top-10** | 41.9% | 46.3% | **35.2%** |
| **A top-10** | 40.3% | 40.7% | **28.2%** |
| cost (`capacity_fill→3`) | — | 93 | **66** |

B 는 여전히 기준선 최대 25.9% 위이고(INCREASED), A 는 25~45% 의 HALF MATCHED 다.
**통과선은 안 깼다.**

### 2-1. ★ 꺾인 것은 분자가 아니라 분모다

| | 08-20 | 08-21 |
|---|---:|---:|
| `ranked` (분모) | 54 | **71** (+31%) |
| A seats (분자) | 22 | 20 |
| B tape (분자) | 25 | **25** (동일) |

**B 의 분자는 25 로 한 건도 안 변했고 분모만 54 → 71 로 늘었다.**
`25/54 = 46.3%`, `25/71 = 35.2%`. A 도 같은 모양이다(22 → 20).

**이것은 관측이지 설명이 아니다.** 왜 그 밤에 랭킹 진입 심볼이 31% 많았는지는 안 갈랐다.

### 2-2. ★★ 이 세션에는 다른 여섯에 없는 교란이 있다 — **결론 금지**

**수집기가 개장 3.1 분 전에 냉기동했다.** 이동 공백 때문에 직전 3 시간(19:27~22:26)의
프리장 랭킹·호가가 **0** 이다(`daily/2026-08-21.md` §8-3). 다른 여섯 세션은 전부
몇 시간치 프리장 이력을 갖고 개장을 맞았다.

**따라서 08-21 의 A/B 를 나머지 여섯과 나란히 놓고 추이로 읽으면 안 된다.**
분모가 왜 늘었는지, 그것이 냉기동과 관계있는지 **아직 모른다.**

> **다음에 확인할 것** — `ranked` 71 의 심볼들이 프리장 이력을 요구하는 경로로
> 들어왔는지. 답이 "그렇다" 면 이 세션은 확증 팔에서 **빼야** 한다.
> 판정 러너는 이 교란을 모른다 — 6-2 는 창 **안**의 공백만 본다.

---

## 3. W5 배정 — **보냈다**

| | |
|---|---|
| 명세 | `coordination/specs/w5_restart_evidence.md` |
| 워크트리 | `w5-ops` / `feat/ops` / `e7c9437` |
| 터미널 | `term_8571efd4-c312-4649-9ec8-f2e85722e181` |
| 창 | **08-24(월) 22:30 까지** 휴장 |

일: **워치독이 죽이기 직전에 표본을 뜬다** (`ops/watchdog.ps1` 867~872, `taskkill` 직전).
가장 크게 박은 제약은 **증거 수집 실패가 `RESTART` 를 막으면 안 된다**는 것이다.

**화면을 읽어 착수를 확인했다** — `accepted: true` 는 증거가 아니다(§2-1).

---

## 4. 정리된 것

**PR #49 병합** (`c99fa60`) — 문서뿐이라 코드 게이트 대상 0. 브랜치 삭제.

**소진된 예약작업 둘이 사라졌다.** `coord-d21verdict`·`w1-probe-d` 는 `ObjectNotFound`
다. 남은 것은 수집 사슬 **다섯, 전부 `S4U`**.

> ⚠ **08-21 §8-5a 에 적은 *"다시는 안 뜬다"* 는 근거가 덜 찼었다.** 지난 1 회성
> 트리거와 빈 `NextRunTime` 만 보고 단언했는데, **`StartWhenAvailable` 을 안 봤다** —
> 그것이 켜져 있으면 놓친 1 회성 작업이 기계가 깨어날 때 뒤늦게 실행된다.
> 지금은 작업이 없어져 확인이 불가능하다. **다음에 같은 판단을 할 때는 그 설정부터 봐라.**

---

## 5. ★★ py-spy 가 라이브에 붙는다 — 확인됨

W5 의 PR #52 가 남긴 미결(*"라이브 수집기에 py-spy 가 붙는지는 검증하지 못했다"*)을
휴장 창에 갈랐다. **워치독과 같은 조건**(S4U · `/RL LIMITED` · `dongh`)의 일회성 작업으로
py-spy 를 붙였다. 관리자 창이 필요해 사용자가 실행했다.

### 5-1. 대상 고르기에서 한 번 틀렸다

라이브 python 사슬은 **넷이고 앞의 셋은 얇은 스텁**이다:

```
19340 → 21684 → 9316 → 6292
1thr    1thr     1thr    26thr / 240hnd   <- 진짜 수집기
```

첫 번째(19340)에 붙였더니 `Failed to find python version from target process`.
**이것은 권한 거부가 아니다** — `os error 5` 가 아니라는 것이 요점이다.
**S4U/Limited 에서 프로세스 열기는 성공했다.**

### 5-2. 6292 에서 나온 것

```
Process 6292: ...\.venv\Scripts\python.exe -m tossmon.collector --config config/config.yaml
Python v3.13.15

Thread 13672 (idle): "MainThread"
    _poll (asyncio\windows_events.py:778)
    select (asyncio\windows_events.py:446)
    _run_once (asyncio\base_events.py:2023)
    run_forever (asyncio\base_events.py:684)
    run_until_complete (asyncio\base_events.py:713)
    run (asyncio\runners.py:119) / run (asyncio\runners.py:196)
    main (tossmon\collector\__main__.py:76)
    <module> (tossmon\collector\__main__.py:82)
Thread 12620 (idle): "asyncio_0"   _worker (concurrent\futures\thread.py:90)
Thread 21140 (idle): "asyncio_1"   _worker (concurrent\futures\thread.py:90)
```

**표본 §[3] 은 실전에서 작동한다.** 다음 조용한 정지에서 스택이 남는다.

### 5-3. ★ 이것은 **건강한 상태의 기준선**이다 — 그래서 값이 있다

정상 가동 중(휴장, 수집기 uptime 19 시간)의 모양이 위와 같다: **MainThread 가 asyncio
`_poll`/`select` 에서 대기**, 워커 스레드 둘이 `_worker` 에서 대기.

**사고 때 스택 하나만 보면 못 읽는다.** 같은 자리에 있으면 *"루프는 살아 있는데 일이 안
들어온다"* 이고, 락·IO·`recv` 에 걸려 있으면 다른 결함이다. **위 블록을 비교 대상으로 써라.**

> py-spy 는 파이썬 프레임이 있는 스레드만 보인다. OS 스레드 26 개 중 셋이다 —
> 나머지는 네이티브(proactor·sqlite 등)라 이 자에는 안 잡힌다.

### 5-4. 그래서 남은 것 — **`/RL HIGHEST` 재등록은 필요 없다**

권한이 이미 되므로 워치독 등록을 건드릴 이유가 사라졌다. **자가 복구 경로를 안 건드린다.**

---

## 6. ★★ G-2 세 번째 삽 (PR #54, `docs/66`) — **과녁이 없었고 자도 없었다**

명세 `w3_g2_identification.md` §1 이 물은 *"닫을 수 있는 문제인가"* 에 답이 나왔다.
**답은 (나) — 정합으로는 안 닫힌다. 다만 이유가 내 가설과 다르다.**

### 6-1. 내가 낸 가설은 부분 반증됐다

내 가설: *"전방 깊이는 처치후 변수라 원리상 편향이 들어오고, 짝 잃음 62.6 → 80.9% 가
그 증상이다."* → **짝 잃음은 처치후 편향이 아니라 밴드의 선택 효과다**(짝 내 `nbar300`
잔차가 셋째 밴드 전에 이미 중앙 ±12.5 막대). 결론은 맞았고 **기전이 틀렸다.**

### 6-2. 실제 이유 셋

| | |
|---|---|
| **과녁이 없다** | 무처치 순간에도 실현 깊이가 사전 값의 **±40%** 를 흔든다. 종목 내 ρ 중앙 **+0.09~+0.58**. → **짝 단위 격차 0 은 어떤 사전 축으로도 달성 불가** |
| **자가 없다** | `docs/65` [11] 의 격차 통계가 **12 칸 중 11 칸** 짝 내 교환 순열 잡음과 구분 안 됨 |
| **기전 자체는 미결** | 사전 3 키 정합 뒤 깊이 반응 CI 가 세 칸 전부 0 교차(폭 ±40~80 막대), 부호 불안정. **지지도 배제도 안 됐다** |

### 6-3. ★ 코디네이터가 직접 재현했다 — 12 칸 전부

`python -m tossmon.analysis.measure.forward_depth_mechanism` (exit 0) 을 내 손으로 돌려
`gap_permutation` 12 칸을 읽었다. 본페로니 문턱 **0.05/12 = 0.00417**:

```
TOSS_SECURITIES N50  +깊이대리   n=31  gap=+43.0  p=0.0032   ★ 유일하게 구분됨
나머지 11 칸                                p=0.017 ~ 0.825   구분 안 됨
```

**보고와 정확히 일치한다.** 그 유일한 칸도 같은 칸의 짝 통계가 반대쪽(+6.0)이라
풀링 중앙값 차가 짝 불균형을 대리하지 않는다.

> **그래서 `docs/65` 의 간판 숫자 *"중앙 19.5 → 39.5 막대"* 는 잡음이다.**
> 내가 그것을 `COORDINATOR-STATE` 와 `USER-INPUT-QUEUE` 에 옮겨 적었고 둘 다 고쳤다.

### 6-4. 게이트 — 내가 재실행

| | 워커 주장 | 재현 |
|---|---|---|
| `pytest tests/ -q` | 2342 passed, 1 skipped, 4 deselected | **동일** |
| `pytest -k analysis -q` | 282 passed | **동일** |
| 러너 | exit 0 | **동일** |
| 병합 증거 `W3-G2-DIG3-FDM66` | — | **있음** |
| 확증 팔 차단 | 삼중 가드 | **코드·테스트로 확인** — 테스트가 `2026-08-14` 를 주입해 예외를 확인한다. 실행 세션 9 개, `exploration_only=true` |

> 소유 밖 두 줄(`docs/INDEX.md` 한 줄, `tests/test_design_b_wiring.py` 모듈 등록 한 줄)은
> 등록 목적이라 통과시켰다.

## 7. 그래서 지금 무엇이 열렸나

**`E` 후보가 처음으로 표에 올라왔다** (`docs/66` §8). 고르는 것은 코디네이터지만
**D-28 이 먼저**다(`E` 커밋에 들어갈 확인 문장). 그리고 W3 가 사전등록 해석 하나를
물어 왔다 — **D-31 신설**.

---

## 8. ★★★ D-28 · D-31 답 나옴 — **`E` 를 채웠다. G-2 는 08-24 밤을 기다린다**

| | 답 | 집행 |
|---|---|---|
| **D-28** | **(나)** 오염 셋을 확증 팔에서 뺀다 | `G2G3-PREREG` §2-1 **개정 2** |
| **D-31** | **(가)** 사전 상태 정합으로 확정 | `G2G3-PREREG` §3-1 **개정 3** |

### 8-1. 확증 팔이 다시 그어졌다

```
버림   08-13 · 08-14 · 08-17     <- 어느 팔에도 안 들어간다. 되돌릴 수 없다
확증   08-18 · 08-19 · 08-20 · 08-21  = 4 세션
5 번째 2026-08-24(월) 정규장
```

**얻은 것**: §5-1 의 확인 문장이 **사실이 됐다.** 남은 확증 팔에 G-2 분석이 닿은 적이
없다 — 산출물 셋(`docs/64`·`65`·`66`)이 본 세션을 표로 대조해 §2-4 에 박았다.

### 8-2. `E`

```
E = TOSS_SECURITIES_..._VOLUME 랭킹에서, 연속한 두 스냅 사이에
    순위가 10 계단 이상 개선된 지점. 두 스냅 모두에 그 종목이 있어야 한다.
    (= E2_rank_jump / K10.  hires_events.py:rank_jump_events)
```

`docs/66` §8 의 후보 둘 중 하나를 골랐다. 다른 하나(`$0~5` 층 제한)는 층 CI 가 0 을
교차해 근거가 더 약하고, 깊이 축 `E` 는 `docs/66` [M2]·[M3] 이 닫았다.

> ⚠ **근거는 얇다.** `docs/65` §6-1.4 — 08-13 한 세션을 빼자 이 칸이 `+1.494%(0 교차)`
> 에서 `+1.857%(0 제외)` 로 바뀌었다. **한 세션이 부호 판정을 바꾼다.** 하한 +0.033%
> 도 0 에 붙어 있다. **지명이지 증거가 아니다 — G-2 가 실패로 끝날 가능성이 높고,
> 그것도 답이다.**

### 8-3. 남은 경로

```
08-24(월) 밤 정규장  ->  §2-3 통과하면 확증 팔 5 세션
                     ->  G-2 판정 (E 는 이미 얼렸다)
                     ->  통과하면 G-3 = 알파 검증 (비용표는 §4-1 에 이미 얼려 있다)
```

**`E` 를 오늘 얼린 것이 요점이다** — 5 번째 세션이 끝나기 이틀 전이라 사전등록의
보호가 가장 강한 상태다.

---

## 9. ★★ W5 2 차 (PR #59) — **표본이 런처 스텁만 보고 있었다**

내가 낸 명세는 *"스택 대상에 정렬이 없다"* 였다. W5 가 그걸 고치려다 **더 앞의 것**을 찾았다.

### 9-1. `.venv\Scripts\python.exe` 는 인터프리터가 아니라 **런처**다

| 파일 | 크기 | `InternalName` |
|---|---|---|
| `<repo>\.venv\Scripts\python.exe` | 249 KB | **`Python Launcher`** |
| `...\Programs\Python\Python313\python.exe` | 103 KB | `Python Console` |

런처는 **진짜 인터프리터를 자식으로 띄운다.** 둘의 `CommandLine` 이 완전히 같고
**`ExecutablePath` 만 다르다.**

### 9-2. 그래서 운영에서 스택이 영원히 안 나왔을 것이다 — 코디네이터 실측

```
watchdog.ps1:114   [string]$ExeLike = ""          # default: <RepoRoot>\.venv\*
watchdog.ps1:201   if ($ExeLike -eq "") { $ExeLike = <RepoRoot>\.venv\* }
라이브 작업 인자    powershell -File watchdog.ps1  <- 인자 없음 = 기본값
watchdog.ps1:422   ExecutablePath -like $ExeLike  <- 진짜 인터프리터가 여기서 탈락
```

**씨앗 집합이 스텁뿐이고, 스텁에는 파이썬 상태가 없다.** py-spy 는 언제나
`Failed to find python version` 을 낸다. **정렬을 고쳐도 스택은 안 나왔다.**

> ### ★ 게이트 교훈 — 내가 PR #52 에서 이걸 놓쳤다
>
> PR #52 의 게이트 4a 는 `procs=8` 로 라이브 사슬을 포함해 돌았다. **그 하네스가
> 운영과 다른 `-ExeLike` 를 썼기 때문**이다(기본값이 아니라 `*`). 게이트가
> **운영 구성을 재현하지 않으면 통과가 통과를 뜻하지 않는다.** 내가 통과시켰다.

### 9-3. 수리

- **(a) 대상 확장** — `Get-SampleProcs` 가 씨앗에서 **부모-자식 폐포**를 취한다.
  범위 근거: **`taskkill /T` 가 지우는 트리와 같은 집합** — 표본이 덮는 범위와
  재기동이 지우는 범위가 일치한다. **깊이 8 로 제한**해 PID 재사용 순환을 막았다
- **(b) 정렬** — 수집기 패턴 → 감시자 패턴 → 나머지, 등급 안에서 스레드 수 내림차순.
  **스레드 수는 정렬 키이고 필터가 아니다** — 아무것도 안 버린다(사슬이 **어디서**
  끊겼는지는 스텁이 목록에 있어야 보인다)
- **`Get-TossProcs`(재기동 판단·kill 대상)는 안 건드렸다** — 코디네이터가 diff 로 확인

### 9-4. 게이트 — 내가 재실행

| | 워커 주장 | 재현 |
|---|---|---|
| `watchdog_selftest.ps1` | **149** passed (130 → +19) | **동일** |
| `pytest -k "ops or watchdog"` | 347 passed | **동일** |
| `pytest tests/` | 2219 passed, 1 skipped | **동일** |
| 병합 증거 5 | — | **전부 있음** |
| `RS1~RS5` 실행 | — | **직접 출력 확인** — `RS1: THE RESTART STILL HAPPENED` |

**새 테스트가 진짜 회귀를 잡는지 돌연변이로 검증했다** (워커가, `watchdog.ps1` **사본**에):
`catch` 에 `throw` 추가 → RS1 red · `BUDGET EXHAUSTED` 제거 → RS2 red ·
py-spy 하드 의존 → RS3 red · `Sort-SampleProcs` 제거 → RS4/RS5 red.

### 9-5. ⚠ 딸려 나온 미수리 하나 — **`sup=1 col=1` 도 스텁을 센 것이다**

같은 원인이다. 워치독의 프로세스 계수도 `-ExeLike` 를 거치므로 **네 프로세스 사슬을
스텁만 보고 센다.** W5 는 `Get-TossProcs` 를 안 건드렸고 **그게 옳다** — 재기동 판단이
거기 걸려 있어 한 창에 둘을 바꾸면 안 된다.

**오늘 깨진 것은 없다**(스텁이 죽으면 사슬이 죽은 것이므로 판단은 맞는다).
**다만 그 수가 뜻하는 바가 문서와 다르다.** 다음 창의 후보로 남긴다.
