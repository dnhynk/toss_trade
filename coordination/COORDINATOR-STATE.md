# 코디네이터 상태 — 새 세션 인수인계용

갱신: **2026-08-04 09:50 KST** · main = `ca0af08` · **1,328 passed, 1 skipped** (직접 재실행 실측)
> **먼저 읽어라: §4.4-F(전략 후보 0개) 와 §4.6(오늘 아침 실행 기록).**
> 아래 §1~§3 은 07-31~08-01 시점 기록이라 상당수가 낡았다 — 사실 확인 없이 인용하지 마라.
> 전략 지형의 최신 상태는 §4.4-A ~ §4.4-F 순으로만 읽으면 된다.

> **새 세션에서 이 프로젝트의 코디네이터를 이어받는 경우 이 문서부터 읽어라.**
> 배경·전략·아키텍처는 `docs/00_HANDOFF.md`, 계약 정본은 `docs/04_contracts.md`.

---

## 0. 인수 절차 (3분)

```bash
# 1) 내 터미널 핸들 확인 (루트 워크트리에서 실행 중인 에이전트 터미널)
orca terminal list --worktree "id:12e59c9d-6eff-4602-8e62-802907e489b4::C:/Users/dongh/toss_trade" --json

# 2) 기존 Run 에 바인딩 — 이것만 하면 워커·수집은 그대로 이어진다
orca orchestration run-use --id run_92948a1f80a5 --from <내_핸들> --json

# 3) 이제 표준 대기가 동작한다 (내용까지 담아 깨워준다)
orca orchestration check --wait --types worker_done,escalation,question,status --timeout-ms 570000 --json
```

**주의**
- `orchestration check` 는 `--from` 을 **안 받는다**. 다른 명령(`task-create`/`dispatch`/`reply`/`send`)은
  이 환경에서 `--from <핸들>` 을 **반드시 명시**해야 한다(자동 해석이 안 됨).
- `consumer_fenced` 가 나오면 바인딩이 풀린 것이다 → `run-use` 로 재바인딩.
- **감시 스크립트를 새로 만들지 마라.** 세션 초반에 `check --wait` 가 죽어서 우회 감시기를 만들었는데,
  런타임 재시작 이후에는 `run-use` 한 번으로 표준 방식이 정상 동작한다.
  우회 감시기는 "브랜치가 움직였다"만 알려줘 rebase 를 완료로 오인하게 만든다.

---

## 1. 지금 돌아가고 있는 것

| 대상 | 상태 |
|---|---|
| **W5 라이브 재수집** | **진행 중 — 22:39:18 기동본** (작업 스케줄러, 최신 main 코드). 오늘 사고 2건: **사고1** 18:07:30 Orca PTY 정리가 수집기 트리 살해 → 8분 공백 → 18:15 스케줄러 재기동. **사고2** 재기동 env 에 `TOSS_BASE_URL` 누락 → 20:48 토큰 만료 후 재발급 전부 실패(프로세스는 생존, API 만 사망 — 모니터 사각) → 22:39 env 수정 재기동. **공백: 20:48:20~22:30:59, 22:31:19~22:39:18** (docs/11 매니페스트 기록). 재기동 중간 발견: 툴콜 안에서 schtasks /Run 후 대기하면 샌드박스 정리가 새 트리도 죽임(3회 연속) — **fire-and-forget 후 별도 콜 검증**으로 해결. 애프터(~08:50)까지 계속 |
| **W5-b 관찰 교대** | 진행 중 — `task_999bd2c75b52`/term_9727154b. 체크포인트: 22:25 개장준비 → 22:30~22:50 본 시험 보고 → 심야 status → 내일 ~09:00 검증·worker_done. 원 태스크 `task_04201cf60649` 는 dispatched 로 남음 — W5-b 완료 시 코디네이터가 수동 정리 |
| **§3-(B) 정리 3건** | **전부 머지 완료** — W2 U-4(`0dc427f`, 실유출 1건 수정: `UnicodeDecodeError.args` 바이트 원문), W7 사전등록 개정(`a492810`, A6/A7 충돌 없음 판정), W3 사전등록 정합(`2961e7a`, P0 3+P1+P2 2, 신규 22테스트). **통합 773 passed·1 skipped 실측** |
| **W7 후속 개정** | **머지 완료** — §7-e 분할 탐지 문언 확정(r=1d수정/1m원주가 비율, 경계 급변 ≥1.5배, `split_dates` 인자, `split_excluded` 카운트, 미전달 시 주 분석 금지)·§2.2 일봉 as-of 앵커 의무·§2.7 시총 ±10% 밴드 |
| **W3 구현 후속** | **머지 완료** (`fa6b45a` 머지, **795 passed·1 skipped 실측**) — 분할 파이프라인·일봉 as-of 앵커·시총 밴드. 분석 착수 전 코드 작업 끝 |
| **W7 최종 추인** | **머지 완료** — A5 혼입 검사는 "현 데이터로 실행 불가 — 미실행+사유 보고"로 강등(순환참조: 혼입이 r 점프 자체를 지움), 대체 안전망 2개(백필 매니페스트에 `adjusted=false` 기록·전 심볼 r 분포 스팟체크=전면 혼입만 탐지), W3 해석 2건·scope 컬럼 추인, 사각 2개 명기. W7 터미널 핸들은 **term_01b11d1b** (재발급됨) |
| 라이브 리스 | **W5 단독 보유.** 다른 워커·코디네이터는 라이브 호출 금지 |

> 참고: w7-prereg 워크트리의 저수준 `terminal create`는 "Timed out waiting for terminal
> handle" 로 반복 실패했고 기존 셸 2개는 유령(PTY 무반응, 1개는 tab_not_found)이었다.
> `orchestration worker-start` 조합 경로는 정상 동작 — 같은 증상이 나오면 이쪽을 쓸 것.
> 스펙 본문에 큰따옴표가 있으면 PowerShell 5.1 이 native 인자를 깨뜨린다 —
> `task-create --spec` 은 Bash 로 실행할 것.

**정규장 개장(22:30) 본 시험 결과 (22:50 보고 — 통과)**:
- tier3 **13→17/20** 실 소형주로 채워짐 (어제 4/20 대형주 쏠림과 대조). 11분간 신규 승격 12건:
  confirm 8 (ACRE·CIGL·RANI·REBN·VFF·CAAS·EARN·LAKE), **precursor 4 (PMA·TYGO·WALD·AMCI —
  전조 경로 첫 발화)**
- `evicted`-as-promotion **0 유지** (어제 6,653 병리 소멸). 승격 1,164건 중 price_activity 93.6%
- 예산 거버너: 개장 풀부하에서 MARKET_DATA 최대 6.27/7.00, **429 0건**, api_errors=0
- 유니버스 게이트: `watch_outside_universe=0` 시종 유지, INTC 등 대형주 tier0 거부
- 실 이벤트: **EVENT CIGL kind=win path=confirm score=0.712** (22:40:05)
- **유의**: 개장 첫 분 유실 — 아래 사고 2 참조. 커버는 22:31:06~19 + 22:39:18~

**주의 — 돌고 있는 collector 는 기동 시점 코드다.** 15:43 현재도 로그에
`budget: RANKING predicted 0.33 req/s > target 3.50` 이라는 **가짜 ERROR** 가 찍히는데,
이건 W4 가 이미 고쳐 머지한 것(`e512463`)이고 **실행 중 프로세스에는 반영되지 않았을 뿐**이다.
수정이 실패한 것으로 오해하지 마라. 다음 재기동 때 사라진다.
같은 이유로 `ForbiddenEndpoint` 명시 처리도 이 프로세스에는 없다 — 프로세스가 조용히
사라지면 그것부터 의심하라.

검증 스크립트: `C:/Users/dongh/.claude/jobs/236dc45f/tmp/verify_recollect.py`
(단, `PYTHONIOENCODING=utf-8` 없이 실행하면 cp949 로 죽는다 — 콘솔 비ASCII 출력 문제)

---

## 2. 오늘(7/31) 완료된 것

- **오케스트레이션 리셋 후 복구** — `run_92948a1f80a5` 신설, 문법 변경(`run-create` → `task-create` → `dispatch`)
- **감사 blocker 3건 해소**: 토큰 리스 자격증명 단위(A6), 유니버스 필터 수집경로 연결, 룩어헤드/중복
- **유니버스 필터 실작동 검증**: `symbols` 1,678개, 대형주 5종목(NOK·AMD·ASML·META·GS) **전부 차단**,
  `$` 우선주 387개 거부, `watch_outside_universe=0` 유지
- **W1 감사 잔여 22항목 전수 판정** (해소 16 / 수정 2 / 미수정 4) + **새 시크릿 유출 U-4 발견·수정**
- **W3 M-3(겨울 매매일 분할)·M-4(반일장 곡선 오염)** — 감사는 "나중"으로 분류했으나
  백필 분석을 지금 오염시키므로 코디네이터가 blocker 로 격상
- 계약 개정 **A6**(토큰 리스·ForbiddenEndpoint·CAS·limiter) **A7**(KR 제거·상태파일 권한)
- **A7 코드 반영 완료**(`6def9b5`) — ALLOWLIST 13개로 축소.
  Windows 의 `os.chmod` 는 읽기전용 비트만 만지고 **ACL 에는 영향이 없다**는 것을 W1 이 짚어
  `icacls /inheritance:r` 로 분기했다. `os.chmod` 만 썼으면 "권한을 걸었다"고 믿으며
  실제로는 아무 보호가 없었을 것이다.

---

## 3. 다음에 할 일 (우선순위)

### (A) 머지 게이트 5종 (워커 완료 보고를 받을 때마다)
① pytest 재실행 ② **merge-base 기준** 소유권 diff ③ 계약 준수 ④ 금지사항 스캔
⑤ **머지 후 통합 스모크** (브랜치 단독 통과가 조합 통과를 보장하지 않는다 — 실제로 걸린 적 있다)

> W1 의 A7(KR 제거 + 상태파일 권한)은 `6def9b5` 로 **머지 완료**. 현재 열린 태스크 없음.

### (B) 분석 착수 전 정리 — **전부 완료·머지됨** (17:00 기준)
1. ~~사전등록 반영~~ → W7 개정 3회 머지(M-3/M-4 반영 → 문언 공백 3건 → 최종 추인).
2. ~~HANDOFF-W3 §3 미정합~~ → W3 2회 머지(20일 창·가격 계열·전일종가 사슬·표본 필터,
   그리고 분할 파이프라인·일봉 as-of 앵커·시총 밴드). **795 passed·1 skipped.**
3. ~~U-4 점검~~ → W2 머지. 실유출 1건 발견·수정(`UnicodeDecodeError.args` 바이트 원문).

### (C) Phase 1-C 분석 — **다음 작업. 단 백필은 라이브 리스가 필요하다**

> **사용자 지시 (7/31 밤, 구속력 있음): 아침에 백필 라이브 실행을 승인하기 전에
> W6 3차 감사(`task_bdbca590ecbe`, docs/14) 결과를 사용자에게 먼저 보고할 것.**
> 순서: ① W6 감사 결과 수령 ② (W4 백필 러너 완성 시 그 코드까지 감사 범위에 추가 검토)
> ③ 사용자에게 감사 요약 보고(턴 마지막 텍스트로) ④ blocker 없으면 그때 백필 승인.
> blocker 가 있으면 승인 보류하고 수정 먼저.

**밤사이 진행 — 전부 머지 완료 (08-01 01:40 기준, main **821 passed·1 skipped**)**:
① W6 3차 감사(docs/14 — 치명 F-1·F-2, 전부 재현 실측) ② W4 **대량 백필 러너**
(`tools/backfill.py`, §2.8 집행, mock E2E, 3단계 실행 절차·호출량 추정은
`W4_REPORT_task_e99983f09016.md` (e)) ③ W7 문언 추인(§7-e 보수 등록·§2.2 date-단위)
④ W3 blocker 수정(F-1 date-단위 소속·F-2 (마지막관측,관측] 보수 등록 + fixture 실규약화
+ F-3/F-4/F-5/F-8 — 감사 repro 로 자체 검증, `split_scan_report` 에 n_r_uncomputable·
n_widened 노출).
**추가 (01:10)**: **E2E 리허설 디스패치** — `task_2c12d19559dc`/`ctx_3822bbb36eaa`/W3.
백필(mock)→Reader→분할스캔→검출→prereg 베이스라인→필터→run_all→리포트 전 순서 총연습.
오늘 밤 머지 조각들의 조합 실행이 0회라는 공백을 메운다. 산출: 내일 재사용할 실행
스크립트 + Phase 1-C 절차 초안. 메일함 감시는 하니스 Monitor(`bg852623o`, peek 폴링)로
전환(PTY 백그라운드 대기 반복 즉살 우회).

**E2E 리허설 결과 (01:45 머지, 824 passed)**: 전 순서 green (provenance 행 0).
**실행 순서 blocker 2건 발견·해소** — 내일 절차에 필수 반영:
- **유니버스 빌드가 백필보다 먼저다** (백필은 `symbols` 를 안 채움 → Reader.symbols()
  비면 §2.7 필터가 전 이벤트를 meta_missing 으로 버림)
- **캘린더 영속화** — W3 이 `save_calendar`/`load_calendar` 추가(day=None·반일장 왕복
  보존). API 가 살아 있는 백필 창에서 캘린더를 떠서 저장해야 분석이 오프라인으로 돈다
- W3 권고: 러너는 provenance 행이 하나라도 있으면 비정상 종료해야 함 (Phase 1-C 스펙에 반영할 것)
- Phase 1-C 단계별 절차 초안은 W3 리허설 보고서에 있음 (스크래치패드 w3_e2e 계열)

**아침 절차 — 진행됨 (09:20 기준)**: ① W5-b 최종 검증 green·worker_done 수동 복구 완료
(태스크 2건 completed, docs/11 머지 `63ac979`, main 826 passed) ② 감사 요약 사용자 보고
완료(09:05) ③ 수집기 STOP 파일로 정상 정지·리스 해제(09:15) ④ **W4 백필 실행 중** —
`task_bf64c4c19022`/`ctx_69b1466adb5b`(term_8017b557). 순서: dayMarket 실측 1콜 →
유니버스 빌드(backfill.db) → 캘린더 캡처(save_calendar) → screen-only → --estimate →
**정지선 도달 (11:27) — 본실행 GO 는 사용자가 직접 결정** (08-01 11:15 사용자 지시.
11:45 사용자: 노트북 닫았다가 재개 시 옵션+GO 를 함께 준다. 그 전까지 1분봉 호출 0 유지).
- **GO 메뉴** (1분봉 보관 ~320일이라 날짜 경계는 데이터 손실 0):
  [B]=--from 2025-09-01 전심볼 4,002창 17.6~35h (**권고** — 훈련 데이터 전부+§2.8 프레임 정합)
  [A]=2026-01-01 전심볼 2,651창 11.6~22.9h (훈련 구간 포기) [C]/[D]=러너 한정(프레임 이탈)
- **GO B 발사됐으나 403 ip-not-allowed 로 중단 (08-01 20:19~20:45)** — 기동 3연속
  "5초 사망"의 진짜 원인은 킬러가 아니라 **공인 IP 가 토스 허용목록에서 이탈**
  (스크린 2h 는 성공, 이후 전 호출 403 — 아침에 성공한 캘린더 콜도 지금은 403).
  현재 공인 IP **106.246.92.137** — **사용자가 토스 개발자 포털에 재등록해야 재개 가능**.
  데이터 손실 0: 체크포인트 21,850창 전부 todo, 스크린·캘린더·유니버스 디스크 보존,
  재개는 단일 명령(run.log/w4m.cmd). W4 가 잔여 프로세스·스케줄 태스크 3종 전부 정리.
  **★ 백필 완주 (08-02 07:51:50, W4 worker_done 수리·태스크 종결)**:
  **1분봉 1,771,840개** (1,465/1,707 심볼), 창 4,002 전부 종결(done 220 / partial 3,782 —
  partial 은 보관 바닥 도달의 정직한 기록, 심볼별 oldest 매니페스트에 있음), 호출 14,171,
  캘린더 6,781 매매일. §7-e adjusted=false 증거·§6.2 홀드아웃 counts-only 매니페스트 확인.
  경미: PermissionError 1(일시), day_not_in_calendar 93(2.3%, 캘린더 가장자리) — 기록됨.
  슈퍼바이저가 밤새 IP 플랩 2회를 자가 복구하고 무개입 완주 — §4.5 표준의 첫 실증.
  산출물(w4-collector/data/): backfill.db, backfill_manifest.json/csv, checkpoint.json,
  calendar_us.json, supervisor.log. 커버리지 확장은 --from 앞당겨 재실행(체크포인트 멱등).
  **★ 밤샘 캠페인 (08-02 10:45 사용자 포괄 승인: "20h+ 켜둠, 무겁고 오래 OK, 필요한
  것만, 끊김 없이")** — IP `122.40.242.91` 등록 완료(라이브 가능). 계획(분기 포함):
  **★ 캠페인 종료 상태 (08-02 20:00)**: 재백필 완주(봉 11,099,864 = 6.3배, 이벤트일
  충전 중앙값 0%→74.4%, 잔여 미충전은 API 보관창 밖 소멸분). Phase 1-C v2 머지(`d6da95c`)
  — 이벤트 51→904, §1.7 게이트 **2/5**(v1 0/5, 코어 러너 지표 2개 진입), 판정 없음(규정).
  이탈 3개 분해: 유동성 아님·§2.7 아님(전후 모두 2/5, fill 바닥 추가 시 악화 3/5) →
  **측정 프레임 불일치**(문헌=개장 갭퍼, §2.1=전세션 롤링; T0-타이밍 층화만으로 4/5).
  W3 권고: **§1.7(HOD 2지표 개장창 한정) + §3(VWAP 신뢰조건) 개정, §2.7 아님** —
  docs/15 v2-5 초안. **아침 사용자 결정 2건**: ① 이 개정(§9 열람 후 절차 — post-hoc
  성격 판단 포함, W7 집행) ② 개정·재게이트 후 판정 → 홀드아웃 열람.
  미판정 기록치: Q4 reach 0.564, Q3 n=55(최소표본 첫 충족), Q5 무조건부 −4.47%(여전히
  음수), Q1 리드타임은 커버리지 교란으로 판독 불가(표준화 필요). 깔때기 0/5·홀드아웃 봉인.
  잔여 백로그: 슈퍼바이저 디스크 preflight, 라이브 prev_close 수정주가(RECT), F-6~F-9,
  backfill todo=1(transient 1창).

  **집행 현황 (11:50)**: 진단 완료 — **A-1 확정** (창 끝 단일 앵커 + merge_spans 광역화
  = 88.6% 창이 25% 미만 수집; 밀집일 페이징 자체는 건전 26/36 접지 일치; 데이터는 서버에
  실재). Phase 1-C v1 머지(`docs/13·15`, 판정 indeterminate, 홀드아웃 봉인 유지).
  **가동 중**: W4 수정+재백필 `task_97f00c03ef63`/`ctx_aa1921f73b08`(1단계 mock 수정 →
  정지선 → 내 게이트 GO → 재백필 --deadline 08-03 09:00), W3 Q2 탐색
  `task_31c3462dd878`/`ctx_e990aaf382dc`(수집 DB 단독, 판정 아님·exploratory 등록).
  - **A**: W4 공백 진단(`task_5832c455a070`, 진행 중) 수령 → 분기:
    A-1 백필 미달이면: W4 수정 태스크(페이지네이션+접지 회귀테스트) → 게이트·머지 →
    **재백필 라이브 승인됨**(포괄 승인 근거, 슈퍼바이저 하 --deadline 없이, 감시 재무장).
    A-2 시장 현실이면: 재백필 없음, indeterminate 확정 + §9 표본 재정의 결정 메모(아침
    사용자 결정용 — 밤새 §9 개정하지 않는다).
  - **B**: W3 감사 리포트 worker_done → 게이트·머지 (docs/13+15 v1).
  - **C** (병렬): **Q2 분석** — 수집 DB 단독(랭킹+라이브 1분봉 완전, 백필 무관). W3.
  - **D** (A-1 시): 재백필 완료 → §1.7 재검 → 통과 시 전체 재분석 → docs/15 v2.
  - **E**: 아침 종합 보고. **홀드아웃은 밤새 절대 열지 않는다** (사용자 결정 사항).
  억지 작업 금지 — 위 체인 밖 신규 작업은 하지 않는다.

  **Phase 1-C 분석 진행 중 (08-02 09:20 사용자 승인·디스패치)** —
  `task_57172ff52ec5`/`ctx_d7ff11768c4c`/W3(term_2c49ee20).
  스펙: `coordination/specs/w3_phase1c_analysis.md`. 범위 = **train+val 만, 홀드아웃
  (2026-05-01~07-29) 봉인** — 열람은 별도 태스크·별도 승인. W3 에 docs/13(시행 레지스트리)·
  docs/15(리포트) 소유권 부여. 입력: w4-collector 백필 DB·매니페스트·캘린더 +
  w5-ops 재수집 DB(Q2 전용, 읽기 전용). status 1~2h 간격.
  완료 시: 머지 게이트 → 사용자에게 Q1~Q6 판정 보고 → 홀드아웃 열람 승인 논의. 정지 후 창 완료/잔여 보고 → **사용자 notify** → 사용자가
  노트북 재개 후 잔여 GO → 같은 명령 재실행이면 체크포인트가 이어받음.
  태스크 `task_bf64c4c19022`/`ctx_69b1466adb5b`, W4 핸들 term_8017b557.
  완료 시 worker_done → 머지 게이트(러너 패치 7a4d0a0 포함) → Phase 1-C 분석 디스패치.
- 스크린 산출물: data/backfill.db (유니버스+일봉 313,934), data/backfill/calendar_us.json
  (W3 save_calendar 형식, 2,107 매매일), 스크린 캐시(재스크린 불요, 멱등 재시작 안전). 완료 후 Phase 1-C 분석 디스패치
(스펙은 W3 리허설 보고서의 절차 초안 + 감사 §VII 러너 강제조항 3줄 기반으로 작성할 것).
잔여 후속(비긴급): 랭킹 클램프 분기 미발화 감시, 토큰 재발급 실패 api_errors 사각,
모니터 closed 세션 예외, F-6~F-9.
잔여 리스크(수용): A5 혼입 검사 구조적 순환(§7-e 강등 문언대로 미실행+보고),
첫 매매일 분할 원리상 미탐지, 보수 등록의 n_widened 은 백필 후 점검.
**`docs/12_preregistration.md` 를 먼저 읽고 그대로 따를 것** (개정 4건 전부 머지된 최신판).
`docs/13_trial_registry.md` 는 **첫 시행 전에** 생성해야 한다(형식은 사전등록에 규정).
분석 대상은 오늘 재수집분 + 백필. **어제(7/30) DB(`tossmon_20260730_polluted.db`)는
표적 모집단이 아니므로 분석에 쓰지 마라** — 파이프라인 검증 증거로만 보존.
- **백필은 W5 수집 종료(내일 ~08:50 애프터 마감) 후 리스를 넘겨받아 실행.**
  사전등록 §6.1: 백필 지연은 훈련 기간 영구 손실 — 리스가 비는 즉시 착수할 것.
- 백필 매니페스트에 `adjusted=false` 파라미터 기록 의무(§7-e 추인 개정),
  주 분석 곡선은 `prereg_volume_curve`, 일봉은 `prereg_daily_baseline`(앵커 필수),
  검출은 `split_dates` 전달 필수 — 미전달 시 리포트에 `(split_dates_not_applied)` 가 찍힌다.

---

## 4. 워커 배치 (워크트리·브랜치·모델)

repo id = `12e59c9d-6eff-4602-8e62-802907e489b4`,
워크트리 = `C:/Users/dongh/orca/workspaces/toss_trade/<name>`

| 워커 | 워크트리 | 브랜치 | 모델 |
|---|---|---|---|
| W1 | `w1-core-api` | `feat/core-api` | opus/xhigh |
| W2 | `w2-universe-store` | `w2-universe-store` | sonnet/high |
| W3 | `w3-analyzer` | `feat/analyzer` | opus/high |
| W4 | `w4-collector` | `w4-collector` | opus/xhigh + fable/xhigh |
| W5 | `w5-ops` | `w5-ops` | sonnet/high |
| W6 / W6b / W7 | `w6-audit` / `w6b-audit-opus` / `w7-prereg` | 동명 | fable / opus / fable |

**터미널 핸들은 재시작마다 바뀐다.** 항상 `terminal list --worktree ... --json` 으로 재확인하고,
워크트리당 핸들이 여러 개면 **에이전트가 아닌 셸에 디스패치하면 `no recognized agent detected`** 가 난다
— 순서대로 시도해 성공하는 것을 쓰면 된다.

스펙 원문: `coordination/specs/` (17+개). 워커별 재개 지점: `coordination/HANDOFF-*.md`.

---

## 4.4 ★ 전략 성립 여부 — 현재 증거 (08-03, 층화 재측정 후)

**나쁜 소식 (확정적)**
- 무조건부 진입은 손실(P5 accept, −4.47%/30분). 640개 조건부 규칙 **전부 CI 하한 음수**
  (얼린 1.0% 비용으로도 동일 — 비용 가정 탓이 아님).
- **"단일 고점 아크" 모델은 틀렸다** — 실제는 1분 미만 슈팅의 연속(사용자 관찰이 옳았음,
  77.3%가 2회+, 중앙 47초). 그러나 **슈팅은 쌓이지 않는다**(k→k+1 중앙 수익 0.0000).
  → 팔고 나서 또 온 슈팅은 **같은 가격**이었다. 후회는 감정적으로 타당·금전적으로 무근거.
- **표적 층($2~5 + §2.7)에서 슈팅 중앙 상승폭 +2.54% < 비용 3.22%.** 슈팅 하나를 통째로
  완벽히 먹어도 부족. 그리고 **우리 표적의 슈팅이 대형주보다 작다**(−0.68pp vs −0.13pp) —
  "동전주가 더 크게 움직인다"는 전략의 전제와 **반대**.

**08-03 저녁 갱신 — 두 항목이 "방향"에서 "판정"으로 굳었다 (3개 계열 수렴)**
- 슈팅 **평균 상승폭 +2.80% (CI +2.67~+2.93)** — **CI 상한이 비용 3.22% 아래**.
  평균 슈팅은 왕복을 못 덮는다(증명됨).
- **k→k+1 평균 −0.24% (CI −0.82~+0.31)** — 슈팅은 쌓이지 않는다(증명됨).
- 금액/거래량/병합 3계열에서 중앙 0.0254/0.0247/0.0252, k→k+1 **전부 정확히 0.0000** —
  계열 선택에 의존하지 않는 수렴 증거.
- **지속성 신호가 우리 층에서 반토막**: Spearman +0.273(전체) → **+0.115(표적)**.
  상위 4분위 슈팅 직후 조건부 평균 **+2.98% (CI +2.70~+3.29)** — **비용 미달**.
  전체 수치는 대형주가 부풀린 것이었다. **유일한 희망 통로가 크게 약해졌다.**

**★★ 08-03 심야 — 설계 A 사망, 설계 B 생존 (사용자 질문에서 촉발)**
사용자 지적: "비용이 슈팅보다 싸도 그 슈팅을 찾아 중앙값을 먹을 수 있는지는 다른 문제다."
정확했고, 우리 비교가 부정합이었다(사후 전구간 상승폭 vs 실집행 비용).

- **설계 A(슈팅을 매매) = 구조적으로 사망.** 사후 전구간 중앙 +2.52%(정의상 100% 양수) vs
  **탐지 후 남은 상승폭 중앙 정확히 0.0000%**(양수 11.2%), 관측가능 이탈 시 −0.86%(13초 지연).
  **코디네이터 독립 검증**: 사후 +3.01%/100% 양수 vs 탐지후 **0.0000%**/24.8% 양수 — 재현됨.
  임계값 1/2/3/5% 전부 스윕해도 남은 상승폭 중앙은 **매번 정확히 0.0000** —
  슈팅은 "이미 임계만큼 올랐음"으로 정의되므로 **탐지가 상승을 소진한다.**
  **데이터를 더 모아도 바뀌지 않는다(정의상).** → 이 방향은 영구 종료.
- **설계 B(과매도 진입 → 보유 중 슈팅에 매도) = 생존, 이 프로젝트 첫 양수 CI.**
  슈팅이 **85.4% 확률로 도래**(예측 불요 — 설계의 요점), 진입가→첫 슈팅 고점
  중앙 **+2.18%**, 평균 **+2.36%**, **CI [+1.36%, +3.47%] 전 구간 0 초과**(n=48, 유니버스 풀).
  미도래는 손실이 아니라 별도 범주로 계상.
  **단 경제성 미달**: +2.36% ≈ $100 클립 왕복비용 2.38% → 최소 클립에서 겨우 본전,
  $500+ 는 확실한 손실. 표적 층은 6건뿐(CI −1.56~+2.11) 이라 미판정.
  필요 수집: 방향 8일, 판정 32일(현재 3.0 진입/일).
- **전략 무게중심 이동**: "슈팅을 맞히는 게임"이 아니라 **"이미 든 포지션을 슈팅에 파는
  게임"**. 이는 사용자의 원래 매매 습관(과매도 진입, 슈팅에 이탈)과 정확히 일치한다.

**★ 남은 결정적 변수 = 실집행 비용 하나**
크기별 실측 비용: $100 **2.38%** / $500 4.45% / $1,000 4.91% / $2,000 6.44%.
중앙 슈팅 +2.54% 와 대조하면:
- **$100 클립에서만 비용(2.38%)이 중앙 슈팅(2.54%) 아래** → 회당 순 +0.16% = **$0.16**.
  월 $700 이려면 회당 4,400번 — 비현실적.
- $500 이상은 비용이 슈팅을 초과 → **크기를 키울수록 확실히 손실**.
→ **엣지는 존재하나 수확 비용보다 작다**는 전형적 미시구조 압착. 이 구도를 뒤집는 것은
**실제 체결 비용이 측정치보다 낮은 경우뿐**이다(사용자 실전 감각이 그 방향을 시사).
그러므로 **큐 D-2(계측 전용 실주문)가 "있으면 좋은 것"에서 "결정적 실험"으로 승격**된다.

**아직 열려 있는 것 (유일한 희망 통로)**
- **꼬리**: 표적 층에서도 **28.2% 의 슈팅이 비용을 넘고** p90 = +4.15%.
  "중앙 슈팅이 비용을 못 넘는다"와 "어떤 슈팅도 못 넘는다"는 다르다 — 후자는 거짓.
  단 우리 층의 꼬리가 4개 층 중 **가장 얇다**(28.2% vs 대형주 46.3%).
- **사전 관측 가능한 선별 신호 1개**: **지속성** — Spearman(직전 슈팅 상승폭, 이번 슈팅
  상승폭) **+0.273**(n=900), **상위 4분위 슈팅 직후 다음 슈팅 중앙 +3.80% > 비용**.
  누적 슈팅 수는 약함(+0.108), 간격은 **음수**(−0.231, 길수록 작다).
  → **선별은 종목 선택이 아니라 타이밍**(큰 슈팅 직후)이다. **가설이지 판정 아님**
  (표적 층 85 슈팅으로는 미검정).
- 표본: 표적 층 85 슈팅/8 심볼일/2 수집일. **약 2주 더** 모으면 §8 을 제대로 판정 가능
  (상승폭 분포 5일, 꼬리 p90 10일, 이탈 규칙 8일, 지속성 검정 10일).

**사이징·비용은 미결**: 크기별 비용 실측($100 2.38% → $2,000 6.44%)과 사용자 실전 감각
($2,000 무리 없음)이 충돌. **실체결 데이터 0건**이라 판정 불가(큐 D-2).

### 4.4-A 감사 5차 판정 (2026-08-03) — **설계 B 의 첫 양수 결과는 기각됐다**

`docs/25_audit5.md`, 머지 `0774f57`. 코디네이터 독립 확인 완료.

- **설계 B (+2.36%, 도래 85.4%) 기각.** 그 수치는 시장이 아니라 슈팅 정의 임계
  `min_rise=0.02` 의 함수다(임계 1/2/3/5% -> +1.71/+2.53/+3.24/+5.18%, 거의 1:1).
  도래율도 같은 함수(1% -> 97.9%, 5% -> 43.8%).
- **위약이 실제 규칙만큼 좋다** (종목 무작위 치환 도래시 +2.44~+4.87%).
  진입 규칙은 **크기가 아니라 도래율**만 움직인다. -> USER-INPUT-QUEUE D-5.
- **이탈이 사후 고점**(관측 불가). 접두사 불변성 위반: +2.17 -> +8.70 -> +14.13%.
  **진입(`find_oversold_entry`)은 정직하다** — 대조군 통과.
- **48건 중 39건이 하루(07-31).** 유효 군집 2 -> CI 성립 안 함.
  정직 기준으로 그 하루의 CI 는 [-0.60%, +2.34%] 로 0 교차.
- **전부 보정: 평균 +1.25%, CI [+0.02%, +2.42%], $100 비용 차감 후 -1.13%.**
- **룩어헤드 회귀 테스트 14건은 죽은 설계 A 만 지켰다.** 심은 변이 5건 중 2건만 사망,
  설계 B 의 "그날 최고가 매도"조차 48건 전부 통과.
- **§10 수치에 커밋된 러너가 없었다**(호출부 0) — 재실행 불가 상태였다.
- 살아남은 것: **설계 A 사망 판정**(관측 가능 이탈 9종 전부 음수, CI 전부 0 미만),
  `find_oversold_entry` 접두사 안정성, §10.3 비판정 재현, 미도래 정직 계상,
  `rules.simulate_exit` 정직성.

**운영 결론: 지금 추가 수집으로는 판정이 안 된다** — 같은 임계를 더 큰 표본에서 다시 잴
뿐이다. 단 **수집은 계속한다**(데이터는 설계 중립이고, 재측정에도 필요하다).

**진행 중 조치:**
- W3 `task_94950086e99e` — 커밋된 러너 + **슈팅 정의를 참조하지 않는 관측 가능 이탈**로
  전면 재측정, 위약을 상시 대조군으로, 날짜별 보고(군집 5 미만이면 통합 CI 금지),
  비용 차감 후를 헤드라인으로. **성공 기준: `min_rise` 를 쓸어도 답이 안 움직일 것.**
- W6 `task_7aad9a516fa0` — 접두사 불변성 속성 테스트 일반화(미등록 함수는 실패),
  변이 시험 CI 게이트(변이 적용 자체를 검증), H-3 픽스처 수정. **직무 분리로 W3 아님.**


### 4.4-B 설계 B 정직 재측정 결과 (2026-08-03) — **설계 A·B 둘 다 사망**

W3 `docs/23` §10-N, 머지 `7be2845`. **코디네이터가 러너를 직접 재실행해 전 수치 재현.**

**성공 기준 충족**: 러너가 `shots` 를 호출하지 않아 `min_rise` 가 답에서 **구조적으로**
사라졌다(비슷한 게 아니라 임계 1/2/3/5% 에서 값이 동일). 옛 +2.36% 중 약 **1.85pp 가
우리 임계값**이었다.

**결과 — 비용 이전에 이미 0 과 구분되지 않는다:**

| 이탈 | gross 평균 | gross 중앙 | gross 95% CI | 위약 대비 |
|---|---:|---:|---|---:|
| downtick_1 | +0.51% | -0.04% | [-0.16%, +1.28%] | +0.55pp |
| downtick_2 | +0.52% | 0.00% | [-0.38%, +1.47%] | +0.55pp |
| trail_1.0pct | +0.49% | -0.13% | [-0.22%, +1.29%] | +0.61pp |
| time_30s | +0.37% | 0.00% | [-0.18%, +0.99%] | +0.39pp |
| hold_horizon | +0.45% | -0.13% | [-1.22%, +2.32%] | +0.50pp |

- **12개 규칙 전부 gross CI 가 0 을 교차한다.** 비용을 빼기 전에 이미 그렇다.
- **중앙값이 0.00% 또는 음수** — 절반의 진입은 아무 일도 일어나지 않는다(유동성 사실).
- 최저 비용(시장가 $100 왕복 2.38%) 대비 최고 점추정치는 **1/5**.
  $2,000 클립은 -5.93%, 비용을 넘는 거래가 **6.0%** 뿐.
- **위약 대비 차이에는 CI 가 없다** — 따라서 "+0.55pp 엣지가 있다"고 주장할 수 없다.
  이 CI 를 내는 것이 다음 작업(아래 미결).
- 통합 CI 는 **보류**(거래일 군집 2 < 5). 러너가 규율이 아니라 **코드로 강제**한다.
- 날짜별로는 둘 다 순수익 CI 전부 음수(07-31 [-2.71%,-0.99%], 08-03 [-2.78%,-0.68%]).

**결론: 슈팅 역학 기반 설계(A: 슈팅을 탐지해 산다 / B: 슈팅에 판다)는 둘 다 끝났다.**

**비용 모델 주의**: 2.38% 는 `docs/21` 의 **호가창을 가로지르는(시장가성)** 실측이다.
사용자 전제는 처음부터 **SOR + 지정가**였고 그 비용은 **아직 측정된 적이 없다.**
따라서 "비용 때문에 안 된다"는 결론은 **시장가 가정에 한정**된다 -> D-2 가 결정적 실험.

**아직 시험되지 않은 것 — 프로젝트의 원래 논제 자체**:
"토스/커뮤니티 개미 유동성이 **그날의 러너들 사이를 옮겨다닌다**" 는 회전 논제는
직접 시험된 적이 없다. 지금까지 시험한 것은 **슈팅 역학**(과매도 진입·슈팅 이탈)이지
회전이 아니다. 사용자 직관 C(랭킹 상승 속도)·D(유동성 점유율)가 여기에 해당하고,
이를 재려면 티어2 호가·랭킹 시계열이 필요한데 **그 수집은 이제 막 쌓이기 시작했다**
(t2book 794건, 08-03 기준).

### 4.4-C 재측정 마무리 + 배선 가드 (2026-08-03) — **국면 종료**

머지 `3e9afa0`(차이 CI·비용 시나리오) -> `3a94b98`(배선) -> `da1f4e2`(가드 뒤집기).
**전 단계 코디네이터 독립 검증 완료** — 러너 직접 실행, 차이 CI 직접 재계산, 가드 직접 파괴 시험.

**측정 결론 (최종):**
- **위약 대비 차이: 0 초과 0건 / 0 교차 12건 / 0 미만 0건.** 본페로니 이전에 이미 전부 교차.
  최대 `hold_horizon` +1.35%p [-0.67, +3.74], `downtick_1` +0.53%p [-0.23, +1.41].
  -> **"과매도 규칙에 엣지가 있다"고 이 데이터로 주장할 수 없다.**
- 쌍 n 이 185 가 아니라 124~142 인 이유(위약이 뽑은 종목에 그 시각 호가 없음)를
  **세어서 보고**했다. 조용히 줄이지 않았다.
- **비용 3종:** 시장가 2.38% -> 12/12 사망. 절충 1.48% -> 12/12 사망.
  **지정가 0.2%(수수료만) -> 6개가 +0.17~0.32% 로 생존.**
  **약 0.2%면 살고 약 1.5%면 죽는다. 이것이 남은 질문 전부다.**
- (ii) 지정가는 **미체결·역선택 0 을 가정한 상한**이다. 그 대가는 이미 우리 데이터에
  보인다 — `target_2pct` 중앙값 +2.00%(목표가 그대로)인데 **평균 +0.01%**,
  미체결 40.5% 가 흘러내려 평균을 0 으로 끌어내린다.
- **필요 일수**: 점추정치가 참이라면 5~48일, 2개는 도달 불가.
  러너가 단서를 함께 출력한다 — **차이 CI 가 0 을 교차하는 한 며칠을 모아도 0 을 배제 못 한다.**

**재발 방지 (이번 국면의 진짜 산출물):**
- H-1 이 **재발했다가** 잡혔다: `paired_difference`·`COST_SCENARIOS`·`days_needed` 가
  테스트에서만 호출되고 `main()` 은 안 불렀다. 문서 수치가 애드혹 스크립트 산물이었다.
- 1차 가드는 **허용 목록**이라 뚫렸다. 코디네이터가 배선 안 된 함수를 심었더니 **16/16 통과**.
- **거부 기본값으로 뒤집었다**: 공개 함수를 AST 로 **발견**해 전부 `main()` 도달을 요구,
  예외는 `NOT_WIRED` 에 **사유와 함께**만. `REPORTED_FIELDS` 는 부분집합이 아니라 **동치** 검사.
- 뒤집자마자 **`min_rise_independence` 가 미배선인 것을 잡았다** — C-2 성공 기준 표(§10-N.2)
  자체가 러너에서 안 나오고 있었다. 옛 가드는 목록 밖이라 질문조차 안 했다.
- **가드 자신을 시험하는 메타 테스트 4종** 추가(고의 파괴 3 + 배선 후 침묵 1).
  코디네이터가 심었던 `sharpe_by_rule` 재현이 이제 **red**(직접 확인).
- 가드가 **못 잡는 것도 문서화**됐다(중첩 함수, 모듈 상수, "도달 가능 != 정확").

> **패턴 기록 — 이 프로젝트에서 세 번 반복된 "성공을 반환하는 조용한 실패":**
> (1) 룩어헤드 테스트가 죽은 설계만 지킴, (2) 분석 함수가 테스트에서만 호출됨,
> (3) 가드가 목록에 적힌 것만 검사함. **통과가 보증이 아니다.**
> 코디네이터 규율: 워커가 "가드/테스트를 만들었다"고 하면 **직접 깨보고** 넘어간다.

**다음 단계는 측정으로 풀 수 없다** — 지정가 체결 기록이 우리에게 없다.
페이퍼 트레이딩도 불가(시뮬레이터에 체결 가정을 넣어야 하므로 답이 가정을 되돌려준다).
**계측 목적 실주문(D-2)** 이 유일한 경로이며 사용자 결정 사항이다.

### 4.4-D 이탈 축도 닫혔다 (2026-08-03) — **여지는 우리 진입 덕이 아니다**

머지 `e3d3c07`(상한 측정) -> `3e94d67`(통제 위약). 코디네이터 러너 직접 재실행 검증.

**1차 결과(양수)**: 완전예지 상한 +5.85%, 위약 대비 **+4.55%p [+2.74, +6.75]** — 프로젝트
최초로 0 을 넘는 CI. 본페로니 통과.

**통제를 걸자 죽었다** — 러너가 직접 출력한다: `ceiling edge survives EVERY control: False`

| 통제 | 차이 | 95% CI | 판정 |
|---|---:|---|---|
| 변동성 정합 위약 | +1.06%p | [-0.60, +2.87] | **0 교차** |
| 변동성 정합 + 사전RV 정규화 | — | 0 교차 | **0 교차** |
| **같은 종목·무작위 시각 (최청정)** | +3.80%p | [+2.48, +5.38] | 0 초과(원값) |
| **└ 사전RV 정규화 (관측 가능한 정규화)** | **-2.68** | **[-5.64, -0.10]** | **0 미만** |

- 변동성을 맞추자 위약 상한이 +1.17% -> **+4.47%** 로 뛰었다. 원래 격차 4.94%p 중
  **약 3.3%p 가 순전히 "우리가 변동성 큰 종목을 골랐다"** 였다.
- 최청정 대조군(같은 종목·무작위 시각)을 **관측 가능한 사전 변동성으로 정규화하면
  점추정치가 음수**다 — 같은 종목의 **아무 시각이 우리 진입 시각보다 단위 변동성당 여지가 컸다.**
- 남는 잔여물: **"우리 진입은 변동성 큰 순간을 고르고, 변동성 큰 순간은 최댓값이 크다"**
  — 이는 발견이 아니라 **정의에 가깝다.** 감사 5차가 죽인 것과 같은 모양이며
  **이번에는 행동에 옮기기 전에 잡았다.**
- 정합 후보가 없어 짝을 잃은 진입 25/185 는 **세어서 출력**한다.

**부수 성과 — 조용한 짝짓기 결함 발견·수정:**
`collect_entries` 가 `entry_idx` 를 **날마다 0 부터** 매겨서, 두 날을 이어붙이면 키가
충돌하고 pandas 가 **중복 라벨을 조용히 브로드캐스트**해 **날짜를 넘나들며 짝을 지었다.**
오류가 없어 드러나지 않았고, 한 대조군이 **짝 161개**를 보고했는데 정합된 진입은 **160개**여서
꼬리를 잡혔다. 날짜 접두 키 + 회귀 테스트 4종으로 수정, 두 러너 모두 재실행.
**판정은 불변**(여전히 12/12 0 교차), 짝 n 136 -> 125, 차이 소폭 이동.

> **"성공을 반환하는 조용한 실패" 네 번째 사례.** pandas 중복 인덱스 브로드캐스트.
> 앞선 셋: 룩어헤드 테스트가 죽은 설계만 지킴 / 분석 함수가 테스트에서만 호출됨 /
> 가드가 허용 목록만 검사함.

**현재 전략 지형 — 측정 가능한 축은 전부 닫혔다:**
- 설계 A(슈팅 탐지 매수): 사망 (감사5가 재확인)
- 설계 B(슈팅에 매도): 사망 (관측 가능 이탈 20종 전부 비용 미달)
- 이탈 여지: 존재하나 **우리 진입에 귀속되지 않음**
- **남은 것 둘**: (1) 비용/체결 품질 — 우리 데이터로 불가, **D-2 실주문 필요**,
  (2) **회전 논제 자체 — 프로젝트 원래 전제인데 아직 한 번도 시험 안 함**

### 4.4-E 회전 논제도 닫힘 + 새 후보(감속 진입) 착수 (2026-08-04)

**회전 논제 종료 — 두 방향에서 독립적으로.**
- **비대칭 검정**: `corr(+k) − corr(−k)` 를 쌍체 부트스트랩 + 본페로니(21셀)로.
  **21셀 전부 대칭.** 정규장 k=1 비대칭 **−0.0004**(선행 +0.0200 vs 후행 +0.0203).
  **어느 세션 어느 시차에서도 선행이 후행보다 세지 않다.**
- **경제성**: 점유율 상위 10분위 매수 → 1분 후 매도 = **분당 +1.76bp** vs **왕복 66bp**.
  **37배 부족.** n=10만이면 상관 0.02 도 유의하지만 분산의 0.04% 다.
- 옛 규칙("선행 셀이 하나라도 0 초과")은 같은 데이터에서 **PROCEED** 라고 한다.
  실행 시점에 따라 답이 바뀌었다 — 코디네이터가 재실행으로 발견, 규칙을 교체했다.
- Q0 정정: 랭킹 윈도우는 스냅샷마다 7/11/15분으로 흔들린다(관측 95~121).
  **"7~15분, 분 단위"** 로만 말한다. 결론(1분 미만 선행 불가)은 그것만으로 성립.
- **홀드아웃 누출을 W3 가 스스로 발견·차단**했다 — 캔들이 백필이라 봉인 구간(5/1~7/29)을
  품고 있었다. `session.drop_holdout` 신설, 297,506행 버림/582,293행 유지를 산출물에 기록.

**새 후보 — 감속 진입 (`docs/27`, 사용자와 직접 설계).**
사용자 실전 행동: **"떨어지는 칼날을 잡는 쪽. 반등 확인 전에 바닥을 잡고 싶은 심리.
손해본 적도 많고 의외로 바닥이 잡힌 경우도 많았어 — 떨어지는 속도가 느려지다가 살짝 올랐거든."**
- 조건: **`v<0` 이면서 `a>0`** (하락 중이되 감속 중). 1차 도함수 0(바닥)이 아니다 —
  그건 사후 정보다. **과거 값만으로 계산되므로 접두사 불변.**
- 사용자 지적("휴리스틱 하드코딩으로는 알파를 얻기 힘들다")에 따라
  **1단계는 임계값 없이 연속 면**으로 시작. 운용점은 데이터가 정한다.
- **구조 반전**: 떨어지는 칼날 잡기 = **아래에 지정가 걸고 대기** = 스프레드 미지불.
  사용자 관찰("오를 땐 지정가 체결이 안 됐다")과 정합 — 방향이 반대다.
- **반드시 통제**: 호가 튕김(가장 위험, 기존 개미털기 +0.5% 가 통째로 이것일 수 있다),
  무거래를 감속으로 오인, 2차 도함수 잡음 증폭.
- 중단 기준 5개 사전 등록. W3 `task_f508d416d946` 진행 중.

**운영 사건 — 코디네이터 배포 실수와 복구:**
1. 00:07:28 W4 티어 수정 배포(공백 34초). 승격 136/분 → 1/분 미만, 테이프 결손 정지(313).
2. **그런데 `compete=False` 가 축출을 통째로 껐다.** tier2 가 300 상한에서 **닫힌 집합**이
   되고, tier2 가 tier3 의 유일한 입구라 **tier3 이 10→3 으로 말라죽었다.**
   체결 수집이 43종목 → 4종목으로 붕괴. **W4 시뮬의 "전이 20→0" 은 안정화가 아니라 동결
   신호였고 코디네이터가 그것을 못 읽고 배포했다.**
3. 01:20:36 재배포. 활동 승격이 **고정 진입점수 0.30**(강등선 0.22 + 마진 0.08)으로
   경쟁 복귀 + 재진입 쿨다운 600초. tier3 회복 시작(4→5), 승격 69/분(진동 136 과 동결 5 사이).
4. **워치독이 내 재시작 창과 겹쳐 자체 재시작을 발동**했다(STOP 을 9초만 유지해서).
   결과는 수렴(프로세스 트리 1개)했으나, **계획 정지와 장애를 구분하는 표시가 필요**하다.
   -> W5 후속 후보.

> **교훈 기록: "전이 0" 을 안정화로 읽지 마라.** 건강한 시스템은 정당한 신규 승격을
> **계속** 낸다. 시뮬레이션 성공 기준을 "진동 0 **그리고** 신규 승격 > 0" 으로 바꿨다.

### 4.4-F 감속 진입도 사망 — **잴 수 있는 축이 전부 닫혔다** (2026-08-04)

머지 `3a9656d`(1차) → `dcc7b27`(창 안정성 게이트 + 본페로니 20칸 + 경제적 크기 열).
main **1,328 passed · 1 skipped** (08-04 09:40 코디네이터 직접 재실행 실측).

**W3 가 자기 유의성 주장을 철회했다** (docs/27 §7.4). 이것이 이 결과의 핵심이다:
- 1차 보고: "정규장 최급락 −7.84 bp, 본페로니 CI [−17.69, −0.29] → 유의하게 음수".
- 보정 분모를 5 에서 **20**(실제로 들여다본 칸 수)으로 바로잡자 CI 가 [−16.10, +2.81] 로
  **0 을 교차**. 코디네이터 재실행의 `v분위 1` +8.01 bp 도 **창을 바꾸면 사라진다**
  (3/5/10봉 = 9.75 / 8.11 / **2.94** bp). 20칸 중 창에 일관된 칸은 6개, 정규장은 1개뿐.
- 정확한 진술: **"정규장 하락 구간에서 가속도는 어느 방향으로도 유의하지 않다."**

**그리고 크기가 판정을 무의미하게 만든다** — 가장 큰 "유의한" 효과 **8.33 bp** vs
정규장 실측 왕복 비용 **66 bp** = **0.13배**. **20칸 전부 `economically_dead`.**
어느 쪽으로 판정되든 거래로 옮길 수 있는 크기가 아니었다.

    stopped at: stage2 / proceed: False
    reason: acceleration adds nothing once velocity is controlled -
            this is just 'buy the dip', which was already near zero

**남은 것 (docs/27 §7.7)**: 속도 구조 자체는 호가 튕김 검정(3단계)을 받지 못했다 —
**"평균회귀가 있다"고 인용하지 마라.** 날 군집 3일, CI 폭 ±10~15 bp, 효과 5~10 bp 라
잡음을 재고 있을 가능성이 높다. 그리고 **진입 순간 비용이 평상시의 6.8배로 보이는데
표본 11건** (§10-R.6) — D-2 설계에 직접 영향이므로 큐 D-6 에 사용자 질문으로 올렸다.

> **후보 현황: 설계 A 사망 / 설계 B 사망 / 이탈 여지 귀속 안 됨 / 회전 논제 사망 /
> 감속 진입 사망. 살아 있는 전략 후보 0개.** 남은 축은 **비용·체결 품질(D-2)** 하나이고
> 그것은 우리 데이터로 못 푼다. **큐 D-6 이 답을 받기 전까지 분석 워커에 시킬 일이 없다** —
> 억지로 만들지 마라. 수집은 계속한다(데이터는 설계 중립).

---

## 4.6 일일 코디네이터 실행 기록 — 2026-08-04 09:30 (자동화)

**수집: 정상.** 지난 24h 랭킹 폴 25,723회(중앙 간격 0.3s), 90초 초과 공백 **단 2건**
(00:08→00:11 배포 2.1분, 08:49→09:00 아래 참조). 캔들 259,654 / 체결 107,130 /
호가 48,214 / 이벤트 42. `api_errors=0`, `auth_failures=0`, `fetch_success_pct=100.0`,
`watch_outside_universe=0`.

**★ 어젯밤 tier3 수정이 실제로 들었다 (DB 직접 확인, 워커 보고 아님):**

| 시각(KST) | tier3 승격/시간 | 체결 수집 종목 |
|---|---:|---:|
| 00:00 (고사 중) | 5 | 47 |
| 01:00 (최저) | **1** | 47 |
| 02:00 (`fill_to_capacity` 배포 후) | 39 | 84 |
| 03:00 | 67 | **101** |
| 04:00 | 90 | **107** |

사고 전(08-03 23:00) 78종목보다 **높다.** `tier3_capacity_fills` 가 계속 오른다(393).
**정원을 상대 순위로 채우는 방식이 맞았다.**

**오늘 아침 발견한 결함 2건 — 둘 다 "거짓 CRIT" 계열이다** (이 프로젝트가 세 번 데인
"성공을 반환하는 조용한 실패"의 거울상: **정상을 사고로 보고하는 시끄러운 실패**):

1. **워치독이 멀쩡한 수집기를 재시작했다** (08:55, 실제 발생). 근본 원인 확정:
   `closed` 세션에는 랭킹 루프가 쉬고(설계대로) 활동이 없어 `collector_state.json` 이
   **`session="after"` 인 채로 얼어붙는다**(마지막 저장 08:49:44). 어제 W5 가 넣은
   `c5a3a5f`("상태 파일을 1차 소스로")가 그 언 값을 믿어 `$openSession` 가드를 통과하고,
   얼어붙은 `last_ranking_snap_ms` 로 계산한 age 382s 로 CRIT + 재시작.
   **08:50~09:00 KST 는 토스 캘린더에 세션이 없는 구간**(after 05:00–08:50 / day 09:00–17:00)
   이라 **매일 재발한다.** DB 실측: 폴이 08:49:59 에 끊기고 09:00:00 에 정확히 재개(그 사이 0건).
   → **어제의 실명 수정이 오늘의 거짓 경보를 만들었다.** W5 `task_0c77aa33b2fc` 발행.
2. **`schema_mismatch` CRIT 5건이 전부 `http-404 code=stock-not-found`** (AVAT·BLRK·CABR·
   MACI·JSM = 없는/상폐 종목). 경보 문구는 "API 응답 모양이 바뀌었다, 재시작으로 안 고쳐진다,
   **오늘 데이터를 믿기 전에 계약을 검사하라**" — 멀쩡한 하루를 의심하게 만든다.
   `_guarded` 가 `SchemaMismatch` 를 통째로 한 카운터에 넣는 것이 원인. W4 `task_8280709b754a`.

**부수 발견 — 429 는 우리 사용량과 무관하다.** 오늘 09시 7건, 사용량은 한도의 15~20%
(`MARKET_DATA 1.05~1.40/7.00`, `api_errors=0`). 발생 시각이 **전부 KST 09~15시**(미국장 마감 중)
이고 **정규장·프리·애프터에는 3일간 0건**이다. 그런데 대응이 **티어 정원 축소**라
오늘 tier3 정원이 6→4→3→2 로 깎였다. 장이 닫혀 무해했지만 **같은 일이 22:30 개장 직후에
나면 어제 우리를 태운 tier3 고사가 재발한다.** W4 태스크 (2)번으로 조사 지시(수정 의무 아님).

**워커 상태 정정**: `task_7aad9a516fa0`(W6 룩어헤드 테스트 게이트)는 08-03 14:32 KST 에
dispatched 였으나 **워커에 도달한 적이 없다** — W6 터미널은 프롬프트에서 놀고 있었고
`w6-audit` 브랜치는 감사5 문서 이후 움직이지 않았다. **`ready` 로 되돌렸다**(추적기가
"진행 중"이라고 거짓말하지 않게). 오늘 재디스패치하지 않은 이유는 **디스크뿐**이다 —
큐 D-7 참조. 디스크가 풀리면 바로 올려라. 급하지 않지만 가치는 그대로다.

**미해결 (내가 못 하는 것)**: 디스크 여유 6.71 GB, 시간당 0.7 GB 하락, 상위 소비자가
WSL 10.6 GB / Docker 10.0 GB / Claude VM 8.7 GB — **전부 사용자 소유**. 수집 데이터는
1.0 GB(전체의 3%)뿐이라 **내가 지워서 벌 수 있는 게 없다.** 큐 D-7.

**운영 메모**: `orchestration` 바인딩이 이 세션에서 **두 번 풀렸다**(`consumer_generation`
4→6). `run_required` 가 나오면 조용히 실패하니 — `task-create` 한 건이 그렇게 유실됐다 —
**`--json` 출력의 `"ok"` 를 매번 확인하고, 실패하면 `run-use` 로 재바인딩 후 재시도하라.**

---

## 4.8 ★ 개장 사고 — 예산 거버너가 정원을 통째로 날렸다 (2026-08-04 22:30, 실시간 관측)

**사용자가 "개장을 지켜봐"라고 지시해서 코디네이터가 붙어 있다가 5분 만에 잡았다.**
자동 감시(워치독)는 **이 사고를 못 잡는다** — 프로세스도 API 도 멀쩡했고
`api_errors=0 auth_failures=0 fetch=100%` 였다. **에러 없이 수집량만 사라지는** 종류다.

```
22:30:09  개장. 정원 리셋 tier2_cap=300 tier3_cap=10, tier2=18 tier3=3      <- 정상
22:30:38  ERROR CHART 1초에 6회(한도 5) + "정원이 아니라 리미터가 1초 창을 못 지킨다"
22:31:22  ERROR MARKET_DATA 1초에 11회(한도 10), 같은 문구
22:33:14  budget shrink {MD:9, CHART:299} -> caps={tier2:1, tier3:1} demoted=316
22:35:11  tier2=3 tier3=1 tier2_cap=2 tier3_cap=2
```

**크기**: 어제(08-03) 같은 시간대 tier2 는 **307~314 종목**. 오늘은 **3** 이었다.

**핵심은 코드가 옳은 진단을 찍고 틀린 행동을 한다는 것이다.**
경보 문구가 *"티어를 깎아도 같은 버스트는 또 난다"* 라고 말해놓고 티어를 깎았다.
1초짜리 **버스트**(타이밍 문제)에 **정원**(지속 속도 손잡이)으로 대응했다.
> **"성공을 반환하는 조용한 실패" 다섯 번째 — 이번엔 사촌이다: 정확한 진단을 출력하고 무관한 조치를 취함.**

**비대칭이 치명적이다**: 내려갈 땐 한 번에 **299**, 올라올 땐 **+1**(5분에 한 번).
tier3 복구 ~40분, **tier2 복구 ~25시간**. 즉 **한 번 깎이면 그날 밤은 끝난다.**

**조치 (사용자 승인 후 코디네이터 집행, 22:39~22:41)**: §4.7-3 절차 그대로 —
`PLANNED` 마커 -> `STOP` -> 종료 확인 -> `STOP` 제거 -> **워치독이 되살림**.
`schtasks /run` 은 쓰지 않았다. **공백 78초**(22:39:44~22:41:02, 예상 5~6분보다 짧았다 —
워치독 5분 주기가 마침 맞았다). 재기동 후 `tier2_cap=300 tier3_cap=10 over_limit_1s=0`,
축소 재발 없음.

**원인 미확정 — 오늘 배포가 둘이라 못 가른다.** ① 초당 첨두 판정(13:09) ② `usage_ratio` 0.85(13:39).
②가 원인처럼 보이지만, ①이 없었으면 **어제도 똑같이 넘었는데 못 봤을 뿐**일 수 있다.
그래서 `usage_ratio` 를 임의로 되돌리지 않았다. W4 `task_cc850c75a2fd` 가 이걸 가른다.

**교훈**: 이날 아침 §4.6 이 *"같은 일이 22:30 개장 직후에 나면 tier3 고사가 재발한다"* 고
정확히 예측했다. **예측해놓고 막지 않았다.** 알려진 재발 위험은 관찰 항목이 아니라 작업 항목이다.

### 4.8-1 해결 (같은 날 23:36 배포, main `ff83d68`)

**원인은 `usage_ratio` 가 아니라 13:09 배포(초당 첨두 판정)였다.** W4 가 시뮬로 갈랐다 —
사고 당시 코드에 첨두 7 을 넣으면 **0.70 에서도 0.85 에서도 `drop=299` 로 동일**하고,
13:09 이전 평균 기반 규칙이었으면 429 바닥값인 30 만 나왔다. 코디네이터의 라이브 실험
(22:49 롤백 -> 5분 만에 재발)과 독립적으로 같은 답에 닿았다.

**산수 전모**: `ceil(excess/per_symbol_cost)`, tier2 한 종목 = 1/110 = **0.00909 req/s**.
CHART 첨두 7 -> 원 지시 **350** -> `min(n, have-1)` 에 잘려 299.
**첨두가 정확히 한도(5)일 때조차 130종목 제거를 지시한다** — 넘지 않아도 깎는다.

**진짜 결함은 계측이었다** (W4 자기 회귀로 인정): `_unaccounted_attempts` 가 **전역**
요청 카운터 델타를 통째로 한 그룹에 얹었다. 평균을 보던 시절엔 "총량 정확·방향 보수적"
이라 넘어갔고 **판정이 초당 첨두로 바뀐 순간 사고가 됐다.** 붉은 테스트가 실물로 보여준다:
**CHART 가 실제 2콜을 내고 11 로 계상**됐다.

**수정 3건**: ① 축소 판정·크기를 지속률로(첨두는 경보·텔레메트리 계속) ② 귀속을
`호출+재시도`로 자르고 나머지는 `budget_unattributed_attempts` 로 명시 ③ `SHRINK_MAX_STEP_FRAC=0.25`
로 축소를 회복과 대칭으로.

**코디네이터 독립 검증 (직접 재실행·직접 파괴):**
- 전체 1544 passed·1 skipped 재현.
- 주 수정(첨두->지속률) 되돌리면 **빨감**. 귀속 자르기 되돌리면 **빨감**(`assert 11 <= 2`).
- 축소를 통째로 끄면 **13개 빨감** — `진짜 지속 과부하는 여전히 축소`, `실제 429 는 워밍업
  중에도 즉시 축소` 포함. **경보를 그냥 끈 게 아니라는 증거.**
- **구멍 1개 발견·기록**: `SHRINK_MAX_STEP_FRAC` 을 1.0(상한 없음)으로 바꿔도 **132개 전부 통과**.
  2차 방어선이 시험되지 않는다. **후속 테스트 필요.**

**배포 후 실측 (23:41)**: `tier2=310 tier3=10 cap2=300 cap3=10` — 어제 수준 완전 복구.
그리고 **같은 버스트가 여전히 난다**(배포 후 `CHART 1초에 6회`·`MD 1초에 12회` 4건)
그런데 **축소 지시 0건**. 탐지는 그대로, 반응만 바뀌었다.

**남은 수수께끼 (W1 이 내일 잰다)**: 그 첨두 경보가 뜰 때 **`http_429` 는 0** 이다.
낮에는 반대로 `over_limit_1s=0` 인데 429 12건이었다. **계측기가 서버와 양방향으로 어긋난다.**
`docs/32` 참조. 그리고 `docs/30` §2 가 "고쳤다"고 적은 429 헤더 계측은 **안 고쳐져 있다** —
`client.last_429` 를 `loops.py` 가 한 번도 읽지 않는다(W1 발견, 다섯 번째 조용한 실패).

**오늘 밤 정규장의 인공 구멍 3개** (분석 시 사고로 읽지 말 것):
22:39:44~22:41:02(78s) / 22:49:16~22:51:09(113s) / 23:34:46~23:36:10(84s) — 전부 계획 정지.
그리고 **22:33~23:36 은 정원이 깎인 채 운영**돼 커버리지가 정상이 아니다(tier2 1~21종목).

> **교훈 — 하루에 배포를 넷 하면 사고의 원인을 못 가른다.**
> 오늘 밤 원인 규명이 오래 걸린 유일한 이유가 그것이다. 13:09 와 13:39 가 붙어 있어서
> 라이브 롤백 실험을 한 번 태워야 했다. **배포는 하나씩, 그리고 개장 같은 부하 전환을 한 번 넘겨보고.**

## 4.9 아침에 할 일 (2026-08-05) — 밤사이 코디네이터가 남긴 것

**밤 상태**: 수집기 정상(23:36:00 기동, W4 수정 + `usage_ratio` 0.85 적용본),
`tier2 305~310 / tier3 10`, 429 0건, 에러 0. main `1580 passed·1 skipped`.
워커 3명 전원 `worker_done`, 열린 태스크 없음.

**아침 리포트는 자동으로 뜬다** — `tossmon-dailyhealth` 08:52, 이번부터 **결손 감사 포함**
(`ops/gap_audit.py` 배선 완료). 어제와 형식이 다르니 놀라지 마라.

### 사용자와 논의할 것 (혼자 판정 금지)

1. **★ `candles_1m.ts_ms` 가 종료 시각 라벨인데 분석 코드가 시작 시각으로 읽고 있을 수 있다.**
   W5 실측(`docs/31` §2): 21:51 봉이 `[21:50,21:51)` 을 덮는다(99.1% presence,
   93.4% 정확 거래량 일치 at +1min). 그런데 `tossmon/analysis/features.py` 의 컷오프 근거는
   *"T0 봉의 종가·거래량은 그 분이 끝난 뒤에만 관측되므로"* — **시작 시각 라벨 전제의 문장**이다.
   테스트는 합성 시나리오라 자기 규약끼리 일관돼 이 불일치를 못 잡는다.
   **사실이면 1분봉 기반 특징 정렬이 한 칸 어긋나 있었다는 뜻**이고, 과거 판정 해석에 닿는다.
   방향은 **누출이 아니라 과보수** 쪽으로 보이지만(종료 라벨이면 T0 봉은 이미 완결)
   `_minute_volumes` 같은 창 계산은 통째로 1분 밀린다. **코디네이터가 확정하지 않았다.**
   W5 도 "확인하지 않았다, `tossmon/**` 소유자가 볼 일"이라고 정확히 넘겼다.

2. **W1 2단계 프로브는 승인됐다 — 그런데 누가 깨울지 정해야 한다.**
   실행 창이 **오늘 12:00~15:00 KST**(429 가 실제로 나는 시간대)다. W1 은 `worker_done` 후
   idle 이라 **누군가 다시 디스패치해야 돈다.** 120콜·버스트 0·중단조건 있음.

### 워커에게 바로 낼 수 있는 것 (판정 아님, 기계적)

3. **`SHRINK_MAX_STEP_FRAC` 이 시험되지 않는다** — 1.0(상한 없음)으로 바꿔도 132개 전부 통과.
   코디네이터가 직접 확인했다. W4 후속 테스트. **작지만 이 프로젝트가 이걸로 데인 적 있다.**
4. **`client.last_429` 가 `loops.py` 에서 한 번도 안 읽힌다** — `docs/30` §2 의 "고쳤다"가
   현재 거짓이다. W1 이 `docs/32` §5-1 에 한 줄 제안만 남겼다(파일은 W4 소유라 안 고침). W4 몫.
5. **`usage_ratio` 가 `config_sig` 에 없다** — 오늘 밤 0.85→0.70→0.85 가 **데이터에 안 남는다.**
   `DATA-QUALITY-PROGRAM` 규칙 3("경계를 데이터에 남긴다")이 지금 안 지켜지고 있다.
   `config/config.yaml` 자체가 gitignore 라 git 에도 안 남는다 — 지금 유일한 기록이 §4.8 이다.
6. **개장 승격 버스트 357건/5분** 자체를 아무도 안 봤다. W4 가 "자기 진단을 받을 만하다"고 지적했다.
   이 버스트가 오늘 밤 사고의 **입력**이었다.

### ★ 7. 우리 예산 숫자가 전부 무효일 수 있다 (00:20 발견, 사고 아님)

배포 후 `tier2_orderbook_snaps` 가 **23:40 부터 완전히 멈췄다**(`_skipped_rate` 만 5분에 147씩 증가,
`_429` 는 불변). 처음엔 회귀인 줄 알았으나 **아니다 — 수정이 든 결과다.**

- tier3 호가(`orderbook_snaps`)는 **정상**(22:11 87,640 → 00:20 96,474). 체결·분봉도 정상.
- 멈춘 것은 tier2 의 **넓은 훑기**(600초 주기 × ~300종목)이고 사유는 **예산 양보**다.
  워치독 문구대로 *"tier2 호가는 제일 먼저 희생된다"* — **설계된 희생 순서**다.
- 어제 같은 시각 tier2=308 에서는 이게 돌았다(5분에 135건). **왜 어제는 됐나?**
  **어제는 tier3 가 굶고 있었기 때문이다.** W5 측정(`docs/31`): *"tier3 는 설정된 10 에
  한 번도 도달한 적이 없다 — 모든 창에서 평균 2.86~2.89종목."*
  **오늘 밤이 tier3 가 10 을 채운 첫 밤**이고, 그 4초 호가·4초 체결이 예산을 제대로 쓰자
  tier2 훑기가 밀려났다.

> **함의: 우리가 가진 예산·여유 숫자는 전부 tier3 가 ~3종목으로 굶은 상태에서 잰 것이다.**
> `docs/30` §5-3 의 *"늘어난 예산 여유 +2.047 req/s 를 어디 쓸지 미결, W4 권고는 체결 2.5초
> 또는 12종목"* 은 **그 전제 위에 서 있어 지금 무효다.** 정원이 실제로 차면 여유가 없다.
> **재측정 전에는 어떤 예산 재배분도 하지 마라.** `DATA-QUALITY-PROGRAM` 축 2 의
> "예산 여유 재배분"은 이 재측정이 선행 조건이다.

**사용자 결정이 필요한 트레이드오프**: 지금 구도는 **tier3 10종목을 촘촘히 vs tier2 300종목을
600초마다 훑기** 중 전자를 택한 것이다. 자동으로 그렇게 된 것이지 누가 고른 게 아니다.
tier2 훑기가 승격 후보 발굴에 쓰이는 것이라면 이 100% 희생이 맞는지 물어야 한다.

### 오늘 밤 데이터를 분석에 쓸 때 (인공 구멍)

계획 정지 3회 — 22:39:44~22:41:02(78s) / 22:49:16~22:51:09(113s) / 23:34:46~23:36:10(84s).
그리고 **22:33~23:36 은 정원이 깎인 채 운영**(tier2 1~21종목, 정상은 ~310).
**23:36 이후만 정상 수집이다.** 이 밤을 하나로 뭉쳐 "정규장 결손"을 계산하면 틀린다.

## 4.5 무인 장기 실행 표준 (08-02 사용자 지시 — 구속력 있음)

**"중단돼도 사용자 개입 없이 빠르게 복구"가 모든 무인 장기 실행의 기본 요건이다.**
백필 2연속 사망(PTY 킬 8분 공백, IP 플랩 5시간 미발견)에서 채택. 3층 구조:
1. **자가 복구 슈퍼바이저** — `tools/backfill_supervisor.py` (W4-b `task_b0bae554f20c` 구축):
   403 은 10분 프로브 대기 후 자동 재개(IP 플랩 자가 치유, 포털 재등록도 등록 즉시 자동
   재개), 일시 오류 백오프 재기동, 3연속 크래시만 ESCALATION. STATUS 는 러너 생존을
   함께 찍는다(죽은 숫자 위장 불가). schtasks 기동 + STOP 파일 + --deadline.
2. **코디네이터 독립 감시** — 로그 기반 Monitor(FATAL/RUNNER-DEAD/ESCALATION 문자열
   + 40분 침묵). 워커 전멸에도 작동. 새 코디네이터는 인수 시 재무장할 것.
3. **사용자 개입은 진짜 필요한 것만** — 현재 유일: 토스 포털 IP 등록(자동화 불가).
   그 외 모든 복구는 기계가 한다.

## 4.7 코디네이터 운영 규율 — 2026-08-04 에 실증된 것

이날 코디네이터(나)가 같은 실수를 반복했고 **워커가 세 번 잡았다.** 규칙으로 남긴다.
그날의 인프라 변경 자체는 `docs/30_infra_20260804.md`.

### 4.7-1 ★ `dispatched` 는 "보냈다"이지 "받았다"가 아니다 — **하트비트를 봐라**

이날 네 번 당했다:
- W6 룩어헤드 게이트가 하루 걸려 있다가 `ready` 로 되돌아옴 (워커가 안 받았다)
- W3 틱 계측기 — 대상 터미널에 **에이전트가 없어** 디스패치 거부
- W4 검출기 수정 — 워커가 **턴을 끝낸 뒤** 내가 그 우편함에 메시지를 넣음. 읽을 사람이 없었다
- W5 워치독 — **13시간 동안 `dispatched`, 하트비트 0.** 아무도 일하지 않았다

```bash
orca orchestration dispatch-show --task <id> --json    # last_heartbeat_at 을 봐라
```

`last_heartbeat_at: None` 이면 **아무도 안 하고 있다.** 상태값만 보고 "진행 중"이라 말하지 마라.
죽은 디스패치 복구: `worker-abandon --dispatch <id>` -> 태스크가 `blocked` 로 떨어지면
`task-update --id <task> --status ready` -> `worker-start --task <task> --worktree <path> --agent claude`.

### 4.7-2 ★ 병합했다고 말하기 전에 **내용으로** 확인해라

이날 두 번, 병합이 실패했는데 성공으로 보고했다.
`git merge w1-core-api` 를 쳤는데 실제 브랜치는 `feat/core-api` 였다.
Git 은 `not something we can merge` 를 냈는데 배경 작업 출력에서 스크롤로 지나갔고,
**안 바뀐 HEAD 줄을 성공으로 읽었다.**
**워커가 저장소를 직접 확인해 "그 산출물이 없다"고 잡아냈다** — 내 명세가 "머지 완료"라고
단언했는데도 확인한 것이 옳았다.

**규칙**: 머지 후 `git log` 한 줄이 아니라 **바뀐 파일의 내용**(상수·함수·문서 절)을 grep 으로 확인한다.

### 4.7-3 ★ 수집기 재시작을 **내 세션에서 띄우지 마라**

`Start-ScheduledTask` / `schtasks /run` 을 내 셸에서 실행하면 세션의 콘솔 정리가
**Ctrl+C 를 전파**한다. 이날 `LastTaskResult=3221225786`(0xC000013A)로 두 번 죽었다.
직접 실행하면 멀쩡하므로(시작 오류 아님) **예약작업 경로만** 죽는다.

**안전한 절차**: `STOP` 파일 생성 -> 종료 확인 -> `STOP` 제거 -> **워치독이 5분 주기에 되살리게 둔다.**
워치독은 svchost 밑이라 안전하고, 이날 실제로 6분 공백 뒤 자동 복구했다.
계획 정지에는 `data/ops_state/PLANNED` 를 함께 놓아 경보가 `PLANNED_` 로 분류되게 한다.

### 4.7-4 같은 파일을 건드리는 태스크를 같은 워크트리에 겹쳐 보내지 마라

이날 W4 워커 둘이 `loops.py` 를 동시에 편집할 뻔했다(한 명이 멈춰 물어봐서 막혔다).
발행 전 **소유 파일이 겹치는지 확인**하고, 겹치면 순서를 명시하거나 워크트리를 나눈다.

### 4.7-5 판정하지 말고 숫자를 가져와라

사용자 지시(2026-08-04): **"중요한 안건에 대해서는 함부로 판단하지 말고 나의 신경망으로 토론할 것."**
근거: 초 단위 전략을 **1분봉으로 분석하고 후보 6개를 닫았다.**
자세한 것은 `resolution-must-match-phenomenon` 메모리와 `docs/29`.

**워커 명세에 "판정 금지"를 명시**하고, 결론에 **측정 조건**(창 길이·판정 방법·오차율·표본 수)을
붙이게 한다. "성립 안 함"이 아니라 "N초 창, 오차 X% 에서 관측 안 됨".

### 4.7-6 "평균으로 뭉개면 안 보인다" — 분석과 인프라에서 같은 모양이었다

- **분석**: 1분봉 한 칸이 **6초짜리 슈팅**을 통째로 삼켰다.
- **인프라**: 60초 평균 0.178 req/s 가 **한 초에 한도의 80%** 를 쓰는 버스트를 숨겼다.

**둘 다 "평균이 사건을 삼킨 것"이다.** 새 지표를 만들 때 **사건의 시간축과 측정 창을 먼저 대조**한다.

## 5. 이 세션에서 비싸게 배운 운영 규율

- **워커 주장을 믿지 말고 게이트를 직접 재실행하라.** "전체 통과"라는 보고가 실제로는
  환경 문제로 일부 미실행이었던 적이 있다.
- **소유권 검증은 `git diff main..branch` 가 아니라 merge-base 기준.** main 이 앞서면 오탐이 난다.
- **idle 워커는 오케스트레이션 메시지를 안 읽는다.** 긴급 지시는 `orca terminal send`.
- **`git checkout <ref> -- <path>` 는 인덱스에 스테이징까지 한다** — 워커에게 안내할 때
  `git reset` 으로 언스테이지하라고 함께 알릴 것.
- **콘솔 출력에 비ASCII 금지** — Windows cp949 에서 `UnicodeEncodeError` 로 죽는다.
  실제로 supervisor 가 이것 때문에 STOP 직후 죽어 collector 가 고아 프로세스로 남을 뻔했다.
- **무인 프로세스를 에이전트 세션의 자식으로 띄우지 마라** — 세션 정리 때 함께 죽는다.
  **`Start-Process` 분리로는 부족하다는 것이  7/31 18:07 실증됐다** (Orca PTY job object 가
  트리째 죽임 — 수집기 8분 공백 사고). **반드시 작업 스케줄러(schtasks)로 띄워
  부모가 svchost(Schedule) 이 되게 하라.** `ops/register_task_scheduler.ps1` 참조.
- **에이전트 턴이 Monitor 대기로 열려 있으면 `terminal send` 프롬프트가 큐에 갇힌다** —
  ESC 를 보내면 턴이 끊기며 큐가 소비되지만 **큐된 긴 텍스트가 잘릴 수 있다**(실제로 잘렸다).
  ESC 후에는 상대가 받은 내용을 확인하고 전문을 재전송하라 (orchestration `reply` 가 안전).
- **환경이 바뀌면 우회 수단을 굳히지 말고 원래 방식을 재시험하라** (§0 의 `check --wait` 사례).
- codex 워커가 지출 한도에 걸리면 **증액 요청하지 말고 claude 로 교체**.
