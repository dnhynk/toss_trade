# 코디네이터 상태 — 지금 무엇이 참인가

> **이 파일은 "현재"만 담는다.** 그날의 기록은 `coordination/daily/<날짜>.md` 로 갈랐다.
> 여기 있는 진술이 낡으면 **고쳐라.** 이력을 남기고 싶으면 daily 에 새 날짜를 만들어라.
>
> 갱신: **2026-08-14 10:0x KST** · 이 시각에 직접 확인한 것만 적었다.
> **부분 갱신 2026-08-15 05:1x KST** (무인 판정 세션) — 위 표의 `main`, §1-2c, §2 의
> 공백 회귀 한 줄, §3-2 의 *"다음 한 걸음"*.
> **부분 갱신 2026-08-15 09:3x KST** (일일 코디네이터) — 위 표, §1-2a 에 4 줄, §2 의 공백
> 회귀 행, §2-1 디스크, §3 에 D-23, §3-2 *"다음 한 걸음"*, §4 의 W4.
> **★ 부분 갱신 2026-08-18 10:0x KST** (일일 코디네이터) — 위 표, §1-2a, **§1-2c (D-21
> 판정이 났다)**, §2 에 결함 셋, §3 (D-23 닫힘 · D-24·D-25 신설), §3-2, §4.
> **★★ 부분 갱신 2026-08-19 11:0x KST** (일일 코디네이터) — 위 표, §1-2a, **§1-2d 신설
> (프로브가 쐈다 · G-2 첫 결과)**, §3 (D-26·D-27 신설, D-17 재측정), §3-2, §4.
> **★★★ 부분 갱신 2026-08-20 10:0x KST** (일일 코디네이터) — 위 표, §1-2a, **§1-2e 신설
> (08-19 판정 · 사슬 A · 29% 정정)**, §2 에 결함 둘, §3 (D-17·D-26·D-27 닫고 **D-28·D-29 신설**),
> §3-2 전면 교체, §4.
> **★★★ 부분 갱신 2026-08-21 19:0x KST** (일일 코디네이터) — 위 표, **§1-2f 신설 (08-20 판정 ·
> 워치독이 스스로 복구했다 · 우선순위 가설 반증)**, §2, §3, §3-2, §4.
> **위 표와 §4 는 2026-08-22 00:1x KST 에 하드웨어 이동 뒤 새 기계에서 다시 실측했다.**
> **★★ 부분 갱신 2026-08-23 13:2x KST** — 위 표(`main`·라이브 워크트리·수집기), §2 의
> 표본 행과 조용한 정지 행. **PR #39 loop-lag 계측을 라이브에 배포했다** (`daily/2026-08-23.md` §2).
> **그 밖의 절은 08-14 시점 그대로다.**

| | |
|---|---|
| `main` | **`26364a6`** (2026-08-22 18:54). 열린 PR **0** |
| **라이브 워크트리** | **`w5-ops`** = **`26364a6`**, 브랜치 **`feat/ops`**. 2026-08-23 13:15 에 `main` `26364a6` 으로 `git merge --ff-only` 했다 → **런타임 diff 0** (`daily/2026-08-23.md` §2). ★ **그 뒤 `main` 이 앞서 있어도 문서 커밋뿐이면 런타임 diff 는 그대로 0 이다 — `tossmon/`·`ops/`·`config/` 에 코드가 들어온 순간 다시 재라**(`git diff --stat <live> <main> -- tossmon/ ops/ config/`). 미뤄 뒀던 **PR #39 계측이 이것으로 배포됐다.** 배포 시 `config/`·`ops/` diff 는 비어 있었고 `config_sig` 도 안 바뀌었다. 미추적 **2 개**: `config/config.yaml.bak-20260814-preD21`(일부러 둔 것), `ops/ops_config.yaml` |
| 수집기 | ✅ **가동 중.** 워치독 **2026-08-23 13:15:54** `RESTART outcome=OK sup=1 col=1`(계측 배포에 따른 계획 재기동), 13:16:00 telemetry `fetch_success_pct=100.0 auth_failures=0 loop_lag_max_ms=0 db_write_max_ms=0`. 토스 IP 화이트리스트 통과(403 없음) |
| **공인 IP (토스 화이트리스트 대상)** | **`124.49.14.184`** — 2026-08-23 13:3x 관측(제공자 둘 일치). **토스가 거는 것은 이 주소이지 기계가 아니다** — 사설 IP 는 NAT 뒤라 토스에 안 보인다. 그래서 **하드웨어를 바꿔도 같은 공유기면 재등록이 필요 없다**(08-20 5,545 스냅 → 08-21 5,577 스냅, 이동 후 세션이 더 깨끗했다). ⚠ **이 값이 낡는 것 자체가 신호다** — ISP DHCP 갱신·공유기 재부팅으로 바뀔 수 있고, 바뀌면 **전 요청 403 · `fetch_pct` 붕괴 · 워치독 `auth_fail≠0`** 로 즉시 드러난다. 403 을 보면 **먼저 이 값과 현재 IP 를 대조해라** |
| **여유 디스크** | ✅ **308.34 GB (2026-08-21 23:3x, 새 기계 C:).** 옛 기계의 만성 압박(39.67 GB, 이틀에 3 GB 씩 증발)이 **이동으로 사라졌다** — `docker_data.vhdx` 가 24.75 GB → **2.39 GB**(안 따라왔다). `tossmon.db` **5.84 GiB**. ⚠ **옛 기계의 미규명 하나가 관측 불가로 넘어갔다**: *"멎은 수집기가 2.7 GB 를 붙들고 있었다"* 는 이제 재현 대상이 아니다 |
| **최근 창 (08-21)** | ✅ **유효, 일곱 세션 중 가장 깨끗하다.** distinct snap **5,577**(최다), 60 초 초과 공백 **0 건**, 최대 간격 **14.7 초**(최소), `config_sig` distinct 1. ⚠ **미관측 수치는 판정 러너 출력만 인용해라** — 손으로 센 값과 러너의 6-2 계산은 다르고 섞으면 없는 회귀를 본다. `daily/2026-08-22.md` §1-1 |
| **스케줄러** | ✅ **다섯 개, 전부 `S4U`** (08-22 실측): `watchdog`·`sentinel`·`logrotate`·`dailyhealth`·`collector-oneshot`. `watchdog`·`sentinel` 은 하트비트로 실제 실행까지 확인. ★ **새로 등록할 때 반드시 `S4U` 로 해라** — `Interactive` 로 등록하면 대화형 세션 안에서 돌아 콘솔 이벤트를 같이 맞는다(`LastTaskResult=3221225786 = STATUS_CONTROL_C_EXIT`, 08-19 에 그걸로 복구가 실패했다). **등록·삭제 둘 다 관리자 창이 필요하고 에이전트는 못 한다** — `ops/register_task_scheduler.ps1` |
| **G-2** | **★★★ 판정 끝. 통과 못 했다 (2026-08-26).** 확증 팔 **6 세션**(08-18~08-25)에서 얼린 `E`(`E2_rank_jump`/`K10`)를 쟀다 — **관측성 6/6 · 매일성 6/6 통과, 분리에서 떨어졌다.** 사전 상태 정합 대비 `max_ret_300s` 차이가 **네 칸 전부 0 교차**(all **+0.0002**, CI [−0.0127, +0.0055] / 표적 층 u5 는 부호가 **음수** −0.0021). **종목은 2.4% 움직이는데 `E` 가 없어도 똑같이 2.4% 움직인다.** ⚠ **"성립하지 않는다" 로 인용 금지** — 짝 80·군집 6, 구간 안에 돈이 될 크기가 들어 있다. **못 갈랐다 ≠ 없다.** ★ **확증 팔 6 세션은 소진됐다 — 되돌릴 수 없다.** 새 `E` 는 08-26 이후로 새 팔을 쌓아야 한다. **G-3 은 안 돌렸다**(§4-2 (1)), **검증 카드 0/5 그대로**. 전문 `docs/67` |
| **D-21** | **★★★ 여섯 번 연속 통과.** 08-24 · 08-25 둘 다 **exit 0**, 무효 조건 넷 다 닫힘(미관측 0.051%·0.063%, `config_sig` distinct 1, 프로브 없음 — 사용자 확인). 추이 **B 29.1 → 41.9 → 46.3 → 35.2 → 37.3 → 33.9**, **A 21.8 → 40.3 → 40.7 → 28.2 → 34.3 → 28.6**. B 는 여섯 번 다 통과선 25.9% 위. 비용은 08-24·08-25 둘 다 기준선 최소(155) 아래(115·125). ⚠ **08-21 을 추이로 읽지 마라** — 그 밤만 수집기가 개장 3.1 분 전 냉기동이라 프리장 이력이 0 이고, 꺾인 것은 분자가 아니라 분모다. 판정 러너는 이 교란을 모른다. 전문 `daily/2026-08-26_d21_verdict_0824.txt`·`_0825.txt` |
| **프로브** | ★ **2026-08-18 22:31~23:14 에 쐈고 완주했다.** 529 콜 · `aborted: null` · 우리 429 **0** · 수집기 429 **0**. 결과가 크다 — §1-2d |
| **판정 러너** | ✅ **고쳐졌다 (PR #33 병합, 2026-08-18).** 이제 `--log` 없이 `collector.log.N` 까지 읽는다. 병합 후 `main` 에서 직접 재실행 — **exit 0**, `distinct=1/77`, **29.1 / 21.8 / 106** 재현 |
| 테스트 (갱신) | **2,318 passed · 1 skipped · 4 deselected** (380.01s, exit 0) — **2026-08-20 에 PR #45 브랜치(w3-analyzer 워크트리·그 venv)에서 내가 직접 실행.** ⚠ **`main` 단독 회귀는 안 돌렸다** — 수집(collect-only)으로 `main` **2,303** / 브랜치 **2,319** 를 세서 **+16 = 그 파일 하나의 증가분(37→53)** 과 일치함만 확인했다. 옛 **2,287** 은 PR #40 병합 전 값이라 낡았다 |

---

## 0. 3 분 인수

```bash
# 1) 내 터미널 핸들 확인 (루트 워크트리에서 도는 에이전트 터미널)
orca terminal list --worktree "id:dd0ef363-0d50-4054-9a4a-d7c56ee8e141::C:/Users/dongh/toss_trade" --json

# 2) Run 에 바인딩 — 현행 Run 은 run_fd93b3d00b4b (2026-08-26 신설)
orca orchestration run-use --id run_fd93b3d00b4b --from <내_핸들> --json

# 3) 표준 대기 (내용까지 담아 깨워준다). 폴링하지 마라 — 이게 이벤트로 깨운다
orca orchestration check --run run_fd93b3d00b4b --wait \
  --types worker_done,escalation,question,status --timeout-ms 900000 --json
```

> ### ★ 현행 감독 상태 (2026-08-26 08:4x KST)
>
> | | |
> |---|---|
> | **Run** | **`run_fd93b3d00b4b`** — 옛 `run_92948a1f80a5` 는 **사라졌다.** 옛 ID 로 `run-use` 하지 마라 |
> | **Task** | `task_f8a72951ce2e` — 다음 `E` 설계 (`specs/w3_e2_design.md`) |
> | **Dispatch** | `ctx_eb789325d778` · `state=ready` · `stage=input_accepted` |
> | **워커** | `w3-analyzer` / `feat/e2-design` / `term_d7a94785-e5c9-46be-9cd1-c91ccfa6e8f7` |
> | **모델** | **Fable 5 · max effort** — `launch.effective` 영수증 + 화면 둘 다로 확인 |
> | `ops_config.yaml` | `orchestration_run_id` 를 새 Run 으로 고쳤다 → `dispatch_sweep.py` 다시 작동 |
>
> **끊겼을 때 복구 순서** (`ORCA-OPERATIONS` §5):
> 1. `worker-show --dispatch ctx_eb789325d778 --json` — `ready` 면 **그냥 더 기다려라.**
>    코딩 작업은 15~60 분이 예사고 **타임아웃은 실패가 아니라 체크포인트다**
> 2. `failed`/`stopped` → `worker-start --task task_f8a72951ce2e --retry-of <옛 dispatch>`
>    + 워크트리·에이전트·모델을 **명시**해라 (자동 상속 안 된다)
> 3. `outcome_unknown` → `worker-stop` 후 재점검, 또는 `worker-abandon`
> 4. 워커 출력은 `worker-read --dispatch ctx_eb789325d778 --limit 50 --json`
>
> ⚠ **하트비트와 터미널 활동은 "살아 있다" 이지 "끝났다" 가 아니다.** 완료 메시지가
> 없다고 워커를 죽이거나 재시작하지 마라.
>
> ⚠ **`worker-start --terminal <handle>` 로 이미 도는 에이전트를 묶으려면 그 에이전트가
> 놀고 있어야 한다.** 생각 중이면 `agent_readiness` 에서 **60 초 뒤 `timeout`** 으로
> 죽는다 (2026-08-26 실측, `ctx_7a6e9707f78f`). **Run·Task 를 먼저 만들고
> `worker-start --agent claude --model <m> --effort <e>` 로 띄우는 것이 정석이다** —
> 그 경로가 수명주기 preamble 을 같이 넣어줘서 워커가 `worker_done` 을 낼 수 있게 된다.
>
> ### ⚠⚠ `check --wait` 가 지금 **못 쓴다** — 런타임 결함 (2026-08-26 실측)
>
> 인박스에 **ack 이 안 되는 메시지 하나**가 박혀 있다:
>
> ```
> id=msg_fe2f01df41e5  type=heartbeat  run_id=run_fd93b3d00b4b  read=0  seq=1492
> ack -> {"code":"stale_delivery","message":"Delivery ... does not belong to this Run."}
> ```
>
> **메시지 자신은 `run_id` 가 이 Run 이라고 적고 있는데 ack 은 아니라고 한다.**
> `run-use --from <핸들>` 로 소비자 바인딩을 해도 같다.
>
> **그 결과 `check --wait` 가 매번 즉시 반환한다** — `--types` 로 걸러도 replay 경로로
> 다시 나온다. 반증 시도까지 했다: `--timeout-ms 60000` 으로 걸었는데 **0 초 만에
> `timedOut:false` 로 반환**했다. 그대로 재무장 루프를 돌리면 **헛도는 바쁜 루프**다.
>
> **그래서 이벤트 층을 갈아끼웠다** — 인박스 대신 **워커의 산출물**을 본다:
> `Monitor` 가 60 초마다 (1) `feat/e2-design` 새 커밋 (2) 그 브랜치의 PR (3) 디스패치
> 상태 변화·`failed`/`stopped`/`outcome_unknown`/`unreachable` 을 감시한다.
> **실패 상태를 반드시 함께 덮어라** — 성공만 보는 감시자는 크래시에 침묵한다.
>
> ⚠ **`worker_done` 메시지는 여전히 인박스로 온다.** 그건 30 분 틱에서 `--wait` 없는
> 평범한 `check` 로 훑는다. **원인을 모른 채 이 결함을 "고쳤다" 고 적지 마라** —
> ack 이 왜 거부되는지는 **미규명**이다.

**주의**
- `orchestration check` 는 `--from` 을 **안 받는다.** 다른 명령
  (`task-create`/`dispatch`/`reply`/`send`)은 이 환경에서 `--from <핸들>` 을 **반드시** 적어야 한다.
- `consumer_fenced` 가 나오면 바인딩이 풀린 것이다 → `run-use` 로 재바인딩.
- **감시 스크립트를 새로 만들지 마라.** 우회 감시기는 *"브랜치가 움직였다"* 만 알려줘
  rebase 를 완료로 오인하게 만든다.
- `orchestration reply` 는 긴 본문을 자른다. **30~40 줄로 끊고** 워커에게 `--body` 에
  직접 넣게 하라.
- **워커 디스패치 직후 작업 표식을 세라.** 명세가 입력창에 앉은 채 멈추는 일이 잦다
  (빈 `--enter` 로 제출).

---

## 1. 지금 하고 있는 일

> ### ★★ 목표와 성공 판정 — **한 문단으로. 헷갈리면 여기로 돌아와라** (2026-08-14 확정)
>
> **목표는 사용자가 준 그대로다**: *"지금까지 쌓인 고해상도 데이터로 **알파가 있을지
> 흐릿하게라도 본다**."* 그것이 성공 판정이고, 아직 **아무도 그 답을 모른다.**
>
> **그런데 첫 삽에서 잴 상대가 없다는 것이 나왔다** (`docs/59`). 그래서 지금 하는 일은
> 목표가 아니라 **그 앞의 중간 관문**이다:
>
> ```
> [중간 관문]  잴 수 있게 됐는가   ← 지금 여기. D-21 배포 뒤 08-15 아침에 판정
>                    ↓
> [목표]       잴 만한 것이 있는가  ← docs/58 G-2 · G-3. 아직 착수 안 함
> ```
>
> **중간 관문의 판정 규칙은 `coordination/D21-COVERAGE-PREREG.md` 에 사전등록돼 있다**
> (커밋 `7019f11`, 대상 창보다 8.1 시간 앞섬). 통과선 = `TVOLUME` top-10 정규장 테이프
> 커버리지가 D-21 이전 10 세션 **최대 25.9% 를 넘는다.**
>
> **중간 관문을 넘어도 "돈이 된다"는 뜻이 아니다.** 그건 *"잴 수 있게 됐다"* 일 뿐이고,
> 목표는 그다음이다. **둘을 섞어 보고하지 마라.**

> ### ★ 2026-08-13 — **분석이 다시 열렸다. 두 갈래가 병렬로 돈다**
>
> 사용자 지시: *"가장 중요한 작업은 지금까지 쌓인 고해상도 데이터로 먼저 전략검증을
> 해보는 거야. 아직 수집기간이 짧아서 정확하진 않겠지만 그래도 어느정도 알파가 있을지
> 흐릿하게라도 볼 수 있잖아."*
>
> | 갈래 | 무엇 | 관문 |
> |---|---|---|
> | **A. 전략 탐색** | 고해상도 9 세션으로 랭킹 사건 → 가격 경로. **탐색이고 판정이 아니다** | `docs/58` **G-2 · G-3** |
> | **B. 속도** | 계측기 2 건 → 폴 주기 5 초 비용 → 휴장 창 배포 → 재측정 | `docs/58` **G-0 → G-1a** |
>
> **A 가 B 를 건너뛰는 것이 아니다.** A 가 쓰는 9 세션은 전부 **G-1a 미충족 상태**
> (폴 12.4 초 · 틱 29% 미수신 · 중앙 16.1 초 지연)에서 쌓인 것이고, 그 사실을 모든
> 인용에 병기한다. **9 세션은 탐색 전용이며 앞으로 확증에 재사용할 수 없다**
> (사용자 결정, `docs/58` §G-2).

**B 갈래의 배경**: 분석이 멈췄던 것은 가설이 고갈돼서가 아니라 **재시작의 토대를 쌓는
중이었기 때문**이다 (`USER-INPUT-QUEUE` D-6).

착수 관문은 `docs/58_gate.md` — **1 번 관문이 "우리가 정말 인간보다 빠른가" 이고,
2026-08-13 결정으로 그 통과선은 G-1a**(*"랭킹을 앱만큼 빨리·빠짐없이 받는가"*)**다.**

> ### ★ 2026-08-13 에 G-1 이 크게 움직였다 — 새 세션은 이것부터 봐라
>
> 사용자 관측: **토스 앱의 급상승 랭킹은 10 초쯤 멈췄다가 한 번에 바뀐다.**
> 서버 실측(`docs/35`, W1)과 맞는다 — 서버가 10 초 격자에서 재계산하고 앱은 그 주기를
> 그대로 낸다. 그런데 **우리는 그 격자를 중앙 16.1 초 늙은 채로 받고, 틱의 29% 는
> 아예 못 받는다.**
>
> **즉 지금 우리는 앱 화면을 보는 사람보다 늦다.** 골격 문장의 *"인간보다 빠르게"* 가
> 인지 층위에서 거짓이라는 뜻이다. 원인은 **폴 주기 12.4 초** 하나이고 되돌릴 수 있다.
>
> → **`USER-INPUT-QUEUE` D-8(폴 주기)은 예산 안건이 아니라 G-1 그 자체다.** 미결.

### 최근에 닫은 것 (2026-08-12 밤, 실행 출력을 직접 본 것)

| 항목 | 값 |
|---|---|
| **첨두** — `md_peak_1s` 한도 초과 비율 | **73.1% → 0.0%** (같은 세션끼리, `tools/peak_session_compare.py`) |
| 호가 스킵률 | **65.4% → 0.3%** (tier2 수집 2,980 → 10,575) |
| 예산 진동 | `budget_shrinks` 55 → 2 · `over_limit_1s` 118 → 1 |
| 서버측 429 | `http_429_under_own_limit` **57 → 2** (서버 응답 기준이라 우리 시계와 독립) |
| 부하는 오히려 늘었다 | 같은 창 체결 52,011 → **116,435**(2.2 배) |

**한 번에 하나만 배포했기 때문에 이 변화가 오롯이 시계 수정의 것이다.**
경위는 `docs/52`, 그날 기록은 `daily/2026-08-12.md`.

---

## 1-1. ✅ 셋 다 쟀다 (2026-08-14 아침) — **전부 통과. (B)/(C) 는 (C) 다**

08-13 밤 정규장이 끝난 뒤 `§1-1` 이 **미리 적어둔** 세 판정을 그대로 집행했다.
경위·원문 출력은 `daily/2026-08-14.md` §1.

| # | 판정 | 결과 |
|---|---|---|
| 1 | **첨두** — 한도 초과 비율 0% 근처 유지 | ✅ **0.0% → 0.0%** (양쪽 78 창, 중앙 10.0 → 10.0). **429 증가는 2 → 0 으로 더 내려갔다.** 시계 수정이 이틀째 버틴다 |
| 2 | **(B)/(C)** — RANKING `frnmax` 최대 | ✅ **최대 = 2 → (C) 창 어긋남.** 정규장 78 창 전수, 분포 `{0:55, 2:23}`. 08-13 의 12:11 관측(표본 7 초)과 같은 값이 6.5 시간 전수에서 재현됐다 |
| 3 | **클램프** — 텔레메트리가 0 이 아닌가 | ✅ **`limit_header_clamped=203028`**, `groups=MARKET_DATA:15>10x160546,MARKET_DATA_CHART:20>5x42482`. *"계속 0 이면 그게 다음 결함"* 은 **일어나지 않았다** |

> **판정 2 가 남긴 것 — (C) 가 "왜 하필 2 인가" 를 설명하지는 못한다.**
> `frnmax` 는 **0 아니면 정확히 2** 이고 **1 이 한 번도 안 나온다.** 이 양자화는
> 미규명이다. `foreign` 탐지기는 **아직 한 번도 교정된 적이 없고**, 교정은
> `docs/62` §8-3 이 프로브에 맡겨 두었다.
>
> **이 창에 프로브가 없었으므로** `docs/62` §8-3 조건 2(*"프로브 창의 `foreign` 은
> (B)/(C) 판정에 쓰지 않는다"*)를 어기지 않는다 — 판정에 쓸 자격이 있는 창이다.

> **하지 말 것: 천장을 올리거나 경보 문턱을 손대지 마라.** (§3-1 기각 목록)

---

## 1-2. **내가 돌린 것 / 남의 주장** — 인용 전에 이 표를 봐라

**"동작합니다" 로 인용하기 전에 직접 돌려라.** 이 프로젝트에서 가장 비싼 실패는 전부
*조용히 틀리는* 것이었다.

### 1-2a. 코디네이터가 **직접 실행하고 출력을 본 것** (2026-08-13~14 세션)

| 무엇 | 근거 |
|---|---|
| 전체 회귀 | 브랜치별 **7 회** 직접 실행. 최종 `41ae212` 기준 **2,159 passed · 1 skipped · 4 deselected, exit 0** |
| 계상 변이 게이트 | `tools/mutation_accounting.py` → **19/19 killed** (2 회) |
| 랭킹 차선 변이 게이트 | `tools/mutation_ranking_promotion.py` → **22/22 killed**. **옛 테스트(`1ade64e`)에 걸면 17/22** 이고 살아남는 다섯이 `F5·L1·L2·L7·L12` — 게이트가 수리 전후를 **실제로 가른다** |
| 첨두 이틀째 | `tools/peak_session_compare.py` → **0.0% → 0.0%**, 429 증가 **2 → 0** |
| **가드 사보타주 7 회** | 필드 제거→2 failed · `holdout_floor_ms→0`→3 failed · `E1=모든 존재`→4 failed · `server_seconds_unknown→0`→1 failed · `slots_for` 2 배→4 failed · **`reserve` 무시→(수리 전 30 passed / 수리 후 1 failed)** · **`compete=True`→(수리 전 30 passed / 수리 후 1 failed)** |
| 랭킹 타입별 테이프 보유 | 독립 질의로 5 행 전부 재현 (`TVOLUME` 1,787/515 **28.8%**, 이상값 `17>15` 포함) |
| **tier3 = 테이프 항등** | `promotions(to_tier=3)` 종목 **990** = `trades_snap` 보유 **990**, 양쪽 차집합 0. `ranking_entry` **1,020 건 전부 `to_tier=2`**, `to_tier=3` 0 건 |
| **(B)/(C) → (C)** | 로그 원문 재현: 78 창 `frnmax {0:55, 2:23}` 최대 **2**, `1` 이 한 번도 안 나옴 |
| **사전등록 검증** | `ed71581` = `2026-08-13 13:33:20 KST` 에 규칙 실재, 측정 창보다 **8.9 시간 앞섬**. 그 문구는 **지금 HEAD 에 없다**(grep 0) |
| W1 클램프 로그 | 원문 재대조: `0` 인 줄 파일 전체 **1 개**(`uptime=1`, 첫 클램프보다 앞섬), 이후 **27 줄 중 0 은 0 개** |
| W4 사각지대 | 원문 재현: `unk [0,18,6,23]`=**47**, `http_429`=**50**, R1 52 구간 위반 **0**, R2 51 줄 · `own` 48×1+3×2 |
| 프로브 검정력 | 재계산: Wilson n=30 → **15.6pp**, n=34 → **14.7pp**, 반폭 10pp = **76 슬롯 = 760 콜(1 초 팔) / 63 콜(12 초 팔)** |
| **D-21 배포** | 파싱 검증 → **정지를 데이터로 확인** → 워치독 재기동 09:16:01 → 새 서명. 사슬: `RCON`(1 위)·`GPUS`(2 위) → `to_tier=3` → 신규 체결 **79·9 행** |
| **S4U 전환** | 비관리자 → `Access is denied`(무변경) → 승격 후 넷 다 `S4U` → **다음 주기 실행 확인** (`10:00:53 → 10:05:53`, `sup=1 col=1`) |
| 수집 공백 | DB 직접: **224.5 분**(05:18:55→09:03:26). **08-13 정규장 온전** — 스냅 **5,511**, 2 분 넘는 공백 **0 건** |
| 커버리지 기준선 (**개정 전 n=10**) | `tools/d21_coverage.py` 직접 실행. top-10 **3.3 / 17.1 / 25.9%**, top-100 **2.6 / 4.7 / 8.0%**. ★ **현행 기준선이 아니다** — D-23 개정으로 **n=6**(top-10 8.6 / 19.0 / 25.9%)이다. 사전등록 §6-6 |
| **`main` 회귀 기준선** | `3dbbf68` 에서 직접 실행 — **2,159 passed · 1 skipped · 4 deselected (428.11s, exit 0)** |
| **PR #19 게이트 전수** | 다섯 + 추가 셋을 직접 재실행. 병합 후 상태 회귀 **2,197 passed (409.88s, exit 0)** = 2,159 + 새 테스트 38. 상세는 `AUTOMATION.md` §3-3 |
| **판정 러너 직접 실행** | `tools/d21_verdict.py --session 2026-08-13` → 자가검사 **10/10 + 요약 둘 PASS**, 공백>60s **0**, `config_sig` distinct **1**, `rank_peak_1s` max **3**, top-10 A **0.0%** / B **19.0%**, 비용 **202**, 밴드 **판정 보류**. 대상 창(08-14) 부재 시 `exit 3` |
| **05:10 기동 사슬** | 같은 모양의 스모크 작업을 실제 시간 트리거로 띄워 확인 — `Result=0`, `SMOKE OK`. 확인 뒤 스모크는 제거 |
| **공백 4 건 재현** (08-15 09:3x) | 05:1x 세션 표를 **독립 질의로 재현** — `5179` 스냅, 4 건, `62.7/65.3/71.6/74.1s` 완전 일치 |
| ★ **공백 안에서 호가도 멈췄다** | 공백 4 구간 안 `orderbook_snap.snap_ms` = **0·9·2·18**, 바로 앞 같은 길이 대조군 = **35·23·43·82**. → **랭킹만의 문제가 아니다.** ~~같은 구간 캔들은 불변~~ **← 이 부분은 틀렸다. 정정 아래** |
| ⚠ **정정 — `candle` 열은 근거가 못 된다** | `candles_1m.ts_ms` 는 **거래소 봉의 분**이지 우리 폴 시각이 아니고, 캔들 폴 주기 **110 초**가 공백 62~74 초보다 **길다.** W4 가 잡았고 내가 인정했다. **제대로 된 계측기로 다시 보니 셋 다 0 이다** (아래) |
| ★ **세 그룹이 동시에 0** (내가 원문 재확인) | `collector.log` 텔레메트리: `23:11:54` 과 `23:27:21` 에 `md_peak_1s=0 chart_peak_1s=0 rank_peak_1s=0`, 앞뒤 줄은 `9/2/2` 와 `10/3/2`. **캔들도 같이 멈췄다 = 전역 송신 정지** |
| **08-13 은 다른 현상이다** | 다섯 정규장 전수로 `>20s` 공백이 08-10·11·12 **0 건**, 08-13 **3 건**, 08-14 **33 건** 인 것은 맞다. **그러나 W4 가 08-13 의 3 건을 열어 보니 모양이 다르다** — 호가가 계속 폴했고, 하나는 재기동과 겹치고, `config_sig` 는 08-12 와 같다. → **내 *"D-21 보다 앞선다"* 논증은 철회했다.** D-21 은 확정도 배제도 안 됐다 |
| **디스크 추세 기각** | 워치독 전 이력에서 **매일 09:30 정각** `free_gb` 만 추출 — 100.15→96.23→91.13→73.60→75.88→87.01→85.84→**76.67**. **추세 없음**(08-11→08-13 은 +11 GB). *"하루 9 GB 감소"* 가설 **반증됨** |
| **판정 예약 부재 확인** | `schtasks /query /v` 직접 — `Schedule Type: One Time Only`, `Next Run Time: N/A`, `Last Result: 0`. 05:1x 세션 보고가 **참** |
| **러너 CLI 계약** | 소스 직접 — `--session` 은 **UTC 날짜**, 창 **13:30~20:00 UTC**, 종료코드 `0/2/3/4`. ~~오늘 밤 창 = `--session 2026-08-15`~~ → **08-15 는 토요일이라 정규장이 없다. 다음 대상은 `--session 2026-08-17`(월)** |
| ★ **D-21 판정 (08-18)** | `d21_verdict.py --session 2026-08-17 --log <collector.log.1>` → **exit 0**. 자가검사 10 행+요약 넷 OK. **B top-10 29.1% · A 21.8% · 비용 106**. 전문 `daily/2026-08-18_d21_verdict_output.txt` |
| ★ **러너 회전 사각지대** | 기본 `--log` 로 **exit 4** (`config_sig distinct=0`, `collector.log` 552 줄). 원인을 소스에서 확인 — `log_sources()` 는 `collector.<stamp>.log.gz` 만 찾고 **`collector.log.N` 을 모른다.** `notifier.py:16-17` `MAX_BYTES=32MiB, BACKUP_COUNT=3` |
| ★ **창 안 telemetry 손으로 확인** | `collector.log.1` grep — 창 안 telemetry **77 줄**, `config_sig` **distinct 1**. **러너를 재실행하기 전에** 했다 |
| ★ **미관측 시간 독립 재현** | DB 직접 질의(`mode=ro`) — `rankings_snap` distinct `snap_ms` **5,079**, head 4.5s · tail 2.3s · 60 초 초과 공백 **5 건**, 합계 **106.2s = 0.454%**. 러너 값과 일치 |
| ★ **토큰 락 가설 반증** | `collector.log.1` telemetry 77 줄에서 카운터 델타 추출 — 가장 큰 공백 셋(69.6·72.1·**122.2**초)을 품은 버킷에서 `retries`·`http_5xx` 델타 **0**, `auth_refresh` 는 창 전체 **0 → 0** |
| ★ **telemetry 자신이 늦었다** | 같은 77 줄의 간격 계산 — 중앙 **301s**, 20 초 넘게 밀린 버킷은 **정확히 둘**(+37s·+65s)이고 **가장 큰 공백 둘과 같은 구간**. 작은 공백들은 안 밀렸다 |
| ★ **기계가 잤다** | `watchdog.log` 타임스탬프 전수 — 08-17 에 **668.1 분**·**33.5 분** 구멍. 워치독은 schtasks 5 분 주기다. 16:39 기상 시 랭킹 나이 **2,028s** vs 잠 **2,011s** = **17 초 차** |
| ★ **측정 창은 깨끗했다** | 같은 로그 — 창 안 워치독 틱 **91 회**, 최대 구멍 **5.3 분**. 잠 없음 |
| ★ **프로브 없음** | 전 워크트리 + 루트에서 창 안(08-17 22:30~08-18 05:00) mtime 파일 **0 개**. 최신 fixture 는 08-11 09:57 |
| ★ **워커 판 상태** | `dispatch_sweep` **조치 0 건**, 태스크 126 개 전부 종결, 브랜치 다섯 **ahead 0**, 열린 PR **0**, 라이브 워크트리 런타임 diff **0** |
| ★★ **08-19 창 판정** (08-20) | `d21_verdict.py --session 2026-08-19` → **exit 0**. 자가검사 10 행 + 요약 넷 OK. **B top-10 41.9% · A top-10 40.3% · 비용 149**. 전문 `daily/2026-08-20_d21_verdict_output.txt` |
| ★ **08-19 창 공백 독립 재현** (08-20) | DB `mode=ro` 직접 질의 — distinct `snap_ms` **5,130**, head 7.5s · tail 1.3s, 60 초 초과 **0 건**, 미관측 **8.8s = 0.037%**. 러너 값과 일치 |
| ★★ **프로브 오탐을 갈랐다** (08-20) | 러너가 신고한 fixture 둘의 `captured_at_ms` = **1787059862172 = 08-18 22:31:02 KST**(08-18 프로브 캡처), mtime 08-19 23:51:19 는 PR #40 병합(`ebe2a3c`, 23:50:38)보다 **41 초 뒤** = git 체크아웃. 전 워크트리 훑기에서 창 안 `probe_*` **0 개** |
| ★★ **사슬 A 가 3 시간 24 분 뒤 깨어났다** (08-20) | `Win32_Process` 생성시각 **22:11:20** (넷 다), `collector.log` 첫 줄 **01:35:41**(`resumed`, `saved_ms=1787157317280`=01:35:17) → 01:36:03 `collector start`. **그 사이 재기동 없음.** 이후 `AUTH-FAILURE` **2,482 줄** |
| ★ **판정 계열은 안 더러워졌다** (08-20) | 창 안 telemetry 줄의 `proc_uptime_s` 전수 — **단조 증가 단일 계열**(1 → 17537...). 사슬 A 는 telemetry 를 **한 줄도 안 낸다** |
| ★ **리스 키는 자격증명 하나** (08-20) | `tossmon/api/tokens.py:143-146` 소스 직접 — `sha256(client_id)[:16]`. `state_path`·`cwd`·config 안 들어감. `_acquire_lease` 는 `timeout=0` (즉시 실패) |
| **PR #45 게이트 다섯** (08-20) | 회귀 **2,318 passed (380.01s, exit 0)** · merge-base 소유권 5 파일(`coordination/`·사전등록 무접촉) · 비 ASCII **증분** 중 `print()` 안 **0** · 병합 후 스모크 53 passed + 러너 exit 0 |

### 1-2b. **남의 주장 — 내가 안 돌렸다.** 인용할 때 *"미재현"* 을 병기한다

> **사용자 결정 (2026-08-14)**: 그대로 인용하되 **반드시 `[미재현]` 을 병기한다.**
> 특히 아래 시뮬 55.1% 는 `D21-COVERAGE-PREREG` §3-2 에 **사전 예측으로 걸려 있으므로**,
> 08-15 판정문에서 인용할 때 이 표시가 빠지면 안 된다.

| 무엇 | 누구 |
|---|---|
| `docs/59` 헤드라인 — 사건 **1,437,699 건 중 1.78%** 만 결과를 잴 수 있다 **[미재현]** (나는 랭킹 타입별 테이프 보유 표만 재현했다) | W3 |
| `docs/59` — 표적 층에서 *"잴 수 있는 것 최대 166 건"* **[미재현]** | W3 |
| `docs/60` — 배율 **2.251 배**, 디스크 수명 **214 → 109 일**, 행당 190 B, `rankings_snap` = DB 바이트 **79.8%** **[미재현]** | W1 |
| `docs/61` 시뮬 — 48 조합 **진동 0 · 동결 0**, 커버 20.9~57.0%, **`TVOLUME` rotate 좌석 2 = 55.1%** **[미재현]** | W4 |
| `docs/61` — `md_peak_1s` 가 표본의 **48.4%** 에서 한도 10 에 닿음, `tier3_cap` 이 **72.8%** 에서 설정값 아래 **[미재현]** | W4 |
| `docs/62` mock 드라이런 — `open` 프로파일 **454 콜 / 724.6 초 / aborted null** **[미재현]** | W1 |
| `daily/2026-08-14.md` §2-1 테이블별 행 수 **[미재현]** | 데일리 런 |
| (이전 세션 잔여) W4 변이 게이트 10/10 · W4 빨강 실측 · W4 (A) 기각 통계 `P≈2.9e-4` · W5 자기시험 130 passed · W1 회귀 1,448 · W3 코드 훑기 17 지점 | 여러 |

### 1-2c. **아직 아무도 모르는 것**

**D-21 이 커버리지를 늘렸는가** — 설계상 지금은 못 잰다. **2026-08-14 정규장이 첫 관측**이고
판정 규칙은 `coordination/D21-COVERAGE-PREREG.md` 에 **미리** 박혀 있다.

> **2026-08-14 20:3x 갱신 — 자는 준비됐고, 판정은 예약됐다.**
>
> - 자: `tools/d21_verdict.py` (PR #19 병합). **기대값이 소스 상수라 문서를 고쳐도 안 움직인다**
> - 집행: schtasks `tossmon-coord-d21verdict` 가 **08-15 05:10 KST** 에 헤드리스 세션을
>   띄우고 `coordination/specs/coord_d21_verdict_execution.md` 를 실행한다.
>   기동 사슬은 스모크로 실측 확인했다
> - **그래도 결과는 아무도 모른다.** 창은 22:30 에 열린다
>
> ### ★ 2026-08-15 05:1x 갱신 — **집행했다. 판정은 여전히 없다**
>
> **헤드리스 세션은 떴다** (05:10:22 KST, 기동 사슬 정상). 자도 안 움직였다 —
> 기준선 10 세션 자가검사 **10/10 + 요약 둘 PASS**.
>
> **그런데 창이 무효였다**: 08-14 정규장에 60 초 넘는 수집 공백 **4 건**
> (사전등록 §3-4.1). 넷 다 개장 첫 30 분(23:03~23:34 KST)에 몰려 있고,
> `TVOLUME` 단독으로 세도 4 건이다. → **표를 안 만들었고 밴드를 안 씌웠다.**
>
> - **무효는 *"안 늘었다"* 가 아니다.** 자를 못 댄 것이지 재서 모자란 것이 아니다
> - **수집기는 안 죽었다** — `col_up` 되감김 0, 공백 구간 안에서 텔레메트리가 정상으로
>   찍힌다. **프로세스가 돌면서 랭킹 스냅만 멈췄다.** 원인 미규명 (`daily/2026-08-15.md` §2-2)
> - **다음 대상 창 = 08-15 밤 정규장.** 규칙은 안 바꿨다
>
> ### ★★ 2026-08-18 — **답이 나왔다. 이 절의 제목은 이제 이 항목에 대해 거짓이다**
>
> `--session 2026-08-17` 판정 **exit 0**. 전문은
> `coordination/daily/2026-08-18_d21_verdict_output.txt`, 경위는 `daily/2026-08-18.md` §3.
>
> | | 값 | 밴드 |
> |---|---:|---|
> | **B (테이프) top-10** | **29.1%** (55 중 16) | **> 25.9% → 늘었다** ✅ |
> | A (좌석) top-10 | **21.8%** (55 중 12) | 시뮬 예측 **55.1%** `[미재현]` 과 크게 어긋남 |
> | B / A top-100 | 5.9% / 2.0% | |
> | 비용 `capacity_fill` | **106** | 기준선 최소 155 보다 **낮다** |
> | `ranking_tier3` 58 · `hold_expired` 35 · 쿨다운 위반 **0** | | 사전등록 §5 ④ |
> | `RANKING` 첨두 **3** (n=77, p95 3) | | `docs/62` §5-3 의 3+1=4 ≤ 5 **성립** |
>
> 자가검사 **10 행 + 요약 넷 전부 OK**. 무효 조건 넷 전부 통과 — (3) 프로브 없음은
> **내가 전 워크트리를 훑어 확인**했다 (창 안 mtime 파일 0 개).
>
> **★ 그런데 러너가 처음엔 `exit 4` 를 냈다.** `config_sig distinct=0`. **데이터가 아니라
> 러너가 로그를 못 찾은 것**이었다 — 08-18 06:44 에 `collector.log` 가 32 MiB 에 닿아
> `collector.log.1` 로 회전됐고 창이 통째로 거기 있었다. **다시 돌리기 전에** 손으로
> grep 해서 창 안 telemetry 77 줄 · `config_sig` 1 종을 확인하고, 그 다음에 `--log` 로
> 파일만 지정해 재실행했다. **규칙·문턱·기대값 상수는 하나도 안 건드렸다.** §2 의 새 결함.
>
> **★ 개정 1(D-23)이 없었으면 오늘도 판정이 없었다** — 이 창엔 60 초 넘는 공백이
> **5 건**(최대 122.2 초)이라 **옛 규칙이면 또 무효**였다. 미관측 합계 106.2 초
> = 창의 **0.454%** 로 117 초 예산 안이었다.
>
> ### 이것이 무엇이 **아닌지** 를 같이 읽어라
>
> **"돈이 된다" 가 아니다.** `docs/58` 이 정의한 **중간 관문**(*"잴 수 있게 됐는가"*)을
> 넘은 것이고, **목표**(*"잴 만한 것이 있는가"* = G-2 · G-3)는 **아직 착수 안 했다.**
> 사전등록 §4 제한이 그대로 따라붙는다 — **6 세션 기준선에 한 세션**이다.
> **"확인됐다" 라고 쓰지 마라.** 교환가능성 논증은 1/7 ≈ 0.14 이고 **신뢰구간이 아니다.**
>
> **A 가 시뮬과 어긋난 것은 열린 안건이다** — 러너 스스로 *"다음 의제는
> `ranking_promotion_sim.py` 가 무엇을 놓쳤는가"* 라고 적었다. 아무도 안 봤다.

---

## 1-2d. ★★ 2026-08-19 — **프로브가 쐈고, 알파를 처음 들여다봤다**

### (가) 프로브 — **12 배 빨리 두드려도 1.3 초밖에 안 빨라진다**

`out/probe_d_20260818_223100.json` (W1 워크트리). **22:31:01 → 23:14:02 KST**,
**529 콜**, `aborted: null`, 우리 429 **0**, 수집기 429 **0**. 사용자가 고른 **D 안 그대로**.
**아래 수치는 코디네이터가 JSON 에서 직접 읽었다** (W1 은 아직 문서로 안 썼다).

| 폴 주기 | 폴 수 | 랭킹 변화 | **받은 데이터 노후 (중앙)** |
|---|---:|---:|---:|
| **1 초** | 180 | **4.02 회/분** | **16.8 초** |
| **5 초** | 240 | **4.07 회/분** | **18.0 초** |
| **12 초** (현행) | 100 | **3.99 회/분** | **18.1 초** |

**랭킹 변화 횟수가 셋이 같다.** 서버 도장(`rankedAt`) 증분은 1 초 팔에서
**`{10 초: 6, 20 초: 5}`**, 평균 14.55 초 — **서버가 10 초 격자에서 재계산하되 매 칸마다
내용을 바꾸지 않는다.** 지연의 대부분은 우리 폴 간격이 아니라 **서버가 들고 있던 시간**이다.

> ### 이것이 흔드는 것 둘 — **큐 D-26**
> 1. **D-8(폴 5 초)의 근거가 없다.** 대가는 디스크 **2.251 배**(수명 214 → 109 일,
>    `docs/60`)인데 사는 것은 **약 1 초**다.
> 2. ⚠ **`docs/35` 의 *"10 초 격자 틱 29% 미수신"* 이 의심스럽다.** 그 값은 **12.4 초
>    폴로 잰 것**이고, 1 초 팔에서 보면 **서버 자신이 칸을 건너뛴다.** 그것을
>    *"우리가 놓쳤다"* 로 셌을 가능성이 있다. **그 29% 는 지금 G-2 산출물 전체와
>    §3-2 의 관문 문구에 조건으로 붙어 있다.** 아직 아무도 재확인 안 했다.

**한 밤 · 한 종목 목록 · 한 번의 관측이다.** 반복 안 했다.

### (나) G-2 첫 결과 — **분리가 안 됐다** (PR #37, `docs/64`)

랭킹 사건 뒤 **300 초 최고 상승폭**을 **같은 종목·같은 세션에서 변동성 + 테이프 밀도를
맞춘 위약**과 비교했다. **여섯 칸 전부 0 을 교차한다.** 점추정 **−0.506% ~ +1.494%**,
**부호도 갈린다**(음 3, 양 3). 보정 전 0 제외 셋 중 본페로니 뒤 남는 하나는 **음수**였고
**밀도를 맞추자 +0.004% 로 사라졌다.**

> **정확한 진술**: *"300 초 창 · 정규장 10 세션 · 짝 43~240 건에서 정합 위약과
> 구분되지 않았다."* — **"알파가 없다" 가 아니다. 이 자료로 분리를 못 한 것이다.**

**조건이 무겁다**: 9 세션 전부 **폴 12.4 초** 아래 데이터(그 조건 자체가 위 (가)에서
흔들린다) · **확증 재사용 불가** · 앵커 최대 **80.2%** 가 창을 공유해 **n 이 독립 표본
수가 아니다.** W3 가 **미확인 아홉 가지를 스스로 신고**했고 그중 큰 것 하나 —
**전방 창 테이프 깊이 미정합**(여섯 칸 중 넷에서 위약이 더 깊다, 예 72 vs 94).
**음(−) 쪽 차이가 그것 때문일 수 있다.** 큐 **D-27**.

### (다) 확증은 **오늘 밤이 열쇠다**

확증은 **2026-08-13 이후** 데이터로만 하고(사용자 결정), 거래일 군집 **5 미만이면 통합
CI 금지**가 규율이다. 08-13·14·17·**18**(프로브 밤도 G-2 표본으로는 쓴다 — D-25)
= **4 개**. → **오늘 밤(08-19)이 5 번째이고, 그때 확증이 열린다.**

---

## 1-2e. ★★ 2026-08-20 — **두 번째 판정, 그리고 어젯밤 사고의 정체가 반쯤 갈렸다**

### (가) 08-19 창 — **커버리지가 또 늘었고, A 가 크게 움직였다**

| | 08-17 | **08-19** | 밴드 |
|---|---:|---:|---|
| **B (테이프) top-10** | 29.1% | **41.9%** (62 중 26) | **> 25.9% → 늘었다** |
| **A (좌석) top-10** | 21.8% | **40.3%** (62 중 25) | **HALF MATCHED (25~45%)** |
| B / A top-100 | 5.9% / 2.0% | **5.8% / 4.1%** | |
| 비용 `capacity_fill` | 106 | **149** | 기준선 최소 155 보다 여전히 낮다 |
| `ranking_tier3` / `hold_expired` / 쿨다운 위반 | 58 / 35 / 0 | **128 / 104 / 0** | 사전등록 §5 ④ |
| `md_peak_1s` (n=78) | — | **max 10 · p95 10**, hist `{6:1,7:1,8:8,9:19,10:49}` | **78 중 49 가 우리 천장에 닿는다** |

**08-18 판정이 남긴 열린 안건 — *"A 가 시뮬 55.1% `[미재현]` 과 크게 어긋난다"* — 이
절반 이상 좁혀졌다.** 아직 밴드는 `HALF MATCHED` 이고 **왜 좁혀졌는지 아무도 안 봤다.**

> **세션이 둘이다.** 사전등록 §4 제한이 그대로 붙는다 — 6 세션 기준선 위의 두 세션이고
> **"확인됐다" 라고 쓰지 마라.** 그리고 **커버리지가 늘었다는 것은 알파와 무관하다**(§4-2).

**확증 팔이 5 세션을 채웠다** (08-13·14·17·18·19). **다만 쓰기 전에 큐 D-28 이 먼저다.**

### (나) 무효 조건 (3) 은 **오탐이었다** — 러너가 못 하는 확인을 내가 했다

러너가 창 안 mtime fixture 둘을 신고했지만, 파일 안 `captured_at_ms` 가 **08-18 22:31:02**
(그날 프로브 캡처)이고 mtime 은 **PR #40 병합보다 41 초 뒤**였다. **git 이 쓴 것이다.**
전 워크트리에서 창 안 `probe_*` 산출물은 **0 개**. → 조건 (3) 미발동.

### (다) ★ 어젯밤 "먹통" 은 **영구 사망이 아니라 3.4 시간 지연**이었다

**어제 세운 두 가설이 다 죽었다.** 파이프 교착은 08-20 01:1x 정독이 죽였고
(`supervisor.py:79-88` 이 파일 핸들로 넘긴다), *"자식이 로깅 설정 전에 멈춘다 = 영구
사망"* 은 오늘 관측이 죽였다:

```
22:11:20  프로세스 넷 생성 (Win32_Process)
   ...    collector.log 에 사슬 A 의 줄이 하나도 없다 (3 시간 24 분)
01:35:41  resumed from collector_state.json (saved_ms = 01:35:17)
01:36:03  collector start / COLLECTION-CONFIG start     <- 정상 기동
01:36:06  AUTH-FAILURE (token lease held by pid 50724)  <- 이후 10 초마다, 2,482 줄
```

**재기동이 아니다** — 생성시각이 넷 다 22:11:20 그대로다.
**막히는 것에는 끝이 있고 그 길이가 3.4 시간이다.** 무엇이 그만큼 기다렸는지가 정체다.

### (라) ★ 토큰 리스가 어제 아침 인계의 전제를 무효로 만든다

`tokens.py:143-146` — 리스 파일은 **`client_id` 해시 하나**로 정해지고 `timeout=0` 이다.
→ **콘솔 수집기가 사는 한 라이브로 두 번째 수집기를 못 띄운다.**
인계가 적은 *"새 경로가 로그를 쓰는 것을 확인한 뒤에 갈아탄다"* 는 **라이브에서
원리상 불가능**하다. W5 게이트 3 을 **비라이브 재현**으로 고쳐 적었고, 라이브 전환은
**큐 D-29** 로 뺐다.

### (마) ★ `docs/35` 의 29% — **내가 틀렸고 W1 이 잡았다**

내가 `§1-2d` 와 큐 D-26 과 W1 명세에 *"그 값은 **12.4 초 폴로 잰 것**"* 이라고 썼는데
**거짓이다.** 29.4% 는 애초에 **1 초 팔**(08-07 프리마켓)에서 나왔고 `docs/35` §0-1 이
바로 그 인용을 막으려고 붙여 둔 표식이었다.

정규장 재측정(`docs/35` §7-2, W1): **1 초 33.3% · 5 초 32.8% · 12 초 33.9% — 셋이 같다.**
→ **빠지는 것은 우리가 놓친 것이 아니라 서버가 자기 10 초 격자를 건너뛰는 것이다.**
W1 이 계기 결함도 잡아 고쳤다(히스토그램이 첫 델타를 빼먹어 **미수신을 과소보고**).

**`docs/00_HANDOFF.md` 106 행의 같은 오인은 오늘 정정했다.** `docs/58`·`59`·`62`·`64`
에 남은 자리는 남의 소유라 안 건드렸다 — 목록은 `docs/35` §7-5.

---

## 1-2f. ★★ 2026-08-21 — **세 번째 통과, 그리고 자가 복구가 처음으로 제 몫을 했다**

### (가) 08-20 창 판정 — **세 번 연속 늘었다**

`--session 2026-08-20` → **exit 0**. 전문 `daily/2026-08-21_d21_verdict_output.txt`.

| | 08-17 | 08-19 | **08-20** | 밴드 |
|---|---:|---:|---:|---|
| **B (테이프) top-10** | 29.1% | 41.9% | **46.3%** (54 중 25) | **> 25.9% → 늘었다** |
| **A (좌석) top-10** | 21.8% | 40.3% | **40.7%** (54 중 22) | **HALF MATCHED (25~45%)** |
| B / A top-100 | 5.9/2.0% | 5.8/4.1% | **7.4 / 3.6%** | |
| 비용 `capacity_fill` | 106 | 149 | **93** | 기준선 최소 155 보다 **계속 낮다** |
| `ranking_tier3` / `hold_expired` / 쿨다운 위반 | 58/35/0 | 128/104/0 | **80 / 48 / 0** | 사전등록 §5 ④ |
| `md_peak_1s` (n=78) | — | max 10, hist `10:49` | **max 10, hist `9:5 10:73`** | **78 중 73 이 천장에 닿는다** |
| `rank_peak_1s` (n=78) | max 3 | max 3 | **max 3, hist `3:78`** | 3+1=4 ≤ 5 성립 |

무효 조건 넷 전부 통과. **(3) 프로브는 기계 증거도 0 건**이고(어제와 달리 오탐도 없었다)
**전 워크트리 훑기에서도 창 안에 쓰인 파일이 0 개**임을 내가 확인했다.

> **세 세션이 모두 통과선 위다.** 그래도 **사전등록 §4 제한은 그대로**다 — 6 세션
> 기준선이고 **"확인됐다" 라고 쓰지 마라.** 그리고 **커버리지는 알파가 아니다.**
>
> ★ **`md_peak_1s` 가 78 표본 중 73 에서 천장 10 에 닿는다**(08-19 은 49). 서버는 15 를
> 준다. **우리가 우리 천장에 눌려 있고 그 정도가 세션마다 커진다.** 천장 재검토는
> D-25 가 *"프로브·확증 표본 확보 뒤"* 로 미뤄 둔 안건이다 — 아직 안 열었다.

### (나) ★★ 18:30 에 네 번째 조용한 정지 — **그런데 이번엔 워치독이 잡았다**

```
18:30:53  사용자 콘솔 수집기가 멎었다 (로그·DB 동시에 정지, 프로세스는 생존)
18:35:53  워치독 session=unknown(raw=pre,stale=321s)
18:45:55  ALERT[CRIT] watch_log_stale (15.4 분)
18:45:55  RESTART reason=log_stale launcher=ops/launch_collector.cmd
18:46:08  프로세스 넷 생성   cmd 52812 -> py 44592 -> 22584 -> 40348 -> 24956
18:46:21  collector.log 첫 줄 (resumed from ...)      <- 13 초
18:46:27  DB 랭킹 스냅 재개
18:50:59  sup=1 col=1 col_up=291s rank_age=9 fetch_pct=100
```

**공백은 934.1 초(15.6 분), 전부 프리장이다** (DB 직접 질의로 확인 — 18:00~19:05 KST
구간에 60 초 초과 공백 **1 건**). **정규장 손실 0.**

**이 재기동이 세 가지를 한꺼번에 했다:**
1. **D-29(전환)가 저절로 풀렸다.** 수집기가 08-19 이후 처음으로 **감독 경로 위**에 있다
2. **사용자 콘솔 사슬과 먹통 사슬 A 가 같이 정리됐다** (`col=2` → `col=1`)
3. **자가 복구 3 층이 실제로 작동하는 것을 처음 눈으로 봤다** — 08-19 §9-4 가
   *"세 층이 전부 프로세스가 있다를 성공으로 읽는다"* 고 적은 그 층이, **로그 신선도**
   라는 다른 축으로는 제대로 잡았다

### (다) ★ 그래서 우선순위 가설이 반증됐다

새 사슬 넷은 **BasePriority=6** 이다(`Get-Process` 직접 확인). **08-19 에 3 시간 24 분
걸린 사슬도 6 이었다.** 같은 경로·같은 우선순위인데 **한 번은 13 초, 한 번은 3.4 시간**이다.

→ **§2-3 (다)의 "BelowNormal 때문이다" 는 단독으로 성립하지 않는다.**
그리고 **굶주림 가설과 방향이 반대다** — 08-19 22:11 에는 다른 수집기가 아예 안 돌고
있었으므로(21:37 정지) 그때가 디스크 부하가 **더 낮았다.**

**W5 에게 이 관측을 보냈다.** 워커는 같은 시각에 독립적으로 같은 자리에 도달해 있었다.

### (라) 남은 물음 둘 — **이게 지금 더 중요할 수 있다**

1. **왜 멎는가.** 이번이 네 번째 조용한 정지다(08-14 · 08-15 · 08-19 · 08-21).
   프로세스는 살아 있고 로그·DB 만 멈춘다. **후보는 여전히 0 개다**(`docs/63` §12).
2. **워치독 문턱이 15.4 분이다.** 프리장이라 손실이 0 이었지만 **정규장이었으면
   15 분을 잃는다.** 사전등록 미관측 예산은 창의 0.5% = **117 초**다 — **15.6 분은
   그 8 배**이고 그 창은 **무효**가 된다.

> ★ **부수 관측 하나 더**: 재기동 직후 여유 디스크가 **36.75 → 39.43 GB (+2.7 GB)**
> 로 돌아왔다. **멎은 수집기가 무언가를 붙들고 있었다.** 08-20 아침에 원인을 못 붙였던
> *"09:15~09:30 에 2.67 GB 증발"* 과 **크기가 같다.** 우연일 수도 있다 — 안 갈랐다.

### (마) ★★ W5 가 저녁에 답을 냈다 — **이 결함의 표제가 바뀐다**

보고 전문 `daily/2026-08-21_w5_report.md`. **코드 변경 0**(원인을 못 잡았으므로 —
`git diff origin/main...HEAD` 빈 출력을 **내가 직접 확인**했다. 미추적은 보고서 파일과
pytest 캐시뿐이고 라이브 `data/` 접촉 0, **`STOP`·`PLANNED` 없음**).

**죽은 가설 셋** — 파이프 교착 · **우선순위 굶주림** · **"기동 경로가 문제다"** 라는 표제 자체.
우선순위는 **독립적으로 두 번** 죽었다: (가) 18:45:55 워치독이 같은 런처를 **라이브 ·
6.2 GB 실 DB · BasePriority 6** 으로 띄워 첫 로그 **11 초** (나) 통제된 비라이브 A/B 에서
**BelowNormal 2.8s vs Normal 1.6s = 차이 1.2 초.** 가설이 요구한 크기는 3.4 시간이었다.
→ **§2 의 `<Priority>` 재등록 안건은 근거가 사라졌다. 관리자 권한을 쓸 이유가 없다.**

**대신 나온 것**: 18:30 에 멈춘 사슬 B 는 **사용자 콘솔의 Normal(8) 우선순위**이고
**44 시간 정상 가동 중**이었다. `col_up` 은 늘고 `t2book` 은 15 분 고정 — 살아 있는데
일을 안 한다. **08-19 21:19 과 같은 모양이고, 스케줄러도 supervisor 도 아니다.**
→ **결함은 기동 경로의 성질이 아니다. 돌던 수집기도 걸린다.**

**곁가지 정정**: 사슬 A 를 통틀어 *"먹통"* 이라 부른 것은 틀렸다. 01:36 이후 41 시간은
**정상적인 재시도 루프**였다(`AUTH-FAILURE` 14,176 줄, 10 초 간격). **진짜 먹통은
08-19 22:11:20 ~ 08-20 01:35:41 구간뿐이다.**

---

## 2. 열려 있는 결함

| 무엇 | 상태 |
|---|---|
| ~~**스케줄러 `LogonType=Interactive`**~~ ✅ | **닫혔다 (2026-08-14 10:01)** — 넷(`watchdog`·`sentinel`·`logrotate`·`dailyhealth`)을 **S4U** 로 바꿨다. 사용자가 관리자 PowerShell 로 실행. **바꾸고 끝내지 않고 다음 주기를 기다려 실제 실행을 확인했다**(10:00:53 → 10:05:53 정시, 하트비트 갱신, `sup=1 col=1`). 종료코드 1 은 실패가 아니라 `watchdog.ps1:1981` 의 *"문제를 기록함"* 이고 전환 전에도 1 이었다. 되돌림: `coordination/scheduler_principals_backup_20260814.txt`. 두 번의 청구서(08-12 콘솔 피살 4 회, 08-14 애프터마켓 3 시간 38 분)는 `README.md` §2026-08-14 |
| **`supervisor.stdout.log` 에 타임스탬프가 없다** ★ 신규 (2026-08-14) | 00:55 에 수집기·감독자가 **같이** 조용히 사라졌다(트레이스백·`FATAL`·종료 로그 0). 감독자 로그에 종료 코드 `0x40010004`(terminate) 와 `0xC000026B`(윈도 스테이션 종료 중)가 남아 있는데 **줄에 시각이 없어 00:55 것인지 05:19 재부팅 것인지 못 가른다.** 시스템 이벤트 로그는 그 창에 0 건, `Security`·`TaskScheduler/Operational` 은 꺼져 있다. **피해는 없었다**(60 초 넘는 랭킹 공백 0). **문제는 피해가 아니라 진단 불가** — 조용한 사망을 이미 세 번 겪었다. 고치는 것은 한 줄(`ops/**`, W5). **다음 휴장 창에 보낸다** (오늘은 D-21 측정 밤이라 라이브 워크트리를 안 건드린다) |
| ~~**텔레메트리 클램프 카운터**~~ ✅ | ~~`LIMIT-CLAMP` 로그는 뜨는데 텔레메트리는 `limit_header_clamped=0`~~ → **닫힘 (2026-08-13, 병합 `cbc3b08`).** **어긋난 적이 없었다** — `0` 인 줄은 파일 전체에 하나뿐이고 `proc_uptime_s=1` 이며 첫 클램프 로그보다 3.9 초 **앞선다**. 진짜 결함은 `counter_scope` 가 클램프 3 필드를 설치 수명으로 **오선언**한 것이었다. 동작 코드 0 줄. 아래 §2-2 · `docs/56` §9 · `docs/58` G-0 |
| ~~**`srv_s_unknown` 이 0 이 아니다**~~ ✅ | **닫혔다 (2026-08-13, W4 `docs/52` §13).** **사각지대는 429 다** — `srv_s_unknown` 은 *"429 가 그 서버 초에서 우리 유일한 요청이었던 초"* 의 수이고 관측 창에서 **47 = 47, 잔차 0**. 크기는 프로세스당 최대 23 · 중앙 12, 정산 서버 초의 **0.18%**. *"0 → 8"* 은 변동이 아니라 **프로세스 재시작**이었다. **0 으로 안 만들었다** — 429 잔량 불신은 옳은 규칙이고 이건 그 대가다. ★ **다만 무작위가 아니다**: 서버가 우리를 거절한 초에 몰려 있고 그 초에 `foreign` 이 구조적으로 0 이라 `md_foreign_s`·`quota_not_ours` 경보·복원 거부·tier2 게이트가 **전부 조용해진다** |
| **서버 20/s 의 정체** | **진짜 사양 상향인지 헤더 의미 변경인지 모른다.** 모르면 천장을 못 올린다 |
| **`rankings_snap` 보존정책 없음** | 단조 증가. **2026-08-13 재측정 — 옛 "231 일" 추정은 양쪽으로 다 낡았다.** 아래 §2-1 |
| `docs/29`+`docs/13` 판정 불가 1 건 | 수집기 3 번째 경계 재확인 여부. **라이브 관측이라 문서만으로 못 가른다** |
| **`docs/39` 제목이 거짓** | *"배포는 안 했다"* 인데 `TOP_GAINERS` 는 2026-08-07 22:26 KST 부터 수집 중(2,140,182 행). `docs/INDEX.md` §정정 대기 |
| **로그 회전이 작동한 적이 없다** ★ 신규 (2026-08-14) | **매일 실패하면서 매일 성공으로 보고된다.** 아래 §2-3 |
| ~~★ **판정 러너가 회전된 로그를 못 본다**~~ ✅ **닫혔다 (2026-08-18, PR #33)** | **유효한 창이 무효로 나왔다.** `tools/d21_verdict.py:215` 의 `log_sources()` 는 `collector.<stamp>.log.gz` — 즉 **`ops/rotate_logs.py` 방식만** 찾는데, **그 회전은 한 번도 성공한 적이 없다**(§2-3). 실제로 도는 것은 수집기 자신의 `RotatingFileHandler`(`notifier.py:16-17`, 32 MiB × backup 3)이고 산출물은 **`collector.log.1/.2/.3`** 이다. 08-18 06:44 에 첫 회전이 일어나 08-17 창이 통째로 `.1` 로 갔고, 러너는 `config_sig distinct=0` → **`exit 4`** 를 냈다. **데이터는 멀쩡했다**(창 안 telemetry 77 줄, `config_sig` 1 종). ★ **종료코드가 *"config 가 바뀌었다"* 와 *"못 보겠다"* 를 구분하지 않는다** — 그게 진짜 결함이다. **W3 가 같은 날 고쳤다** (`task_9ccdb6b764d5`, PR #33): 번호 백업을 `log.name` 전체로 앵커해 찾고(`collector.stdout.log.N` 배제 유지), `.1` 이 `.2` 보다 새 것이므로 번호 내림차순 = 시간 오름차순, 이름에 시각이 없으니 건너뛰기를 흉내내지 않는다. **게이트 다섯 전부 내가 재실행했다** — pytest 2,221 passed / merge-base 소유권(`tools/` + `tests/` 뿐) / 러너 네 수치 재현 / 금지문자열 0 / 병합 후 스모크 exit 0 |
| ★ **워치독이 자기 하트비트 구멍을 안 본다** (2026-08-18 신규) | **멀쩡한 수집기를 재시작했다.** 08-17 16:05:54 → 16:39:25 에 **기계가 33.5 분 잤고**(워치독 로그 자체가 비어 있다 — 5 분 주기인데), 깨어나자 랭킹이 **2,028 초** 늙어 있었다. 워치독은 그것을 *"랭킹 루프가 조용히 죽었다"* 로 읽고 재시작했다. **잠 2,011 초와 17 초 차이 = 같은 사건이다.** 워치독의 `ALERT` 문안은 *"session 이 TRUSTED 면 결함은 진짜다"* 라고 안내하는데 **자기가 방금 33 분을 통째로 놓쳤다는 경우를 안 따진다.** 고치면 `NOTE_` 로 갈렸을 것이다. **잠 자체는 사용자만 막을 수 있다 — 큐 D-24** |
| ★★ **조용한 정지가 네 번째다 — 그리고 워치독 문턱이 정규장엔 너무 느슨하다** (2026-08-21 신규) | 08-21 18:30:53 에 수집기가 또 조용히 멎었다(프로세스 생존, 로그·DB 동시 정지). **워치독이 `log_stale` 로 15.4 분 만에 잡아 스스로 재기동했고 13 초 만에 살아났다** — 자가 복구가 처음으로 제 몫을 했다. **공백 934.1 초, 전부 프리장이라 손실 0.** ⚠ **그런데 그 문턱이 정규장이면 치명적이다** — 사전등록 미관측 예산이 창의 0.5% = **117 초**인데 **15.6 분은 그 8 배**라 그 창은 통째로 무효가 된다. **문턱을 손대지 않았다** — 경보 문턱 변경은 §3-1 이 금지한 축이고, 먼저 *왜 멎는가*(후보 0 개)를 답해야 한다 |
| ~~★★ **수집기가 둘인데 아무도 경보를 안 낸다**~~ ✅ **닫혔다 (2026-08-21 18:46)** | 08-19 22:11 에 스케줄러가 띄운 사슬 넷이 **아직 살아 있다.** 22:11:20 생성 → **01:35:41 첫 로그 줄**(3 시간 24 분) → 그 뒤로는 토큰 리스를 못 얻어 **10 초마다 `AUTH-FAILURE`** 만 쓴다(08-21 09:2x 기준 누적 수천 줄). 워치독은 `sup=1 col=2` 를 **정상으로 읽는다** — 프로세스를 세지 *수집하는* 프로세스를 안 센다. **수집 피해는 0**(사슬 B 가 전담, 창 둘 다 깨끗) 이고 **telemetry 도 안 더럽힌다**(사슬 A 는 telemetry 를 한 줄도 안 낸다). **문제는 (가) 3.4 시간의 정체 미규명 (나) 자가 복구 3 층이 이 상태를 성공으로 읽는다** 이다. W5 `task_5e2073cc2256` |
| ⚠ **`sup=1 col=1` 은 런처 스텁을 센 것이다** (2026-08-22 신규) | `.venv\Scripts\python.exe` 는 **`Python Launcher`**(249 KB)이고 진짜 인터프리터를 **자식**으로 띄운다. 둘의 `CommandLine` 이 같고 `ExecutablePath` 만 다른데, `Get-TossProcs` 의 `-ExeLike`(기본 `<repo>\.venv\*`)가 자식을 뺀다 → **네 프로세스 사슬을 스텁만 보고 센다.** **오늘 깨진 것은 없다**(스텁이 죽으면 사슬이 죽은 것이라 판단은 맞다). **그 수가 뜻하는 바가 문서와 다를 뿐이다.** PR #59 는 표본 쪽만 고치고 `Get-TossProcs` 는 일부러 안 건드렸다 — 재기동 판단이 거기 걸려 있어 한 창에 둘을 바꾸면 안 된다. `daily/2026-08-22.md` §9-5 |
| ~~**워치독이 재기동하면서 표본을 파괴한다**~~ ✅ **수리됨 (2026-08-22, PR #52·#59)** | `RESTART` 직전에 `NOTE_*_restart_sample.txt` 를 남긴다 — 스택(py-spy) · IO/핸들/스레드 2 회 측정과 델타 · 프로세스 트리 · 직전 판정값. **표본 수집 실패가 `RESTART` 를 막지 못한다**(셀프테스트 RS1 이 지킨다). **py-spy 가 라이브에 붙는 것을 확인했다**(`daily/2026-08-22.md` §5). ★ **표본 파일은 2 개 났지만 둘 다 비어 있다** (08-22 19:05 재부팅 · 08-23 13:15 계획 재기동, 둘 다 `procs=0 stacks=0`) — **설계대로 맞다.** 이 장치가 노리는 것은 *멈춰 있는* 프로세스의 스택이고 `process_dead` 는 그 경로가 아니다. **노린 경로(`log_stale`)의 표본은 여전히 0 개다** |
| ~~⚠ **예약작업이 전부 `BelowNormal` 로 돈다**~~ ❌ **기각 (2026-08-21 저녁)** | 세 작업 XML 에 **`<Priority>` 요소가 없어** 스케줄러 기본값 **7 = `BELOW_NORMAL_PRIORITY_CLASS`** 가 걸린다(실측 `BasePriority=6`). **★ 같은 날 저녁에 반증됐다** — 18:46 재기동으로 뜬 새 사슬도 **BasePriority=6 인데 13 초 만에 떴다.** 08-19 의 3.4 시간 사슬도 6 이었다. **같은 우선순위인데 13 초 vs 3.4 시간**이므로 우선순위 단독으로는 설명이 안 된다. 게다가 **굶주림 가설과 방향이 반대다**(08-19 22:11 엔 다른 수집기가 아예 안 돌았다). **행은 남긴다** — 사실은 사실이고 다른 가설과 결합할 수 있다. §1-2f (다) |
| ★★ **돌던 수집기가 조용히 멈춘다 — 다섯 번째이고 원인 후보가 0 개다** (표제 교체 2026-08-21) | 08-14 · 08-15 · 08-19 21:19 · 08-21 18:30 (+ 08-19 22:11 의 기동 판본 3.4 시간). **살아 있음 + 출력 0 + 진행 0.** 죽은 가설: 중복 억제 ✗ · 서버 ✗ · 토큰 락 ✗ · 파이프 ✗ · 우선순위 ✗ · **기동 경로 ✗**(08-21 에 같은 경로가 라이브에서 11 초 만에 떴고, 멈춘 쪽은 오히려 Normal 우선순위로 44 시간 돌던 콘솔 사슬이었다). **후보 0 개.** 다음 재현 때 스택을 못 뜨면 또 같은 자리다 → 위 행. ⚠ **08-22 19:05 을 이 목록에 더하지 마라** — 그것은 `both_dead`(사용자가 시작 메뉴로 누른 재부팅, 이벤트 1074)이고 여기 넷은 전부 `log_stale`(프로세스 생존 + 로그 정지)이다. 섞으면 조사가 오염된다. `daily/2026-08-23.md` §1 |
| ★ **`tier2_orderbook_flat` 경보의 근거가 관측과 어긋난다** (2026-08-18 신규) | 08-17 23:15 (측정 창 **안**) 경보가 *"skip 카운터가 안 움직였으니 스윕이 도는 게 아니다"* 라고 단언한다. **그런데 `tier2_orderbook_skipped` 는 계속 늘었다** — 106,352 → 106,942 (25 분 +590). 멈춘 것은 `tier2_orderbook_snaps`(90,584) 뿐이고 23:25 에 재개했다. → **세 번째 스킵 사유가 있는데 경보가 그것을 안 센다.** 호가 자체는 창 전체 정상(distinct `snap_ms` 45,415). 조치 안 함 |
| **수집 공백 회귀** — ★ **2026-08-19 갱신: 후보가 다시 0 개다** | **토큰 락이 죽었다** (PR #39, `docs/63` §12). 못 셋: (1) **코드 천장이 63.5 초가 아니라 15 초** — `_issue()` 예외 시 `async with` 가 풀려 `_alock` 이 놓이고 재시도는 락 **밖**에서 돈다 (`docs/63` §7-가 가 틀렸다) · (2) 큰 공백 셋의 버킷에서 `retries`·`http_5xx`·`http_429`·`auth_refresh` **넷 다 0** · (3) `token_state.json` 이 창 내내 다시 안 쓰였다. ★ **`auth_refresh=0` 을 *"토큰 POST 없음"* 으로 읽으면 안 된다** — 그 카운터는 **401 에서만** 오르고 성공한 재발급은 아무것도 안 올린다 (코디네이터가 명세에 그렇게 잘못 썼고 W4 가 잡았다). **늦은 텔레메트리는 두 번째 현상이 아니라 산수다** — 밀림 = (정지 끝 − 마감), 표본 밖에서 오차 0.5 초 안 재현. **개장 몰림도 성질이 아니다** (08-14 첫 70 분 94% vs 08-17 마감 전 120 분 56%). 동기 블로킹 후보 12 개 중 6 개 사망, **WAL 은 부분 반증**, **Defender 는 표본 1 의 겹침이 미설명**. `>20초` 공백이 `0·0·0·3·33·48` 로 늘지만 **D-21 과 DB 크기가 얽혀 못 가른다** |
| ~~**수집 공백 회귀** (2026-08-15 원문)~~ | 08-14 정규장에 60 초 초과 공백 **4 건** → **D-21 판정이 무효로 끝났다.** 수집기는 안 죽었다. **최종 진단(`docs/63`, PR #24 병합)**: **멈춘 것은 랭킹이 아니라 송신 전체다.** 프로세스 안 계측기가 `md_peak_1s`·`chart_peak_1s`·`rank_peak_1s` **셋 다 0** 인 줄을 두 번 찍었고(23:11:54·23:27:21, 재기동 없음) **내가 원문으로 재확인했다.** 리미터 바구니가 셋인데 셋이 동시에 0 이라 **그룹 예산으로 설명 불가.** 가설 2(중복 억제)는 **코드로 죽었고**, 3(서버)은 **범위로 죽었다**(`loop_errors`·`api_errors` 증분 0). **남은 단일 후보 = `TokenManager._alock` 을 쥔 채 15 초 토큰 POST + 최대 3 회 재시도(최악 63.5 초 vs 관측 60.2~76.1 초).** ★ **확인 못 했다** — `retries`·`http_5xx`·`auth_refresh` 가 텔레메트리에 한 번도 안 나가서 사후 판별이 불가능하다. W4 가 **그 셋을 내보내는 계측만** 넣었다(동작 0 줄). **개장 첫 70 분 몰림은 미설명**(티어 교체 가설은 21 건 전수 검정 후 기각). ⚠ **오늘 밤엔 그 계측이 안 켜진다** — 병합은 배포가 아니고 측정 창이라 일부러 안 켰다. 큐 #12 |

### 2-1. 디스크 — 2026-08-13 13:2x 재측정 (인계 문서가 *"재확인 안 함"* 으로 남긴 것)

| 항목 | 값 | 어떻게 쟀나 |
|---|---|---|
| C: 여유 | **86 GB** (458 G 중 373 G 사용) | `df -h /c` |
| `tossmon.db` | **3,496 MB** | `ls -l` |
| `rankings_snap` | **15,403,823 행** — DB 전체 약 21.5 M 행의 **72%** | `mode=ro` 질의 |
| 누적 평균 | 하루 **1,187,755 행** (13.0 일치) | 같은 질의 |
| **최근 3 일** | 하루 **1,734,528 행** — **누적 평균의 1.46 배** | 같은 질의 |

**옛 "231 일" 추정은 양쪽으로 다 낡았다.**

- **여유는 늘었다**: 08-11 기록 75.88 GB → 지금 **86 GB**. 우리가 지운 것은 없다.
- **DB 증가는 빨라졌다**: 2.27 GB(08-09) → 2.7(08-11) → **3.496(08-13)**.
  하루 **0.215 GB → 0.398 GB**, **2 일 만에 1.85 배**다.
  `TOP_GAINERS` 도입 때 예상한 +141.5 MB/일보다 훨씬 크다.

> **그래도 우리 것은 병목이 아니다.** 0.398 GB/일이면 86 GB 는 약 216 일이다.
> 진짜 위험은 08-09~08-11 에 관측된 **하루 약 7 GB 의 정체불명 감소**이고
> **그중 우리 것으로 설명되는 것은 1 GB 미만**이었다. 그 원인은 지금도 미규명이다.
> **여유가 늘어난 이유도 우리는 모른다** — 선형 외삽을 믿지 마라.

> ### ★ 2026-08-15 09:3x 정정 — **감소가 아니라 진동이다**
>
> 위 *"하루 약 7 GB 의 감소"* 를 **추세로 읽으면 안 된다.** 워치독 전 이력에서
> **매일 09:30 정각**의 `free_gb` 만 뽑아 봤다 (이 로그는 회전이 한 번도 안 돼서
> 전 이력이 남아 있다 — §2-3 의 결함이 여기선 도움이 됐다):
>
> ```
> 08-07 100.15   08-08 96.23   08-09 91.13   08-10 73.60   08-11 75.88
> 08-13  87.01   08-14 85.84   08-15 76.67
> ```
>
> **73~87 GB 대역에서 오르내린다. 추세가 없다.** 08-11 → 08-13 에는 **+11 GB 늘었다.**
> 그래서 08-14→08-15 의 **-9.4 GB 를 보고 세운 *"8 일 뒤 고갈"* 가설은 내가 기각했다.**
>
> **새로 관측한 것**: 하루 안의 진폭이 크고 **장 시간에 붙어 있다** —
> 개장 1 시간 전 86.78 → 개장 1 시간 후 75.90(**2 시간에 -10.9 GB**) → 08:00 최저 71.22
> → 09:30 **76.67 로 +5.5 GB 되돌아옴**. **같은 밤 우리 DB 증가는 0.4 GB 미만이다.**
>
> **여전히 미규명이고 조치 안 했다.** 원인을 모른 채 지우거나 끄지 않는다.
>
> ### ★ 2026-08-18 재측정 — **대역을 벗어났다. 다만 아직 "추세" 라고 부르지 않는다**
>
> 같은 방법(워치독 전 이력에서 **매일 09:30 정각**의 `free_gb`)으로 다시 뽑았다:
>
> ```
> 08-07 100.15  08-08 96.23  08-09 91.13  08-10 73.60  08-11 75.88
> 08-13  87.01  08-14 85.84  08-15 76.67  08-16 71.74  08-18 62.29
> ```
> (08-12·08-17 은 그 시각에 워치독이 안 돌았다 — 08-17 은 **기계가 자고 있었다**, §2)
>
> **08-14 이후 넷 연속으로 내려갔고(85.84 → 62.29, −23.5 GB), 08-15 에 적은
> 73~87 GB 대역 아래로 내려왔다.** 그 항목이 *"추세 없음"* 이라고 적은 근거는
> 08-11 → 08-13 의 +11 GB 였는데, **그 뒤로는 되돌아온 날이 없다.**
>
> **그런데 그중 우리 것은 1 GB 남짓이다** — `tossmon.db` 는 08-13 **3.496 GB** →
> 08-18 **4.438 GB** (`data/` 전체 4.7 GB). **−23.5 GB 중 22 GB 이상이 우리가 아니다.**
>
> **그래서 아직 "고갈 D-day" 를 계산하지 않는다.** 08-15 세션이 −9.4 GB 하루치를 보고
> *"8 일 뒤 고갈"* 을 세웠다가 스스로 기각한 전례가 있다. **선형 외삽을 믿지 마라.**
> 다만 **대역이 깨진 것은 새 사실**이고, 63 GB 는 08-04 의 디스크 사고(D-7, 사용자가
> 수동 정리) 때보다는 훨씬 여유롭다. **다음 세션이 같은 자리를 다시 재라.**

**보존정책은 여전히 없다.** `rankings_snap` 이 DB 의 72% 이고 단조 증가한다.
`tossmon/store/retention.py` 는 `candles_1m`·`trades_snap` 만 다룬다 (W2 안건).

### 2-2. 클램프 — 2026-08-13 라이브 실측 (W1, `docs/56` §9). **조치 안 함**

계측기를 고치다 옆에서 나온 사실이다. `collector.log` 원문에서 읽었다 (라이브 API 0 건).

| 그룹 | 서버가 주는 한도 | 우리 천장 | 2 시간 11 분 계상 |
|---|---:|---:|---:|
| `MARKET_DATA` | **15.0** | 10.0 | **19,085 건** |
| `MARKET_DATA_CHART` | **20.0** | 5.0 | 3,965 건 |

**`MARKET_DATA` 도 잘리고 있었다** — 지금까지 클램프 이야기는 `MARKET_DATA_CHART`
쪽이었다. 그리고 **분당 쿼터 가설과 안 맞는다**: 그 가설이면 600/300 이어야 하는데
15/20 이다. **§2 의 "서버 20/s 의 정체" 가 여전히 미규명인 이유가 이것이다.**

> **아무 조치도 안 했다. 천장 상향은 이미 기각된 결정이다** (§3-1). 다만 이 수치는
> **폴 주기 5 초 비용 계산의 입력**이다 — 서버가 이미 더 주고 있다면 5 초가 예산
> 문제가 아닐 수도 있다. W1 태스크 `task_1bf3b5ac73fe` 가 RANKING 그룹의 같은 값을
> 읽어서 답한다.

### 2-4. ★ 공백 진단 계측을 배포했다 (2026-08-15 20:54~20:56, 사용자 결정)

**사용자 질문**: *"공백 원인은 그냥 덮어두는거야?"* — 그 질문이 이 배포를 열었다.

일일 코디네이터는 계측 배포를 **판정 뒤 휴장 창**으로 미뤘다(`daily/2026-08-15.md` §15-3).
근거는 *"판정이 계측보다 앞선다"* 였고 그 자체로 옳다. **다만 그 선택의 대가는
"오늘 밤 또 서면 이번에도 못 가른다"** 였고, **되돌릴 시간이 아직 있었다.**

**배포 전에 두 가지를 직접 확인했다** (이게 없었으면 안 했다):

| 확인 | 결과 |
|---|---|
| 동작을 바꾸나 | `loops.py` **+16 줄 전부 텔레메트리 추가**. 제어 흐름 키워드(`if`/`for`/`return`/`await`/`raise`) 추가 **0 줄**. 라이브 워크트리 ff 후 런타임 코드 diff 는 **이 파일 하나뿐** |
| 판정 러너를 깨나 | **안 깬다.** `scan_log` 은 필드명 정규식으로만 읽는다(`config_sig`·`md_peak_1s`·`rank_peak_1s`). 필드가 늘어도 무관 |

**절차** — 문서화된 경로만 썼다 (`docs/08` §10, `README`):

```
20:54:18  PLANNED(until=21:30) + STOP    -> 프로세스 넷 소멸을 프로세스 목록으로 확인
                                            (파일이 아니라 데이터로 — §5-2)
20:54:4x  git merge --ff-only origin/main    1e090f2 -> e7c9437
20:54:52  STOP 제거                      -> 워치독 5분 주기가 되살림 (schtasks /run 안 씀)
20:56:06  수집기 재개
```

**검증 셋**

| | |
|---|---|
| 프로세스 | 20:56:03~04 부모→자식 사슬 넷. 배포 전과 같은 모양 |
| **`config_sig`** | **배포 전과 글자 하나까지 동일** → 사전등록 §3-4.2 안전 |
| 새 필드 | `retries=0 http_5xx=0 auth_refresh=0` 이 텔레메트리에 **실제로 찍힌다**. 0 인 것은 프로세스가 방금 떴기 때문이고, 그래서 W4 가 이 셋을 `PROC_SCOPED_COUNTERS` 에 등록한 것이다 |

> **함정 하나에 내가 빠졌다 기록해 둔다.** 재기동 뒤 프로세스 조회가 **0 개**로 나와
> 워치독의 `col=1` 과 모순됐다. 원인은 수집기가 죽은 것이 아니라 **`Win32_Process` 의
> `CommandLine` 이 비어서** `-like '*toss*'` 필터에 안 걸린 것이었다. 로그
> (`20:56:06 resumed` → 5 분 주기 텔레메트리)가 갈랐다.
> **프로세스 조회 하나로 생사를 판정하지 마라 — 로그와 함께 봐라.**

> ### ★ 이번 배포가 §2 의 열린 결함 하나를 실물로 보여줬다
>
> `supervisor.stdout.log` 에 `collector exited (code=1073807364 / 3221226091)` 두 줄이
> 있는데 **줄에 시각이 없어서 내 정지 때문인지 예전 것인지 못 가른다.**
> 그 결함(§2 의 *"`supervisor.stdout.log` 에 타임스탬프가 없다"*)의 대가를 오늘 직접 치렀다.

**남은 것**: 이 계측은 *"재시도였나"* 를 가를 **분모**를 만들 뿐이다.
**개장 첫 70 분 몰림은 여전히 설명 안 됐다** (W4 가 티어 전이 가설을 21 건 전수로 기각).

### 2-3. ★ 로그 회전 — **매일 실패하면서 매일 성공으로 보고된다** (2026-08-14 발견)

W3 가 판정 러너 PR 에 *"오늘 밤 00:10 회전이 실제로 일어나면 `.gz` 병합 경로가 처음
실행된다"* 고 적어서, 회전이 정말 걸릴지 재보다 나왔다.

**관측** (전부 이 세션에서 직접)

```
logrotate            LastRun 2026-08-14 00:10:01   Result=0   <- 매일 돌고 성공으로 끝난다
log_max_bytes        20,000,000 (20 MB)
collector.log        28.9 MB    첫 줄 2026-07-31   -> 한 번도 회전 안 됨
collector.stdout.log 152.9 MB                      -> 08-04 이후 회전 안 됨
data/*.gz            collector.stdout.20260804-001003.log.gz  단 하나
```

**기전**: `ops/rotate_logs.py:33-40` 의 `rotate_file` 이 `path.rename()` 의
`PermissionError` 를 잡아 `None` 을 돌려준다. `run()` 은 그것을 `rotate_skipped_busy`
에 담지만 **종료 코드는 0** 이라 작업 결과가 성공으로 남고 아무 경보도 안 난다.

모듈 docstring 은 *"실행 중인 프로세스가 파일을 계속 쓰고 있어도 안전하도록 rename
방식을 쓴다"* 고 적었는데, **Windows 에서 그 rename 은 쓰는 쪽이 파일을 잡고 있으면
실패한다.** docstring 도 그 가능성을 알고 있고(*"다음 주기에 재시도"*) — 다만
**재시도가 영원히 실패한다**는 것이 여기서 빠진 부분이다. 수집기는 계속 도니까.

> **유일하게 성공한 회전이 그 기전을 뒷받침한다** — `collector.stdout` 이 회전된
> `2026-08-04 00:10:04` 은 `ALERT_20260804_001058_watch_process_dead.txt` 와 같은
> 분이다. **프로세스가 죽어 있던 순간**이었다. (정황이다. 파일 핸들을 직접 확인한 것은
> 아니다 — 라이브 로그에 rename 을 시도해 볼 수는 없다.)

**피해는 아직 없다.** 디스크 여유 88 GB 에 두 파일 합쳐 182 MB 다. **문제는 크기가
아니라 (가) 보존 설계가 죽어 있는데 살아 있다고 믿고 있었다는 것** — `ops_config` 주석이
*"사후 재구성이 읽는 `collector.log` 의 회전 보존이 `log_retention_days`(14)일"* 이라고
적어 두었다 — **(나) 실패가 성공으로 보고된다는 것**이다.

**오늘 밤에는 오히려 다행이다.** 00:10 회전이 또 걸릴 것이므로 판정 러너의 **미검증
`.gz` 병합 경로가 안 탈 가능성이 높다.** 측정 창의 위험이 하나 줄었다.

**고치지 않았다.** `ops/**` 는 W5 소유이고 라이브 워크트리이며, 오늘 밤은 측정 창이다.
`AUTOMATION.md` §3 큐 **#9** 로 세웠다.

---

## 3. 사용자 결정 대기

정본은 **`coordination/USER-INPUT-QUEUE.md`** 다. 여기는 지금 무엇이 걸려 있는지만 적는다.

| # | 무엇 | 왜 급한가 |
|---|---|---|
| ~~**D-29**~~ ✅ **저절로 풀렸다 (2026-08-21 18:46)** | ~~콘솔 → 스케줄러 경로 전환~~ → **워치독이 `log_stale` 을 잡아 스스로 재기동했고 13 초 만에 떴다.** 수집기는 이제 감독 경로 위에 있고 콘솔 사슬·먹통 사슬이 같이 정리됐다 | **묻지 않아도 됐다.** 다만 **그 답을 얻은 방식이 또 한 번의 조용한 정지**였다(공백 15.6 분, 전부 프리장). §1-2f (나) |
| ~~**D-28**~~ ✅ · ~~**D-31**~~ ✅ | 둘 다 **2026-08-22 답 나옴** — (나)·(가). `G2G3-PREREG` 개정 2·3 집행 완료, **`E` 를 채웠다** | **G-2 를 막던 것이 사라졌다.** 남은 것은 확증 팔 5 번째 세션(**08-24 월**)뿐이다 |
| **D-26** (잔여) | ~~(가) D-8 닫기~~ ✅ · ~~(나) 29% 재측정~~ ✅ (W1, PR #40) · **(다) G-1a 를 통과로 볼지** 는 **아직 열려 있다** | **1 번 관문의 판정이다. 코디네이터 단독 금지.** ★ **(나)에서 내가 틀렸다** — 29% 는 12.4 초 폴로 잰 값이 아니라 **1 초 팔** 값이었고, 세 팔이 33% 로 같다. §1-2e (마) |
| ~~**D-27**~~ ✅ | ~~알파 첫 측정에서 분리가 안 됐다~~ → **답 나옴 (08-20 01:0x): (가) 이 축을 더 판다.** 집행 완료 = **PR #45** (`docs/65`) | 결과: **깊이 정합은 접는다** — 세 번째 삽(PR #54, `docs/66`)이 그 축을 닫았다. 표적 층은 갈렸다(`$0~5` 가 83~88%) |
| ~~**D-17**~~ ✅ | ~~RDP 추적 디스크~~ → **답 나옴 · 집행 완료 (08-20 01:0x): (가) 지운다.** 3,634 파일 **16.01 GB** 삭제 | ★ **그런데 새는 곳은 그게 아니었다** — `docker_data.vhdx` 가 계속 자란다(08-19 23.53 → 08-21 실측 필요). (나) Docker 는 **사용자가 안 골랐다** |
| ~~**D-25**~~ ✅ | ~~중간 관문을 넘었다. 다음은?~~ → **답 나옴 (2026-08-18): "D-25 부터 순차적으로 전부 진행"** — 셋 다 하고 순서만 잡는다 | 순서는 코디네이터가 잡았고 큐에 근거를 적었다. **(가) 프로브는 08-18 밤에 집행했다** |
| ~~D-25 (원)~~ | **중간 관문을 넘었다. 다음은 (가) 프로브 / (나) G-2·G-3 / (다) 세션 더 쌓기.** 같이 볼 관측: **우리가 우리 천장에 붙어 있다**(표본 90.9% 에서 초당 최대 = 천장 10, 서버는 15 를 준다) | **지금 무엇을 할지가 여기서 갈린다.** 전략 프레이밍이라 코디네이터 단독 금지. 프로브 조건은 이번 창에서 풀렸다(`RANKING` 첨두 3 → 3+1=4 ≤ 5) |
| ~~**D-24**~~ ✅ | ~~기계가 잔다~~ → **사용자가 직접 해결 (2026-08-18).** `STANDBYIDLE=0`·`HIBERNATEIDLE=0` 실측 확인 | 남은 것은 사용자 안건이 아니라 **코디네이터 안건**이다 — 워치독이 *"내가 방금 N 분을 놓쳤다"* 를 스스로 못 알아챈다. §2 의 결함 행 |
| ~~D-23~~ ✅ | ~~판정 문턱이 그 문턱 자신의 규칙에 걸리는 세션 위에 서 있다~~ → **답 나옴 (2026-08-15): (다) 공백 규칙 개정** | 사전등록 §6 개정 1(`1ac0b61`). ★ **08-18 에 값을 했다** — 08-17 창은 60 초 초과 공백이 **5 건**이라 **옛 규칙이면 또 무효**였는데, 총량 0.454% 로 유효해져 **판정이 났다** |
| ~~D-22~~ ✅ | ~~스케줄러를 `S4U` 로~~ → **같은 날 해결됨 (2026-08-14 10:01)** | 사용자가 관리자 PowerShell 로 실행. 넷 다 `S4U` 이고 **다음 주기 실제 실행까지 확인**했다. 되돌림 백업 있음 |
| ⚠ **D-8** (다시 열림) | ~~답 나옴: 5 초로 내린다 (2026-08-13)~~ → **2026-08-19 프로브가 그 근거를 무너뜨렸다** — 5 초로 내려도 노후가 18.0 초로 12 초(18.1 초)와 사실상 같다 | **배포 안 한 것이 다행이었다.** 대가는 디스크 2.251 배인데 사는 것이 약 1 초다. **닫는 판단은 사용자** — 큐 **D-26** |
| ~~D-18~~ ✅ | ~~배포 시점·권한~~ → **답 나옴: 휴장 창에 코디네이터가 알아서, 한 창에 하나** (2026-08-13) | 매번 묻지 않는다. 첫 적용 대상이 폴 주기 5 초 |
| ~~**§1-4 의 고리**~~ ✅ | ~~G-1 이 마지막 관문을 요구한다~~ → **답 나옴: (나) G-1a · G-1b 로 쪼갠다** (2026-08-13) | **현재 1 번 관문은 G-1a** = *"랭킹을 앱만큼 빨리·빠짐없이 받는가"*. 주문 없이 잴 수 있다. G-1b(체결)는 D-2 때 |
| D-19 · D-20 | 발화 몰림의 종목 고유 변동성 분리 / 성긴 테이프 374 건 | `docs/44` 안에서는 **D-17 · D-18** 이다 — 번호 충돌 주의 |
| D-1 | 비용 전제 개정 (권고 B안) | 카드 소각 전에는 반드시 |
| D-2 | 계측 전용 실주문 | **후보가 살아난 뒤로 유예** (사용자: *"전략이 어느정도 보장되면 투자할게"*) |
| D-3 | 검증기간 오염 (권고 D안) | 다음 탐색 그리드 설계 때 |
| D-4 | 진출 규칙 품질 하한 | 자기불리 개정이라 안전한 축 |

> ### ★ 오래 열린 여섯 개(D-19·D-20·D-1·D-2·D-3·D-4)를 어떻게 다룰까 — **사용자 결정 (2026-08-14)**
>
> **"다음 세션이 하나씩 다시 묻는다."**
>
> 목록으로 쌓아 두고 가리키기만 하면 **아무도 안 읽는다** — 실제로 몇 달 열려 있다.
> 그래서 **한 번에 하나씩**, 그 안건이 실제로 길을 막을 때 그 자리에서 묻는다.
>
> **묻는 순서는 "지금 무엇이 막히는가" 가 정한다.** 미리 순서를 박지 않는다.
> 지금 기준으로는 **D-1(비용 전제)** 이 가장 가깝다 — G-3 통과 판정이 비용 차감 뒤
> 양수인지를 묻는데(`docs/58` G-3) 그 비용이 아직 얼린 1.0% 이기 때문이다.
> 다만 **카드를 안 태우는 동안은 판정이 없으므로 아직은 막히지 않았다.**
>
> **한 세션에 여섯 개를 한꺼번에 묻지 마라.** 그건 지금까지 안 통한 방식이다.


**`docs/58_gate.md` 가 새로 묻는 것 4 개**도 대기 중이다 (관문 문구 확정, 월 $700 조건,
G-0 미충족 상태로 G-1 진입 가부).

> ### ★ 번호 충돌 — 코디네이터 잘못이다
> `docs/44` 의 **D-17 · D-18** 과 `USER-INPUT-QUEUE` 의 **D-17 · D-18** 은 서로 다른
> 안건이다. `docs/44` 가 08-08 에 먼저 썼고(분석 안건) 큐가 나중에 같은 번호를 운영
> 안건에 다시 썼다. 분석 쪽 둘은 **D-19 · D-20** 으로 큐에 신설했다.
> **번호만 떼어 인용하지 말고 어느 문서의 번호인지 함께 적어라.**

---

## 3-2. ★ 다음에 할 일의 순서 — 사용자가 2026-08-13 에 정했다

> ### ★★★ 2026-08-21 저녁 — **수집기는 감독 경로 위로 돌아왔다. 남은 것은 "왜 멎는가" 다**
>
> **18:45:55 에 워치독이 `log_stale` 을 잡아 `launch_collector.cmd` 로 스스로 재기동했고
> 13 초 만에 떴다.** 08-19 이후 처음으로 **복구 경로가 있는 상태**로 밤에 들어간다.
> 사용자 콘솔 사슬과 먹통 사슬 A 도 그때 같이 정리됐다.
>
> | | |
> |---|---|
> | 지금 수집기 | `sup=1 col=1`, `launch_collector.cmd` → supervisor → collector, **BasePriority 6** |
> | 오늘 밤 창 | **08-21 22:30 ~ 08-22 05:00 KST** (금요일 정규장). 그 뒤 **주말 이틀은 휴장** |
> | 워커 | **W5** · `task_5e2073cc2256` · `ctx_7f7d3af3d7f1` · 활동 중 |
> | 명세 | `specs/w5_supervisor_pipe.md` — §2-1 파이프 ✗ · §2-2 3.4 시간 · §2-3 우선순위 **✗ (오늘 반증)** · §5-2 **무효**(표본이 사라졌다) |
>
> **★ 19:1x 에 W5 가 답을 냈고, 급한 것이 또 바뀌었다.** 기동 경로도 우선순위도
> **범인이 아니다**(§1-2f (마)). 멈춘 것은 **Normal 우선순위로 44 시간 돌던 콘솔 사슬**
> 이었다. 즉 **결함은 기동 경로 밖, 수집기 안에 있다.**
>
> ### ★★ 주말 첫 일 — **워치독이 재기동 전에 표본을 뜨게 한다**
>
> **재현 기회가 두 번 있었고 두 번 다 워치독이 5 분 안에 증거를 지웠다**(08-19, 08-21).
> 원인 후보가 0 개인 상태에서 **다음 사고에서도 스택을 못 뜨면 또 같은 자리다.**
>
> | | |
> |---|---|
> | 무엇 | `RESTART` **직전에** `py-spy dump` + IO/CPU 카운터 스냅샷을 남기고 죽인다 |
> | 선행 | ⚠ **`py-spy` 미설치** (PATH·`w5-ops` venv 둘 다 확인). 그것부터 |
> | 소유 | `ops/**` = **W5**, 라이브 워크트리 |
> | 언제 | **주말 (08-22 05:00 이후)** — 정규장이 없어 대가가 0 이다 |
>
> **★ 오늘 밤엔 배포하지 않는다.** 이유를 적어 둔다: 수집기가 18:46 에 막 돌아와
> 40 분째 안정이고, 창이 **3 시간 뒤**다. 바꾸려는 것이 **자가 복구 경로 자신**이라
> 실수하면 오늘 되찾은 안전망을 잃는다. **08-19 의 저녁 서두름이 정확히 그 값을 치렀다.**
> 대가는 *"오늘 밤 또 멈추면 표본을 또 잃는다"* 하나이고, 그건 되돌릴 수 있다.
>
> **워치독 문턱 15.4 분은 안 건드렸다** — §3-1 이 금지한 축이고, *왜 멎는가* 를 모른 채
> 문턱만 조이면 오탐 재시작이 늘어난다. **사용자에게 물었다 (큐 D-30).**

> ### ★★★ 다음 한 걸음 — **집행 끝. 이제 사용자 차례다 (2026-08-18)**
>
> **아래 상자 전체는 이제 이력이다.** 그 상자가 시킨 일(08-17 창 판정)은 **2026-08-18 에
> 집행됐고 통과했다** (§1-2c). 다음 한 걸음은 **더 이상 코디네이터가 혼자 정하지 않는다** —
> **큐 D-25** 가 (가) 프로브 / (나) G-2·G-3 / (다) 세션 더 쌓기를 묻고 있다.
>
> **사용자 답이 오기 전까지 코디네이터가 할 일** (전부 방향 전환이 아니다):
>
> | | |
> |---|---|
> | ~~**W3**~~ ✅ | 판정 러너 회전 사각지대 **수정·병합 완료** (PR #33). 다음 창은 `--log` 없이 그냥 돌리면 된다 |
> | **W4** | 공백 진단 계속 — 토큰 락이 죽었으므로 **후보가 다시 0 개다** (`task_0b343c9c2a66`) |
> | **매일** | 정규장이 열리면 그날 창을 판정해 **세션을 쌓는다.** (다)를 고르든 아니든 손해가 없다 |
>
> ✅ **회전 사각지대는 같은 날 닫혔다 (PR #33).** 다음 창은 `--log` 없이 그냥 돌려라.
> 그래도 **`exit 4` 를 보면 이유를 읽어라** — `config_sig distinct=0` 은
> *"창이 나쁘다"* 가 아니라 여전히 *"러너가 로그를 못 봤다"* 쪽 신호다.
>
> ---
>
> ### (이력) ★★ 다음 한 걸음 — **08-16 아침, 이 일일 코디네이터가 집행한다**
>
> > **2026-08-15 09:3x 갱신 — 집행자를 정했다. 아래 05:1x 정정은 그대로 유효하다.**
> >
> > ### ★★★ 정정 (2026-08-15 21:1x, **사용자가 잡았다**) — **주말이다. 다음 창은 월요일이다**
> >
> > ~~다음 세션(08-16 09:30 자동화)이 할 일~~ → **08-16(일) 09:30 세션은 판정을 돌리지 마라.**
> >
> > **2026-08-15 는 토요일, 08-16 은 일요일이다. 그 이틀에 정규장은 없다.** 실측:
> > `08-08(토)·08-09(일)·08-15(토)` 전부 `rank_snaps=0, trades=0`.
> > 기준선 세션 목록이 08-01/02·08-08/09 를 건너뛰는 것도 같은 이유다.
> >
> > | | |
> > |---|---|
> > | **다음 정규장** | **2026-08-17(월)** 13:30~20:00 UTC = **08-17 22:30 ~ 08-18 05:00 KST** |
> > | **판정 명령** | `.venv/Scripts/python.exe tools/d21_verdict.py --session 2026-08-17` |
> > | **판정 세션** | **08-18(화) 09:30 KST** 일일 코디네이터 (창 종료 4.5 시간 뒤) |
> > | 08-16(일)·08-17(월) 아침 세션 | **판정 없음.** 큐의 다른 일을 하고, 판정을 시도했다가 `exit 3` 을 보고 *"데이터를 잃었다"* 로 오해하지 마라 |
> >
> > (미국 8 월 중순에 증시 휴일은 통상 없다. 다만 **휴일이면 그 주도 밀린다** — 창 전에
> > `rankings_snap` 이 실제로 들어오는지 보고 판정해라.)
> >
> > ~~```bash
> > .venv/Scripts/python.exe tools/d21_verdict.py --session 2026-08-15
> > ```~~
> >
> > - **`--session` 은 UTC 날짜다** (소스 직접 확인). 창은 그 UTC 날짜의
> >   **13:30~20:00 UTC** 이고, 판정은 창이 닫히고 **4.5 시간 뒤**인 다음날 09:30 KST 다
> > - ★ **`exit 3` 은 두 가지를 구분하지 못한다** — *"장이 안 열렸다"* 와
> >   *"수집기가 죽어서 못 받았다"* 가 **같은 출구로 나온다.** 큐 #13 참조.
> >   `exit 3` 을 받으면 **요일부터 확인해라**
> > - **종료코드**: `0` 판정 냄 · `2` 기준선 자가검사 불일치(자가 고장, 판정 금지) ·
> >   `3` 데이터 부재/부분 · `4` §3-4 무효조건. **`0` 이 아니면 표를 만들지 말고
> >   왜 아닌지만 적고 다음 정규장으로 미뤄라** — 08-15 새벽에 그렇게 했다
> > - ~~**규칙은 안 바꿨다.** 사전등록의 보호가 그대로 살아 있다~~
> >
> > > ### ★★ 정정 (2026-08-15 20:1x) — **규칙이 바뀌었다. D-23 에 답이 나왔다**
> > >
> > > 사용자가 **(다) 공백 문턱 자체를 고친다** 를 골랐다. 사전등록에 **§6 개정 1** 이
> > > 붙었다 (`1ac0b61` = **19:53:05 KST**). ~~대상 창이 열리기 2.6 시간 전~~ → **정정: 08-15 는
> > > 토요일이다. 다음 실제 창(08-17 월)보다 약 2 일 앞선다. 틀린 것은 시간차의 크기이지
> > > 순서가 아니다.**
> > >
> > > | | 옛 | **현행 (§6)** |
> > > |---|---|---|
> > > | 무효 조건 | 단일 공백 > 60 초 | **`head+tail+Σmax(0,gap−60)` 이 창의 0.5%(117 초) 초과** |
> > > | 늘었다 | > 25.9% | **> 25.9%** (안 움직였다) |
> > > | 판정 보류 | 17.1 ~ 25.9% | **19.0 ~ 25.9%** |
> > > | 안 늘었다 | < 17.1% | **< 19.0%** |
> > > | 기준선 | n=10 | **n=6** (08-03·06·10·11·12·13) |
> > >
> > > **적용은 08-15 창부터. 08-14 는 무효로 유지한다** (사용자 결정, §6-4).
> > >
> > > ### ⚠ 판정 전에 **반드시** 확인할 것
> > >
> > > **러너가 §6 을 집행하는지 먼저 확인해라.** W3 가 고치는 중이고
> > > (명세 `coordination/specs/w3_d23_gap_rule.md`), **그 PR 이 병합되기 전의 러너는
> > > 옛 규칙·옛 밴드를 집행한다.**
> > >
> > > ```bash
> > > grep -c "unobserved" tools/d21_verdict.py     # 0 이면 아직 옛 러너다
> > > ```
> > >
> > > **0 이면 판정하지 마라.** 열린 PR 이 있으면 `AUTOMATION` §2-3 게이트를 돌려
> > > 병합하고, 없으면 **판정을 미루고 그 사실만 적어라.** 옛 러너로 낸 숫자에 새
> > > 밴드를 손으로 갖다 대지 마라 — 그게 정확히 자를 흔드는 일이다.
> >
> > **왜 새 예약을 안 걸었나** — `tossmon-coord-d21verdict` 는 **일회성이고 이미 소진됐다**
> > (`Next Run Time: N/A`, 내가 직접 확인). 그런데 **일회성의 진짜 문제는 시각이 아니라
> > 재시도가 없다는 것이다** (`AUTOMATION` §6 위험 (나)). **매일 뜨는 자동화에 얹으면
> > 그 위험이 구조적으로 사라진다** — 오늘 밤이 또 무효여도 다음 날이 자동으로 집어든다.
> > 대가는 판정이 **4.5 시간 늦는 것뿐이고 측정 손실은 0** 이다.
> >
> > **동시에 도는 것**: 큐 **#10** — 수집 공백 회귀를 W4 가 갈라고 있다
> > (`task_0a78f70aac8c`, 진단만 · **배포 금지**). **이것이 안 닫히면 다음 밤도 막힌다.**
> >
> > ~~**그리고 사용자 답을 기다리는 것**: **D-23**~~ → ✅ **답 나옴 (2026-08-15 19:5x)**.
> > 사용자가 **(다)** 를 골랐고 위 정정 상자가 현행이다. **더 이상 대기 안건이 아니다.**
> >
> > 05:10 세션이 *"새 기준선을 세지 않았다 — 세면 문턱이 어디로 움직이는지 알게 된다"*
> > 고 남긴 규율은 **지켜졌다**: 규칙을 먼저 커밋(`1ac0b61`, 19:53:05)하고 **그 뒤에**
> > 기준선을 계산했다(`00815b0`, 19:54). 그래서 문턱이 움직이는 방향을 모르고 규칙을
> > 골랐다. 결과적으로 **통과선 25.9% 는 안 움직였고** 아래 경계만 엄격해졌다.
>
> **`coordination/D21-COVERAGE-PREREG.md` §1 정의 그대로 D-21 커버리지를 판정한다.**
>
> ~~```
> python tools/d21_coverage.py      # 08-14 행이 새로 붙는다
> ```~~
>
> **정정 (2026-08-14 19:4x, 코디네이터 실측)**: **안 붙는다.** `tools/d21_coverage.py:16-17`
> 의 `SESSIONS` 는 `'2026-08-13'` 에서 끝나는 하드코딩 리스트다. 게다가 그 도구는
> **`B`(테이프)와 비용만** 세고 사전등록 §1 이 요구하는 **`A`(좌석 커버리지,
> `promotions.reason='ranking_tier3'`)를 아예 안 센다.** 아래 §5 요구 넷 중 ②③④ 도 없다.
>
> **자를 결과 본 뒤에 깎으면 사전등록의 보호를 잃으므로**, 데이터가 생기기 전에
> 전용 러너 `tools/d21_verdict.py` 를 짓는다 (W3, `feat/analyzer`). `d21_coverage.py` 는
> **안 건드린다** — 사전등록된 기준선 표를 낸 물건이라 손대면 그 표의 재현성이 흔들린다.
> 새 러너는 기준선 10 세션을 다시 계산해 §2 표와 **일치하지 않으면 판정을 거부한다.**
>
> **완료 판정 — 그 문서 §5 가 요구한 넷을 전부 낸다:**
> ① top-10·top-100 각각의 **좌석 커버리지(A)·테이프 커버리지(B)·비용(`capacity_fill`)**
> ② **§3-4 무효 조건 넷**을 각각 확인한 결과 (60 초 넘는 공백 건수 · `config_sig` 불변
>    여부 · 프로브 없음 · 부분 데이터 아님)
> ③ 그 창의 `md_peak_1s` 분포와 **`RANKING` 첨두** — `docs/62` 프로브 여유 계산
>    (*"한 초 최악 4/5"*)이 **D-21 이후에도 유효한가**가 여기 달려 있다
> ④ `ranking_tier3` 승격 수 · `ranking_hold_expired` 해제 수 · **쿨다운(600 초) 위반 건수**
>
> **무효 조건이 하나라도 걸리면 부분 데이터로 표를 만들지 말고 다음 정규장으로 미룬다.**
>
> **그 판정이 끝나야** 다음 둘이 열린다 — `supervisor.stdout.log` 타임스탬프 수정(W5,
> 휴장 창)과 08-15 밤 프로브(D 안).


**이 순서를 지켜라. 사용자가 명시적으로 "먼저 닫고 시작" 이라고 했다.**

```
1) 계측기 고장 2건을 닫는다
     · limit_header_clamped 가 로그와 다른 값을 말한다   ✅ 닫힘 cbc3b08 (W1)
     · srv_s_unknown 사각지대의 크기를 모른다            ✅ 닫힘 PR#3 (W4) — 429였다
                ↓
2) 폴 주기 5초의 비용을 계산해 보고한다                  ✅ 완료 PR#5 (W1, docs/60)
     · 배율 2.251배 · API 무해(천장 10.7%) · 디스크 214일 -> 109일
                ↓
3) 휴장 창에 폴 주기만 배포한다                          ⏸ **보류 (2026-08-13 저녁 사용자)**
     · 근거가 무너졌다 — 29%도 16.1초도 1초 팔에서 잰 값이다 (docs/58 §1-1 정정)
     · 먼저: 정규장 재측정. 계획서는 docs/62 에 있고 세 결정이 §8 에 박혔다
         팔 구성 = D안 (529콜·44분·슬롯 18/120/120)   ← 사용자
         발사 시점 = D-21 배포 뒤                      ← 사용자
         foreign 오염 = (나) 교정에 쓴다 + 창 표시     ← 코디네이터
     · A·B 안은 쏘는 목적을 달성 못 한다 — 슬롯은 폴 간격이 아니라 지속시간이
       정하므로 둘 다 30슬롯이고 Wilson 반폭 15.6pp 로 프리마켓(14.7pp)보다 넓다
       (코디네이터 독립 재계산 확인)
                ↓
4) 앱만큼 빨리·빠짐없이 받게 됐는지 잰다  (docs/58 G-1a) ← 이것이 현재 1번 관문이다
```

### ★ 그 옆에 새 줄기가 생겼다 — **수집 범위** (사용자 결정 D-21, 2026-08-13)

```
A) 랭킹 진입을 승격 사유로 추가한다            ✅ 배포됨 2026-08-14 09:16 KST (docs/61 §11)
     · 토스 거래량 top10 · 좌석 2석 · rotate · hold 300s · 재배분(tier3_max 불변)
     · config_sig 꼬리: ,rkpTVOLUME@10/k2/h300s/rotate/cd600s
     · 공백 2분 49초, 전부 휴장. 워치독이 PLANNED_ 로 찍고 되살렸다
     · 증거: RCON(1위)·GPUS(2위) → to_tier=3 → 신규 체결 79·9 행
     · ★ 커버리지가 늘었다는 증거는 아직 없다 — 정규장 뒤 docs/59 재실행이 그것을 잰다
     · 지금 랭킹 top100 종목의 71~99%가 테이프에 없다 (docs/59, W3)
     · 그래서 사건의 1.78%만 결과를 잴 수 있고 표적 층은 최대 166건이다
     · 되돌릴 수 없다. 예산·디스크를 먼저 재서 보고하고 한 창에 하나만
     · ✅ 2026-08-14 라이브 확인 — 배포 후 텔레메트리 4줄 전부에 꼬리가 붙어 있다
                ↓
B) 범위가 넓어진 뒤 다시 표본을 센다
     · ★ 판정 규칙은 coordination/D21-COVERAGE-PREREG.md 에 **미리** 박았다
       (2026-08-14 10:5x, 대상 창이 열리기 11.5시간 전). 기준선 10정규장
       top-10 테이프 커버리지 중앙 17.1% / 최대 25.9% · 문턱 = 최대 초과
     · docs/59 재실행은 별개로 하되 판정 근거는 그 파일의 정의 하나뿐이다
     · ✅ **판정 완료 2026-08-18 — 통과 (B top-10 29.1% > 25.9%).** 대상 창은 08-14 도
       08-15 도 아닌 **08-17(월) 밤**이었다 (주말). 상세 §1-2c
     · ⚠ **docs/59 재실행은 아직 안 했다** — 판정과 별개이고 아무도 안 돌렸다
     · 오늘 밤(08-14 22:30)이 D-21 의 첫 정규장이고 이것이 그 기준선이다
     · 재실행은 커밋된 러너 한 줄이다 (준비 작업 없음):
         .venv/Scripts/python.exe -m tossmon.analysis.hires_events [db] [--until-ms N] [--out DIR]
     · config_sig 경계(08-14 09:16)를 넘어 두 구간을 뭉치지 마라 (docs/59 §4)
                ↓
C) 그때 비로소 전방 수익·대조군·비용 차감을 잰다 (docs/58 G-2 · G-3)
```

### ★ 프로브(`docs/62` D 안)는 **오늘 밤에 안 쏜다** — 코디네이터 판단 (2026-08-14)

`docs/62` §8-2 는 *"D-21 배포 → **안정 확인** → 그다음 정규장에 프로브"* 다.
글자만 보면 오늘 밤이지만 **가운데 "안정 확인" 이 아직 불가능하다.**

1. **D-21 은 정규장을 한 번도 안 겪었다.** 배포가 휴장 창이라 §3 의 `RANKING` 첨두 3 은
   **휴장 값**이다. `docs/62` §5-3 의 여유(*"한 초 최악 4/5, 여유 1"*)는 **D-21 이전**
   정규장 실측 위에 서 있고, 그 여유 1 이 이 계획의 유일하게 빡빡한 자리다.
2. **오늘 밤은 위 B) 의 기준선이고 그것을 잴 수 있는 첫 정규장이 오늘 하나뿐이다.**
3. **프로브의 44 분 창이 개장 폭주와 정확히 겹친다**(§5-1, 22:31 착수) — 거기가 D-21
   효과가 가장 큰 자리다. 겹치면 §8-2 가 피하려던 *"어느 것 때문인지 못 가른다"* 가
   그대로 재현된다.

**그래서**: 오늘 밤 = D-21 첫 정규장(프로브 없음) → 08-15 아침에 `docs/59` 재실행 +
`RANKING` **정규장** 첨두 재측정 → 여유가 확인되면 **08-15 밤**에 프로브.

> **방향 전환이 아니다.** 사용자가 정한 것(D 안 · D-21 뒤)은 그대로다. **하루 늦추는
> 것뿐이고 되돌릴 수 있다.** 사용자가 오늘 밤을 원하면 그렇게 한다 — 큐 `D-22` 에 적었다.

**A 와 3) 은 둘 다 배포다. 한 창에 하나만이므로 A 가 먼저다** — 3) 은 보류됐고
A 는 *"알파를 볼 수 있는가"* 를 가르는 병목이기 때문이다.

**2 단계를 1 단계 완료 전에 시작한 이유**: 2 단계는 **계산과 보고**뿐이고 배포가 아니다.
1 단계의 남은 절반(0-4)은 **3 단계 배포 전에** 닫히면 된다. 순서가 지키려는 것은
*"고장난 계측기 위에서 잰 숫자로 배포를 정하지 않는다"* 이므로, 계산을 미리 해두는 것은
그 규율을 어기지 않는다. **다만 0-4 가 열려 있는 동안 3 단계로 넘어가지 마라.**

**왜 계측기가 먼저인가**: 이 프로젝트가 세 번 겪은 것과 같다 — 첨두 11~13 도,
이중 계상도, 클램프도 전부 *"수집이 고장났다"* 처럼 보였는데 **전부 자가 계측 문제**였다.
**계측기가 자기 자신에 대해 거짓말하면 그 위에서 잰 모든 숫자를 못 믿는다.**

---

## 3-1. 확정된 결정 — 다시 열지 마라

| 결정 | 이유 |
|---|---|
| **예산의 사건 타임라인만 monotonic 으로 분리** | 서버 오프셋이 초 해상도 헤더에서 나와 ±180ms 출렁이는데 송신 간격이 118~157ms 라, 한 번 눌릴 때마다 여러 송신이 같은 순간으로 압축됐다. **첨두 11~13 은 실재가 아니었다** |
| 워밍업·쿨다운·지속·429 회복은 **벽시계 유지** | 세션 경계가 서버 시각으로 정의된다 |
| **계획 표식을 창이 아니라 키가 덮게** | 표식 하나가 2 시간짜리 워치독 사망을 `PLANNED_` 로 삼켰다. 수집 부재 키 18 개만 덮고 나머지 39 개는 계획 창에도 `ALERT_` |
| **침묵한 감시점 셋을 서버 `date` 초로 재조준** | 송신시각 계상 아래서 그 셋은 **구조적으로 발화 불가**가 됐다. *"조용하다"를 "안전하다"로 읽으면 안 된다* |
| **한 번에 하나만 배포** | 08-12 밤 정규장에 시계 수정만 넣었다. 그 덕에 **73.1%→0.0% 이 오롯이 시계의 것** |
| **천장 유지 + 클램프 로깅만** (사용자) | 지금 5/s 도 다 안 쓴다(avg 0.60). **두 번째 리미터 정체를 모른 채 천장을 4 배로 올리면 그것을 더 세게 때린다** |
| 클램프 카운터의 **방향을 가른다** | `clamped`(위로 잘림=기회 손실)와 `lowered`(정상 하향)를 한 통에 세면 이 관측이 또 묻힌다 |
| **`docs/07` §4.1 의 `≥ 1` 문언을 지우지 않음** | `strict_lt` 에서는 참이다. 두 모드를 나란히 적고 **섞지 말라**고 박았다 |

### 기각한 대안 — 같은 것을 다시 제안하지 마라

| 대안 | 기각 이유 |
|---|---|
| **천장을 20 으로 올린다** | 지금 5/s 도 다 안 쓰고 있어(avg 0.60/s) 당장 얻는 것이 없다. 그리고 **우리 모델에 없는 두 번째 서버 리미터의 정체를 모르는데** 천장을 4 배로 올리면 그 리미터를 더 세게 때린다 |
| ~~**스케줄러를 S4U 로 바꾼다**~~ | ~~**기각이 아니라 막혔다** — `Set-ScheduledTask` 가 `Access is denied`(관리자 필요)~~ → ✅ **2026-08-14 10:01 실행됨.** 사용자가 관리자 PowerShell 로. **이 행은 이제 기각 목록이 아니다** |
| **08-12 밤 정규장 전에 W4 경보까지 같이 배포** | 그 밤이 **시계 수정의 시험대**였다. 두 변경을 얹으면 첨두 변화가 어느 것 때문인지 못 가른다. 게다가 세 자리 중 둘은 보고가 아니라 **실제로 수집을 조인다** — 오탐이 나면 8 월 초 개장 붕괴가 재현된다 |
| `foreign` 경보의 문턱을 올려 조용하게 만든다 | **명세에서 금지**했다 — 원인 모른 채 경보를 끄거나 문턱을 올리는 것은 금지다 |

#### 2026-08-13~14 세션에서 갈린 것 — **기각과 보류를 나눠 적는다**

> **이유 칸은 비어 있다. 사용자가 채운다.** 코디네이터가 사후에 이유를 지어 넣으면
> 그건 기록이 아니라 합리화다. `근거 문서` 칸만 코디네이터가 채웠다 — 그건 이유가
> 아니라 **어디를 열어야 하는지의 포인터**다.

**(가) 기각 — 측정·논증으로 닫혔다. 다시 제안하려면 새 근거가 필요하다**

| 대안 | 기각 이유 (사용자 기입) | 근거 문서 |
|---|---|---|
| 프로브 팔 구성 **A** (`open` 그대로, 454 콜) | | `docs/62` §2-1·§4 |
| 프로브 팔 구성 **B** (A + 진짜 5 초 팔, 514 콜) | | `docs/62` §4 |
| 프로브 팔 구성 **C** (539 콜 · 30 분) | | `docs/62` §4 |
| D-21 `policy = sticky` | | `docs/61` §3-1 |
| D-21 **안 B·C** (tier3 정원 순증) | | `docs/61` §2·§5 |
| 최근 3 세션(08-10~08-12)을 봉인하고 6 세션만 탐색 | | `docs/58` §G-2 |
| 폴 주기 5 초를 **그대로** 배포한다 | | `docs/58` §1-1 정정 · `docs/60` §10 |

**(나) 보류·미착수 — 닫힌 게 아니다. 조건이 오면 다시 연다**

| 대안 | 무엇을 기다리나 (사용자 기입) | 근거 문서 |
|---|---|---|
| **(가) D-2 를 앞으로 당긴다** (계측 전용 실주문) | | `USER-INPUT-QUEUE` D-2 · `docs/58` §1-4 |
| **(다) 관문 순서를 바꾼다** (속도를 1 번에서 내린다) | | `docs/58` §1-4 |
| 폴 주기 5 초 배포 | | `docs/58` §1-3 · `USER-INPUT-QUEUE` D-8 |
| 프로브(D 안) 발사 | | `docs/62` §8-2 |
| 폴 5 초 대신 같은 예산을 랭킹 진입 수집에 쓴다 | | `docs/60` §10-3 |
| D-21 **좌석 1 석** | | `docs/61` §3-1 |
| D-21 랭킹 타입 = 시장 거래량 / 급상승 | | `docs/59` §7-2 · `docs/61` §3-1 |
| 배포 직전에 매번 사용자에게 묻는다 | | `USER-INPUT-QUEUE` D-18 |
| `foreign` 오염 **(가) 창 분리** | | `docs/62` §5-6·§8-3 |
| 프로브를 아예 안 쏜다 | | `docs/62` §5-7 |


---

## 4. 워커 배치

repo id = `dd0ef363-0d50-4054-9a4a-d7c56ee8e141`,
워크트리 = `C:/Users/dongh/orca/workspaces/toss_trade/<name>`

> **실측 2026-08-22 00:1x** (`git worktree list`):
>
> | 워크트리 | 브랜치 | HEAD | `.venv` |
> |---|---|---|---|
> | 루트 `C:/Users/dongh/toss_trade` | `main` | `d98f865` | (없음, 원래 그렇다) |
> | `w1-core-api` | `feat/core-api` | `3150cca` | ✅ py 3.13.15 |
> | `w3-analyzer` | `feat/analyzer` | `0db2aa8` | ⚠ **없음 — 새로 만들어야 한다** |
> | `w4-collector` | `feat/collector` | `4b9bb40` | ✅ py 3.13.15 |
> | **`w5-ops`** ★ 라이브 | `feat/ops` | `e7c9437` | ✅ py 3.13.15 |
> | `w7-prereg` | `feat/preregistration` | `ba5be66` | (원래 없다) |
>
> ⚠ **`w3-analyzer` 에 일을 주기 전에 venv 를 만들어야 한다** — 없으면 §2-3 의 회귀
> 게이트를 그 워크트리 안에서 못 돌린다(위 표 *테스트* 행의 2,318 이 거기서 나온 수다).
> `python -m venv .venv && .venv/Scripts/pip install -e ".[dev]"`
>
> **venv 는 editable 설치라 각자 자기 워크트리의 `tossmon/` 을 가리킨다.**
> **확인은 반드시 워크트리 안에서 해라** — 루트에서 치면 `sys.path[0]` 이 `main` 의
> `tossmon/` 이라 **셋 다 루트를 가리키는 것처럼 보인다.** 그 오독은 *"라이브가 `main`
> 코드를 돌린다"* 라는 틀린 결론으로 이어진다:
> `cd <워크트리> && ./.venv/Scripts/python.exe -c "import sys; sys.path.remove(''); import tossmon; print(tossmon.__file__)"`

> **★ 2026-08-14 에 이 표를 실측으로 고쳤다.** 브랜치 이름 **셋이 틀렸고** 워크트리
> **셋은 존재하지 않았다.** 옛 표대로 `git merge w5-ops` 를 치면 §5-2 의 08-04 사고
> (*"안 바뀐 HEAD 줄을 성공으로 읽었다"*)가 그대로 재현된다.
> 확인법: `git -C <워크트리> rev-parse --abbrev-ref HEAD`

| 워커 | 워크트리 | 브랜치 (실측 08-14) | HEAD | 모델 | 소유 |
|---|---|---|---|---|---|
| W1 | `w1-core-api` | `feat/core-api` | **PR #38·#40 병합됨.** `task_c2b64e01a0ec` 는 08-20 에 내가 `completed` 로 닫았다(PR #40 이 08-19 밤에 병합됐는데 태스크가 `ready` 로 남아 있었다). **대기 중** | opus/xhigh | `tossmon/api/limiter.py` · `docs/35`·`45`·`56`·`62` |
| W2 | **워크트리 없음** | — | — | sonnet/high | `tossmon/store/**` · `tossmon/universe/**` |
| W3 | `w3-analyzer` | **`feat/analyzer`** | **PR #33·#37·#45 병합됨** (#45 는 08-20 에 내가 게이트 다섯 거쳐 병합). **대기 중** — 다음 일은 D-28 이 답해져야 열린다 | opus/high | `tossmon/analysis/**` · `docs/13`·`29`·`36`·`44`·`47`·`49`·`51`·`54`·`59`·**`64`·`65`** |
| W4 | `w4-collector` | **`feat/collector`** | **PR #39 병합됨** (08-19). ★ 08-19 에 *"PR 올릴까요?"* 로 **47 분 뒤 서 있길래 내가 풀었다** — 워커는 커밋 권한을 다시 묻는다. 대기 중 | opus/xhigh + fable/xhigh | opus/xhigh + fable/xhigh | `tossmon/collector/**` · `tossmon/api/client.py` · `docs/50`·`52`·`55`·**`63`** |
| W5 | `w5-ops` | **`feat/ops`** | **PR #52·#59 병합됨** (표본 수집기: `RESTART` 직전에 스택·IO·트리·판정값을 남긴다). **대기 중.** ⚠ 라이브 워크트리라 **커밋이 곧 배포**다 — 다음 창은 08-24(월) 22:30 까지 | ~~sonnet/high~~ → **opus/high** | **라이브 수집기를 돌린다.** `ops/**` · `config/config.yaml` · `docs/31`~`34`·`38`·`53`·`57`·`61` |
| W6 / W6b | **워크트리 없음** | — | — | fable / opus | 감사 |
| W7 | `w7-prereg` | **`feat/preregistration`** (표가 `w7-prereg` 라 적었었다) | `ba5be66` | fable | `docs/12`·`15`·`48` |
| 코디네이터 | 루트 | `main` | **`c99fa60`** (08-22) | — | `docs/00`·`04`·`07` · `coordination/**` |

> ### ★★ repo id 는 **하드코딩하지 마라. 기계마다 바뀐다**
>
> **틀린 id 는 오류가 아니라 빈 목록을 낸다** — `terminal list` 가 `terminals: []` 를
> 내고 *"워커가 없다"* 로 읽힌다. **배정 전에 매번 id 부터 뜬다**:
>
> ```bash
> orca worktree list --json    # repoId 를 여기서 읽는다. 문서의 값을 믿지 마라
> ```
>
> **현재 터미널: 워커 워크트리 넷 전부 0 개**(루트에 코디네이터 하나). 배정은
> `terminal create` 부터다.

**터미널 핸들은 재시작마다 바뀐다.** 항상 `terminal list --worktree ... --json` 으로
재확인하라. 워크트리당 핸들이 여러 개면 **에이전트가 아닌 셸에 디스패치하면
`no recognized agent detected`** 가 난다 — 순서대로 시도해 성공하는 것을 쓴다.

**소유를 어기면 병합이 충돌하거나 남의 판정을 덮어쓴다.**
**워커 재개 지점의 정본은 위 표다.** `coordination/archive/HANDOFF-W*.md` 는 전부
**2026-07-31 에 일어나지 않은 리셋에 대비해 쓴 문서**이고 2 주 전 HEAD 를 못 박아
두었다 — 그대로 재개하면 2 주치를 덮어쓴다. 옛 태스크 명세는
`coordination/archive/specs/`(104 개). 자세한 것은 `coordination/archive/README.md`.

---

## 5. 운영 규율 (구속력 있음)

### 5-1. 무인 장기 실행 표준 (2026-08-02 사용자 지시)

**"중단돼도 사용자 개입 없이 빠르게 복구"가 모든 무인 장기 실행의 기본 요건이다.**
백필 2 연속 사망(PTY 킬 8 분 공백, IP 플랩 5 시간 미발견)에서 채택. 3 층 구조:

1. **자가 복구 슈퍼바이저** — 403 은 10 분 프로브 대기 후 자동 재개(IP 플랩 자가 치유),
   일시 오류 백오프 재기동, 3 연속 크래시만 ESCALATION. STATUS 는 러너 생존을 함께
   찍는다(**죽은 숫자 위장 불가**). schtasks 기동 + STOP 파일 + `--deadline`.
2. **코디네이터 독립 감시** — 로그 기반 Monitor(FATAL/RUNNER-DEAD/ESCALATION + 40 분 침묵).
   워커 전멸에도 작동한다. **새 코디네이터는 인수 시 재무장할 것.**
3. **사용자 개입은 진짜 필요한 것만** — 현재 유일: 토스 포털 IP 등록(자동화 불가).

### 5-2. 여섯 개는 2026-08-04 하루에 배웠다 (워커가 세 번 잡았다)

**`dispatched` 는 "보냈다"이지 "받았다"가 아니다 — 하트비트를 봐라.**
이날 네 번 당했다(하루 걸린 게이트, 에이전트 없는 터미널, 턴 끝난 뒤의 우편함,
**13 시간 `dispatched` 에 하트비트 0**).
```bash
orca orchestration dispatch-show --task <id> --json    # last_heartbeat_at
```
`last_heartbeat_at: None` 이면 **아무도 안 하고 있다.** 상태값만 보고 "진행 중"이라 하지 마라.
복구: `worker-abandon --dispatch <id>` → `task-update --status ready` → `worker-start`.

**병합했다고 말하기 전에 내용으로 확인해라.**
`git merge w1-core-api` 를 쳤는데 실제 브랜치는 `feat/core-api` 였다. Git 은
`not something we can merge` 를 냈지만 배경 출력에서 스크롤로 지나갔고 **안 바뀐 HEAD
줄을 성공으로 읽었다.** 워커가 저장소를 직접 확인해 잡아냈다 — 내 명세가 "머지 완료"라고
단언했는데도 확인한 것이 옳았다. → **`git log` 한 줄이 아니라 바뀐 파일의 내용을 grep 하라.**

**수집기 재시작을 내 세션에서 띄우지 마라.**
`schtasks /run` 을 내 셸에서 실행하면 콘솔 정리가 **Ctrl+C 를 전파**한다
(`LastTaskResult=3221225786`, 두 번 죽었다). 직접 실행은 멀쩡하므로 **예약작업 경로만** 죽는다.
안전한 절차: `STOP` 생성 → **정지를 파일이 아니라 데이터로 확인** → `STOP` 제거 →
**워치독이 5 분 주기에 되살리게 둔다.** 계획 정지에는 `data/ops_state/PLANNED` 를 함께 놓는다.

**같은 파일을 건드리는 태스크를 같은 워크트리에 겹쳐 보내지 마라.**
W4 워커 둘이 `loops.py` 를 동시에 편집할 뻔했다(한 명이 멈춰 물어봐서 막혔다).

**판정하지 말고 숫자를 가져와라.** (2026-08-04 사용자 지시)
> *"중요한 안건에 대해서는 함부로 판단하지 말고 나의 신경망으로 토론할 것."*

근거: **초 단위 전략을 1 분봉으로 분석하고 후보 6 개를 닫았다.**
워커 명세에 **"판정 금지"를 명시**하고 결론에 **측정 조건**(창 길이·판정 방법·오차율·표본
수)을 붙이게 한다. *"성립 안 함"* 이 아니라 *"N 초 창, 오차 X% 에서 관측 안 됨"*.

**"평균으로 뭉개면 안 보인다" — 분석과 인프라에서 같은 모양이었다.**
분석에서는 1 분봉 한 칸이 **6 초짜리 슈팅**을 삼켰고, 인프라에서는 60 초 평균
0.178 req/s 가 **한 초에 한도의 80%** 를 쓰는 버스트를 숨겼다.
→ 새 지표를 만들 때 **사건의 시간축과 측정 창을 먼저 대조**한다.

### 5-3. 그 밖에 비싸게 배운 것

- **워커 주장을 믿지 말고 게이트를 직접 재실행하라.** *"전체 통과"* 보고가 실제로는
  환경 문제로 일부 미실행이었던 적이 있다.
- **소유권 검증은 `git diff main..branch` 가 아니라 merge-base 기준.** main 이 앞서면 오탐.
- **idle 워커는 오케스트레이션 메시지를 안 읽는다.** 긴급 지시는 `orca terminal send`.
- **`git checkout <ref> -- <path>` 는 인덱스에 스테이징까지 한다** — 워커에게 안내할 때
  `git reset` 으로 언스테이지하라고 함께 알려라.
- **콘솔 출력에 비 ASCII 금지.** Windows cp949 에서 `UnicodeEncodeError` 로 죽는다.
  실제로 supervisor 가 이것 때문에 STOP 직후 죽어 collector 가 고아로 남을 뻔했다.
  (파이썬 일회성 스크립트도 `PYTHONIOENCODING=utf-8` 없이 돌리면 같은 이유로 죽는다.)
- **무인 프로세스를 에이전트 세션의 자식으로 띄우지 마라.** `Start-Process` 분리로는
  **부족하다** — Orca PTY job object 가 트리째 죽인다(7/31 18:07, 수집기 8 분 공백).
  **반드시 작업 스케줄러로 띄워 부모가 svchost(Schedule) 이 되게 하라.**
- **에이전트 턴이 Monitor 대기로 열려 있으면 `terminal send` 프롬프트가 큐에 갇힌다.**
  ESC 를 보내면 큐가 소비되지만 **긴 텍스트가 잘릴 수 있다**(실제로 잘렸다).
- **환경이 바뀌면 우회 수단을 굳히지 말고 원래 방식을 재시험하라** (§0 의 `check --wait` 사례).
- **codex 워커가 지출 한도에 걸리면 증액 요청하지 말고 claude 로 교체.**

---

## 6. 어디에 무엇이 있나

| 찾는 것 | 파일 |
|---|---|
| ****문서 60 개** 전수 목록 + 상태** | **`docs/INDEX.md`** |
| 레포 정문 (3 분) | `README.md` |
| 계약 정본 | `docs/04_contracts.md` |
| 전략 방향 정본 | `docs/16_strategy_design_notes.md` |
| **Phase 2 착수 관문** | **`docs/58_gate.md`** (초안) |
| **전략 후보 5 개가 어떻게 죽었나** | **`coordination/STRATEGY-VERDICTS.md`** |
| 사용자 결정 대기 | `coordination/USER-INPUT-QUEUE.md` |
| **무인 운영 체계 (배정→PR→게이트→병합→다음)** | **`coordination/AUTOMATION.md`** — 권한 경계는 그 §1 |
| 그날그날의 기록 | `coordination/daily/<날짜>.md` |
| Orca 운영법 | `coordination/ORCA-OPERATIONS.md` (정본은 `orca skills get orchestration`) |
| 데이터 품질 프로그램 | `coordination/DATA-QUALITY-PROGRAM.md` |

**`coordination/daily/` 에 있는 것** — 2026-07-31 · 08-04 · 08-05 · 08-07 · 08-08 ·
08-09 · 08-11 · 08-12 · 08-14 · 08-15 · **08-18** (+ `2026-08-18_d21_verdict_output.txt`
= D-21 판정 전문). (08-13 은 날짜 파일이 없다 — 그날 것은 이 파일 본문에 있다.
**08-16·08-17 도 없다** — 그 두 아침 세션의 흔적이 저장소에 없다, `daily/2026-08-18.md` §1.)

전부 이 파일에서 **원문 그대로** 잘라 옮겼다(2026-08-13). **13 개 조각 전부 원본과
바이트 일치하고 어디에도 안 간 행은 0 개**임을 스크립트로 대조했다.

- `§4.11` 이 서로 다른 두 절에 **중복**으로 붙어 있었다. **둘 다 08-07 기록**이라
  `daily/2026-08-07.md` 안에서 *아침* / *밤* 으로 갈랐다.
- `2026-07-31.md` 는 옛 `§1`~`§3` 이다. 이 파일이 스스로 *"상당수가 낡았다 — 사실 확인
  없이 인용하지 마라"* 고 적어 두었던 구간이라 **현재 상태에서 뺐다.**
