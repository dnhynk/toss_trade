# HANDOFF — W7 (분석 사전등록)

> 작성: 2026-07-31, 오케스트레이션 런타임 리셋 대비 (코디네이터 공지에 따름).

## 식별 정보

| 항목 | 값 |
|---|---|
| 워커 ID | W7 |
| 브랜치 | `w7-prereg` |
| 워크트리 경로 | `C:\Users\dongh\orca\workspaces\toss_trade\w7-prereg` |
| 현재 HEAD | `2088982` (base: `e1ed22d`) |
| 이 핸드오프 커밋 | HEAD 직후 커밋 (git log 참조) |

## 직전 태스크 요지 (지시받은 것)

데이터 분석에 들어가기 **전에** 분석 절차를 구속하는 사전등록(pre-registration) 문서
`docs/12_preregistration.md` **하나만** 작성하라. 검증 질문 6개(docs/03 §3)를 방향·크기가
명시된 반증 가능한 예측으로 재작성하고, 이벤트 정의·RVOL·라벨·세션·비용을 단일 값으로
동결하고, 질문별 accept/reject 수치 기준(특히 Q5의 Phase 2 착수 승인선), 시행 횟수 집계
규약(best-of-N 보정 사전 선택), 날짜 고정 홀드아웃 정책(랭킹의 Q2 예외 포함), 라이브
리허설에서 알려진 데이터 오염별 탐지·처리 규칙, 분석 열람 순서 잠금을 명문화하라.
코드 수정·데이터 분석·라이브 호출·`api_keys` 접근 금지.

## 완료한 것

- **`docs/12_preregistration.md` 작성 완료 — 커밋 `2088982`** (이 파일 1개, 541줄).
  §1 사전 예측 P1~P6 + 문헌 기저율 ±15%p 대역, §2 확정 정의(primary 1세트 =
  계약 C-7 기본값, 민감도 9종 secondary 강등), §3 측정 정의(Q1 대조군 규약 포함),
  §4 판정 기준(질문별 최소 표본·CI·판정불가 보수 기본값, Q5 승인선 + G1~G5 게이트),
  §5 시행 레지스트리(`docs/13_trial_registry.md` 형식 규정, 훈련무제한→검증5→홀드아웃1
  깔때기 + Bonferroni), §6 홀드아웃(train ~2025-12-31 / val 2026-01-01~04-30 /
  holdout 2026-05-01~07-29 1회 열람 / Q2 유보 의무), §7 오염 7건(a~g) 탐지+처리 규칙,
  §8 열람 순서 11단계, §9 개정 절차.
- worker_done 보고 전송 완료 (구 dispatch `ctx_116e2fc32f2b` — 리셋 후 무효).
- 코디네이터 공지 기준 **docs/12 는 이미 main(`4869293`)에 머지됨** → W7의 직전 태스크는
  **완결 상태**다.

## 다음에 할 것 (재개 지점)

**W7 자체에 미완 작업은 없다.** 재개 시 확인·후속 후보:

1. **계약 개정 A6 대조 (권장 첫 작업)**: docs/12 는 `e1ed22d` 시점 계약(A1~A5)의 기본값과
   일치함을 확인하고 동결했다. 공지에 언급된 **개정 A6 는 W7 이 보지 못했다.** A6 가
   C-7 기본값(EventParams·RVOL 규약·비용)이나 A2 §4를 건드렸다면 docs/12 §9 개정 절차로
   충돌 항목을 코디네이터 결정 하에 재동결해야 한다. 데이터 집계 열람 전인 지금이
   개정 가능한 마지막 창이다(§9: 열람 후에는 post hoc 강등).
2. `docs/13_trial_registry.md` 는 **분석 워커가 첫 시행 전에 생성**한다(docs/12 §5.2에
   형식 고정). W7 이 만드는 것이 아니다.
3. 봉 타임스탬프 시작/끝이 라이브 프로브로 확정되면 docs/12 §7-f 의 ±1분 감도 병기
   의무를 해제할 수 있다(§9 절차로).

## 미해결·막힌 것

- 없음. 코디네이터 답변 대기 중이던 질문 없음 (`ask` 사용한 적 없음).

## 다음 사람이 모르면 손해 보는 사실

- **백필은 지연될수록 훈련 기간이 영구 소실된다.** 소형주 1분봉 보관이 ~320일뿐이라
  (docs/11 §1, SNTI 실측) 보관창이 매일 미래로 민다. docs/12 §6.1 이 "분석 착수 즉시
  백필 실행"을 요구하는 이유. 리셋 후 우선순위 산정 시 이걸 고려하라.
- **홀드아웃(2026-05-01~07-29)은 단 1회 열람이다.** 어떤 사전 작업(기술통계 포함)도
  이 기간을 건드리면 G4 실패 = Phase 2 승인 근거 소멸(docs/12 §6.2·§8). 분석 스크립트
  기본 날짜 상한 2026-04-30 규칙을 지켜라.
- **collector 의 `events` 테이블은 통계에 직접 쓰면 안 된다** — UNIQUE 제약 이전 수집분에
  중복이 있다(17분에 26행/고유 10건). 모든 이벤트 통계는 candles 에서 오프라인 재유도
  (docs/12 §7-a).
- docs/12 의 예측(§1)은 **2026-07-30, DB 집계 무열람 상태**에서 작성됐다(§0.1 사전 노출
  고지). 이 사실이 문서의 신뢰 근거이므로, 분석 착수 후 §1 을 소급 수정하면 안 된다.
- 이 브랜치(`w7-prereg`)는 docs/12 커밋 하나만 얹은 상태이고 main 에 이미 머지됐으므로,
  리셋 후 W7 브랜치 자체는 폐기해도 무방하다(핸드오프 커밋만 회수하면 됨).

## 테스트 상태

- W7 작업은 문서 전용 — 코드 무접촉이라 이 브랜치에서 pytest 를 돌리지 않았다.
- 참고: 코디네이터 공지 기준 main `4869293` = **694 tests green**. 실행법:
  리포지토리 루트에서 `pytest` (mock 환경: `TOSS_LIVE=0`,
  `TOSS_BASE_URL=http://127.0.0.1:8899` — 계약 C-9).
