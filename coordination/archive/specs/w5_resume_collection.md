# W5 — 주중 실시간 수집 재개 (사용자 승인 — 본론 데이터 축적, 08-03 월 09:00 주간장 전 가동)

너는 W5(ops 소유자)다. 사용자 확정: **본론은 러너 간 유동성 이동 포착이고, 랭킹은 과거
조회가 안 되므로 실시간 수집일 하나하나가 자산**이다. 월요일 09:00 KST 주간장부터 한 주
내내(24/5) 수집한다.

## 작업

1. `git rebase main` (w5-ops 워크트리 — main 최신, 847 green).
2. **기존 DB(`data/tossmon.db`)에 이어서 수집** — 07-31 검증분은 행 불변이라 오염되지
   않는다. docs/11 에 새 수집 구간 개시를 기록(세션 경계 명시).
3. 수집기(supervisor)를 **schtasks(현재 사용자)** 로 기동 — 검증된 절차
   (`ops/register_task_scheduler.ps1`, env 에 TOSS_LIVE=1·TOSS_BASE_URL 명시,
   IP `122.40.242.91` 등록 확인됨). 지금 켜두면 closed 세션에서 대기하다 09:00 에
   자동으로 수집을 시작한다.
4. 기동 검증: 프로세스 부모가 Orca 트리 밖인지, 로그 흐름, 텔레메트리 카운터 노출
   (`rankings_clamped`·`rankings_write_failures` 포함 — 클램프 분기 첫 발화도 감시 대상).
5. **감시 재무장**: 네 모니터에 지난번 후속 3건 반영 — closed 세션은 동결 텔레메트리
   경보에서 제외, API 사망 감지(RuntimeError 폭증·카운터 동결) 유지, 토큰 재발급 실패
   가시화는 코드 수정 없이는 불가하니 로그 패턴 감시로 보완.
6. 보고: 기동 확인 `status` 1회 → 이후 **세션 전환·이상 시에만** (heartbeat 금지).
   월요일 09:00 주간장 개시 확인 `status` 1회. 이후 무인.

## 불변 규칙

라이브 리스는 수집기가 보유(다른 워커 라이브 금지 상태로 복귀함을 인지). 소유 경로 밖
수정 금지, `w5-ops` 에만 커밋. `worker_done` 은 이 태스크에선 **기동+월요일 개시 확인
완료 시** 1회(ASCII) — 수집 자체는 무기한 계속된다. lifecycle 거부가 나면 재시도 말고
status 1통 후 idle (전례 있음 — 코디네이터가 수동 처리).
