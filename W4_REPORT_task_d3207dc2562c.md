# W4 — 침묵한 감시점 셋을 서버 초 위로 옮겼다

작성: W4, 2026-08-12 20:00 KST (마감 21:30 전)
커밋: `03e6031` (브랜치 `w4-collector`). **배포 안 함 — `w5-ops` 미접촉.**
전체 문서: `docs/52_send_time_accounting.md` **§12** (§6 원문은 한 글자도 안 고쳤다)

---

## 0. 한 문단 요약

세 자리 전부 `peak_1s > limit_of` 를 보고 있었다. 그 값은 **우리 송신을 우리 시계로 센
것**이라 리미터 하드캡 아래에서 참이 될 수 없다 — §6 이 옳았다. 옮긴 자리는 서버가 매
응답에 싣는 두 가지다: `date` 헤더의 초(**창의 이름**)와 `x-ratelimit-remaining`(**그
창의 소진량**). `limit - remaining` 에서 우리 몫을 빼면 **우리가 보내지 않은 요청**이
나온다. 우리 시계도 우리 계상도 개입하지 않으므로 동어반복이 없고, docs/06 §9-6 이
"판별되지 않았다" 고 남긴 것을 **429 없이** 가리킨다.

---

## 1. 왜 이 자리인가 — 실측 둘

```
옛 배선 마지막 프로세스 (08-12 05:00:07):  over_limit_1s=435  md_peak_1s=11  http_429=305
§5.5 배포 후          (19:11 재기동, 19:21):  over_limit_1s=0    md_peak_1s=10  http_429=0
같은 줄 (설치 수명):                          http_429_under_own_limit=558
```

* 첫 두 줄: **435 → 0.** 그 0 은 안전이 아니라 §6 이 예고한 발화 불가다.
* 셋째 줄: **558번**, 429 를 맞았는데 그 서버 초에 우리 송신은 한도 미만이었다.
  원인이 우리 밖에 있었다는 뜻인데, 그 판별 수단은 **429 를 맞아야만** 값이 생긴다.
  이번 관측은 429 없이 같은 것을 본다.

---

## 2. 자리별 결론

**합성 상황은 셋 다 하나다**: 같은 자격증명의 다른 발신자가 MARKET_DATA 한도 10 중
**4 를 먹는다.** 우리 리미터는 완벽하다(우리 송신 5/초, 어떤 1초도 한도 이하).
서버가 센 소진량은 매 초 **9**.

②는 **말이 아니라 실측**이다 — 같은 스크립트를 `git stash` 로 수정 **전** 코드에 흘렸다.

| 자리 | ① 울려야 하는 상황 | ② 수정 전 (빨강) | ③ 수정 후 (초록) |
|---|---|---|---|
| `budget._note_over_limit` | 서버가 라벨한 초에서 (a) 우리 송신만으로 한도 초과 또는 (b) 소진량 > 우리 송신 | `over_limit_1s=0`, **경보 0건** | `quota_not_ours=1`, 경보 1건(에피소드당 1회) |
| `budget.should_grow` 거부 | 회복 조건은 다 만족했는데 **이 그룹 한도가 우리 것만이 아니다** | `should_grow()={'MARKET_DATA': 3}` → **복원 허용** | `should_grow()=None` → 거부 |
| `loops.tier2_orderbook_allowed` | 같은 상황. 마지막 여유는 그것이 **우리 것일 때만** 쓴다 | `allowed=True why=''` → **통과** | `allowed=False why='quota'` → 스킵 |

경보 문구는 원인별로 갈랐다 — (a)는 "리미터를 안 거치는 송신 경로를 의심하라",
(b)는 "발신자를 하나로 만들어야 한다". 조치가 다르기 때문이다.

### ④ 정상 부하 대조군 (이쪽이 본체)

`tests/test_server_second_watch.py` 24건. 고정한 것:

| 대조군 | 결과 |
|---|---|
| 한도를 **꽉 채운** 합법 부하 60초 (우리 10, 남 0) | 경보 0 · 게이트 열림 · 복원 허용 (`own == limit` 은 초과 아님) |
| 우리가 유일한 발신자 | `consumed == own` → `foreign` 구조적으로 0 |
| **산발적** 아티팩트 (30초 중 2초) | 판정 안 함 |
| 관측이 아직 없음 (기동 직후) | 복원을 막지 않음 |
| 기존 두 사유 (429 · 지속 과부하) | **그대로** 게이트를 닫음 |
| 창(60초)을 지난 사고 | 상태값에서 빠짐 (누적 카운터는 남음) |

오탐 문턱의 근거: `foreign > 0` 이 우리 잘못 없이 나오는 실측 경로 둘 —
**경계 렌더**(docs/06 §9-5: `date` 와 카운터가 한 초 어긋난 실측)와 **응답 없이 끝난
송신**. 둘 다 산발적이고 다른 발신자는 지속적이라 `FOREIGN_SECONDS_MIN=3` 으로 가른다.
도착 순서 오탐은 **초를 닫은 뒤 판정 + 소진량 최댓값**으로 막고, 429 의 잔량은 안 쓴다.

---

## 3. 못 살린 것

**`foreign` 은 "다른 발신자" 와 "그룹 밖 한도" 를 못 가른다** (docs/06 §9-6 의 두 후보).
둘 다 `consumed > own` 으로 보인다.

* 볼 수 있는 것은 **"이 초의 한도를 우리 혼자 쓰고 있지 않다"** 이고, 세 자리의
  조치(경보 · 복원 보류 · 마지막 여유 양보)는 그것만으로 전부 옳은 방향이다.
* **어디서 봐야 하나**: 그룹별 `foreign` 을 **나란히** 놓는다. 다중 기동이면 그가 도는
  그룹에서만, 그룹 밖 한도면 **여러 그룹에서 동시에** 보인다. `budget.foreign_seconds(g)`
  는 이미 그룹마다 계산되므로 텔레메트리 한 줄이면 는다 (지금은 MARKET_DATA 만 실었다).

**헤더 의존**도 숨기지 않았다: 소진량을 못 읽은 초는 깨끗한 초로 세지 않고
`srv_s_unknown` 으로 **사각지대의 크기**를 찍는다.

---

## 4. 변이 게이트 / 스위트

* `python -m tools.mutation_accounting` → **19/19 killed** (기존 10 + 새 9).
  새 9 는 되돌림 넷과 **오탐 방향 셋**(S4 문턱 1 · S5 `own >= limit` · S6 지평 제거)을
  같이 심는다 — 이 자리는 조용해도 실패하고 시끄러워도 실패한다.
* `python -m pytest -q` → **2,009 passed / 1 skipped** (4 deselected = mutation 마커).

---

## 5. 혼자 내린 판단 (자기 신고)

1. **`FOREIGN_SECONDS_MIN = 3` 문턱을 뒀다.** 지시에 없었다. 근거는 위 오탐 경로 둘이고,
   문턱이 없으면 경계 렌더 한 건이 정원을 깎는다. **값 3 자체는 선택이다** — 관측
   데이터가 아직 없다. 정규장 로그의 `md_foreign_s` 분포를 보고 조정하는 것이 맞다.
2. **`over_limit_1s()` 메서드를 원래 뜻으로 되돌렸다.** 처음엔 갈아끼웠는데 전체 회귀에서
   **W1 소유 테스트**(`test_limiter_vs_counter.py`)가 죽었다 — 그쪽은 이 메서드를
   *계상 오류 탐지기*로 쓰고 있었다. 그 파일은 손대지 말라고 받았으므로 내 쪽에서
   이름을 나눴다(`server_over_limit_seconds` / `quota_not_ours` / `server_window_violated`).
   대안(W1 에게 수정 요청)은 정규장 3시간 전이라 안 골랐다.
3. **tier2 스킵 사유를 `burst` → `quota` 로 바꿨다.** 카운터 이름이 바뀐다.
   옛 이름이 로그 전체에 **한 줄도 없다는 것을 먼저 확인하고** 바꿨다 (발화 불가였으므로).
4. **`_note_over_limit(group, peak)` 의 `peak` 인자를 없앴다** (새 술어가 내부 계산).
   `should_shrink` 의 지역변수 `peak` 도 같이 지웠다 — 다른 데서 안 쓴다.
5. **`_settle_server_seconds` 를 전수 검사로 썼다.** `OrderedDict` 순서가 생성 순서가
   아니라 마지막 접근 순서라 앞 하나만 보고 멈추면 오래된 초가 영영 안 정산된다.
   길이가 16 으로 묶여 있어 비용이 없다.

---

## 6. 안 한 것

* **배포 안 했다.** 수집기 재시작 0, `STOP`/`PLANNED` 생성 0, `schtasks` 0,
  라이브 API 호출 0, 홀드아웃 0, `w5-ops` 미접촉.
* `config.yaml` · `limiter.py` · `test_limiter_vs_counter.py` **한 줄도 안 고쳤다.**
* docs/52 **§6 원문 수정 0** — §12 를 새로 붙였다.
* **"이제 다중 발신자가 없다" 를 선언하지 않는다.** 만든 것은 관측이다.
  `md_foreign_s` 가 정규장에서 무엇을 찍는지는 배포 후에야 안다.
